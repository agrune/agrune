node_modules/
.github/scripts/node_modules/
.github/scripts/package-lock.json
*.log
.DS_Store

# Health-check runtime state — committed by the weekly workflow itself,
# but locally generated runs should not be committed as part of contributor PRs
# (the health-check workflow owns this file via its dedicated commit step).
.github/health-state.local.json
