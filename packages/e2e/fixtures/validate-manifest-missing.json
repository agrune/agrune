{
  "version": 3,
  "groups": [
    {
      "groupId": "page",
      "targets": [
        { "targetId": "signin", "actionKinds": ["click"], "selector": { "role": { "name": "button", "level": "Sign in" } } },
        { "targetId": "ghost_button", "actionKinds": ["click"], "selector": { "css": "button.does-not-exist" } }
      ]
    }
  ]
}
