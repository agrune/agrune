{
  "name": "@agrune/bench",
  "version": "0.0.0",
  "description": "Agentic chained-workflow benchmark: a model drives scenarios through the agrune control surface vs a raw Playwright a11y baseline, scored from localStorage.",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "node src/accuracy/multistep2.mjs"
  },
  "dependencies": {
    "@agrune/backend": "workspace:*",
    "@agrune/core": "workspace:*",
    "@agrune/manifest": "workspace:*",
    "gpt-tokenizer": "^3.0.1",
    "playwright": "^1.48.0"
  }
}
