{
  "name": "@agrune/bench",
  "version": "0.0.0",
  "description": "Token/accuracy benchmark harness: raw a11y snapshot (depth-tuned) vs agrune manifest snapshot, measured through the real agent-facing serializer.",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "node src/run.mjs",
    "capture": "node src/accuracy/capture.mjs",
    "eval": "node src/accuracy/eval.mjs"
  },
  "dependencies": {
    "@agrune/backend": "workspace:*",
    "@agrune/core": "workspace:*",
    "@agrune/manifest": "workspace:*",
    "@agrune/mcp": "workspace:*",
    "gpt-tokenizer": "^3.0.1",
    "playwright": "^1.48.0"
  }
}
