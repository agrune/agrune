---
phase: "14"
plan: "03"
type: tdd
wave: 3
depends_on: ["14-02"]
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/driver.ts
  - packages/mcp/src/mcp-tools.ts
  - packages/mcp/src/index.ts
  - packages/mcp/src/tools.ts
  - packages/mcp/tests/tools.spec.ts
  - packages/mcp/tests/macro-run-tool.spec.ts
  - packages/browser/src/cdp-driver.ts
autonomous: true
requirements: [MACRO-02]
tags: [mcp-tool, macro, 3file-sync, parity, error-codes, cdp, hitl, redaction, security]
must_haves:
  truths:
    - "`agrune_macro_run` tool 이 mcp-tools.ts / index.ts / tools.ts 세 파일에 동기화 등록된다"
    - "`COMMAND_ERROR_CODES` 에 4개 신규 코드가 추가된다 — MACRO_NOT_FOUND, MACRO_CIRCUIT_OPEN, MACRO_PRECONDITION_FAILED, MACRO_POSTCONDITION_FAILED"
    - "`BrowserDriver.runMacro?(tabId, macroId, params)` optional 메서드가 인터페이스에 선언된다"
    - "`CdpDriver.runMacro` 가 Runtime.evaluate 하나로 PageAgentRuntime.runMacro 를 호출한다 (step 별 CDP round-trip 없음)"
    - "macroId 미존재 → MACRO_NOT_FOUND"
    - "result.status === 'already-satisfied' → ok:true 로 반환"
    - "result.status === 'circuit-open' → MACRO_CIRCUIT_OPEN"
    - "result.status === 'precondition-failed' → MACRO_PRECONDITION_FAILED"
    - "result.status === 'postcondition-failed' → MACRO_POSTCONDITION_FAILED"
    - "HitlController.awaitGate('agrune_macro_run') 이 pause 상태면 block"
    - "sensitive step 포함 macro 는 CommandBroker event args 에 hasSensitiveSteps + sensitiveStepIndices 를 emit"
    - "sensitive step 의 step.value 는 CommandBroker args 에서 '[REDACTED]' 로 치환"
    - "driver.runMacro 없음 → INVALID_COMMAND (typeof 가드)"
    - "tabId null → SESSION_NOT_ACTIVE"
    - "tools.spec.ts 도구 개수가 12 → 13 으로 증가하고 parity assertion 이 포함된다"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "4개 신규 에러 코드"
      contains: "MACRO_NOT_FOUND"
    - path: "packages/core/src/driver.ts"
      provides: "BrowserDriver.runMacro optional method"
      contains: "runMacro"
    - path: "packages/mcp/src/mcp-tools.ts"
      provides: "agrune_macro_run zod schema 등록"
      contains: "agrune_macro_run"
    - path: "packages/mcp/src/index.ts"
      provides: "switch case + HITL gate + CommandBroker emit + error mapping"
      contains: "case 'agrune_macro_run'"
    - path: "packages/mcp/src/tools.ts"
      provides: "agrune_macro_run JSON Schema"
      contains: "agrune_macro_run"
    - path: "packages/mcp/tests/macro-run-tool.spec.ts"
      provides: "MACRO-02 단위 + parity 테스트"
      contains: "MACRO_NOT_FOUND"
    - path: "packages/browser/src/cdp-driver.ts"
      provides: "CdpDriver.runMacro 구현 (Runtime.evaluate 단일 호출)"
      contains: "runMacro"
  key_links:
    - from: "packages/mcp/src/index.ts"
      to: "packages/browser/src/cdp-driver.ts::runMacro"
      via: "driver.runMacro(tabId, macroId, params)"
      pattern: "driver\\.runMacro"
    - from: "packages/browser/src/cdp-driver.ts"
      to: "packages/runtime/src/runtime/page-agent-runtime.ts::runMacro"
      via: "Runtime.evaluate → window[QUICK_MODE_RUNTIME_KEY].runMacro({ macroId, params })"
      pattern: "runMacro"
    - from: "packages/mcp/src/index.ts"
      to: "packages/mcp/src/hitl-controller.ts::awaitGate"
      via: "handleToolCall wrapper (기존 경로 — MCP 층에서 자동)"
      pattern: "awaitGate"
---

<objective>
Phase 14의 MCP 외부 노출 층 — `agrune_macro_run` tool 을 Phase 12-03 에서 확립한 3파일 동기화 패턴(mcp-tools.ts + index.ts + tools.ts)에 따라 등록한다. `BrowserDriver.runMacro` optional 메서드를 core 에 선언하고, `CdpDriver.runMacro` 가 단일 `Runtime.evaluate` 로 브라우저 내 `PageAgentRuntime.runMacro` (Plan 14-02) 를 호출하는 얇은 브리지를 구현한다. 4개 신규 에러 코드(MACRO_NOT_FOUND / MACRO_CIRCUIT_OPEN / MACRO_PRECONDITION_FAILED / MACRO_POSTCONDITION_FAILED) 를 `COMMAND_ERROR_CODES` 에 추가하고 MacroResult status → error code 매핑을 정의한다. HITL gate 는 기존 `handleToolCall` wrapper 에 의해 자동 적용 (per Phase 12 패턴). sensitive step 포함 매크로 감지 시 CommandBroker args 에 redacted payload 브로드캐스트.

Purpose: MACRO-02 (MCP tool 노출) 의 완결. 이 plan 이 끝나면 Claude Code 가 `agrune_macro_run({ macroId, params })` 로 로그인 flow 등을 in-page 에서 실행할 수 있다.
Output: 3파일 동기화된 `agrune_macro_run` + CdpDriver 브리지 + 4개 에러 코드 + 단위/parity 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-macro/14-CONTEXT.md
@.planning/phases/14-macro/14-RESEARCH.md
@.planning/phases/14-macro/14-01-PLAN.md
@.planning/phases/14-macro/14-02-PLAN.md
@.planning/phases/12-inject/12-03-SUMMARY.md
@packages/core/src/index.ts
@packages/core/src/driver.ts
@packages/mcp/src/mcp-tools.ts
@packages/mcp/src/index.ts
@packages/mcp/src/tools.ts
@packages/mcp/tests/tools.spec.ts
@packages/browser/src/cdp-driver.ts
@packages/runtime/src/runtime/page-agent-runtime.ts

<interfaces>
<!-- 기존 COMMAND_ERROR_CODES (Phase 12-03 완료) -->
From packages/core/src/index.ts (line 6-23):
```typescript
export const COMMAND_ERROR_CODES = [
  'STALE_SNAPSHOT', 'TARGET_NOT_FOUND', 'NOT_VISIBLE', 'DISABLED',
  'FLOW_BLOCKED', 'TIMEOUT', 'SESSION_NOT_ACTIVE', 'AGENT_STOPPED',
  'INVALID_TARGET', 'INVALID_COMMAND', 'INVALID_MANIFEST',
  'CANVAS_PAN_FAILED', 'CONNECTION_LOST', 'CHROME_CRASHED',
  'RECOVERY_FAILED', 'TAB_NOT_FOUND',
] as const
```

<!-- 기존 BrowserDriver (Phase 12-02 완료) -->
From packages/core/src/driver.ts:
```typescript
export interface BrowserDriver {
  // ... 기존 ...
  injectManifest?(tabId: number, manifest: AgruneManifest): Promise<void>
  // 이 plan 추가:
  // runMacro?(tabId: number, macroId: string, params?: Record<string, unknown>): Promise<MacroRunResponse>
}
```

<!-- 기존 agrune_manifest_load 등록 패턴 (Phase 12-03 완료) — 그대로 적용 -->
From packages/mcp/src/mcp-tools.ts (line 200-212):
```typescript
mcp.tool(
  'agrune_manifest_load',
  'Load an AgruneManifest v3 ...',
  {
    manifest: z.object({ version: z.literal(3), ... }),
    ...optionalTabId,
  },
  async (args) => toMcpToolResult(await handleToolCall('agrune_manifest_load', args)),
)
```

From packages/mcp/src/index.ts (line 129-165): `case 'agrune_manifest_load'` 패턴.

<!-- Plan 14-02 이 export — 이 plan 의 입력 -->
From packages/runtime/src/runtime/macro-runner.ts (Plan 14-02):
```typescript
export type MacroResult =
  | { status: 'ok' }
  | { status: 'already-satisfied' }
  | { status: 'precondition-failed'; reason: string }
  | { status: 'postcondition-failed'; reason: string }
  | { status: 'circuit-open'; failedStep: number }
  | { status: 'step-error'; stepIndex: number; error: string }
  | { status: 'target-not-found'; stepIndex: number; targetId: string }
```

<!-- 이 plan 이 export — 다른 plan 이 소비 -->
Target (to be added):
```typescript
// packages/core/src/driver.ts
export interface MacroRunResponse extends MacroResult {
  macroId: string
  stepCount: number
}

interface BrowserDriver {
  runMacro?(
    tabId: number,
    macroId: string,
    params?: Record<string, unknown>,
  ): Promise<MacroRunResponse>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: COMMAND_ERROR_CODES 4개 추가 + BrowserDriver.runMacro interface + CdpDriver 구현 (RED→GREEN)</name>
  <files>packages/core/src/index.ts, packages/core/src/driver.ts, packages/browser/src/cdp-driver.ts</files>
  <read_first>
    - packages/core/src/index.ts (lines 1-25) — COMMAND_ERROR_CODES 배열 구조 + INVALID_MANIFEST 삽입 패턴 (12-03-SUMMARY.md)
    - packages/core/src/driver.ts (전체) — injectManifest optional 선언 패턴
    - packages/browser/src/cdp-driver.ts (lines 340-410, 540-570) — injectManifest 구현 + evaluateInSession 헬퍼
    - .planning/phases/12-inject/12-03-SUMMARY.md — "INVALID_MANIFEST 삽입 위치" 결정 근거
    - .planning/phases/14-macro/14-RESEARCH.md §"Q4" / §"Q9"
  </read_first>
  <behavior>
    **Unit tests (기존 core/browser 테스트에 추가)**:

    packages/core/tests/ (기존 배열 검증 테스트 파일 있으면 확장, 없으면 skip — acceptance 는 grep):
      - 4개 코드가 정확히 COMMAND_ERROR_CODES 에 포함
      - 순서: INVALID_MANIFEST 뒤, CANVAS_PAN_FAILED 앞

    packages/browser/tests/cdp-driver.runMacro 테스트 (신규 또는 기존 cdp-driver 테스트에 append):
      - Test: runMacro(tabId, macroId, params) → CdpConnection.send('Runtime.evaluate', ...) 1회만 호출 (step 수와 무관). round-trip 횟수 = 1 보장.
      - Test: evaluate expression 에 JSON.stringify(macroId) 와 JSON.stringify(params) 가 포함됨 (문자열 literal 검증)
      - Test: evaluate 반환 객체를 그대로 MacroRunResponse 로 전달
      - Test: U+2028/U+2029 이스케이프 적용 (12-02 injectManifest 패턴 재사용 — T-12-05 회귀 방지)
      - Test: target session 없음 → createCommandError('TAB_NOT_FOUND')
      - Test: evaluate exceptionDetails → Error throw (기존 evaluateInSession 경로)
  </behavior>
  <action>
    **RED step**:
    1. packages/core/src/index.ts — `COMMAND_ERROR_CODES` 배열 수정:
       ```typescript
       export const COMMAND_ERROR_CODES = [
         'STALE_SNAPSHOT', 'TARGET_NOT_FOUND', 'NOT_VISIBLE', 'DISABLED',
         'FLOW_BLOCKED', 'TIMEOUT', 'SESSION_NOT_ACTIVE', 'AGENT_STOPPED',
         'INVALID_TARGET', 'INVALID_COMMAND', 'INVALID_MANIFEST',
         'MACRO_NOT_FOUND',              // ⬅ Phase 14-03
         'MACRO_CIRCUIT_OPEN',            // ⬅ Phase 14-03
         'MACRO_PRECONDITION_FAILED',     // ⬅ Phase 14-03
         'MACRO_POSTCONDITION_FAILED',    // ⬅ Phase 14-03
         'CANVAS_PAN_FAILED', 'CONNECTION_LOST', 'CHROME_CRASHED',
         'RECOVERY_FAILED', 'TAB_NOT_FOUND',
       ] as const
       ```
       (INVALID_MANIFEST 바로 뒤 4연속 배치 — 의미 근접 우선, 12-03 Decisions 패턴 준수)

    2. packages/core/src/driver.ts — import 추가 + interface 확장:
       ```typescript
       import type { MacroResult } from '@agrune/runtime' 
       // 위 import 불가 (runtime 이 core 를 참조하므로 순환). 대신 MacroResult 타입을 core 에 local 복제 OR @agrune/manifest 에 이전.
       ```
       권장: MacroResult 타입을 **`packages/core/src/driver.ts` 내부에 local 복제** — 순환 import 회피. 주석으로 "source of truth: @agrune/runtime/macro-runner" 명시.
       ```typescript
       export type MacroResult =
         | { status: 'ok' }
         | { status: 'already-satisfied' }
         | { status: 'precondition-failed'; reason: string }
         | { status: 'postcondition-failed'; reason: string }
         | { status: 'circuit-open'; failedStep: number }
         | { status: 'step-error'; stepIndex: number; error: string }
         | { status: 'target-not-found'; stepIndex: number; targetId: string }

       export type MacroRunResponse = MacroResult & { macroId: string; stepCount: number }

       export interface BrowserDriver {
         // ... existing ...
         injectManifest?(tabId: number, manifest: AgruneManifest): Promise<void>
         /** Phase 14: in-page MacroRunner 실행 — 단일 Runtime.evaluate 로 step loop 완결 */
         runMacro?(
           tabId: number,
           macroId: string,
           params?: Record<string, unknown>,
         ): Promise<MacroRunResponse>
       }
       ```

    3. packages/browser/src/cdp-driver.ts — `runMacro` 구현 (injectManifest 패턴 바로 아래):
       ```typescript
       async runMacro(
         tabId: number,
         macroId: string,
         params: Record<string, unknown> = {},
       ): Promise<MacroRunResponse> {
         const target = this.targetManager.getTarget(tabId)
         if (!target?.sessionId) {
           throw createCommandError('TAB_NOT_FOUND', `No session for tabId ${tabId}.`, { tabId })
         }
         const macroIdLiteral = JSON.stringify(macroId)
         const paramsJson = JSON.stringify(JSON.stringify(params))
           .replace(/\u2028/g, '\\u2028')
           .replace(/\u2029/g, '\\u2029')
         const expression =
           `(async () => {` +
           `  const rt = window[${JSON.stringify(QUICK_MODE_RUNTIME_KEY)}];` +
           `  if (!rt || typeof rt.runMacro !== 'function') {` +
           `    return { status: 'step-error', stepIndex: -1, error: 'runtime not ready', macroId: ${macroIdLiteral}, stepCount: 0 };` +
           `  }` +
           `  return await rt.runMacro({ macroId: ${macroIdLiteral}, params: JSON.parse(${paramsJson}) });` +
           `})()`
         const raw = await this.evaluateInSession<MacroRunResponse>(target.sessionId, expression)
         if (!raw || typeof raw !== 'object' || typeof raw.status !== 'string') {
           throw createCommandError('INVALID_COMMAND', 'MacroRunner returned invalid result.', { raw })
         }
         return raw
       }
       ```
       - MacroResult / MacroRunResponse 는 @agrune/core 에서 re-import.
       - `window.agruneDom` 또는 `window[QUICK_MODE_RUNTIME_KEY]` 에 `runMacro` 가 있음을 전제 — Plan 14-02 에서 page-agent-runtime.ts 에 publish 필요. **Plan 14-02 의 Task 2 action 에 이 publish 가 포함되어 있음을 재확인**: `window.agruneDom.runMacro` OR runtime store 에 runMacro 등록. 만약 Plan 14-02 완료 시점에 글로벌 publish 가 누락되어 있으면 이 Task 내에서 page-agent-runtime.ts 의 return object publish 단계에 `runMacro` 가 포함되었는지만 확인 (이미 Plan 14-02 interface 에 선언됨).

    4. typecheck + test 실행 → GREEN.
    5. git commit: `feat(14-03): add 4 MACRO_* error codes + BrowserDriver.runMacro + CdpDriver bridge (TDD GREEN)`.
  </action>
  <acceptance_criteria>
    grep-verifiable:
    - `grep -n "MACRO_NOT_FOUND" packages/core/src/index.ts` → 1 match
    - `grep -n "MACRO_CIRCUIT_OPEN" packages/core/src/index.ts` → 1 match
    - `grep -n "MACRO_PRECONDITION_FAILED" packages/core/src/index.ts` → 1 match
    - `grep -n "MACRO_POSTCONDITION_FAILED" packages/core/src/index.ts` → 1 match
    - `grep -c "MACRO_" packages/core/src/index.ts` = 4 (배열 내 4 항목)
    - `grep -n "runMacro" packages/core/src/driver.ts` ≥ 2 (import/type + optional method)
    - `grep -n "MacroRunResponse" packages/core/src/driver.ts` ≥ 1 (type 정의)
    - `grep -n "runMacro" packages/browser/src/cdp-driver.ts` ≥ 2 (method + body)
    - `grep -n "u2028\\|u2029" packages/browser/src/cdp-driver.ts` ≥ 2 lines (기존 injectManifest 1곳 + runMacro 1곳 이상)
    - `pnpm --filter @agrune/core run typecheck` exit 0
    - `pnpm --filter @agrune/browser run typecheck` exit 0
    - `pnpm --filter @agrune/core run test` 모두 pass
    - `pnpm --filter @agrune/browser run test` 모두 pass (cdp-driver runMacro 추가 테스트 포함)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agrune/core run typecheck &amp;&amp; pnpm --filter @agrune/core run test &amp;&amp; pnpm --filter @agrune/browser run typecheck &amp;&amp; pnpm --filter @agrune/browser run test</automated>
  </verify>
  <done>
    - 4 개 에러코드 추가 + 타입 추론 자동 반영 (CommandErrorCode union 확장 확인)
    - BrowserDriver.runMacro optional 선언 + CdpDriver 구현 + U+2028/U+2029 회귀 방지 1곳 이상
    - core/browser typecheck + 테스트 전부 green
    - RED + GREEN 커밋 각 1회
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agrune_macro_run 3파일 동기화 + parity + sensitive redaction (RED→GREEN)</name>
  <files>packages/mcp/src/mcp-tools.ts, packages/mcp/src/index.ts, packages/mcp/src/tools.ts, packages/mcp/tests/tools.spec.ts, packages/mcp/tests/macro-run-tool.spec.ts</files>
  <read_first>
    - packages/mcp/src/mcp-tools.ts (lines 200-213) — agrune_manifest_load 등록 패턴
    - packages/mcp/src/index.ts (lines 100-180) — case 'agrune_manifest_load' 패턴 + handleToolCall wrapper
    - packages/mcp/src/tools.ts (lines 200-215) — agrune_manifest_load JSON Schema 패턴
    - packages/mcp/tests/tools.spec.ts (전체) — 12 tools parity assertion 패턴 (from 12-03)
    - packages/mcp/tests/manifest-load-tool.spec.ts (전체) — 이 plan 테스트의 템플릿
    - packages/manifest/src/schema.ts (line 143-163) — MacroSchema 구조 (zod macro schema 참조)
    - .planning/phases/14-macro/14-RESEARCH.md §"Q4" / §"Q8" / §"Pitfall 4" / §"Pattern 5"
  </read_first>
  <behavior>
    **Unit tests — packages/mcp/tests/macro-run-tool.spec.ts (신규 ≥ 250 lines, ≥ 8 describe)**:

    describe('COMMAND_ERROR_CODES 포함 확인'):
      - MACRO_NOT_FOUND / MACRO_CIRCUIT_OPEN / MACRO_PRECONDITION_FAILED / MACRO_POSTCONDITION_FAILED 각 1 assertion

    describe('happy path'):
      - driver.runMacro mock → `{ status: 'ok', macroId: 'login', stepCount: 3 }`
      - handleToolCall('agrune_macro_run', { macroId: 'login', params: {...} }) 결과 text = JSON.parse → { ok: true, status: 'ok', macroId, stepCount }
      - driver.runMacro 호출 횟수 === 1
      - commandBroker.emit — phase 'start' 1회 + 'end' 1회 + durationMs 존재
      - phase 'start' args 에 `macroId` 존재, `params` 는 원시 전달 (sensitive masking 은 아래 별도 테스트)

    describe('already-satisfied'):
      - driver.runMacro → `{ status: 'already-satisfied', ... }` → ok: true, status: 'already-satisfied' 반환, phase 'end' emit

    describe('SESSION_NOT_ACTIVE'):
      - tabId null → errorText 'SESSION_NOT_ACTIVE'. driver.runMacro 호출 0회

    describe('INVALID_COMMAND — driver.runMacro 없음'):
      - driver.runMacro 미구현 → 'INVALID_COMMAND' errorText

    describe('status → error code 매핑'):
      - 'circuit-open' → MACRO_CIRCUIT_OPEN (details: { failedStep })
      - 'precondition-failed' → MACRO_PRECONDITION_FAILED (details: { reason })
      - 'postcondition-failed' → MACRO_POSTCONDITION_FAILED (details: { reason })
      - 'step-error' → INVALID_COMMAND (details: { stepIndex, error })
      - 'target-not-found' → TARGET_NOT_FOUND (details: { stepIndex, targetId })

    describe('MACRO_NOT_FOUND — manifest 의 macro 조회'):
      - driver.listSessions 에 활성 세션 있음, driver.getManifest(tabId) mock → macros 에 macroId 없음
      - → errorText 'MACRO_NOT_FOUND' (driver.runMacro 호출 전 단계에서 조기 반환 — pre-flight check)
      - 주의: `getManifest` 가 기존에 없으면 driver.runMacro 내부에서 404 반환하고 handler 가 매핑하는 경로도 수용. 권장: Plan 14-02 PageAgentRuntime 이 macro not found 를 `{status: 'step-error', stepIndex: -1, error: 'macro not found: xxx'}` 로 반환 → MCP handler 에서 message startsWith "macro not found" 면 MACRO_NOT_FOUND 매핑 (pragmatic, getManifest API 추가 회피)

    describe('sensitive step detection + redaction'):
      - Mock driver.getManifest(tabId) OR driver.listSessions 에서 해당 manifest 조회
      - macro.steps 에 sensitive: true step 포함 → CommandBroker 'start' event args:
        - `hasSensitiveSteps: true`
        - `sensitiveStepIndices: [2, 4]` 등 index 배열
        - `params` redacted: sensitive step 의 value referenced key 는 `'[REDACTED]'` (예: password 키)
      - 정확한 구현: sensitive step 의 step.value (`{{password}}`) 에서 추출한 param 키를 params 카피본에서 `[REDACTED]` 로 치환 후 emit
      - 일반 macro (sensitive 없음) → `hasSensitiveSteps: false`, params 그대로 emit

    describe('parity assertion — Phase 12-03 패턴'):
      - registerAgruneTools(mcp mock, handleToolCall) 호출 후 등록된 tool names = getToolDefinitions() names 집합이 완전 일치
      - 신규 도구 개수 = 13 (기존 12 + agrune_macro_run)

    describe('HITL gate 통과 경로'):
      - hitl.pause() → awaitGate 가 block → resume() 까지 지연
      - resume 후 driver.runMacro 호출 1회 확인
      - hitl.skip() → HitlSkipError → CommandBroker phase 'error' emit (기존 wrapper 동작)

    **tools.spec.ts 확장**:
      - 기존 12 tools 체크를 13 tools 로 업데이트 (`expect(tools).toHaveLength(13)`)
      - agrune_macro_run 순서 체크 (agrune_manifest_load 바로 뒤)
      - tool registration parity describe 블록이 13 tools 로 업데이트
  </behavior>
  <action>
    **RED step**:
    1. packages/mcp/tests/macro-run-tool.spec.ts 신규 작성 (위 8 describe, ≥ 250 lines). mock driver / mock commandBroker / mock hitl 을 manifest-load-tool.spec.ts 패턴 그대로 재사용.
    2. packages/mcp/tests/tools.spec.ts 업데이트 — 12 → 13 tools, parity assertion 에 agrune_macro_run 포함.
    3. `pnpm --filter @agrune/mcp run test` → RED (tool 미등록) 확인.
    4. git commit: `test(14-03): add failing tests for agrune_macro_run + parity (TDD RED)`.

    **GREEN step** — 3파일 동기화 + handler:
    1. **mcp-tools.ts** — `agrune_manifest_load` 직후에 등록:
       ```typescript
       mcp.tool(
         'agrune_macro_run',
         'Run a macro defined in the loaded manifest by macroId. Executes the entire step loop in-page via a single Runtime.evaluate — step-level CDP round-trips do not occur. Returns MacroResult-shaped response. Call agrune_manifest_load first if the page does not ship its own manifest.',
         {
           macroId: z.string().describe('Macro ID as defined in the manifest'),
           params: z.record(z.string(), z.unknown()).optional().describe('Params matching macro.params schema — interpolated into step.value as {{key}}'),
           ...optionalTabId,
         },
         async (args) => toMcpToolResult(await handleToolCall('agrune_macro_run', args)),
       )
       ```

    2. **index.ts** — `case 'agrune_manifest_load'` 바로 뒤에 append:
       ```typescript
       case 'agrune_macro_run': {
         if (tabId == null) {
           return errorText('SESSION_NOT_ACTIVE', 'No active session for macro run.')
         }
         if (typeof driver.runMacro !== 'function') {
           return errorText('INVALID_COMMAND', 'Driver does not support runMacro.')
         }
         const macroId = typeof args.macroId === 'string' ? args.macroId : null
         if (!macroId) {
           return errorText('INVALID_COMMAND', 'agrune_macro_run requires macroId (string).')
         }
         const params = (args.params ?? {}) as Record<string, unknown>

         // Pre-flight: sensitive step 감지 → CommandBroker args 에 주입하기 위해 driver 에게 manifest 조회 요청.
         // driver.getSnapshot(tabId) 는 manifest 가 아니므로, 간단 구현: PageAgentRuntime 이 `{status:'step-error', stepIndex:-1, error:'macro not found: xxx'}` 반환 → 아래 매핑
         // 전체 redaction 은 driver 가 감지한 sensitiveStepIndices 를 반환하도록 Plan 14-02 에서 MacroRunResponse 에 (optional) 추가 고려.
         // 본 plan 의 구현 간편화: MacroRunResponse 에 `sensitiveStepIndices?: number[]` (optional) 추가 요청 → runtime 이 실행 전 감지.

         let response: MacroRunResponse
         try {
           response = await driver.runMacro(tabId, macroId, params)
         } catch (error) {
           const shape = error as Partial<CommandErrorShape>
           if (shape && typeof shape.code === 'string' && shape.code === 'TAB_NOT_FOUND') {
             return errorText('TAB_NOT_FOUND', shape.message ?? 'Tab not found.', shape.details)
           }
           return errorText('INVALID_COMMAND', error instanceof Error ? error.message : String(error))
         }

         // Status → error code mapping (per 14-RESEARCH.md Q4)
         switch (response.status) {
           case 'ok':
           case 'already-satisfied':
             return { text: JSON.stringify({ ok: true, status: response.status, macroId: response.macroId, stepCount: response.stepCount }, null, 2) }
           case 'circuit-open':
             return errorText('MACRO_CIRCUIT_OPEN', 'Circuit breaker opened — consecutive failures.', { failedStep: response.failedStep, macroId })
           case 'precondition-failed':
             return errorText('MACRO_PRECONDITION_FAILED', `Macro precondition failed: ${response.reason}`, { macroId, reason: response.reason })
           case 'postcondition-failed':
             return errorText('MACRO_POSTCONDITION_FAILED', `Macro postcondition failed: ${response.reason}`, { macroId, reason: response.reason })
           case 'step-error':
             // macro not found 는 PageAgentRuntime 에서 error="macro not found: xxx" 로 반환 → 매핑
             if (response.stepIndex === -1 && response.error.startsWith('macro not found')) {
               return errorText('MACRO_NOT_FOUND', response.error, { macroId })
             }
             return errorText('INVALID_COMMAND', `Macro step failed: ${response.error}`, { macroId, stepIndex: response.stepIndex, error: response.error })
           case 'target-not-found':
             return errorText('TARGET_NOT_FOUND', `Macro target not found at step ${response.stepIndex}: ${response.targetId}`, { macroId, stepIndex: response.stepIndex, targetId: response.targetId })
         }
       }
       ```
    3. **index.ts** — `handleToolCall` wrapper 의 `event.args` 빌드 부분에서 sensitive redaction 처리. `agrune_macro_run` 인 경우 `response.sensitiveStepIndices` (MacroRunResponse 에서 optional) 를 확인해 redacted args 생성. 간소화를 위해 별도 helper `redactMacroArgs(args, sensitiveStepIndices)` 를 만들고, commandBroker.emit 시 redacted args 사용.
       - **구현 노트**: `sensitiveStepIndices` 가 없으면 (pre-flight 없음) params 그대로 emit. 있으면 sensitive step 의 value interpolation key 에 해당하는 params 엔트리를 `[REDACTED]` 로 치환.
       - **Plan 14-02 에 추가 작업**: MacroRunResponse 에 `sensitiveStepIndices?: number[]` optional 필드 추가 — MacroRunner.run() 이 step loop 시작 시점에 계산 (Plan 14-02 실행 시 이 필드를 포함하도록 확인).
         - 만약 Plan 14-02 가 이미 완료되어 이 필드가 없다면 **이 plan 의 Task 2 RED step 이전에 Plan 14-02 에 소규모 보강 커밋 필요** — macro-runner.ts 에 sensitiveStepIndices 계산 로직 (onStepStart 이전에 pre-scan) + MacroRunResponse 에 포함.
       - **대안 (v0.5 간소화)**: sensitiveStepIndices 없이 `response.sensitiveStepIndices` optional 만 소비. 미존재 시 redaction 스킵하고 sensitive step 가 있어도 best-effort. 단, 테스트는 sensitiveStepIndices 가 전달될 때의 redaction 만 검증.
    4. **tools.ts** — `agrune_manifest_load` 직후에 JSON Schema append:
       ```typescript
       {
         name: 'agrune_macro_run',
         description: 'Run a macro defined in the loaded manifest by macroId. Executes the entire step loop in-page via a single Runtime.evaluate.',
         inputSchema: {
           type: 'object',
           properties: {
             macroId: { type: 'string', description: 'Macro ID as defined in the manifest.' },
             params: { type: 'object', description: 'Params matching macro.params schema.' },
             tabId: { type: 'number', description: 'Tab ID (omit for active tab).' },
           },
           required: ['macroId'],
         },
       }
       ```
    5. **tools.spec.ts 업데이트** — 12 → 13, parity assertion 확장.
    6. `pnpm --filter @agrune/mcp run test` → GREEN
    7. `pnpm --filter @agrune/mcp run typecheck` → exit 0
    8. `pnpm --filter @agrune/mcp run build` → exit 0
    9. Cross-package 회귀: `pnpm -r run test` → 전체 workspace 그린.
    10. git commit: `feat(14-03): agrune_macro_run 3파일 동기화 + error code mapping + parity (TDD GREEN)`.
  </action>
  <acceptance_criteria>
    grep-verifiable:
    - `grep -n "agrune_macro_run" packages/mcp/src/mcp-tools.ts` ≥ 2 matches (등록 + handleToolCall 분기)
    - `grep -n "case 'agrune_macro_run'" packages/mcp/src/index.ts` → 1 match
    - `grep -n "MACRO_CIRCUIT_OPEN" packages/mcp/src/index.ts` → 1 match
    - `grep -n "MACRO_PRECONDITION_FAILED" packages/mcp/src/index.ts` → 1 match
    - `grep -n "MACRO_POSTCONDITION_FAILED" packages/mcp/src/index.ts` → 1 match
    - `grep -n "MACRO_NOT_FOUND" packages/mcp/src/index.ts` → 1 match (startsWith 'macro not found' 매핑)
    - `grep -n "agrune_macro_run" packages/mcp/src/tools.ts` → 1 match (name field)
    - `grep -n "agrune_macro_run" packages/mcp/tests/tools.spec.ts` ≥ 2 matches (순서 + parity)
    - `grep -c "toHaveLength(13)" packages/mcp/tests/tools.spec.ts` ≥ 1 (혹은 length 가 13 인 assertion 1회 이상)
    - `wc -l packages/mcp/tests/macro-run-tool.spec.ts` ≥ 250
    - `grep -c "describe(" packages/mcp/tests/macro-run-tool.spec.ts` ≥ 8
    - `pnpm --filter @agrune/mcp run test` → 전체 green
    - `pnpm -r run test` → workspace 전체 green (회귀 없음)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agrune/mcp run typecheck &amp;&amp; pnpm --filter @agrune/mcp run test &amp;&amp; pnpm --filter @agrune/mcp run build &amp;&amp; pnpm -r run typecheck</automated>
  </verify>
  <done>
    - 3파일 동기화 완료 + parity assertion 13 tools 갱신
    - 단위 테스트 8 describe ≥ 250 lines 전부 green
    - 에러 코드 매핑 6개 case 전부 테스트 커버
    - sensitive redaction 경로 테스트 포함 (실 구현이 stub 일 수 있으나 테스트로 계약 고정)
    - workspace 전체 typecheck + test green
    - RED + GREEN 커밋 각 1회
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller (LLM) → agrune_macro_run | Claude Code 등 외부 harness 가 `macroId` + `params` 를 전달. tabId 는 number, macroId 는 zod validated string. params 는 `z.record(z.string(), z.unknown())` 으로 key 타입만 제한. |
| MCP handler → CdpDriver.runMacro → Runtime.evaluate | params JSON 이 evaluate expression literal 로 embed. U+2028/U+2029 이스케이프 + JSON.stringify 이중 인코딩이 Phase 12-02 에서 확립된 방어. |
| MacroRunResponse ← page runtime | 신뢰 경계 복귀 — evaluate 반환이 MacroRunResponse shape 임을 handler 가 검증 (`typeof raw.status === 'string'`). |
| CommandBroker.emit → DevTools WebApp | sensitive step 의 params value 가 broadcast 되지 않도록 redaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Info Disclosure | `params.password` 등이 CommandBroker event args 로 DevTools WebApp 에 브로드캐스트 → 클라이언트 측 log 에 유출 | mitigate | `response.sensitiveStepIndices` 가 있으면 sensitive step 의 value interpolation key 에 매칭되는 params 엔트리를 `[REDACTED]` 로 치환 후 emit. MacroRunResponse 에 해당 필드가 없으면 best-effort (v0.5 간소화) — Plan 14-02 의 macro-runner.ts 에 pre-scan 로직 보강 필수. Plan 14-01 의 runtime heuristic 이 step.sensitive 없어도 element 감지하도록 이미 확장됨. |
| T-14-15 | Tampering | 악성 manifest 가 `precondition`/`postcondition` 에 global side-effect 코드 주입 (예: `globalThis.__hack=1`) | mitigate | 1) `validateManifest` (zod MacroSchema — MANIFEST-03/04) 가 manifest 로드 시점에 통과한 입력만 evaluate 에 도달. 2) Plan 14-02 의 `new Function('params', 'return !!(...)')` scope 격리. 3) 결과는 boolean 캐스팅만 사용. 표현식 내 side-effect 는 `return` 값만 참조 — 실행 자체는 막지 않으나 반환만 사용. 4) Phase 18 REGISTRY PR bot 이 manifest 리뷰 단계에서 복잡/의심 식 하이라이트. |
| T-14-16 | Spoofing | MCP caller 가 macroId 로 우회 시도 (e.g., `__proto__`, `constructor`) | mitigate | zod `z.string()` + schema 의 `z.string().min(1)` — 길이 체크. macroId 비교는 `manifest.macros.find(m => m.macroId === macroId)` — 문자열 exact match. prototype lookup 경로 없음. |
| T-14-17 | DoS | agrune_macro_run 을 극도로 짧은 간격으로 반복 호출 → Chrome 과부하 | mitigate | MCP layer 의 `action-queue` / HitlController 구조로 순차 처리. circuit breaker (Plan 14-02) 가 step-level DoS 차단. v0.5 scope 에서는 rate limit 추가 안 함 (HitlController pause/skip 이 사용자 수동 stop 경로). |
| T-14-18 | EoP | `driver.runMacro` 가 없는 stub driver (테스트/mock) 가 production 에 사용됨 | mitigate | `typeof driver.runMacro !== 'function'` → `INVALID_COMMAND` (injectManifest 패턴 — T-12-10 재사용). |
| T-14-19 | Repudiation | macro 실행 실패 시 감사 log 누락 | mitigate | CommandBroker phase 'start' / 'end' / 'error' emit — 기존 handleToolCall wrapper 가 자동. durationMs 포함. devtools WebApp 이 buffer 에서 backfill 가능. |
| T-14-20 | Tampering | MacroRunResponse shape 변조 (page runtime 이 악의 객체 반환) | mitigate | handler 에서 `raw.status === 'string' && known enum value` 체크. unknown status → default case 로 `INVALID_COMMAND` fallthrough (switch exhaustiveness 로 강제). |
</threat_model>

<verification>
- `pnpm --filter @agrune/mcp run typecheck` → exit 0
- `pnpm --filter @agrune/mcp run test` → 전 테스트 green (manifest-load-tool.spec.ts + macro-run-tool.spec.ts + tools.spec.ts 포함)
- `pnpm --filter @agrune/mcp run build` → exit 0
- `pnpm -r run typecheck` → 전체 workspace
- `pnpm -r run test` → 전체 workspace (회귀 없음)
- 3파일 동기화 검증: `grep -l 'agrune_macro_run' packages/mcp/src/mcp-tools.ts packages/mcp/src/index.ts packages/mcp/src/tools.ts` → 3 files 모두 매치
- COMMAND_ERROR_CODES 에 4개 신규 코드: `grep -c 'MACRO_' packages/core/src/index.ts` = 4 (선언), `grep -c 'MACRO_' packages/mcp/src/index.ts` ≥ 4 (매핑 — MACRO_NOT_FOUND / CIRCUIT_OPEN / PRECONDITION_FAILED / POSTCONDITION_FAILED 각각)
</verification>

<success_criteria>
- MACRO-02: `agrune_macro_run` MCP tool 이 3파일 동기화로 등록 ✓
- 4개 에러 코드 (MACRO_NOT_FOUND / MACRO_CIRCUIT_OPEN / MACRO_PRECONDITION_FAILED / MACRO_POSTCONDITION_FAILED) 가 COMMAND_ERROR_CODES 에 추가 ✓
- MacroResult status → error code 매핑 6 case 테스트 커버 ✓
- BrowserDriver.runMacro optional interface + CdpDriver 단일 Runtime.evaluate 브리지 구현 ✓
- HITL gate 자동 적용 (기존 handleToolCall wrapper 재사용) ✓
- sensitive step 감지 시 CommandBroker args redaction 경로 확립 ✓
- parity assertion 13 tools 로 갱신 ✓
- Phase 14 Success Criteria #1 (MCP 가 start/end 만 orchestrate) + #4 (CommandBroker 스트리밍) 달성 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/14-macro/14-03-SUMMARY.md`
</output>
