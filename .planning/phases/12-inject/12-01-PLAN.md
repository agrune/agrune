---
phase: 12-inject
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/index.ts
  - packages/runtime/src/runtime/snapshot.ts
  - packages/runtime/tests/snapshot-v3.spec.ts
  - packages/runtime/tests/runtime.spec.ts
  - packages/runtime/tests/fill-cdp.spec.ts
  - packages/devtools/src/panel.ts
  - packages/mcp/tests/public-shapes.spec.ts
autonomous: true
requirements:
  - RESOLVE-03
must_haves:
  truths:
    - "PageTarget.selector 타입이 SelectorLadder 객체로 변경되어 JSON.stringify 임시 직렬화 제거"
    - "PageSnapshot에 schemaVersion: 3 리터럴 필드가 추가되어 프로토콜 버전을 명시"
    - "snapshot.ts의 captureTarget()이 SelectorLadder 객체를 그대로 반환 (JSON.stringify 삭제)"
    - "devtools panel.ts가 SelectorLadder 객체를 JSON.stringify로 렌더링 ([object Object] 미표시)"
    - "@agrune/runtime 기존 테스트 115/115 + 신규 v3 shape 테스트가 PASS"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "PageTarget.selector: SelectorLadder + PageSnapshot.schemaVersion: 3"
      contains: "schemaVersion: 3"
    - path: "packages/runtime/src/runtime/snapshot.ts"
      provides: "captureTarget 반환 시 SelectorLadder 객체 유지"
      contains: "selector: descriptor.target.selector"
    - path: "packages/runtime/tests/snapshot-v3.spec.ts"
      provides: "v3 shape 회귀 테스트 — schemaVersion + selector ladder 검증"
      min_lines: 40
    - path: "packages/devtools/src/panel.ts"
      provides: "SelectorLadder JSON 직렬화 렌더"
      contains: "JSON.stringify(target.selector"
  key_links:
    - from: "packages/runtime/src/runtime/snapshot.ts:captureTarget"
      to: "packages/core/src/index.ts:PageTarget.selector"
      via: "타입 일치 (SelectorLadder from @agrune/manifest)"
      pattern: "selector:\\s*descriptor\\.target\\.selector"
    - from: "packages/core/src/index.ts:PageSnapshot"
      to: "literal schemaVersion: 3"
      via: "breaking bump 프로토콜 버전"
      pattern: "schemaVersion:\\s*3"
---

<objective>
Phase 11이 남긴 임시 직렬화(`JSON.stringify(ladder)`)를 PageSnapshot v3 shape로 교체해 selector가 객체로 통과되게 만든다. `PageSnapshot.schemaVersion: 3` 리터럴 필드를 추가해 프로토콜 버전을 명시하고, 기존 `version` 카운터는 그대로 유지한다.

Purpose: Phase 12의 모든 후속 작업(prepareSession preload, agrune_manifest_load)이 v3 shape 위에서 돌도록 기반을 먼저 닫는다. 이 plan 1 후에 core types가 안정되어야 browser/mcp layer가 오염 없이 착수 가능.
Output: `@agrune/core` PageTarget.selector가 SelectorLadder 타입, PageSnapshot.schemaVersion: 3, snapshot.ts captureTarget이 객체 직접 반환, devtools panel이 JSON.stringify로 렌더, snapshot-v3.spec.ts 신규 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-inject/12-CONTEXT.md
@.planning/phases/12-inject/12-RESEARCH.md
@.planning/phases/11-manifest/11-03-SUMMARY.md

<interfaces>
<!-- Executor가 코드베이스를 뒤지지 않도록 관련 계약을 직접 제공 -->

packages/core/src/index.ts (현재 상태):
```typescript
import type { ActionKind } from './manifest.js'
export type { ActionKind } from './manifest.js'

export interface PageTarget {
  targetId: string
  groupId: string
  groupName?: string
  groupDesc?: string
  name: string
  description: string
  actionKinds: ActionKind[]
  selector: string              // ← 변경 대상: SelectorLadder 로 교체
  // ... (기타 필드 유지)
}

export interface PageSnapshot {
  version: number               // ← 유지 (카운터)
  capturedAt: number
  url: string
  title: string
  groups: PageSnapshotGroup[]
  targets: PageTarget[]
  // ← schemaVersion: 3 리터럴 필드 신규 추가
}

export * from './manifest.js'   // SelectorLadder, AgruneManifest 등 re-export (Phase 11-03 완료)
```

packages/manifest/src/schema.ts (이미 존재 — 수정 불필요):
```typescript
export type SelectorLadder = AtLeastOne<{
  role: { name: string; level?: string }
  text: string
  testId: string
  attr: string
  css: string
}>
```

packages/runtime/src/runtime/snapshot.ts:406 (Phase 11-03에서 도입한 임시 직렬화):
```typescript
// Phase 12에서 PageSnapshot v3 shape으로 교체 예정 — 현재는 JSON.stringify로 serialize
selector: JSON.stringify(descriptor.target.selector),
```
→ 이 줄을 `selector: descriptor.target.selector,` 로 교체해야 함.

packages/devtools/src/panel.ts:232 (현재 `[object Object]` 문제 발생할 코드):
```typescript
<tr><td>selector</td><td style="color:#89dceb;font-size:9px;">${target.selector}</td></tr>
```
→ `${JSON.stringify(target.selector)}` 로 교체.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: @agrune/core PageTarget.selector → SelectorLadder + PageSnapshot.schemaVersion: 3 추가</name>
  <files>packages/core/src/index.ts, packages/runtime/tests/snapshot-v3.spec.ts</files>
  <read_first>
    - packages/core/src/index.ts (전체 — PageTarget, PageSnapshot 인터페이스 위치 확인)
    - packages/core/src/manifest.ts (SelectorLadder re-export 확인)
    - packages/manifest/src/schema.ts (SelectorLadder 정의)
    - .planning/phases/12-inject/12-RESEARCH.md:244-290 (Section 4: PageSnapshot.version 현황 + Option A 권장)
    - packages/runtime/tests/v3-descriptor.spec.ts (기존 v3 테스트 패턴 참조)
  </read_first>
  <behavior>
    - Test 1: `PageTarget.selector` 가 `SelectorLadder` 타입이며 `{ css: '.foo' }` 같은 객체가 할당 가능 (컴파일 타임 타입 검증)
    - Test 2: `PageTarget.selector = 'string'` 형태는 TS 에러 (`@ts-expect-error` 음성 테스트)
    - Test 3: `PageSnapshot.schemaVersion` 필드가 존재하며 `3` 이외의 값은 타입 에러 (`@ts-expect-error`)
    - Test 4: `PageSnapshot.version` 이 여전히 `number` 타입으로 존재 (카운터 의미 보존)
    - Test 5: role/text/testId/attr/css SelectorLadder 변형 전부 PageTarget.selector에 할당 가능
  </behavior>
  <action>
    1. packages/core/src/index.ts 수정:
       - 파일 상단 `import type { ActionKind } from './manifest.js'` 아래에 `import type { SelectorLadder } from './manifest.js'` 를 명시적으로 추가 (PageTarget 선언 이전에 심볼이 resolved 되도록 — Phase 11-03 ActionKind forward-reference 문제 패턴 재사용)
       - `export type { SelectorLadder } from './manifest.js'` 도 추가 (barrel re-export 일관성)
       - `PageTarget` 인터페이스에서 `selector: string` → `selector: SelectorLadder` 로 교체
       - `PageSnapshot` 인터페이스에 `schemaVersion: 3` 리터럴 필드 추가 (version 필드는 number 그대로 유지). 위치: `version: number` 바로 아래
       - 주석으로 `/** Protocol schema version. v3 = SelectorLadder ladder on PageTarget.selector. Breaking change vs v2 (see Phase 12 RESOLVE-03). */` 를 schemaVersion 위에 명시
    2. packages/runtime/tests/snapshot-v3.spec.ts 신규 생성 (최소 5개 테스트):
       - `describe('PageSnapshot v3 types')` 안에 위 behavior의 5개 케이스 전부 cover
       - 파일 형식은 Vitest — `import { describe, it, expectTypeOf, expect } from 'vitest'` 또는 `describe/it/expect` + `// @ts-expect-error`
       - 실제 런타임 검사 + 타입 전용 검사 둘 다 포함. 예: `const t: PageTarget = { selector: { css: '.foo' }, ... }` 같이 type assertion으로 검증.
    3. 주의: 이 Task는 core만 바꿔서 typecheck가 빨간불이 될 것 — Task 2에서 runtime/snapshot.ts를 수정해 복구. 그 사이 `pnpm --filter @agrune/core run build` 는 PASS 해야 함 (독립 패키지).
  </action>
  <verify>
    <automated>pnpm --filter @agrune/core run typecheck &amp;&amp; pnpm --filter @agrune/core run build &amp;&amp; pnpm --filter @agrune/runtime run test -- snapshot-v3</automated>
  </verify>
  <acceptance_criteria>
    - grep `"schemaVersion: 3"` → matches 2+ (인터페이스 선언 + 주석 본문/테스트)
    - grep `"selector: SelectorLadder"` → matches 1+ (PageTarget 선언)
    - grep `"selector: string"` packages/core/src/index.ts → 0 matches (PageTarget context)
    - grep `"import type { SelectorLadder }"` packages/core/src/index.ts → 1 match
    - packages/runtime/tests/snapshot-v3.spec.ts 존재 + `wc -l` ≥ 40
    - `pnpm --filter @agrune/core run typecheck` exit 0
    - `pnpm --filter @agrune/core run build` exit 0
  </acceptance_criteria>
  <done>
    PageTarget.selector 타입이 SelectorLadder 로 교체됐고, PageSnapshot.schemaVersion: 3 이 추가됐다. 신규 snapshot-v3.spec.ts 가 타입 계약을 회귀 테스트로 가둔다. @agrune/core typecheck + build PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: snapshot.ts captureTarget() selector 객체 반환 + schemaVersion wire-up + devtools panel 렌더링 fix</name>
  <files>packages/runtime/src/runtime/snapshot.ts, packages/devtools/src/panel.ts, packages/runtime/tests/runtime.spec.ts, packages/runtime/tests/fill-cdp.spec.ts, packages/mcp/tests/public-shapes.spec.ts</files>
  <read_first>
    - packages/runtime/src/runtime/snapshot.ts (전체 — 특히 400-430 captureTarget 반환 블록 + makeSnapshot 함수)
    - packages/devtools/src/panel.ts:225-245 (selector 렌더 영역)
    - packages/runtime/tests/runtime.spec.ts (기존 expectTargetSnapshot 같은 헬퍼 찾기 — selector를 문자열로 비교하는지)
    - packages/runtime/tests/fill-cdp.spec.ts (selector 관련 assertion 확인)
    - packages/mcp/tests/public-shapes.spec.ts (PageSnapshot fixture가 schemaVersion 필드 필요한지)
    - .planning/phases/12-inject/12-RESEARCH.md:348-395 (Section 6: consumer 목록)
  </read_first>
  <behavior>
    - Test 1: `makeSnapshot()` 반환값에 `schemaVersion === 3` 필드가 존재 + `version: number` 카운터도 유지
    - Test 2: `captureTarget(...)` 이 `selector: { css: '.btn' }` 같은 객체를 반환 (문자열이 아님) — `typeof snap.targets[0].selector === 'object'`
    - Test 3: SelectorLadder role 변형이 snapshot에 그대로 반영 — 예: `{ role: { name: 'button' } }` 입력 → 출력 일치
    - Test 4: devtools panel HTML이 `[object Object]` 를 포함하지 않음 (JSON.stringify 경유)
    - Test 5: 기존 makeSnapshot signature 계산은 변경 없음 (Research Section 6 마지막 — selector는 signature에 포함되지 않음)
  </behavior>
  <action>
    1. packages/runtime/src/runtime/snapshot.ts 수정:
       - line 406 근처 `selector: JSON.stringify(descriptor.target.selector),` → `selector: descriptor.target.selector,` 로 교체. (이제 PageTarget.selector가 SelectorLadder 타입이므로 타입 일치)
       - 해당 줄 위의 `// Phase 12에서 PageSnapshot v3 shape으로 교체 예정 ...` 주석 삭제
       - `makeSnapshot()` 함수가 반환하는 PageSnapshot 객체에 `schemaVersion: 3 as const` 필드 추가. (검색: `return {` 근처, `version: store.version` 다음 줄 또는 동일 객체 리터럴 안)
       - signature 계산 로직은 변경 금지 (Research Section 6 마지막 확인 — selector는 signature에 포함되지 않음)
    2. packages/devtools/src/panel.ts 수정:
       - line 232: `${target.selector}` → `${JSON.stringify(target.selector)}` 로 교체
       - 다른 위치에 `target.selector` 를 문자열처럼 직접 사용하는 곳이 있다면 동일하게 수정
    3. packages/runtime/tests/runtime.spec.ts 수정:
       - 기존 테스트에서 `selector` 를 문자열로 비교하는 assertion이 있으면 SelectorLadder 객체 비교로 교체 (예: `expect(target.selector).toBe('...')` → `expect(target.selector).toEqual({ css: '...' })`)
       - `makeSnapshot()` 반환 fixture가 `schemaVersion: 3` 을 기대하는지 assertion 추가 (최소 1개)
    4. packages/runtime/tests/fill-cdp.spec.ts 수정:
       - 동일하게 selector assertion + schemaVersion 확인 반영
    5. packages/mcp/tests/public-shapes.spec.ts 수정:
       - PageSnapshot fixture 빌드 시 `schemaVersion: 3` 필드 추가 (타입 에러 방지). 기존 PageTarget fixture의 selector가 string이면 SelectorLadder 객체로 교체
    6. 전체 테스트 스위트를 돌려서 다른 누락 fixture 있으면 함께 수정. TypeScript 컴파일러가 missing field를 전부 알려줄 것 — 그 리스트만큼 수정.
  </action>
  <verify>
    <automated>pnpm --filter @agrune/runtime run typecheck &amp;&amp; pnpm --filter @agrune/runtime run test &amp;&amp; pnpm --filter @agrune/devtools run typecheck &amp;&amp; pnpm --filter @agrune/mcp run typecheck &amp;&amp; pnpm --filter @agrune/mcp run test -- public-shapes</automated>
  </verify>
  <acceptance_criteria>
    - grep `"JSON.stringify(descriptor.target.selector)"` packages/runtime/src/ → 0 matches
    - grep `"selector: descriptor.target.selector"` packages/runtime/src/runtime/snapshot.ts → 1+ match
    - grep `"schemaVersion: 3"` packages/runtime/src/runtime/snapshot.ts → 1 match
    - grep `"\${target.selector}"` packages/devtools/src/panel.ts → 0 matches
    - grep `"JSON.stringify(target.selector)"` packages/devtools/src/panel.ts → 1+ match
    - `pnpm --filter @agrune/runtime run test` 전체 PASS (이전 Phase 11에서 115/115 기준) + snapshot-v3.spec.ts 신규 테스트 포함 PASS
    - `pnpm --filter @agrune/devtools run typecheck` exit 0
    - `pnpm --filter @agrune/mcp run typecheck` exit 0 (public-shapes.ts consumer)
    - `grep -rn "version: 2" packages/runtime/src packages/core/src` → 0 matches (주석 제외)
  </acceptance_criteria>
  <done>
    runtime snapshot.ts captureTarget이 SelectorLadder 객체를 그대로 반환하고, makeSnapshot 결과에 schemaVersion: 3 필드가 포함된다. devtools panel이 selector를 JSON.stringify로 렌더링한다. runtime + devtools + mcp 패키지 전부 typecheck + 테스트 PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime → devtools UI | PageTarget.selector가 이제 객체 — XSS/injection이 변경되는지 |
| runtime → MCP output | public-shapes.ts의 toPublicTarget이 selector를 이미 제외하므로 external trust boundary에 직접 노출 안 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | devtools panel.ts selector 렌더 | mitigate | `JSON.stringify` 사용 — 임의 문자열이 DOM innerHTML로 들어가지만 panel 전체가 이미 trusted author HTML 내부. 단 selector 객체에 `<script>` 같은 값이 있어도 브라우저 HTML parser가 처리 (MANIFEST-05 schema validator가 이미 nth-child / hash-class 등 제한). 추가 escape 불필요 — 기존 panel.ts가 이미 textContent 아닌 innerHTML 패턴이며 selector 값은 author manifest에서 옴 (trust 내부) |
| T-12-02 | Information Disclosure | public-shapes.ts MCP output에 selector 노출 | accept | 현재 `toPublicTarget()`이 selector 필드를 의도적으로 제외 중. v3 변경 후에도 제외 유지. AI 에이전트는 targetId 로만 작업 |
| T-12-03 | Tampering | SelectorLadder 객체 → snapshot 역직렬화 (CDP returnByValue) | accept | SelectorLadder는 interface이지 class 아님 → instanceof 없이 property 접근만 함. Research Section Pitfall 4 확인. JSON-serializable plain object |
</threat_model>

<verification>
전체 관문:
- `pnpm --filter @agrune/core run typecheck` PASS
- `pnpm --filter @agrune/core run build` PASS
- `pnpm --filter @agrune/runtime run typecheck` PASS
- `pnpm --filter @agrune/runtime run test` 전체 PASS (115+ tests)
- `pnpm --filter @agrune/devtools run typecheck` PASS
- `pnpm --filter @agrune/mcp run typecheck` PASS
- `grep -rn "JSON.stringify(descriptor.target.selector)" packages/` → 0 matches
- `grep -rn "selector: string" packages/core/src/` → 0 matches (PageTarget context)
- `grep -rn "schemaVersion: 3" packages/` → matches in core/src/index.ts + runtime/src/runtime/snapshot.ts + 테스트 파일들
</verification>

<success_criteria>
RESOLVE-03 부분 달성 (PageSnapshot v3 shape 완성):
1. PageTarget.selector: SelectorLadder 객체 (string 아님)
2. PageSnapshot.schemaVersion: 3 리터럴 존재
3. runtime/snapshot.ts captureTarget JSON.stringify 제거 완료
4. devtools panel UI [object Object] 미발생
5. 기존 테스트 115/115 + 신규 snapshot-v3.spec.ts 5+ 테스트 전부 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/12-inject/12-01-SUMMARY.md` summarizing:
- What Was Built (각 파일별 변경사항)
- Verification Results (typecheck + 테스트 결과 테이블)
- Threat Model Coverage (T-12-01/02/03 disposition 확인)
- Known Stubs (Phase 17에서 제거할 v2 legacy 없음 — 이 plan은 완전한 breaking change)
- Commits 테이블
</output>
