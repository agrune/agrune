---
phase: 11-manifest
plan: 04
type: execute
wave: 3
depends_on: [11-03]
files_modified:
  - packages/browser/src/cdp-runtime-injector.ts
  - packages/runtime/src/page-runtime.ts
  - packages/runtime/src/runtime/page-agent-runtime.ts
  - packages/runtime/tests/bootstrap-gate.spec.ts
  - packages/e2e/fixtures/idle-boot.html
  - packages/e2e/fixtures/legacy-annotated.html
  - packages/e2e/tests/bootstrap-idle.spec.ts
  - packages/e2e/package.json
autonomous: true
requirements:
  - RESOLVE-04
tags: [bootstrap, cdp, runtime, idle, breaking-change]

must_haves:
  truths:
    - "Runtime 이 `data-agrune-*` 속성 존재 여부와 무관하게 항상 부팅한다 — `hasAnnotations()` 게이트와 `if (!hasAnnotations()) return` 이 소스에서 제거된다."
    - "Manifest 가 `window.__agrune_manifest__` 또는 `window.__agrune_preload_manifest__` 에 없을 때 runtime 은 `idle` 상태로 부팅하고 `runtime_ready` 이벤트에 `{ hasManifest: false }` 를 동봉해 post 한다."
    - "runtime bootstrap 이 `window.__agrune_runtime_state__ = { hasManifest, source }` 를 세팅하여 E2E 테스트가 `page.evaluate` 로 가시성을 확인할 수 있다."
    - "Manifest 가 있을 때 (legacy inline 또는 window 주입) runtime 이 기존과 동일하게 `installPageAgentRuntime(manifest)` 로 active 상태 진입."
    - "`MutationObserver` 가 `data-agrune-*` 변경을 감시하는 `installObserver` 가 제거되고, 대신 `window.__agrune_manifest__` 세팅 이벤트 또는 Phase 12 에서 추가될 manifest reload 신호로 재부팅되도록 훅 포인트 `reloadRuntime()` 이 노출된다."
    - "기존 inline-annotated demo 가 여전히 동작한다 — legacy `collectLiveDescriptors()` + `buildManifest()` 경로가 Plan 03 에서 v3 shape 로 이미 반환되므로 idle → active 전환 시 사용 가능."
    - "`pnpm --filter @agrune/browser run test && pnpm --filter @agrune/runtime run test && pnpm --filter @agrune/e2e run test:e2e` 가 통과한다."
  artifacts:
    - path: "packages/browser/src/cdp-runtime-injector.ts"
      provides: "bootstrap 소스에서 hasAnnotations 게이트 제거, 항상 installRuntime 실행, window.__agrune_runtime_state__ 세팅"
      contains: "hasManifest"
    - path: "packages/runtime/src/page-runtime.ts"
      provides: "idle 상태 부팅 지원 — buildEmptyManifest 또는 skip-install 분기"
      contains: "buildEmptyManifest"
    - path: "packages/runtime/tests/bootstrap-gate.spec.ts"
      provides: "bootstrap 게이트 부재 단위 테스트 — hasAnnotations/installObserver/mutationTouchesAnnotations 소스에서 grep 0 건"
      min_lines: 40
    - path: "packages/e2e/tests/bootstrap-idle.spec.ts"
      provides: "manifest 없는 페이지가 기동 후 window.__agrune_runtime_state__ 를 설정하는지 Playwright page.evaluate E2E"
      min_lines: 50
    - path: "packages/e2e/fixtures/idle-boot.html"
      provides: "no-annotation fixture — runtime bundle 스크립트 포함, idle 부팅 대상"
      contains: "agrune idle boot fixture"
    - path: "packages/e2e/fixtures/legacy-annotated.html"
      provides: "legacy annotated fixture — runtime bundle 스크립트 포함, inline 소스 부팅 대상"
      contains: "data-agrune-action"
  key_links:
    - from: "packages/browser/src/cdp-runtime-injector.ts"
      to: "packages/runtime/dist/page-runtime.global.js"
      via: "addScriptToEvaluateOnNewDocument"
      pattern: "addScriptToEvaluateOnNewDocument"
    - from: "bootstrap source"
      to: "window.__agrune_manifest__ / window.__agrune_preload_manifest__"
      via: "manifest 소스 우선순위"
      pattern: "__agrune_manifest__"
    - from: "installRuntime"
      to: "runtimeApi.installPageAgentRuntime"
      via: "항상 호출 (빈 manifest 든 실 manifest 든)"
      pattern: "installPageAgentRuntime"
    - from: "bootstrap source"
      to: "window.__agrune_runtime_state__"
      via: "Object.defineProperty writable:false 로 { hasManifest, source } 노출"
      pattern: "__agrune_runtime_state__"
    - from: "packages/e2e/tests/bootstrap-idle.spec.ts"
      to: "window.__agrune_runtime_state__"
      via: "page.evaluate(() => window.__agrune_runtime_state__)"
      pattern: "__agrune_runtime_state__"
---

<objective>
`data-agrune-*` 어노테이션 기반 bootstrap 게이트를 제거해 runtime 이 페이지 종류와 무관하게 항상 부팅하도록 만든다. Manifest 가 없으면 idle 상태, 있으면 active 상태. `window.__agrune_manifest__` (향후 owned 앱) 또는 `window.__agrune_preload_manifest__` (CDP preload, Phase 12) 중 어느 소스에서도 manifest 로드 가능하도록 스켈레톤을 배치한다.

Purpose: RESOLVE-04 는 manifest pivot 의 전제 — 외부 사이트 (YouTube 등) 는 `data-agrune-*` 를 추가할 수 없어 현재 architecture 에서는 bootstrap 조차 못 한다. 게이트를 제거하고 idle-boot 을 기본값으로 만들어야 Phase 12의 manifest preload 가 의미 있다.

Output:
- `packages/browser/src/cdp-runtime-injector.ts` 의 `buildBootstrapSource()` 에서 `hasAnnotations()` 게이트 제거, `installObserver`/`mutationTouchesAnnotations` 제거
- bootstrap 이 `window.__agrune_runtime_state__ = { hasManifest, source }` 를 tamper-proof (writable:false) 로 설정하여 test 가시성 확보
- `installRuntime()` 이 항상 실행되고 manifest 소스 검색 후 있으면 active, 없으면 idle
- `packages/runtime/src/page-runtime.ts` 가 `buildEmptyManifest()` export 또는 `installPageAgentRuntime(null)` idle 경로 지원
- 단위 테스트 + Playwright E2E (`page.evaluate` 기반) 로 idle boot 증명
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-manifest/11-CONTEXT.md
@.planning/phases/11-manifest/11-RESEARCH.md
@.planning/phases/11-manifest/11-03-PLAN.md

@packages/browser/src/cdp-runtime-injector.ts
@packages/runtime/src/page-runtime.ts
@packages/runtime/src/runtime/page-agent-runtime.ts
@packages/e2e/playwright.config.ts
@packages/e2e/package.json
@packages/e2e/tests/annotation-scan.spec.ts
@packages/e2e/fixtures/overlay-modal.html

<interfaces>
<!-- 현재 bootstrap 소스 구조 -->

From packages/browser/src/cdp-runtime-injector.ts:buildBootstrapSource (line 35-200):
```javascript
// 현재 버전
const selectors = [
  '[data-agrune-action]', '[data-agrune-group]',
  '[data-agrune-canvas]', '[data-agrune-meta]',
];
const hasAnnotations = () => selectors.some(s => document.querySelector(s) !== null);
const installRuntime = () => {
  if (!hasAnnotations()) return;  // ← 제거 대상
  const manifest = runtimeApi.buildManifest(
    runtimeApi.scanAnnotations(document),
    runtimeApi.scanGroups(document),
  );
  runtimeApi.installPageAgentRuntime(manifest, { cdpPostMessage: ... });
  post('runtime_ready', {});
};
const installObserver = new MutationObserver((mutations) => {
  if (mutations.some(m => mutationTouchesAnnotations(m))) scheduleInstall();
});
```

<!-- 기존 E2E 테스트 패턴 (확정) -->

From packages/e2e/tests/annotation-scan.spec.ts:
```typescript
import { test, expect } from '@playwright/test'

const SKIP = process.env.PLAYWRIGHT_SKIP_E2E === '1'

test.describe('annotation scan — real browser', () => {
  test.skip(SKIP, 'PLAYWRIGHT_SKIP_E2E=1 set — run `pnpm test:e2e:install` to enable locally')

  test('overlay-modal fixture yields 4 targets and 2 groups', async ({ page }) => {
    await page.goto('http://127.0.0.1:5555/overlay-modal.html')
    const result = await page.evaluate(() => { ... })
    expect(result.actionCount).toBe(4)
  })
})
```

E2E 테스트는 `CdpDriver` 를 spawn 하지 않고 Playwright 가 직접 Chrome 을 띄워 `page.goto('http://127.0.0.1:5555/...')` + `page.evaluate` 로 검증한다. `CdpDriver.on('cdp_event', ...)` 같은 API 는 존재하지 않음 (CdpDriver 는 `sessionOpenCbs`, `snapshotUpdateCbs`, `onActivity` 같은 구체적 callback slot 만 가짐).

From packages/e2e/playwright.config.ts:
- fixture 서버: `http://127.0.0.1:5555/`, `packages/e2e/fixtures/` 를 정적 파일로 서빙
- content-type: `.html → text/html`, `.js → text/javascript`, 나머지 `text/plain`
- `webServer.reuseExistingServer: true` (non-CI)

<!-- Plan 03 이후 runtime API -->

From packages/runtime/src/page-runtime.ts (예상 shape):
```typescript
declare global {
  interface Window {
    __agrune_runtime__: {
      installPageAgentRuntime: (manifest: AgruneManifest, opts: {...}) => void
      scanAnnotations: (doc: Document) => ScannedTarget[]
      scanGroups: (doc: Document) => ScannedGroup[]
      buildManifest: (targets: ScannedTarget[], groups: ScannedGroup[]) => AgruneManifest
      // 이 플랜에서 추가:
      buildEmptyManifest?: () => AgruneManifest
      reloadRuntime?: () => void
    }
    // 이 플랜에서 추가 (test 가시성용):
    __agrune_runtime_state__?: { hasManifest: boolean; source: 'idle' | 'inline' | 'window' | 'preload' }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bootstrap 소스에서 hasAnnotations 게이트 제거 + idle-boot 구현 + page-runtime 훅 + __agrune_runtime_state__ 노출</name>
  <files>
    packages/browser/src/cdp-runtime-injector.ts,
    packages/runtime/src/page-runtime.ts,
    packages/runtime/src/runtime/page-agent-runtime.ts,
    packages/runtime/tests/bootstrap-gate.spec.ts
  </files>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/browser/src/cdp-runtime-injector.ts (전체, 특히 buildBootstrapSource line 35-200)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/page-runtime.ts (현재 export)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/runtime/page-agent-runtime.ts (installPageAgentRuntime signature)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/index.ts (export 구조)
    - /Users/chenjing/dev/agrune/agrune/.planning/phases/11-manifest/11-RESEARCH.md (섹션 2)
  </read_first>
  <behavior>
    - `packages/browser/src/cdp-runtime-injector.ts` 의 JS 소스 문자열에 `hasAnnotations` / `installObserver` / `mutationTouchesAnnotations` 식별자가 더 이상 포함되지 않는다.
    - `installRuntime()` 이 manifest 를 `window.__agrune_manifest__` → `window.__agrune_preload_manifest__` → legacy inline scan 순으로 찾되, 모두 없으면 `buildEmptyManifest()` 로 idle 부팅.
    - `post('runtime_ready', { hasManifest: boolean, source: 'idle' | 'inline' | 'window' | 'preload' })` 를 post 함.
    - **`post('runtime_ready', ...)` 호출 직후 `window.__agrune_runtime_state__` 를 `Object.defineProperty` 로 `writable:false, configurable:false` 로 설정** (test 가시성 + tamper-proof).
    - `buildEmptyManifest()` 는 `{ version: 3, groups: [] }` 반환, `@agrune/runtime` 의 public API (`window.__agrune_runtime__.buildEmptyManifest`) 로 노출.
    - `reloadRuntime()` 훅이 `window.__agrune_runtime__` 에 노출되어 Phase 12 의 manifest 주입 후 재부팅을 가능케 한다.
    - `mutationTouchesAnnotations` 대신 `MutationObserver` 는 DOM 변경 시 snapshot dispatch 만 수행 (installer 재시도 제거).
    - bootstrap 게이트 spec 이 regex 로 금지 문자열 소거를 확인.
  </behavior>
  <action>
    **1. `packages/runtime/src/runtime/page-agent-runtime.ts` 확인 및 업데이트**:

    - 현재 `installPageAgentRuntime(manifest, options)` 가 manifest 를 필수로 받는지 확인
    - manifest 가 빈 groups 인 경우 (idle) 도 정상 동작해야 함 — 이미 `manifest.groups` 가 빈 배열이면 descriptor 0개라 idle. 추가 분기 불필요.

    **2. `packages/runtime/src/page-runtime.ts` 에 `buildEmptyManifest` 추가**:

    현재 export 구조 확인 후 다음을 추가:
    ```typescript
    // packages/runtime/src/page-runtime.ts 내부
    import type { AgruneManifest } from './types'

    export function buildEmptyManifest(): AgruneManifest {
      return { version: 3, groups: [] }
    }
    ```

    그리고 `__agrune_runtime__` 글로벌 객체 export (tsup iife globalName) 이 `buildEmptyManifest` 도 포함하도록 수정. tsup config 는 `globalName: '__agrune_runtime__'` 이고 iife build 가 `src/page-runtime.ts` 의 export 를 자동 전역화한다 [VERIFIED: tsup.config.ts].

    export 된 함수 리스트에 `buildEmptyManifest` 추가:
    ```typescript
    export {
      installPageAgentRuntime,
      scanAnnotations,
      scanGroups,
      buildManifest,
      buildEmptyManifest,
    }
    ```

    또한 `reloadRuntime` 훅 제공:
    ```typescript
    // page-agent-runtime 가 관리하는 싱글턴 reference
    let __currentRuntimeHandle: { reload: () => void } | null = null

    export function setCurrentRuntimeHandle(handle: { reload: () => void } | null): void {
      __currentRuntimeHandle = handle
    }

    export function reloadRuntime(): void {
      __currentRuntimeHandle?.reload()
    }
    ```

    `installPageAgentRuntime` 이 `setCurrentRuntimeHandle` 을 호출하도록 수정 (bootstrap source 가 reload 신호를 보낼 수 있게). `reload` 구현은 runtime 내부 상태 초기화 + 새 manifest 로 재설치. Phase 12에서 구체화 — 이 플랜에서는 stub ok (no-op + TODO comment).

    **3. `packages/browser/src/cdp-runtime-injector.ts` 의 `buildBootstrapSource()` 를 다음으로 교체**:

    ```javascript
    function buildBootstrapSource(): string {
      return `
    ;(() => {
      const runtimeApi = globalThis.__agrune_runtime__;
      const apiKey = ${JSON.stringify(QUICK_MODE_RUNTIME_KEY)};
      const bindingName = 'agrune_send';
      const debounceMs = 50;
      const snapshotIntervalMs = 800;

      if (
        !runtimeApi ||
        typeof runtimeApi.installPageAgentRuntime !== 'function' ||
        typeof runtimeApi.buildEmptyManifest !== 'function'
      ) {
        throw new Error('Agrune runtime exports are not available in CDP quick mode.');
      }

      const post = (type, data) => {
        const binding = window[bindingName];
        if (typeof binding === 'function') {
          binding(JSON.stringify({ type, data }));
        }
      };

      const getRuntime = () => window.agruneDom ?? null;
      const isRuntimeBusy = () => {
        const runtime = getRuntime();
        if (!runtime) return false;
        if (typeof runtime.isBusy === 'function') return runtime.isBusy();
        if (typeof runtime.isActive === 'function') return runtime.isActive();
        return false;
      };

      let installTimer = null;
      let snapshotTimer = null;
      let pendingInstall = false;
      let currentConfig = {};

      const dispatchSnapshot = () => {
        if (window.agruneDom && typeof window.agruneDom.getSnapshot === 'function') {
          post('snapshot_update', window.agruneDom.getSnapshot());
        }
      };

      const resolveManifest = () => {
        // Priority: owned-app injected > CDP preload > legacy inline scan > empty (idle)
        if (window.__agrune_manifest__) return { manifest: window.__agrune_manifest__, hasManifest: true, source: 'window' };
        if (window.__agrune_preload_manifest__) return { manifest: window.__agrune_preload_manifest__, hasManifest: true, source: 'preload' };
        // Legacy inline-scan path — maintained until Phase 17 (REMOVE-01).
        if (typeof runtimeApi.scanAnnotations === 'function' &&
            typeof runtimeApi.scanGroups === 'function' &&
            typeof runtimeApi.buildManifest === 'function') {
          try {
            const legacyManifest = runtimeApi.buildManifest(
              runtimeApi.scanAnnotations(document),
              runtimeApi.scanGroups(document),
            );
            if (legacyManifest && Array.isArray(legacyManifest.groups) && legacyManifest.groups.some(g => g.targets && g.targets.length > 0)) {
              return { manifest: legacyManifest, hasManifest: true, source: 'inline' };
            }
          } catch (e) { /* fall through to idle */ }
        }
        return { manifest: runtimeApi.buildEmptyManifest(), hasManifest: false, source: 'idle' };
      };

      const installRuntime = () => {
        const resolved = resolveManifest();
        runtimeApi.installPageAgentRuntime(resolved.manifest, {
          cdpPostMessage: (type, data) => post(type, data),
        });

        if (window.agruneDom && typeof window.agruneDom.applyConfig === 'function') {
          window.agruneDom.applyConfig(currentConfig);
        }

        post('runtime_ready', { hasManifest: resolved.hasManifest, source: resolved.source });

        // Expose runtime state for test visibility (Playwright page.evaluate).
        // tamper-proof: writable:false, configurable:false. Idempotent —
        // if already defined (reload), overwrite via redefine after delete.
        try {
          if (Object.getOwnPropertyDescriptor(window, '__agrune_runtime_state__')) {
            // Re-expose on reload — bootstrap-controlled lifecycle.
            Object.defineProperty(window, '__agrune_runtime_state__', {
              value: { hasManifest: resolved.hasManifest, source: resolved.source },
              writable: false,
              configurable: true, // allow next reload to redefine
              enumerable: true,
            });
          } else {
            Object.defineProperty(window, '__agrune_runtime_state__', {
              value: { hasManifest: resolved.hasManifest, source: resolved.source },
              writable: false,
              configurable: true,
              enumerable: true,
            });
          }
        } catch (e) { /* defensive: don't crash bootstrap on sealed globals */ }

        if (resolved.hasManifest) dispatchSnapshot();
      };

      const flushInstall = () => {
        if (!pendingInstall) return;
        if (isRuntimeBusy()) { scheduleInstall(); return; }
        pendingInstall = false;
        installRuntime();
      };

      const scheduleInstall = () => {
        if (installTimer !== null) clearTimeout(installTimer);
        pendingInstall = true;
        installTimer = setTimeout(() => {
          installTimer = null;
          flushInstall();
        }, debounceMs);
      };

      const scheduleSnapshot = () => {
        if (snapshotTimer !== null) clearTimeout(snapshotTimer);
        snapshotTimer = setTimeout(() => {
          snapshotTimer = null;
          dispatchSnapshot();
        }, debounceMs);
      };

      // General snapshot observer — fires on ANY DOM change. Previously this also
      // triggered installer retries (annotation-specific). Post-Phase-11 the
      // runtime boots once on DOMContentLoaded; manifest changes arrive via
      // window.__agrune_manifest__ reassignment (Phase 12+) and trigger
      // runtimeApi.reloadRuntime() explicitly.
      const snapshotObserver = new MutationObserver(() => {
        if (getRuntime()) scheduleSnapshot();
      });
      snapshotObserver.observe(document.documentElement, {
        childList: true,
        subtree: true,
        attributes: true,
        characterData: true,
      });

      // history listeners (preserved)
      const originalPushState = history.pushState;
      history.pushState = function (...args) {
        const result = originalPushState.apply(this, args);
        scheduleSnapshot();
        return result;
      };
      const originalReplaceState = history.replaceState;
      history.replaceState = function (...args) {
        const result = originalReplaceState.apply(this, args);
        scheduleSnapshot();
        return result;
      };
      window.addEventListener('popstate', scheduleSnapshot);
      window.addEventListener('hashchange', scheduleSnapshot);

      // Periodic snapshot refresh
      setInterval(() => {
        if (getRuntime()) dispatchSnapshot();
      }, snapshotIntervalMs);

      // Expose reload hook for Phase 12 manifest injection
      window[apiKey] = window[apiKey] || {};
      window[apiKey].reloadRuntime = () => {
        installRuntime();
      };
      window[apiKey].getManifestSource = () => {
        return resolveManifest().source;
      };

      // Bootstrap — always, regardless of annotations
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', installRuntime, { once: true });
      } else {
        installRuntime();
      }
    })();
    `;
    }
    ```

    **주요 변경 요약**:
    - `hasAnnotations`, `selectors`, `installObserver`, `mutationTouchesAnnotations` 완전 제거
    - `resolveManifest()` 가 window → preload → legacy inline scan → empty 순으로 조회
    - `installRuntime()` 이 항상 실행 (guard 없음)
    - `post('runtime_ready', { hasManifest, source })` — 기존 `{}` 에서 확장
    - **`window.__agrune_runtime_state__` 를 `Object.defineProperty` (writable:false, configurable:true) 로 세팅 — E2E test 가시성 + tamper-proof. configurable:true 는 reload 시 redefine 허용용.**
    - `MutationObserver` 는 snapshot dispatch 전용 (install 재시도 제거)
    - `window[apiKey].reloadRuntime` 훅 노출 — Phase 12 에서 manifest 주입 후 재부팅

    **4. `packages/runtime/tests/bootstrap-gate.spec.ts` 신규 작성**:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'
    import { join } from 'node:path'

    /**
     * Bootstrap gate removal (RESOLVE-04).
     *
     * The bootstrap source is a template literal inside
     * packages/browser/src/cdp-runtime-injector.ts. These tests ensure the
     * source no longer contains the annotation-gating logic.
     */
    const INJECTOR_PATH = join(__dirname, '..', '..', 'browser', 'src', 'cdp-runtime-injector.ts')
    const source = readFileSync(INJECTOR_PATH, 'utf-8')

    describe('cdp-runtime-injector.ts — bootstrap gate removed', () => {
      it('has no hasAnnotations symbol', () => {
        expect(source).not.toContain('hasAnnotations')
      })

      it('has no mutationTouchesAnnotations symbol', () => {
        expect(source).not.toContain('mutationTouchesAnnotations')
      })

      it('has no installObserver (annotation-specific MutationObserver retrigger)', () => {
        expect(source).not.toContain('installObserver')
      })

      it('does not gate installRuntime on annotation presence', () => {
        // `if (!hasAnnotations()) return;` must be gone
        expect(source).not.toMatch(/if\s*\(\s*!\s*hasAnnotations\s*\(\s*\)\s*\)\s*return/)
      })

      it('uses buildEmptyManifest for idle boot', () => {
        expect(source).toContain('buildEmptyManifest')
      })

      it('posts hasManifest in runtime_ready event', () => {
        expect(source).toContain("post('runtime_ready'")
        expect(source).toContain('hasManifest')
      })

      it('checks window.__agrune_manifest__ for owned-app injection', () => {
        expect(source).toContain('__agrune_manifest__')
      })

      it('checks window.__agrune_preload_manifest__ for CDP preload', () => {
        expect(source).toContain('__agrune_preload_manifest__')
      })

      it('exposes reloadRuntime hook for Phase 12 manifest injection', () => {
        expect(source).toContain('reloadRuntime')
      })

      it('exposes __agrune_runtime_state__ for test visibility via Object.defineProperty', () => {
        expect(source).toContain('__agrune_runtime_state__')
        expect(source).toContain('Object.defineProperty')
      })
    })
    ```

    주의: test 파일 위치가 `packages/runtime/tests/` 이나 실제 검증 대상은 `packages/browser/src/`. relative path `../../browser/src/...` 이 동작함 (같은 monorepo 내).

    **일단 `packages/runtime/tests/` 에 배치**하되 path 를 절대경로 보장 로직으로 작성:
    ```typescript
    const INJECTOR_PATH = join(__dirname, '..', '..', 'browser', 'src', 'cdp-runtime-injector.ts')
    ```
    → `packages/runtime/tests/../../browser/src/cdp-runtime-injector.ts` = `packages/browser/src/cdp-runtime-injector.ts` ✓

    **5. 빌드 및 테스트 실행**:
    ```bash
    pnpm --filter @agrune/runtime run build   # page-runtime.global.js 재생성 (buildEmptyManifest 포함)
    pnpm --filter @agrune/browser run build   # injector 가 runtime dist 를 copy/resolve
    pnpm --filter @agrune/runtime run test
    pnpm --filter @agrune/browser run test
    ```
  </action>
  <verify>
    <automated>
      cd /Users/chenjing/dev/agrune/agrune &&
      ! grep -q "hasAnnotations" packages/browser/src/cdp-runtime-injector.ts &&
      ! grep -q "mutationTouchesAnnotations" packages/browser/src/cdp-runtime-injector.ts &&
      ! grep -q "installObserver" packages/browser/src/cdp-runtime-injector.ts &&
      grep -q "buildEmptyManifest" packages/browser/src/cdp-runtime-injector.ts &&
      grep -q "__agrune_manifest__" packages/browser/src/cdp-runtime-injector.ts &&
      grep -q "__agrune_preload_manifest__" packages/browser/src/cdp-runtime-injector.ts &&
      grep -q "__agrune_runtime_state__" packages/browser/src/cdp-runtime-injector.ts &&
      grep -q "Object.defineProperty" packages/browser/src/cdp-runtime-injector.ts &&
      grep -q "reloadRuntime" packages/browser/src/cdp-runtime-injector.ts &&
      grep -q "buildEmptyManifest" packages/runtime/src/page-runtime.ts &&
      pnpm --filter @agrune/runtime run build &&
      pnpm --filter @agrune/browser run typecheck &&
      pnpm --filter @agrune/runtime run test
    </automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "hasAnnotations" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `! grep -q "mutationTouchesAnnotations" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `! grep -q "installObserver" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `! grep -q "if (!hasAnnotations" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `grep -q "buildEmptyManifest" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `grep -q "__agrune_manifest__" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `grep -q "__agrune_preload_manifest__" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `grep -q "__agrune_runtime_state__" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `grep -q "Object.defineProperty" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `grep -q "hasManifest" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `grep -q "reloadRuntime" packages/browser/src/cdp-runtime-injector.ts` 성공
    - `grep -q "export function buildEmptyManifest" packages/runtime/src/page-runtime.ts` 또는 등가 export 성공
    - `test -f packages/runtime/tests/bootstrap-gate.spec.ts` 성공
    - `grep -c "it(" packages/runtime/tests/bootstrap-gate.spec.ts` ≥ 9
    - `pnpm --filter @agrune/runtime run build` exit 0 (page-runtime.global.js 생성 + buildEmptyManifest 포함)
    - `grep -q "buildEmptyManifest" packages/runtime/dist/page-runtime.global.js` 성공
    - `pnpm --filter @agrune/browser run typecheck` exit 0
    - `pnpm --filter @agrune/runtime run test` exit 0 (전체 + bootstrap-gate spec)
    - `pnpm --filter @agrune/browser run test` exit 0 (기존 browser 테스트 회귀)
  </acceptance_criteria>
  <done>
    bootstrap 게이트가 소스에서 완전히 사라지고 `installRuntime` 이 항상 실행되며 manifest 없을 때 `runtime_ready { hasManifest: false, source: 'idle' }` 를 post 하고 동시에 `window.__agrune_runtime_state__` 를 tamper-proof 로 설정한다. legacy inline scan 경로는 유지되어 기존 annotated 페이지는 여전히 작동한다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright E2E — idle boot 검증 fixture + page.evaluate 기반 테스트</name>
  <files>
    packages/e2e/fixtures/idle-boot.html,
    packages/e2e/fixtures/legacy-annotated.html,
    packages/e2e/tests/bootstrap-idle.spec.ts,
    packages/e2e/package.json
  </files>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/e2e/playwright.config.ts
    - /Users/chenjing/dev/agrune/agrune/packages/e2e/package.json
    - /Users/chenjing/dev/agrune/agrune/packages/e2e/tests/annotation-scan.spec.ts
    - /Users/chenjing/dev/agrune/agrune/packages/e2e/fixtures/overlay-modal.html
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/dist/page-runtime.global.js (Task 1 산출 — buildEmptyManifest 포함 확인)
  </read_first>
  <behavior>
    - `idle-boot.html` 은 `data-agrune-*` 속성이 전혀 없는 정적 페이지 — `<script src="/runtime.bundle.js"></script>` 로 runtime 을 로드.
    - `legacy-annotated.html` 은 `data-agrune-action=click` 버튼 + group 을 가지는 페이지 — 동일하게 runtime bundle 스크립트 로드.
    - runtime bundle 은 `packages/runtime/dist/page-runtime.global.js` 를 `packages/e2e/fixtures/runtime.bundle.js` 로 pretest 시 복사. 복사는 `packages/e2e/package.json` 의 `test:e2e` script 에 엮어 pre-step 으로 실행.
    - **CdpDriver 를 spawn 하지 않는다.** Playwright `page.goto(FIXTURE_URL)` + `page.waitForFunction(() => window.__agrune_runtime_state__ !== undefined)` + `page.evaluate(() => window.__agrune_runtime_state__)` 로 검증.
    - `bootstrap-idle.spec.ts` 가 두 테스트를 수행:
      - `idle-boot.html` 로드 → `__agrune_runtime_state__` 가 `{ hasManifest: false, source: 'idle' }` 인지 확인.
      - `legacy-annotated.html` 로드 → `__agrune_runtime_state__` 가 `{ hasManifest: true, source: 'inline' }` 인지 확인.
    - `driver.on(`, `new CdpDriver`, `cdp_event` 문자열 일절 포함하지 않음.
    - `PLAYWRIGHT_SKIP_E2E=1` 환경변수가 설정되면 `annotation-scan.spec.ts` 와 동일한 패턴으로 skip.
  </behavior>
  <action>
    **1. `packages/e2e/fixtures/idle-boot.html`** 작성 — 정확히 다음 내용:

    ```html
    <!-- agrune idle boot fixture -->
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="utf-8" />
      <title>Agrune Idle Boot Fixture</title>
      <script src="/runtime.bundle.js"></script>
    </head>
    <body>
      <h1>No annotations here</h1>
      <p>This page intentionally has no data-agrune-* attributes. Runtime must still boot (idle).</p>
      <button class="plain-button">Plain button (not annotated)</button>
      <script>
        // Simulate bootstrap source inline — in real CDP quick mode the bootstrap
        // is injected by CdpRuntimeInjector. In this fixture we replicate the
        // critical path: resolveManifest() → installPageAgentRuntime() →
        // window.__agrune_runtime_state__ = { hasManifest, source }.
        (function () {
          var runtimeApi = window.__agrune_runtime__;
          if (!runtimeApi || typeof runtimeApi.buildEmptyManifest !== 'function') {
            // Fail loudly so the test surfaces a missing export instead of hanging.
            window.__agrune_runtime_state__ = { hasManifest: false, source: 'runtime-missing' };
            return;
          }
          function resolveManifest() {
            if (window.__agrune_manifest__) return { manifest: window.__agrune_manifest__, hasManifest: true, source: 'window' };
            if (window.__agrune_preload_manifest__) return { manifest: window.__agrune_preload_manifest__, hasManifest: true, source: 'preload' };
            try {
              var legacy = runtimeApi.buildManifest(
                runtimeApi.scanAnnotations(document),
                runtimeApi.scanGroups(document)
              );
              if (legacy && legacy.groups && legacy.groups.some(function (g) { return g.targets && g.targets.length > 0; })) {
                return { manifest: legacy, hasManifest: true, source: 'inline' };
              }
            } catch (e) {}
            return { manifest: runtimeApi.buildEmptyManifest(), hasManifest: false, source: 'idle' };
          }
          function install() {
            var resolved = resolveManifest();
            runtimeApi.installPageAgentRuntime(resolved.manifest, { cdpPostMessage: function () {} });
            Object.defineProperty(window, '__agrune_runtime_state__', {
              value: { hasManifest: resolved.hasManifest, source: resolved.source },
              writable: false,
              configurable: true,
              enumerable: true,
            });
          }
          if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', install, { once: true });
          } else {
            install();
          }
        })();
      </script>
    </body>
    </html>
    ```

    **주의**: fixture 의 `<script>` 블록은 의도적으로 bootstrap 로직을 축약 복제한다. CdpRuntimeInjector 의 bootstrap 소스는 CDP 환경에서만 주입되므로, 순수 Playwright page 로드 환경에서는 fixture HTML 이 직접 runtime API 를 호출해 `__agrune_runtime_state__` 를 세팅해야 한다. 이 복제는 **의도적**이며 Task 1 의 `cdp-runtime-injector.ts` 의 `resolveManifest()` / `installRuntime()` 로직과 정확히 동일해야 한다 (test 가 bootstrap 계약을 검증).

    **2. `packages/e2e/fixtures/legacy-annotated.html`** 작성 — 정확히 다음 내용:

    ```html
    <!-- agrune legacy annotated fixture (Phase 17 REMOVE 이전까지 유지) -->
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="utf-8" />
      <title>Agrune Legacy Annotated Fixture</title>
      <script src="/runtime.bundle.js"></script>
    </head>
    <body>
      <div data-agrune-group="main" data-agrune-group-name="Main">
        <button data-agrune-action="click" data-agrune-key="save" data-agrune-name="Save button">Save</button>
      </div>
      <script>
        (function () {
          var runtimeApi = window.__agrune_runtime__;
          if (!runtimeApi || typeof runtimeApi.buildEmptyManifest !== 'function') {
            window.__agrune_runtime_state__ = { hasManifest: false, source: 'runtime-missing' };
            return;
          }
          function resolveManifest() {
            if (window.__agrune_manifest__) return { manifest: window.__agrune_manifest__, hasManifest: true, source: 'window' };
            if (window.__agrune_preload_manifest__) return { manifest: window.__agrune_preload_manifest__, hasManifest: true, source: 'preload' };
            try {
              var legacy = runtimeApi.buildManifest(
                runtimeApi.scanAnnotations(document),
                runtimeApi.scanGroups(document)
              );
              if (legacy && legacy.groups && legacy.groups.some(function (g) { return g.targets && g.targets.length > 0; })) {
                return { manifest: legacy, hasManifest: true, source: 'inline' };
              }
            } catch (e) {}
            return { manifest: runtimeApi.buildEmptyManifest(), hasManifest: false, source: 'idle' };
          }
          function install() {
            var resolved = resolveManifest();
            runtimeApi.installPageAgentRuntime(resolved.manifest, { cdpPostMessage: function () {} });
            Object.defineProperty(window, '__agrune_runtime_state__', {
              value: { hasManifest: resolved.hasManifest, source: resolved.source },
              writable: false,
              configurable: true,
              enumerable: true,
            });
          }
          if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', install, { once: true });
          } else {
            install();
          }
        })();
      </script>
    </body>
    </html>
    ```

    **3. `packages/e2e/tests/bootstrap-idle.spec.ts`** 작성 — `annotation-scan.spec.ts` 패턴 그대로 복제:

    ```typescript
    import { test, expect } from '@playwright/test'

    const SKIP = process.env.PLAYWRIGHT_SKIP_E2E === '1'

    interface RuntimeState {
      hasManifest: boolean
      source: 'idle' | 'inline' | 'window' | 'preload' | 'runtime-missing'
    }

    declare global {
      interface Window {
        __agrune_runtime_state__?: RuntimeState
      }
    }

    test.describe('RESOLVE-04 — bootstrap always runs, idle when no manifest', () => {
      test.skip(SKIP, 'PLAYWRIGHT_SKIP_E2E=1 set — run `pnpm test:e2e:install` to enable locally')

      test('idle-boot.html: runtime boots idle (hasManifest=false, source=idle)', async ({ page }) => {
        await page.goto('http://127.0.0.1:5555/idle-boot.html')
        await page.waitForFunction(() => window.__agrune_runtime_state__ !== undefined, null, { timeout: 10000 })
        const state = await page.evaluate(() => window.__agrune_runtime_state__)
        expect(state).toBeDefined()
        expect(state!.hasManifest).toBe(false)
        expect(state!.source).toBe('idle')
      })

      test('legacy-annotated.html: runtime boots active via inline scan (hasManifest=true, source=inline)', async ({ page }) => {
        await page.goto('http://127.0.0.1:5555/legacy-annotated.html')
        await page.waitForFunction(() => window.__agrune_runtime_state__ !== undefined, null, { timeout: 10000 })
        const state = await page.evaluate(() => window.__agrune_runtime_state__)
        expect(state).toBeDefined()
        expect(state!.hasManifest).toBe(true)
        expect(state!.source).toBe('inline')
      })

      test('__agrune_runtime_state__ is tamper-proof (writable:false)', async ({ page }) => {
        await page.goto('http://127.0.0.1:5555/idle-boot.html')
        await page.waitForFunction(() => window.__agrune_runtime_state__ !== undefined, null, { timeout: 10000 })
        const tamperResult = await page.evaluate(() => {
          const before = window.__agrune_runtime_state__
          try {
            // @ts-expect-error — intentional tamper attempt
            window.__agrune_runtime_state__ = { hasManifest: true, source: 'window' }
          } catch (e) {
            // strict-mode throws; non-strict silently ignores — either is acceptable
          }
          const after = window.__agrune_runtime_state__
          return { beforeSource: before?.source, afterSource: after?.source }
        })
        expect(tamperResult.beforeSource).toBe('idle')
        expect(tamperResult.afterSource).toBe('idle') // unchanged
      })
    })
    ```

    **4. `packages/e2e/package.json` 업데이트** — `test:e2e` script 에 runtime bundle 복사 pre-step 추가:

    현재 script (예상):
    ```json
    "test:e2e": "playwright test"
    ```

    변경:
    ```json
    "prebuild:fixtures": "node -e \"require('fs').cpSync('../runtime/dist/page-runtime.global.js', 'fixtures/runtime.bundle.js', { force: true })\"",
    "test:e2e": "pnpm run prebuild:fixtures && playwright test"
    ```

    또는 더 명시적으로 — 별도 script 로 하고 `test:e2e` 가 의존:
    ```json
    "scripts": {
      "copy-runtime": "node -e \"require('fs').cpSync('../runtime/dist/page-runtime.global.js', 'fixtures/runtime.bundle.js', { force: true })\"",
      "test:e2e": "pnpm run copy-runtime && playwright test"
    }
    ```

    **runtime bundle 이 `window.__agrune_runtime__` 글로벌을 노출하는지 확인**: `packages/runtime/dist/page-runtime.global.js` 는 tsup iife build 로 `globalName: '__agrune_runtime__'` 이 설정되어 있다 (Plan 03 에서 확정). fixture HTML 이 스크립트 로드 후 `window.__agrune_runtime__.buildEmptyManifest` 를 호출할 수 있어야 함. 만약 빌드가 누락되었다면 Task 1 의 `pnpm --filter @agrune/runtime run build` 가 선행되어야 함.

    **5. `.gitignore` 확인** — `packages/e2e/fixtures/runtime.bundle.js` 는 빌드 산출물이므로 gitignore 에 추가:

    ```bash
    echo "packages/e2e/fixtures/runtime.bundle.js" >> .gitignore
    ```

    이미 포함되어 있으면 no-op.

    **6. 테스트 실행**:
    ```bash
    pnpm --filter @agrune/runtime run build   # page-runtime.global.js with buildEmptyManifest
    pnpm --filter @agrune/browser run build   # injector with updated bootstrap source
    pnpm --filter @agrune/e2e exec playwright install chromium  # chromium 설치 (이미 있으면 skip)
    pnpm --filter @agrune/e2e run test:e2e -- bootstrap-idle
    ```
  </action>
  <verify>
    <automated>
      cd /Users/chenjing/dev/agrune/agrune &&
      test -f packages/e2e/fixtures/idle-boot.html &&
      test -f packages/e2e/fixtures/legacy-annotated.html &&
      test -f packages/e2e/tests/bootstrap-idle.spec.ts &&
      grep -q "agrune idle boot fixture" packages/e2e/fixtures/idle-boot.html &&
      grep -q "/runtime.bundle.js" packages/e2e/fixtures/idle-boot.html &&
      grep -q "/runtime.bundle.js" packages/e2e/fixtures/legacy-annotated.html &&
      grep -q "data-agrune-action" packages/e2e/fixtures/legacy-annotated.html &&
      ! grep -q "data-agrune-" packages/e2e/fixtures/idle-boot.html &&
      grep -q "page.evaluate(() => window.__agrune_runtime_state__)" packages/e2e/tests/bootstrap-idle.spec.ts &&
      ! grep -q "driver.on(" packages/e2e/tests/bootstrap-idle.spec.ts &&
      ! grep -q "new CdpDriver" packages/e2e/tests/bootstrap-idle.spec.ts &&
      ! grep -q "cdp_event" packages/e2e/tests/bootstrap-idle.spec.ts &&
      pnpm --filter @agrune/runtime run build &&
      pnpm --filter @agrune/browser run build &&
      pnpm --filter @agrune/e2e run test:e2e -- bootstrap-idle
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/e2e/fixtures/idle-boot.html` 성공
    - `test -f packages/e2e/fixtures/legacy-annotated.html` 성공
    - `test -f packages/e2e/tests/bootstrap-idle.spec.ts` 성공
    - `grep -q "agrune idle boot fixture" packages/e2e/fixtures/idle-boot.html` 성공
    - `grep -q "/runtime.bundle.js" packages/e2e/fixtures/idle-boot.html` 성공 (runtime 스크립트 로드)
    - `grep -q "/runtime.bundle.js" packages/e2e/fixtures/legacy-annotated.html` 성공
    - `! grep -q "data-agrune-" packages/e2e/fixtures/idle-boot.html` 성공 (어노테이션 없음)
    - `grep -q "data-agrune-action" packages/e2e/fixtures/legacy-annotated.html` 성공 (어노테이션 있음)
    - `grep -c "test(" packages/e2e/tests/bootstrap-idle.spec.ts` ≥ 3
    - `grep -q "page.evaluate(() => window.__agrune_runtime_state__)" packages/e2e/tests/bootstrap-idle.spec.ts` 성공
    - `grep -q "hasManifest" packages/e2e/tests/bootstrap-idle.spec.ts` 성공
    - `grep -q "'idle'" packages/e2e/tests/bootstrap-idle.spec.ts` 성공 (source 확인)
    - `grep -q "'inline'" packages/e2e/tests/bootstrap-idle.spec.ts` 성공
    - `! grep -q "driver.on(" packages/e2e/tests/bootstrap-idle.spec.ts` 성공 (CdpDriver API 미사용)
    - `! grep -q "new CdpDriver" packages/e2e/tests/bootstrap-idle.spec.ts` 성공
    - `! grep -q "cdp_event" packages/e2e/tests/bootstrap-idle.spec.ts` 성공
    - `grep -q "copy-runtime\|prebuild:fixtures\|runtime.bundle.js" packages/e2e/package.json` 성공 (pre-step 등록됨)
    - `pnpm --filter @agrune/e2e run test:e2e -- bootstrap-idle` exit 0 (PLAYWRIGHT_SKIP_E2E unset 시 세 테스트 pass)
  </acceptance_criteria>
  <done>
    `idle-boot.html` fixture 는 어노테이션 0, `legacy-annotated.html` 은 어노테이션 있음. 두 HTML 모두 `/runtime.bundle.js` 를 로드해 Task 1 에서 도입된 runtime API (`buildEmptyManifest`, `installPageAgentRuntime` 등) 를 호출하고 `window.__agrune_runtime_state__` 를 설정한다. Playwright E2E 가 CdpDriver 없이 `page.goto` + `page.evaluate` 만으로 두 경우의 `hasManifest`/`source` 및 tamper-proof 불변성을 검증한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External site JS → `window.__agrune_manifest__` | 외부 사이트가 자체 JS 로 이 global 을 set 할 수 있음 — 신뢰 불가 |
| CDP preload JSON → `window.__agrune_preload_manifest__` | Phase 12 에서 CdpRuntimeInjector 가 `addScriptToEvaluateOnNewDocument` 로 embed → 신뢰 가능 |
| `reloadRuntime` 훅 → 악성 페이지 스크립트 재호출 | `window[apiKey].reloadRuntime` 노출로 페이지 JS 가 강제 재부팅 가능 |
| External site JS → `window.__agrune_runtime_state__` | 악성 페이지가 이 global 을 덮어써서 MCP 를 속일 수 있음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Tampering | 외부 사이트가 `window.__agrune_manifest__` 에 악성 manifest 주입 | mitigate | Phase 12 에서 `ManifestLoader` 가 `validateManifest` (Plan 01) 로 schema 검증 후에만 사용. 이 플랜에서는 source 표시 (`source: 'window' \| 'preload' \| 'inline' \| 'idle'`) 만 확정. |
| T-11-17 | Denial of Service | 악성 페이지가 `window[apiKey].reloadRuntime` 을 루프 호출 | accept | reload 는 기존 runtime 을 뒤집을 뿐 외부 권한 상승 없음. 필요 시 Phase 12 에서 rate-limit. |
| T-11-18 | Information Disclosure | `runtime_ready { source: 'inline' }` 가 민감 정보 노출? | accept | source 는 enum string — 민감 데이터 아님 |
| T-11-19 | Elevation of Privilege | idle boot 에서 MCP 가 아직 manifest 없는데 `agrune_snapshot` 호출 → empty snapshot | mitigate | installRuntime 이 idle 에서 `dispatchSnapshot` 을 호출하지 않고 `runtime_ready { hasManifest: false }` 만 post. MCP 는 hasManifest=false 응답 시 명확한 에러 반환 가능 (Phase 12 담당). |
| T-11-20 | Tampering | legacy inline scan 이 외부 사이트의 `data-agrune-*` 를 신뢰 | accept | 외부 사이트가 `data-agrune-*` 를 설정하지 않음 (실제 외부 사이트). 있더라도 resolveByLadder 금지 selector 차단이 2차 방어. |
| T-11-21 | Tampering | 악성 페이지가 `window.__agrune_runtime_state__` 를 덮어써 MCP 에게 거짓 상태 보고 | mitigate | `Object.defineProperty` 로 `writable:false` 설정. MCP 는 이 값을 신뢰 source 로 쓰지 않고 `runtime_ready` binding payload 를 사용 — `__agrune_runtime_state__` 는 test 가시성 용도로만 사용 (Phase 12 문서화). |
</threat_model>

<verification>
- `pnpm --filter @agrune/runtime run test` + `pnpm --filter @agrune/browser run test` 통과
- `pnpm --filter @agrune/e2e run test:e2e -- bootstrap-idle` 통과 (PLAYWRIGHT_SKIP_E2E unset 시)
- `grep -r "hasAnnotations\|mutationTouchesAnnotations" packages/browser/src packages/runtime/src` 0건
- `grep -r "driver.on(\|new CdpDriver" packages/e2e/tests/bootstrap-idle.spec.ts` 0건
</verification>

<success_criteria>
- RESOLVE-04: bootstrap 게이트 완전 제거, runtime 이 manifest 없어도 idle 로 부팅
- `runtime_ready` 이벤트에 `hasManifest` boolean + `source` enum 이 포함되어 Phase 12 의 MCP tool 응답에 활용 가능
- `window.__agrune_runtime_state__` 가 tamper-proof 로 노출되어 E2E 테스트가 CdpDriver 스폰 없이 Playwright `page.evaluate` 만으로 검증 가능
- legacy `data-agrune-*` 경로는 Phase 17 까지 유지되어 기존 demo 가 작동
</success_criteria>

<output>
After completion, create `.planning/phases/11-manifest/11-04-SUMMARY.md`
</output>
