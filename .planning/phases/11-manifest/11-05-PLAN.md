---
phase: 11-manifest
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - packages/mcp/package.json
  - packages/mcp/bin/agrune-mcp.ts
  - packages/mcp/src/manifest-validate-cli.ts
  - packages/mcp/src/manifest-file-loader.ts
  - packages/e2e/fixtures/validate-test.html
  - packages/e2e/fixtures/validate-manifest-good.json
  - packages/e2e/fixtures/validate-manifest-missing.json
  - packages/e2e/fixtures/validate-manifest-sensitive-false.json
  - packages/e2e/fixtures/validate-manifest.ts
  - packages/e2e/tests/manifest-validate-cli.spec.ts
autonomous: true
requirements:
  - MANIFEST-05
tags: [cli, validate, playwright, mcp, manifest]

must_haves:
  truths:
    - "`agrune manifest validate <file> --url <url>` 서브커맨드가 `packages/mcp/bin/agrune-mcp.ts` 의 argv 분기로 라우팅되며 기존 MCP 서버 실행 흐름(argv[0] != 'manifest') 은 영향을 받지 않는다."
    - "CLI 가 `.json` manifest 를 직접 파싱하고 `.ts` manifest 는 `tsx` subprocess 로 로드해 `defineManifest` 반환값을 JSON 직렬화 경로로 가져온다."
    - "CLI 가 `validateManifest(manifest)` (@agrune/manifest) 를 항상 먼저 호출해 schema 검증 수행 — `sensitive: false`, 빈 SelectorLadder, hash class, `:nth-child` 가 포함된 manifest 는 DOM 검증 전에 거부된다."
    - "`--url` 이 주어지면 Playwright (`chromium.launch → page.goto → networkidle + waitForSelector('body')`) 로 live DOM 을 로드하고, 각 target 의 `SelectorLadder` 를 `page.evaluate` 내부에서 우선순위대로 해석해 매칭 결과를 보고한다."
    - "매칭 실패한 target 이 있으면 CLI 가 exit code 1 과 `<targetId>: not found (tried: role → text → testId → attr → css)` 형태의 목록을 stderr 로 출력한다. 전부 매칭이면 exit 0 + `All N targets matched.` 를 stdout 으로 출력한다."
    - "`--url` 없이 호출하면 schema 검증만 수행하고, pass 시 `Schema OK (N targets, M macros, K repeats).` 를 stdout 에 출력한 뒤 exit 0."
    - "`pnpm --filter @agrune/e2e run test:e2e -- manifest-validate-cli` 가 4개 시나리오 (valid .ts, valid .json, missing selector, sensitive:false 거부) 전부 통과한다."
  artifacts:
    - path: "packages/mcp/bin/agrune-mcp.ts"
      provides: "`agrune manifest validate ...` 서브커맨드 분기 추가. 기존 MCP 서버 플로우 보존."
      contains: "manifest"
    - path: "packages/mcp/src/manifest-validate-cli.ts"
      provides: "CLI 엔트리 — argv 파싱, schema 검증, Playwright live DOM 검증, exit code"
      exports: ["runValidateCli"]
      min_lines: 120
    - path: "packages/mcp/src/manifest-file-loader.ts"
      provides: ".ts/.json manifest 로더 (tsx subprocess)"
      exports: ["loadManifestFile"]
      min_lines: 60
    - path: "packages/e2e/fixtures/validate-test.html"
      provides: "static HTML fixture — role/testId/text 포함"
    - path: "packages/e2e/fixtures/validate-manifest-good.json"
      provides: "validate-test.html 에 매칭되는 올바른 manifest"
    - path: "packages/e2e/fixtures/validate-manifest-missing.json"
      provides: "존재하지 않는 target 을 포함한 manifest (exit 1)"
    - path: "packages/e2e/fixtures/validate-manifest-sensitive-false.json"
      provides: "`sensitive: false` 포함 manifest — schema 검증 거부 확인"
    - path: "packages/e2e/fixtures/validate-manifest.ts"
      provides: "defineManifest 를 사용하는 TS manifest — tsx 로더 경로 확인"
    - path: "packages/e2e/tests/manifest-validate-cli.spec.ts"
      provides: "CLI E2E — valid/missing/sensitive-false/ts-file 4 시나리오"
      min_lines: 120
  key_links:
    - from: "packages/mcp/bin/agrune-mcp.ts"
      to: "packages/mcp/src/manifest-validate-cli.ts"
      via: "dynamic import on subcommand"
      pattern: "manifest-validate-cli"
    - from: "packages/mcp/src/manifest-validate-cli.ts"
      to: "@agrune/manifest validateManifest"
      via: "schema 검증"
      pattern: "validateManifest"
    - from: "packages/mcp/src/manifest-validate-cli.ts"
      to: "@playwright/test chromium"
      via: "live DOM 로드"
      pattern: "chromium.launch"
---

<objective>
`agrune manifest validate <file> --url <url>` CLI 를 `@agrune/mcp` 에 추가한다. `.ts` / `.json` manifest 를 로드하고, `@agrune/manifest` 의 `validateManifest` 로 schema 검증을 먼저 수행한 뒤, `--url` 이 주어지면 Playwright 로 live DOM 을 로드해 `SelectorLadder` 를 우선순위대로 해석해 selector 1:1 매칭을 확인한다. 실패 target 은 stderr 에 목록으로 보고하고 exit 1.

Purpose: MANIFEST-05. Authoring 시점에 selector 오타·누락·금지 패턴·sensitive:false 를 조기에 감지해 production 주입 전 에러 보고. Phase 12 의 MCP `agrune_manifest_load` 가 도입되기 전에 CLI 를 통해 manual validation 경로를 확립한다.

Output:
- `@agrune/mcp` 의 `bin/agrune-mcp.ts` 가 `manifest validate` 서브커맨드 분기
- CLI 구현 (`src/manifest-validate-cli.ts`) + manifest 파일 로더 (`src/manifest-file-loader.ts`)
- 4개 fixture (good/missing/sensitive-false/ts-file) + Playwright E2E 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-manifest/11-CONTEXT.md
@.planning/phases/11-manifest/11-RESEARCH.md
@.planning/phases/11-manifest/11-01-PLAN.md
@.planning/phases/11-manifest/11-02-PLAN.md

@packages/mcp/bin/agrune-mcp.ts
@packages/mcp/package.json
@packages/mcp/tsup.config.ts
@packages/e2e/package.json
@packages/e2e/playwright.config.ts

<interfaces>
<!-- Plan 01 완료 후 @agrune/manifest public API -->

From packages/manifest/src/index.ts (Plan 01):
```typescript
export { validateManifest, ManifestSchema } from './validator.js'
export type { ValidateResult, ValidateOk, ValidateFail } from './validator.js'
```

`validateManifest(input)` 반환:
```typescript
type ValidateResult =
  | { ok: true; manifest: AgruneManifest }
  | { ok: false; errors: Array<{ path: string; message: string }> }
```

<!-- Plan 01 SelectorLadder 구조 -->

SelectorLadder priority: role > text > testId > attr > css (Plan 01, Plan 02).

<!-- 현재 @agrune/mcp/bin 구조 -->

From packages/mcp/bin/agrune-mcp.ts (line 37-):
```typescript
const args = process.argv.slice(2)
if (args.includes('--help') || args.includes('-h')) { ... }
if (args.includes('--version') || args.includes('-v')) { ... }
// subcommand branch 필요 지점 ← 여기에 삽입
const attachEndpoint = getArgValue('--attach')
// ...
```

<!-- @agrune/mcp tsup 특성 -->

From packages/mcp/tsup.config.ts: `noExternal: [/.*/]` — 모든 dependency 번들링.
따라서 `@agrune/manifest`, `@playwright/test` 를 dependencies 에 추가하면 자동 번들.

From packages/mcp/package.json:
```json
"dependencies": { "@agrune/browser": "workspace:*", "@agrune/core": "workspace:*", "@modelcontextprotocol/sdk": "^1.0.0", "ws": "^8.20.0", "zod": "^4.3.6" }
"devDependencies": { "tsx": "^4.0.0", ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI 서브커맨드 + manifest 파일 로더 + validate 엔트리 구현</name>
  <files>
    packages/mcp/package.json,
    packages/mcp/bin/agrune-mcp.ts,
    packages/mcp/src/manifest-validate-cli.ts,
    packages/mcp/src/manifest-file-loader.ts
  </files>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/mcp/bin/agrune-mcp.ts (전체)
    - /Users/chenjing/dev/agrune/agrune/packages/mcp/package.json
    - /Users/chenjing/dev/agrune/agrune/packages/mcp/tsup.config.ts
    - /Users/chenjing/dev/agrune/agrune/packages/manifest/src/index.ts (Plan 01 export)
    - /Users/chenjing/dev/agrune/agrune/packages/manifest/src/validator.ts (Plan 01 산출)
    - /Users/chenjing/dev/agrune/agrune/packages/manifest/src/schema.ts (Plan 01 산출)
  </read_first>
  <behavior>
    - `agrune manifest validate` (no file): prints usage to stderr, exit 1.
    - `agrune manifest validate path/to/manifest.json`: schema-only validation, exit 0 on pass.
    - `agrune manifest validate path/to/manifest.json --url http://example.com`: schema + live DOM matching.
    - `agrune manifest validate path/to/manifest.ts`: `.ts` 파일 을 `tsx` subprocess 로 로드.
    - 기존 `agrune` (no subcommand), `agrune --headless`, `agrune --attach ...` 흐름이 변경 없이 작동.
    - `manifest` subcommand 분기 시 `driver = new CdpDriver(...)` 와 `createMcpServer` 가 호출되지 않아야 한다 (Chrome launch 방지).
    - `manifest-file-loader.ts` 가 `.ts` 와 `.json` 을 구분하고 각각 로딩 전략 적용.
  </behavior>
  <action>
    **1. `packages/mcp/package.json` 의존성 추가**:

    기존 `dependencies` 에 다음 추가:
    ```json
    "@agrune/manifest": "workspace:*",
    "@playwright/test": "^1.59.1"
    ```

    (`@playwright/test` 는 `packages/e2e` 에 devDep 으로 있음 — 이 CLI 는 packaging 대상이므로 regular dependency 로 추가. tsup `noExternal` 로 번들됨.)

    **2. `packages/mcp/bin/agrune-mcp.ts` 의 argv 파싱 직후 서브커맨드 분기 삽입**:

    현재 line 37 직후:
    ```typescript
    const args = process.argv.slice(2)

    if (args.includes('--help') || args.includes('-h')) { ... }
    if (args.includes('--version') || args.includes('-v')) { ... }

    // ── 서브커맨드 분기 (NEW) ──────────────────────────────────
    if (args[0] === 'manifest') {
      const subArgs = args.slice(1)
      if (subArgs[0] === 'validate') {
        const { runValidateCli } = await import('../src/manifest-validate-cli.js')
        const code = await runValidateCli(subArgs.slice(1))
        process.exit(code)
      }
      process.stderr.write(`Unknown manifest subcommand: ${subArgs[0] ?? '(none)'}\n`)
      process.stderr.write(`Usage: agrune manifest validate <file> [--url <url>]\n`)
      process.exit(1)
    }
    // ── 서브커맨드 분기 끝 ─────────────────────────────────────

    const attachEndpoint = getArgValue('--attach')
    // ... 기존 코드 계속
    ```

    그리고 `HELP_TEXT` 를 확장해 subcommand 를 언급:
    ```
    Subcommands:
      manifest validate <file> [--url <url>]
                             manifest schema 검증 (+ --url 지정 시 live DOM selector 검증)
    ```

    **3. `packages/mcp/src/manifest-file-loader.ts` 신규 작성**:

    ```typescript
    import { readFileSync, existsSync } from 'node:fs'
    import { resolve as resolvePath } from 'node:path'
    import { execFileSync } from 'node:child_process'
    import { fileURLToPath } from 'node:url'

    export interface LoadedManifest {
      manifest: unknown
      path: string
      kind: 'json' | 'ts'
    }

    export function loadManifestFile(filePath: string): LoadedManifest {
      const absolute = resolvePath(filePath)
      if (!existsSync(absolute)) {
        throw new Error(`Manifest file not found: ${absolute}`)
      }

      if (absolute.endsWith('.json')) {
        const text = readFileSync(absolute, 'utf-8')
        return { manifest: JSON.parse(text), path: absolute, kind: 'json' }
      }

      if (absolute.endsWith('.ts') || absolute.endsWith('.mts')) {
        return {
          manifest: loadTsManifest(absolute),
          path: absolute,
          kind: 'ts',
        }
      }

      throw new Error(`Unsupported manifest extension: ${absolute}. Use .ts, .mts, or .json.`)
    }

    function loadTsManifest(absolute: string): unknown {
      /**
       * Strategy: spawn a `tsx` subprocess that imports the manifest file and
       * writes `JSON.stringify(default-export-or-named-export)` to stdout.
       * The runner script is generated inline as a stdin.
       */
      const runnerSource = `
import('${pathToFileUrl(absolute)}').then((mod) => {
  const manifest = mod.default ?? mod.manifest
  if (!manifest) {
    process.stderr.write('Manifest file must default-export the manifest or export it as \`manifest\`.\\n')
    process.exit(2)
  }
  process.stdout.write(JSON.stringify(manifest))
}).catch((err) => {
  process.stderr.write(String(err?.stack ?? err))
  process.exit(3)
})
      `.trim()

      const out = execFileSync(
        process.execPath,
        ['--import', 'tsx/esm', '--input-type=module', '-e', runnerSource],
        { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'inherit'] },
      )
      try {
        return JSON.parse(out.trim())
      } catch (err) {
        throw new Error(`Failed to parse TS manifest JSON output: ${String(err)}`)
      }
    }

    function pathToFileUrl(absolute: string): string {
      return `file://${absolute.replace(/\\/g, '/')}`
    }
    ```

    **중요**: `tsx/esm` 이 `node --import tsx/esm` 로 동작함. Node.js 20+ 필요. 현재 프로젝트는 Node 24 사용 [VERIFIED]. `tsx` 는 이미 `@agrune/mcp` devDep 이나 번들 대상을 위해 production dep 로 옮기거나 `tsup noExternal` 이 처리. 확인: `tsx` 는 런타임에 사용되므로 `dependencies` 로 이동:
    ```json
    "tsx": "^4.0.0"  // devDependencies 에서 dependencies 로 이동
    ```

    **4. `packages/mcp/src/manifest-validate-cli.ts` 신규 작성**:

    ```typescript
    import { loadManifestFile } from './manifest-file-loader.js'
    import { validateManifest } from '@agrune/manifest'
    import type { AgruneManifest, SelectorLadder, ManifestTarget } from '@agrune/manifest'

    export async function runValidateCli(args: string[]): Promise<number> {
      const urlArg = getOption(args, '--url')
      const waitSelector = getOption(args, '--wait-selector')
      const filePath = args.find((a) => !a.startsWith('--') && !isOptionValue(args, a))

      if (!filePath) {
        process.stderr.write('Usage: agrune manifest validate <file> [--url <url>] [--wait-selector <css>]\n')
        return 1
      }

      // 1. Load file
      let loaded
      try {
        loaded = loadManifestFile(filePath)
      } catch (err) {
        process.stderr.write(`Load failed: ${(err as Error).message}\n`)
        return 1
      }

      // 2. Schema validation (always first — even before DOM check)
      const schema = validateManifest(loaded.manifest)
      if (!schema.ok) {
        process.stderr.write(`Schema validation failed in ${loaded.path}:\n`)
        for (const e of schema.errors) {
          process.stderr.write(`  - ${e.path || '(root)'}: ${e.message}\n`)
        }
        return 1
      }
      const manifest = schema.manifest

      const summary = computeSummary(manifest)

      if (!urlArg) {
        process.stdout.write(`Schema OK (${summary.targets} targets, ${summary.macros} macros, ${summary.repeats} repeats).\n`)
        return 0
      }

      // 3. Live DOM validation
      return await runLiveCheck(manifest, urlArg, waitSelector)
    }

    function computeSummary(manifest: AgruneManifest): { targets: number; macros: number; repeats: number } {
      let targets = 0
      let repeats = 0
      for (const g of manifest.groups) {
        targets += g.targets.length
        for (const r of g.repeats ?? []) {
          repeats += 1
          targets += r.targets.length
        }
      }
      return { targets, macros: manifest.macros?.length ?? 0, repeats }
    }

    async function runLiveCheck(manifest: AgruneManifest, url: string, waitSelector?: string): Promise<number> {
      const { chromium } = await import('@playwright/test')
      const browser = await chromium.launch()
      try {
        const page = await browser.newPage()
        await page.goto(url, { waitUntil: 'networkidle' })
        if (waitSelector) {
          await page.waitForSelector(waitSelector, { state: 'attached', timeout: 15000 })
        } else {
          // Default: wait 500ms past networkidle for late JS render (Pitfall 3).
          await page.waitForTimeout(500)
        }

        const flatTargets = flattenTargets(manifest)

        const results = await page.evaluate((targetsJson: string) => {
          const targets = JSON.parse(targetsJson) as Array<{ targetId: string; selector: Record<string, any> }>
          // In-page resolver — must mirror packages/runtime/src/runtime/target-resolver.ts
          const HASH = /\.[a-zA-Z0-9]{8,}(?![a-zA-Z0-9-])/
          const NTH = /:nth-child\(/
          function computeAccessibleName(el: Element): string {
            const al = el.getAttribute('aria-label')
            if (al && al.trim()) return al.trim()
            const lb = el.getAttribute('aria-labelledby')
            if (lb) {
              const parts = lb.split(/\\s+/).map((id) => document.getElementById(id)).filter((x): x is HTMLElement => x !== null).map((x) => (x.textContent ?? '').trim()).filter((s) => s.length > 0)
              if (parts.length > 0) return parts.join(' ')
            }
            return (el.textContent ?? '').trim()
          }
          function resolve(ladder: any): HTMLElement[] {
            if (ladder.role) {
              const els = Array.from(document.querySelectorAll('[role="' + String(ladder.role.name).replace(/"/g, '\\"') + '"]')) as HTMLElement[]
              const m = ladder.role.level ? els.filter((el) => computeAccessibleName(el) === ladder.role.level) : els
              if (m.length > 0) return m
            }
            if (ladder.text) {
              const scope = 'button, a, label, [role="button"], [role="link"], [role="tab"], [role="menuitem"], [role="option"]'
              const cands = Array.from(document.querySelectorAll(scope)) as HTMLElement[]
              const exact = cands.filter((el) => computeAccessibleName(el) === ladder.text)
              if (exact.length > 0) return exact
              const contains = cands.filter((el) => computeAccessibleName(el).includes(ladder.text))
              if (contains.length > 0) return contains
            }
            if (ladder.testId) {
              const els = Array.from(document.querySelectorAll('[data-testid="' + String(ladder.testId).replace(/"/g, '\\"') + '"]')) as HTMLElement[]
              if (els.length > 0) return els
            }
            if (ladder.attr) {
              if (HASH.test(ladder.attr) || NTH.test(ladder.attr)) return []
              const els = Array.from(document.querySelectorAll(ladder.attr)) as HTMLElement[]
              if (els.length > 0) return els
            }
            if (ladder.css) {
              if (HASH.test(ladder.css) || NTH.test(ladder.css)) return []
              const els = Array.from(document.querySelectorAll(ladder.css)) as HTMLElement[]
              if (els.length > 0) return els
            }
            return []
          }
          return targets.map((t) => ({ targetId: t.targetId, found: resolve(t.selector).length > 0 }))
        }, JSON.stringify(flatTargets))

        const failed = results.filter((r) => !r.found)
        if (failed.length === 0) {
          process.stdout.write(`All ${results.length} targets matched.\n`)
          return 0
        }
        process.stderr.write(`${failed.length}/${results.length} targets not found:\n`)
        for (const f of failed) {
          process.stderr.write(`  - ${f.targetId}: not found (tried: role -> text -> testId -> attr -> css)\n`)
        }
        return 1
      } finally {
        await browser.close()
      }
    }

    function flattenTargets(manifest: AgruneManifest): Array<{ targetId: string; selector: SelectorLadder }> {
      const out: Array<{ targetId: string; selector: SelectorLadder }> = []
      for (const g of manifest.groups) {
        for (const t of g.targets) out.push({ targetId: t.targetId, selector: t.selector })
        for (const r of g.repeats ?? []) {
          for (const t of r.targets) out.push({ targetId: `${r.repeatId}:${t.targetId}`, selector: t.selector })
        }
      }
      return out
    }

    function getOption(args: string[], name: string): string | undefined {
      const i = args.indexOf(name)
      if (i === -1) return undefined
      return args[i + 1]
    }

    function isOptionValue(args: string[], value: string): boolean {
      // If previous arg is a recognized option, this is its value (skip it as positional)
      const i = args.indexOf(value)
      if (i <= 0) return false
      const prev = args[i - 1]
      return prev === '--url' || prev === '--wait-selector'
    }
    ```

    **5. `packages/mcp/tsup.config.ts` 확인**: `entry` 에 `bin/agrune-mcp.ts` 가 포함되어야 하고 `manifest-validate-cli.ts` 는 dynamic import 로 자동 resolve. 기존 설정 유지.

    **6. 빌드 및 타입 체크**:
    ```bash
    pnpm install  # @agrune/manifest workspace link
    pnpm --filter @agrune/manifest run build
    pnpm --filter @agrune/mcp run typecheck
    pnpm --filter @agrune/mcp run build
    ```

    **7. 수동 smoke test**:
    ```bash
    echo '{"version":3,"groups":[{"groupId":"g","targets":[{"targetId":"t","actionKinds":["click"],"selector":{"css":"button"}}]}]}' > /tmp/m.json
    node packages/mcp/dist/bin/agrune-mcp.js manifest validate /tmp/m.json
    # Expected: "Schema OK (1 targets, 0 macros, 0 repeats)." + exit 0
    ```
  </action>
  <verify>
    <automated>
      cd /Users/chenjing/dev/agrune/agrune &&
      grep -q "@agrune/manifest" packages/mcp/package.json &&
      grep -q "@playwright/test" packages/mcp/package.json &&
      grep -q "args\\[0\\] === 'manifest'" packages/mcp/bin/agrune-mcp.ts &&
      grep -q "manifest-validate-cli" packages/mcp/bin/agrune-mcp.ts &&
      test -f packages/mcp/src/manifest-validate-cli.ts &&
      test -f packages/mcp/src/manifest-file-loader.ts &&
      grep -q "validateManifest" packages/mcp/src/manifest-validate-cli.ts &&
      grep -q "chromium.launch" packages/mcp/src/manifest-validate-cli.ts &&
      grep -q "tsx/esm" packages/mcp/src/manifest-file-loader.ts &&
      pnpm install &&
      pnpm --filter @agrune/manifest run build &&
      pnpm --filter @agrune/mcp run typecheck &&
      pnpm --filter @agrune/mcp run build &&
      test -f packages/mcp/dist/bin/agrune-mcp.js &&
      echo '{"version":3,"groups":[{"groupId":"g","targets":[{"targetId":"t","actionKinds":["click"],"selector":{"css":"button"}}]}]}' > /tmp/agrune-smoke.json &&
      node packages/mcp/dist/bin/agrune-mcp.js manifest validate /tmp/agrune-smoke.json
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@agrune/manifest": "workspace:\\*"' packages/mcp/package.json` 성공
    - `grep -q '"@playwright/test"' packages/mcp/package.json` 성공 (dependencies 안)
    - `grep -q '"tsx": "\\^4' packages/mcp/package.json` 성공 (tsx runtime dep)
    - `grep -q "args\\[0\\] === 'manifest'" packages/mcp/bin/agrune-mcp.ts` 성공
    - `grep -q "manifest-validate-cli" packages/mcp/bin/agrune-mcp.ts` 성공
    - `grep -q "Unknown manifest subcommand" packages/mcp/bin/agrune-mcp.ts` 성공
    - `test -f packages/mcp/src/manifest-validate-cli.ts` 성공
    - `test -f packages/mcp/src/manifest-file-loader.ts` 성공
    - `grep -q "export async function runValidateCli" packages/mcp/src/manifest-validate-cli.ts` 성공
    - `grep -q "validateManifest" packages/mcp/src/manifest-validate-cli.ts` 성공 (schema 검증 우선)
    - `grep -q "chromium.launch" packages/mcp/src/manifest-validate-cli.ts` 성공
    - `grep -q "networkidle" packages/mcp/src/manifest-validate-cli.ts` 성공
    - `grep -q "role.*>.*text.*>.*testId.*>.*attr.*>.*css" packages/mcp/src/manifest-validate-cli.ts` 성공 (에러 메시지)
    - `grep -q "export function loadManifestFile" packages/mcp/src/manifest-file-loader.ts` 성공
    - `grep -q "tsx/esm" packages/mcp/src/manifest-file-loader.ts` 성공
    - `pnpm --filter @agrune/mcp run typecheck` exit 0
    - `pnpm --filter @agrune/mcp run build` exit 0
    - `test -f packages/mcp/dist/bin/agrune-mcp.js` 성공
    - Smoke test: `node packages/mcp/dist/bin/agrune-mcp.js manifest validate /tmp/agrune-smoke.json` exit 0 + stdout `Schema OK`
  </acceptance_criteria>
  <done>
    `agrune manifest validate <file> [--url <url>]` 가 bundled CLI 로 동작. 서브커맨드 분기가 기존 MCP 서버 플로우를 건드리지 않고, schema 검증 → live DOM 검증 → exit code 순서가 명확하다. 수동 smoke test 로 JSON schema-only 경로가 통과.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fixtures + Playwright E2E 테스트 (valid/missing/sensitive-false/ts-file)</name>
  <files>
    packages/e2e/fixtures/validate-test.html,
    packages/e2e/fixtures/validate-manifest-good.json,
    packages/e2e/fixtures/validate-manifest-missing.json,
    packages/e2e/fixtures/validate-manifest-sensitive-false.json,
    packages/e2e/fixtures/validate-manifest.ts,
    packages/e2e/tests/manifest-validate-cli.spec.ts
  </files>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/e2e/playwright.config.ts
    - /Users/chenjing/dev/agrune/agrune/packages/e2e/package.json
    - /Users/chenjing/dev/agrune/agrune/packages/mcp/src/manifest-validate-cli.ts (Task 1 산출)
    - /Users/chenjing/dev/agrune/agrune/packages/manifest/src/index.ts
  </read_first>
  <behavior>
    - `validate-test.html` 이 3가지 요소를 포함: `<button role="button" aria-label="Sign in">`, `<div data-testid="user-card">`, `<a>Docs</a>`.
    - `validate-manifest-good.json` 이 위 3개 target 을 각각 role/testId/text 로 매칭.
    - `validate-manifest-missing.json` 이 존재하지 않는 target (`.does-not-exist` css) 을 포함.
    - `validate-manifest-sensitive-false.json` 이 `sensitive: false` 를 가짐 → schema 검증에서 거부.
    - `validate-manifest.ts` 가 `defineManifest` 를 사용해 `validate-manifest-good.json` 과 동일한 manifest 를 default export 로 제공.
    - E2E 테스트 4개:
      1. good.json + url → exit 0, stdout `All 3 targets matched.`
      2. missing.json + url → exit 1, stderr 에 `not found`.
      3. sensitive-false.json (url 없음) → exit 1, stderr 에 `sensitive:false is not allowed`.
      4. .ts file + url → exit 0, stdout `All 3 targets matched.` (tsx 로더 경로 검증)
  </behavior>
  <action>
    **1. `packages/e2e/fixtures/validate-test.html`**:
    ```html
    <!-- agrune validate-cli test fixture -->
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="utf-8" />
      <title>Validate Test Fixture</title>
    </head>
    <body>
      <header>
        <button role="button" aria-label="Sign in">Sign in</button>
      </header>
      <main>
        <div data-testid="user-card">
          <h2>Alice</h2>
          <a href="/docs">Docs</a>
        </div>
      </main>
    </body>
    </html>
    ```

    **2. `packages/e2e/fixtures/validate-manifest-good.json`**:
    ```json
    {
      "version": 3,
      "groups": [
        {
          "groupId": "page",
          "targets": [
            { "targetId": "signin", "actionKinds": ["click"], "selector": { "role": { "name": "button", "level": "Sign in" } } },
            { "targetId": "user_card", "actionKinds": ["click"], "selector": { "testId": "user-card" } },
            { "targetId": "docs_link", "actionKinds": ["click"], "selector": { "text": "Docs" } }
          ]
        }
      ]
    }
    ```

    **3. `packages/e2e/fixtures/validate-manifest-missing.json`**:
    ```json
    {
      "version": 3,
      "groups": [
        {
          "groupId": "page",
          "targets": [
            { "targetId": "signin", "actionKinds": ["click"], "selector": { "role": { "name": "button", "level": "Sign in" } } },
            { "targetId": "ghost_button", "actionKinds": ["click"], "selector": { "css": "button.does-not-exist" } }
          ]
        }
      ]
    }
    ```

    **4. `packages/e2e/fixtures/validate-manifest-sensitive-false.json`**:
    ```json
    {
      "version": 3,
      "groups": [
        {
          "groupId": "login",
          "targets": [
            { "targetId": "password", "actionKinds": ["fill"], "selector": { "css": "input[type=\"password\"]" }, "sensitive": false }
          ]
        }
      ]
    }
    ```

    **5. `packages/e2e/fixtures/validate-manifest.ts`**:
    ```typescript
    import { defineManifest, defineGroup, defineTarget } from '@agrune/manifest'

    const signin = defineTarget({
      targetId: 'signin',
      actionKinds: ['click'],
      selector: { role: { name: 'button', level: 'Sign in' } },
    })
    const userCard = defineTarget({
      targetId: 'user_card',
      actionKinds: ['click'],
      selector: { testId: 'user-card' },
    })
    const docsLink = defineTarget({
      targetId: 'docs_link',
      actionKinds: ['click'],
      selector: { text: 'Docs' },
    })

    const page = defineGroup({
      groupId: 'page',
      targets: [signin, userCard, docsLink],
    })

    export default defineManifest({
      groups: [page],
    })
    ```

    **6. `packages/e2e/tests/manifest-validate-cli.spec.ts`**:
    ```typescript
    import { test, expect } from '@playwright/test'
    import { execFileSync, spawnSync } from 'node:child_process'
    import { fileURLToPath } from 'node:url'
    import { dirname, join, resolve as resolvePath } from 'node:path'
    import { createServer } from 'node:http'
    import { readFileSync, existsSync } from 'node:fs'

    const __dirname = dirname(fileURLToPath(import.meta.url))
    const REPO_ROOT = resolvePath(__dirname, '..', '..', '..')
    const CLI = resolvePath(REPO_ROOT, 'packages', 'mcp', 'dist', 'bin', 'agrune-mcp.js')
    const FIXTURE_DIR = resolvePath(__dirname, '..', 'fixtures')
    const GOOD_JSON = resolvePath(FIXTURE_DIR, 'validate-manifest-good.json')
    const MISSING_JSON = resolvePath(FIXTURE_DIR, 'validate-manifest-missing.json')
    const SENS_JSON = resolvePath(FIXTURE_DIR, 'validate-manifest-sensitive-false.json')
    const TS_MANIFEST = resolvePath(FIXTURE_DIR, 'validate-manifest.ts')
    const TEST_HTML = readFileSync(resolvePath(FIXTURE_DIR, 'validate-test.html'), 'utf-8')

    test.beforeAll(() => {
      if (!existsSync(CLI)) {
        // Build mcp package if dist missing (Task 1 expected already built).
        execFileSync('pnpm', ['--filter', '@agrune/mcp', 'run', 'build'], { cwd: REPO_ROOT, stdio: 'inherit' })
      }
    })

    async function serveFixture(): Promise<{ url: string; close: () => Promise<void> }> {
      return await new Promise((resolve) => {
        const server = createServer((_req, res) => {
          res.writeHead(200, { 'Content-Type': 'text/html; charset=utf-8' })
          res.end(TEST_HTML)
        })
        server.listen(0, '127.0.0.1', () => {
          const addr = server.address()
          if (addr && typeof addr === 'object') {
            resolve({
              url: `http://127.0.0.1:${addr.port}/`,
              close: () => new Promise((r) => server.close(() => r())),
            })
          }
        })
      })
    }

    function runCli(args: string[]): { code: number; stdout: string; stderr: string } {
      const r = spawnSync(process.execPath, [CLI, ...args], { encoding: 'utf-8' })
      return { code: r.status ?? -1, stdout: r.stdout, stderr: r.stderr }
    }

    test.describe('MANIFEST-05 — agrune manifest validate', () => {
      test('good manifest + URL — exit 0 and "All N targets matched."', async () => {
        const server = await serveFixture()
        try {
          const r = runCli(['manifest', 'validate', GOOD_JSON, '--url', server.url])
          expect(r.code, `stderr=${r.stderr}\nstdout=${r.stdout}`).toBe(0)
          expect(r.stdout).toMatch(/All 3 targets matched\./)
        } finally {
          await server.close()
        }
      })

      test('missing target + URL — exit 1 and "not found" on ghost_button', async () => {
        const server = await serveFixture()
        try {
          const r = runCli(['manifest', 'validate', MISSING_JSON, '--url', server.url])
          expect(r.code).toBe(1)
          expect(r.stderr).toMatch(/ghost_button/)
          expect(r.stderr).toMatch(/not found/)
        } finally {
          await server.close()
        }
      })

      test('sensitive:false manifest — schema rejection before DOM check', async () => {
        const r = runCli(['manifest', 'validate', SENS_JSON])
        expect(r.code).toBe(1)
        expect(r.stderr).toMatch(/sensitive:false is not allowed/)
        expect(r.stderr).toMatch(/OR-only/)
      })

      test('.ts manifest + URL — tsx loader path matches 3 targets', async () => {
        const server = await serveFixture()
        try {
          const r = runCli(['manifest', 'validate', TS_MANIFEST, '--url', server.url])
          expect(r.code, `stderr=${r.stderr}\nstdout=${r.stdout}`).toBe(0)
          expect(r.stdout).toMatch(/All 3 targets matched\./)
        } finally {
          await server.close()
        }
      })

      test('schema-only (no --url) — passes for good manifest', () => {
        const r = runCli(['manifest', 'validate', GOOD_JSON])
        expect(r.code).toBe(0)
        expect(r.stdout).toMatch(/Schema OK \(3 targets/)
      })

      test('missing file — exit 1 with Load failed', () => {
        const r = runCli(['manifest', 'validate', '/tmp/does-not-exist.json'])
        expect(r.code).toBe(1)
        expect(r.stderr).toMatch(/Load failed|not found/i)
      })
    })
    ```

    **7. `packages/e2e/package.json` 확인** — 기존 `@agrune/mcp`, `@agrune/manifest` 가 dependency 로 있어야 tsx 가 `@agrune/manifest` 를 resolve 할 수 있음. 없다면 devDep 로 추가:
    ```json
    "@agrune/manifest": "workspace:*",
    "@agrune/mcp": "workspace:*"
    ```

    **8. 테스트 실행 순서**:
    ```bash
    pnpm --filter @agrune/manifest run build
    pnpm --filter @agrune/mcp run build  # CLI bundle 재생성
    pnpm --filter @agrune/e2e run test:e2e -- manifest-validate-cli
    ```
  </action>
  <verify>
    <automated>
      cd /Users/chenjing/dev/agrune/agrune &&
      test -f packages/e2e/fixtures/validate-test.html &&
      test -f packages/e2e/fixtures/validate-manifest-good.json &&
      test -f packages/e2e/fixtures/validate-manifest-missing.json &&
      test -f packages/e2e/fixtures/validate-manifest-sensitive-false.json &&
      test -f packages/e2e/fixtures/validate-manifest.ts &&
      test -f packages/e2e/tests/manifest-validate-cli.spec.ts &&
      grep -q "aria-label=\"Sign in\"" packages/e2e/fixtures/validate-test.html &&
      grep -q "data-testid=\"user-card\"" packages/e2e/fixtures/validate-test.html &&
      grep -q "defineManifest" packages/e2e/fixtures/validate-manifest.ts &&
      grep -q "sensitive.*false" packages/e2e/fixtures/validate-manifest-sensitive-false.json &&
      pnpm --filter @agrune/manifest run build &&
      pnpm --filter @agrune/mcp run build &&
      pnpm --filter @agrune/e2e run test:e2e -- manifest-validate-cli
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/e2e/fixtures/validate-test.html` 성공
    - `test -f packages/e2e/fixtures/validate-manifest-good.json` 성공
    - `test -f packages/e2e/fixtures/validate-manifest-missing.json` 성공
    - `test -f packages/e2e/fixtures/validate-manifest-sensitive-false.json` 성공
    - `test -f packages/e2e/fixtures/validate-manifest.ts` 성공
    - `test -f packages/e2e/tests/manifest-validate-cli.spec.ts` 성공
    - `grep -q "role=\"button\"" packages/e2e/fixtures/validate-test.html` 성공
    - `grep -q "aria-label=\"Sign in\"" packages/e2e/fixtures/validate-test.html` 성공
    - `grep -q "data-testid=\"user-card\"" packages/e2e/fixtures/validate-test.html` 성공
    - `grep -q "\"sensitive\": false" packages/e2e/fixtures/validate-manifest-sensitive-false.json` 성공
    - `grep -q "defineManifest" packages/e2e/fixtures/validate-manifest.ts` 성공
    - `grep -q "defineTarget" packages/e2e/fixtures/validate-manifest.ts` 성공
    - `grep -c "test(" packages/e2e/tests/manifest-validate-cli.spec.ts` ≥ 4 (최소 good/missing/sensitive-false/ts)
    - `grep -q "sensitive:false is not allowed" packages/e2e/tests/manifest-validate-cli.spec.ts` 성공
    - `grep -q "All 3 targets matched" packages/e2e/tests/manifest-validate-cli.spec.ts` 성공
    - `pnpm --filter @agrune/e2e run test:e2e -- manifest-validate-cli` exit 0 (모든 test pass)
  </acceptance_criteria>
  <done>
    4가지 시나리오(valid-json, missing-selector, sensitive-false, valid-ts) 가 모두 Playwright E2E 로 검증되고 CLI exit code + stdout/stderr 패턴이 일치한다. MANIFEST-05 요구사항 (live DOM selector 1:1 매칭 + 실패 target 보고) 이 end-to-end 로 증명.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author's manifest file (.ts/.json) → CLI | Author 작성 파일 — tsx subprocess 는 임의 코드 실행 가능 (Node 프로세스 내부) |
| CLI → Playwright chromium → URL | 외부 URL 로드 — 임의 JS 실행 가능성 (외부 사이트 검증용도) |
| CLI output (stdout/stderr) → user shell | 에러 메시지에 민감 정보 포함 방지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-21 | Execution (Tampering) | `.ts` manifest 파일이 악성 코드 포함 | accept | Author 가 자신의 manifest 를 실행 — trust 경계 내부. `tsx` subprocess 는 부모 프로세스와 격리되어 있어 CLI 자체는 보호. Node 프로세스 수준 신뢰로 충분. |
| T-11-22 | Elevation of Privilege | `validateManifest` 우회 — 악성 `.ts` 가 default export 로 유효해 보이는 manifest + 숨은 side effect | accept | CLI 는 `tsx` 실행 후 JSON serialize 한 값만 받음. side effect 는 subprocess 내부에서 완료. |
| T-11-23 | Information Disclosure | Playwright 가 외부 URL 로 내부 네트워크 탐색 (SSRF) | accept | Author 가 자신의 target URL 을 지정 — 서버가 아닌 CLI 툴. SSRF 는 production 서버 context 일 때 문제. |
| T-11-24 | Tampering | `--url` 이 `javascript:` 스킴 | mitigate | Playwright `page.goto` 가 `javascript:` 를 거부 — 네이티브 방어. |
| T-11-25 | DoS | 악성 manifest 가 10000+ targets 로 DOS | accept | Author 의 실수 — exit after memory/timeout. Playwright 자체 timeout. |
| T-11-26 | Information Disclosure | stderr 에 `sensitive:false` 메시지가 민감 value 노출? | mitigate | validator 에러 메시지는 path 와 constraint 만 출력. 원본 value 미노출. |
| T-11-27 | Tampering | Subcommand 분기가 기존 MCP stdio 플로우 충돌 | mitigate | `args[0] === 'manifest'` 만 분기 후 `process.exit` — `StdioServerTransport` 코드에 도달 안 함. |
</threat_model>

<verification>
- `pnpm --filter @agrune/mcp run test` 통과
- `pnpm --filter @agrune/e2e run test:e2e -- manifest-validate-cli` 통과 (6 tests)
- Phase 통합: Phase 11 finale — 이 CLI 가 전체 Phase goal(author → validate → live DOM → pass) 의 end-to-end 진입 경로

**Phase 11 전체 통합 체크** (이 플랜에서 담당):
1. MANIFEST-01 (defineManifest TS 검증) — `.ts` fixture 가 tsx 로드 후 정상 export
2. MANIFEST-04 (sensitive OR-only) — sensitive:false JSON 거부 E2E 확인
3. MANIFEST-05 (validate CLI selector 1:1 매칭) — 4개 E2E 시나리오
</verification>

<success_criteria>
- MANIFEST-05: `agrune manifest validate <file> --url <url>` 가 live DOM 에서 selector 1:1 매칭 검증, 실패 target 보고, exit code 분기 동작
- Phase 11 Authoring → Validation 루프 end-to-end 증명: .ts/.json manifest → schema 검증 → DOM 검증 → 명확한 에러/성공 메시지
</success_criteria>

<output>
After completion, create `.planning/phases/11-manifest/11-05-SUMMARY.md`
</output>
