---
phase: 11-manifest
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/runtime/target-resolver.ts
  - packages/runtime/src/runtime/dom-utils.ts
  - packages/runtime/tests/target-resolver.spec.ts
  - packages/runtime/tests/sensitive-or-only.spec.ts
autonomous: true
requirements:
  - RESOLVE-02
  - MANIFEST-04
tags: [runtime, resolver, css, selector, sensitive, dom]

must_haves:
  truths:
    - "`resolveByLadder({ role, text, testId, attr, css })` 가 role → text → testId → attr → css 우선순위로 DOM을 검색하고 매칭된 HTMLElement[] 를 반환한다. 상위 단계에서 매칭이 있으면 하위 단계를 건너뛴다."
    - "해시 class 패턴(`/\\.[a-zA-Z0-9]{8,}(?![a-zA-Z0-9-])/`) 이나 `:nth-child(`를 포함한 attr/css selector 는 `SelectorForbiddenError` 를 throw 한다. Tailwind utility class (`.flex.items-center.bg-blue-500`) 는 통과한다."
    - "`isSensitive(element, manifestFlag)` 가 manifest `true` OR DOM heuristic (`type=password`, `autocomplete` whitelist, `name/id` 단어 경계 regex) 의 OR 조합을 반환한다. `manifestFlag=false` 는 타입 수준에서 불가하므로 `true | undefined` 만 받는다."
    - "기존 `isSensitive(element)` 호출자가 `isSensitive(element, undefined)` 로 계속 동작하도록 signature 가 backward-compatible 하다 (manifestFlag optional)."
    - "`pnpm --filter @agrune/runtime run test` 가 통과한다."
  artifacts:
    - path: "packages/runtime/src/runtime/target-resolver.ts"
      provides: "CSS fallback ladder resolver + 금지 selector 가드 + 접근성 이름 매칭"
      exports: ["resolveByLadder", "assertNoHashClass", "assertNoNthChild", "SelectorForbiddenError", "HASH_CLASS_PATTERN", "NTH_CHILD_PATTERN"]
      min_lines: 120
    - path: "packages/runtime/src/runtime/dom-utils.ts"
      provides: "`isSensitive(element, manifestFlag?)` 업데이트 — OR-only 결합"
      contains: "manifestFlag === true"
    - path: "packages/runtime/tests/target-resolver.spec.ts"
      provides: "role/text/testId/attr/css 우선순위 테스트 + hash class / nth-child 금지 테스트"
      min_lines: 100
    - path: "packages/runtime/tests/sensitive-or-only.spec.ts"
      provides: "isSensitive OR-only 테스트 (manifest true, DOM password, 둘 다 false)"
      min_lines: 60
  key_links:
    - from: "packages/runtime/src/runtime/target-resolver.ts"
      to: "DOM API (document.querySelector)"
      via: "role selector → querySelectorAll([role=\"...\"])"
      pattern: "document\\.querySelector"
    - from: "packages/runtime/src/runtime/dom-utils.ts"
      to: "target-resolver sensitivity detection"
      via: "manifestFlag 파라미터로 OR 결합"
      pattern: "manifestFlag === true"
---

<objective>
런타임 `TargetResolver` (CSS fallback ladder: role > text > testId > stable attr > CSS) 를 `packages/runtime/src/runtime/target-resolver.ts` 에 구현하고, 해시 class 및 `:nth-child` 사용을 런타임 레벨에서 차단한다. 동시에 `isSensitive` 를 `(element, manifestFlag?: true | undefined)` 시그니처로 변경해 MANIFEST-04 의 OR-only 계약을 런타임에 확정한다.

Purpose: 이 플랜은 외부 사이트(소스 접근 불가)에서 manifest 의 `SelectorLadder` 를 해석하는 런타임 단일 진입점을 만든다. CSS selector 만으로 요소를 안정적으로 찾되, 해시 class·`:nth-child` 같은 position-dependent / build-hash-dependent selector 는 명시적으로 차단해 manifest 품질을 강제한다. `isSensitive` 변경은 Phase 14의 런타임 sensitive mask 확장(word-boundary regex, 다국어 ARIA label) 을 받을 준비이며, 이 플랜에서는 OR 결합의 skeleton 만 확정한다.

Output:
- `packages/runtime/src/runtime/target-resolver.ts` (신규): `resolveByLadder`, 금지 selector guard, 접근성 이름 helper
- `packages/runtime/src/runtime/dom-utils.ts`: `isSensitive` 시그니처 변경 (backward-compatible)
- 테스트 2개: jsdom 기반 resolver 단위 테스트 + OR-only 단위 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-manifest/11-CONTEXT.md
@.planning/phases/11-manifest/11-RESEARCH.md

@packages/runtime/src/runtime/dom-utils.ts
@packages/runtime/src/runtime/snapshot.ts
@packages/runtime/package.json
@packages/runtime/tsconfig.json

<interfaces>
<!-- 현재 isSensitive 시그니처 (변경 전) -->

From packages/runtime/src/runtime/dom-utils.ts:
```typescript
export function isSensitive(element: HTMLElement): boolean {
  return element.getAttribute('data-agrune-sensitive') === 'true'
}
```

<!-- 현재 호출자들 -->

From packages/runtime/src/runtime/snapshot.ts:
```typescript
// captureTarget() 내
const valuePreview = isFillableElement(element) && !state.sensitive ? element.value : null
// state.sensitive 는 captureTarget 내부에서 isSensitive(element) 로 계산됨
```

<!-- SelectorLadder (Plan 01 산출, import 금지 — Plan 02는 plan 01과 wave 1 parallel 이라 @agrune/manifest 의존 불가. 대신 동일한 구조를 runtime 내부 타입으로 복제) -->

Target shape (Plan 02 packages/runtime/src/runtime/target-resolver.ts — local type):
```typescript
export interface SelectorLadder {
  role?: { name: string; level?: string }
  text?: string
  testId?: string
  attr?: string
  css?: string
}
```
Plan 03 에서 `@agrune/core` v3 타입과 통합(현재 local 타입을 @agrune/core SelectorLadder 로 re-map).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `target-resolver.ts` 구현 — role > text > testId > attr > css ladder + 금지 selector guard</name>
  <files>
    packages/runtime/src/runtime/target-resolver.ts,
    packages/runtime/tests/target-resolver.spec.ts
  </files>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/runtime/dom-utils.ts (기존 DOM helper 스타일)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/runtime/snapshot.ts (기존 findElements 패턴)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/tsconfig.json (strict, ES2022)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/tests/runtime.spec.ts (jsdom 사용 패턴 확인)
    - /Users/chenjing/dev/agrune/agrune/.planning/phases/11-manifest/11-RESEARCH.md (섹션 4)
  </read_first>
  <behavior>
    - Test: `resolveByLadder({ role: { name: 'button', level: 'Submit' } })` 가 `<button role="button" aria-label="Submit">` 을 매칭.
    - Test: role 매칭이 있으면 text/testId/attr/css 를 검색하지 않는다 (단계 종료).
    - Test: role 없으면 text 검색 — `<button>Save</button>` 을 `text: 'Save'` 로 매칭.
    - Test: testId — `<div data-testid="user-card">` 을 `testId: 'user-card'` 로 매칭.
    - Test: attr — `<input data-field="email">` 을 `attr: '[data-field="email"]'` 로 매칭.
    - Test: css — `button.submit` 으로 매칭 (마지막 단계).
    - Test: `resolveByLadder({ css: '.abc12345xyz' })` throws `SelectorForbiddenError`.
    - Test: `resolveByLadder({ attr: 'div:nth-child(2)' })` throws `SelectorForbiddenError`.
    - Test: `resolveByLadder({ css: '.flex.items-center.bg-blue-500' })` 통과 (Tailwind utility).
    - Test: 모든 단계에서 매칭 0 → `[]` 반환 (에러 없음).
    - Test: accessible name 은 `aria-label` → `aria-labelledby` referenced textContent → textContent 순으로 확인.
    - Test: text 단계는 exact match 우선, 실패 시 contains match fallback.
  </behavior>
  <action>
    **1. `packages/runtime/src/runtime/target-resolver.ts` 신규 작성**:

    ```typescript
    /**
     * TargetResolver — CSS fallback selector ladder.
     *
     * Priority: role > text > testId > attr > css.
     * Hash classes (`/\.[a-zA-Z0-9]{8,}(?![a-zA-Z0-9-])/`) and `:nth-child(` are
     * rejected at resolve time via `assertNoHashClass` / `assertNoNthChild`.
     *
     * This module is runtime (browser)-only. It runs inside the page agent
     * runtime bundle that CdpRuntimeInjector injects via
     * `addScriptToEvaluateOnNewDocument`. It must not import Node-only modules.
     *
     * Phase 11 — RESOLVE-02. MANIFEST-04 sensitive OR-only runtime portion
     * lives in `dom-utils.ts::isSensitive`.
     */

    export interface SelectorLadder {
      role?: { name: string; level?: string }
      text?: string
      testId?: string
      attr?: string
      css?: string
    }

    // Hash class: 8+ alphanumeric with NO following hyphen — avoids Tailwind FP (Pitfall 2).
    export const HASH_CLASS_PATTERN = /\.[a-zA-Z0-9]{8,}(?![a-zA-Z0-9-])/
    export const NTH_CHILD_PATTERN = /:nth-child\(/

    export class SelectorForbiddenError extends Error {
      constructor(public readonly selector: string, message: string) {
        super(message)
        this.name = 'SelectorForbiddenError'
      }
    }

    export function assertNoHashClass(selector: string): void {
      if (HASH_CLASS_PATTERN.test(selector)) {
        throw new SelectorForbiddenError(
          selector,
          `Selector "${selector}" contains a likely hash-based class. Use role, text, testId, or stable attribute instead.`,
        )
      }
    }

    export function assertNoNthChild(selector: string): void {
      if (NTH_CHILD_PATTERN.test(selector)) {
        throw new SelectorForbiddenError(
          selector,
          `Selector "${selector}" uses :nth-child which is position-dependent. Use a stable identifier instead.`,
        )
      }
    }

    /**
     * Compute accessible name for the purposes of role+name matching.
     * Priority: aria-label > aria-labelledby referenced text > textContent trim.
     *
     * Note: the full WAI AccName algorithm is NOT implemented — this covers
     * the common cases for button/link/input labeling which is sufficient for
     * manifest selector resolution. [ASSUMED, see RESEARCH A2]
     */
    export function computeAccessibleName(element: Element): string {
      const ariaLabel = element.getAttribute('aria-label')
      if (ariaLabel && ariaLabel.trim()) return ariaLabel.trim()

      const labelledBy = element.getAttribute('aria-labelledby')
      if (labelledBy) {
        const parts = labelledBy
          .split(/\s+/)
          .map((id) => element.ownerDocument?.getElementById(id))
          .filter((el): el is HTMLElement => el !== null && el !== undefined)
          .map((el) => el.textContent?.trim() ?? '')
          .filter((s) => s.length > 0)
        if (parts.length > 0) return parts.join(' ')
      }

      return (element.textContent ?? '').trim()
    }

    function resolveByRole(
      doc: Document,
      role: { name: string; level?: string },
    ): HTMLElement[] {
      const elements = Array.from(
        doc.querySelectorAll<HTMLElement>(`[role="${cssEscape(role.name)}"]`),
      )
      if (!role.level) return elements
      return elements.filter((el) => computeAccessibleName(el) === role.level)
    }

    // Text selector queries interactive elements (button, a, label, role=*).
    // Exact match preferred; falls back to contains match if exact match returns empty.
    const TEXT_SELECTOR_SCOPE =
      'button, a, label, [role="button"], [role="link"], [role="tab"], [role="menuitem"], [role="option"]'

    function resolveByText(doc: Document, text: string): HTMLElement[] {
      const candidates = Array.from(doc.querySelectorAll<HTMLElement>(TEXT_SELECTOR_SCOPE))
      const exact = candidates.filter((el) => {
        const name = computeAccessibleName(el)
        return name === text
      })
      if (exact.length > 0) return exact
      return candidates.filter((el) => computeAccessibleName(el).includes(text))
    }

    function resolveByTestId(doc: Document, testId: string): HTMLElement[] {
      return Array.from(
        doc.querySelectorAll<HTMLElement>(`[data-testid="${cssEscape(testId)}"]`),
      )
    }

    function resolveByAttr(doc: Document, attr: string): HTMLElement[] {
      assertNoHashClass(attr)
      assertNoNthChild(attr)
      return Array.from(doc.querySelectorAll<HTMLElement>(attr))
    }

    function resolveByCss(doc: Document, css: string): HTMLElement[] {
      assertNoHashClass(css)
      assertNoNthChild(css)
      return Array.from(doc.querySelectorAll<HTMLElement>(css))
    }

    function cssEscape(value: string): string {
      // jsdom may lack CSS.escape; fallback to character escape.
      if (typeof CSS !== 'undefined' && typeof CSS.escape === 'function') {
        return CSS.escape(value)
      }
      return value.replace(/["\\]/g, '\\$&')
    }

    /**
     * Resolve a SelectorLadder against the given document.
     * Returns matched HTMLElements. Returns [] if no step matches.
     * Throws SelectorForbiddenError if attr/css contain hash class or :nth-child.
     *
     * The caller is responsible for handling the empty case.
     */
    export function resolveByLadder(
      ladder: SelectorLadder,
      doc: Document = document,
    ): HTMLElement[] {
      if (ladder.role) {
        const matched = resolveByRole(doc, ladder.role)
        if (matched.length > 0) return matched
      }
      if (ladder.text) {
        const matched = resolveByText(doc, ladder.text)
        if (matched.length > 0) return matched
      }
      if (ladder.testId) {
        const matched = resolveByTestId(doc, ladder.testId)
        if (matched.length > 0) return matched
      }
      if (ladder.attr) {
        const matched = resolveByAttr(doc, ladder.attr)
        if (matched.length > 0) return matched
      }
      if (ladder.css) {
        const matched = resolveByCss(doc, ladder.css)
        if (matched.length > 0) return matched
      }
      return []
    }
    ```

    **2. `packages/runtime/tests/target-resolver.spec.ts`**:
    ```typescript
    // @vitest-environment jsdom
    import { beforeEach, describe, it, expect } from 'vitest'
    import {
      resolveByLadder,
      computeAccessibleName,
      assertNoHashClass,
      assertNoNthChild,
      SelectorForbiddenError,
      HASH_CLASS_PATTERN,
      NTH_CHILD_PATTERN,
    } from '../src/runtime/target-resolver'

    beforeEach(() => {
      document.body.innerHTML = ''
    })

    describe('resolveByLadder — priority', () => {
      it('role > text > testId > attr > css — role wins when present', () => {
        document.body.innerHTML = `
          <button role="button" aria-label="Submit">Save</button>
          <button data-testid="submit-btn">Submit</button>
        `
        const matched = resolveByLadder({
          role: { name: 'button', level: 'Submit' },
          text: 'Save',
          testId: 'submit-btn',
        })
        expect(matched).toHaveLength(1)
        expect(matched[0].getAttribute('aria-label')).toBe('Submit')
      })

      it('text wins when role absent', () => {
        document.body.innerHTML = `<button>Save</button>`
        const matched = resolveByLadder({ text: 'Save' })
        expect(matched).toHaveLength(1)
        expect(matched[0].textContent).toBe('Save')
      })

      it('testId wins when role+text absent', () => {
        document.body.innerHTML = `<div data-testid="user-card">x</div>`
        const matched = resolveByLadder({ testId: 'user-card' })
        expect(matched).toHaveLength(1)
      })

      it('attr wins when role+text+testId absent', () => {
        document.body.innerHTML = `<input data-field="email" />`
        const matched = resolveByLadder({ attr: '[data-field="email"]' })
        expect(matched).toHaveLength(1)
      })

      it('css is last resort', () => {
        document.body.innerHTML = `<button class="submit">x</button>`
        const matched = resolveByLadder({ css: 'button.submit' })
        expect(matched).toHaveLength(1)
      })

      it('returns [] if no step matches', () => {
        document.body.innerHTML = ``
        expect(resolveByLadder({ css: 'button.missing' })).toEqual([])
      })

      it('falls through to lower priority when higher returns empty', () => {
        document.body.innerHTML = `<div data-testid="t1">x</div>`
        // role and text return empty; testId should be used
        const matched = resolveByLadder({
          role: { name: 'button' },
          text: 'NotThere',
          testId: 't1',
        })
        expect(matched).toHaveLength(1)
      })
    })

    describe('resolveByLadder — forbidden selectors', () => {
      it('throws on hash class in css', () => {
        expect(() => resolveByLadder({ css: '.abc12345xyz' })).toThrow(SelectorForbiddenError)
      })
      it('throws on hash class in attr', () => {
        expect(() => resolveByLadder({ attr: '.abc12345xyz' })).toThrow(SelectorForbiddenError)
      })
      it('throws on :nth-child in css', () => {
        expect(() => resolveByLadder({ css: 'div:nth-child(2)' })).toThrow(SelectorForbiddenError)
      })
      it('allows Tailwind utility classes', () => {
        document.body.innerHTML = `<button class="flex items-center bg-blue-500">X</button>`
        expect(() => resolveByLadder({ css: '.flex.items-center.bg-blue-500' })).not.toThrow()
      })
    })

    describe('computeAccessibleName', () => {
      it('prefers aria-label', () => {
        document.body.innerHTML = `<button aria-label="Alpha">Beta</button>`
        const btn = document.querySelector('button')!
        expect(computeAccessibleName(btn)).toBe('Alpha')
      })
      it('falls back to aria-labelledby referenced text', () => {
        document.body.innerHTML = `<h2 id="hd">Title</h2><button aria-labelledby="hd">X</button>`
        const btn = document.querySelector('button')!
        expect(computeAccessibleName(btn)).toBe('Title')
      })
      it('falls back to textContent', () => {
        document.body.innerHTML = `<button>Only</button>`
        const btn = document.querySelector('button')!
        expect(computeAccessibleName(btn)).toBe('Only')
      })
    })

    describe('HASH_CLASS_PATTERN / NTH_CHILD_PATTERN', () => {
      it('HASH_CLASS_PATTERN matches 8+ char class with no trailing hyphen', () => {
        expect(HASH_CLASS_PATTERN.test('.abc12345xy')).toBe(true)
      })
      it('HASH_CLASS_PATTERN rejects Tailwind bg-blue-500', () => {
        expect(HASH_CLASS_PATTERN.test('.bg-blue-500')).toBe(false)
      })
      it('NTH_CHILD_PATTERN matches :nth-child(', () => {
        expect(NTH_CHILD_PATTERN.test('div:nth-child(2)')).toBe(true)
      })
    })

    describe('assertNoHashClass / assertNoNthChild', () => {
      it('assertNoHashClass throws on hash class', () => {
        expect(() => assertNoHashClass('.abc12345def')).toThrow(SelectorForbiddenError)
      })
      it('assertNoNthChild throws on :nth-child', () => {
        expect(() => assertNoNthChild('div:nth-child(1)')).toThrow(SelectorForbiddenError)
      })
    })
    ```

    **3. `packages/runtime/package.json` 에 jsdom 은 이미 있음 — `@vitest-environment jsdom` 으로 충분.** 추가 devDependency 불필요 [VERIFIED: package.json].

    **4. 테스트 실행 확인**: `pnpm --filter @agrune/runtime run test -- target-resolver` 가 통과해야 한다.
  </action>
  <verify>
    <automated>
      cd /Users/chenjing/dev/agrune/agrune &&
      test -f packages/runtime/src/runtime/target-resolver.ts &&
      grep -q "export function resolveByLadder" packages/runtime/src/runtime/target-resolver.ts &&
      grep -q "HASH_CLASS_PATTERN = /\\\\.\\[a-zA-Z0-9\\]{8,}(?!\\[a-zA-Z0-9-\\])/" packages/runtime/src/runtime/target-resolver.ts &&
      grep -q "NTH_CHILD_PATTERN" packages/runtime/src/runtime/target-resolver.ts &&
      grep -q "computeAccessibleName" packages/runtime/src/runtime/target-resolver.ts &&
      pnpm --filter @agrune/runtime run test -- target-resolver
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/runtime/src/runtime/target-resolver.ts` 성공
    - `grep -q "export function resolveByLadder" packages/runtime/src/runtime/target-resolver.ts` 성공
    - `grep -q "export function computeAccessibleName" packages/runtime/src/runtime/target-resolver.ts` 성공
    - `grep -q "export function assertNoHashClass" packages/runtime/src/runtime/target-resolver.ts` 성공
    - `grep -q "export function assertNoNthChild" packages/runtime/src/runtime/target-resolver.ts` 성공
    - `grep -q "export class SelectorForbiddenError" packages/runtime/src/runtime/target-resolver.ts` 성공
    - `grep -q "role.*text.*testId.*attr.*css" packages/runtime/src/runtime/target-resolver.ts` 성공 (주석 내 priority 문서화)
    - `grep -q "aria-label" packages/runtime/src/runtime/target-resolver.ts` 성공
    - `grep -q "aria-labelledby" packages/runtime/src/runtime/target-resolver.ts` 성공
    - `test -f packages/runtime/tests/target-resolver.spec.ts` 성공
    - `grep -c "it(" packages/runtime/tests/target-resolver.spec.ts` ≥ 15
    - `pnpm --filter @agrune/runtime run test -- target-resolver` exit 0
  </acceptance_criteria>
  <done>
    `resolveByLadder` 가 role > text > testId > attr > css 우선순위로 동작하고 hash class / :nth-child 를 throw 한다. jsdom 기반 15+ 테스트가 모두 통과한다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `isSensitive` OR-only 시그니처 교체 + 회귀 테스트</name>
  <files>
    packages/runtime/src/runtime/dom-utils.ts,
    packages/runtime/tests/sensitive-or-only.spec.ts
  </files>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/runtime/dom-utils.ts (현재 isSensitive 전체 — line 308 주변)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/runtime/snapshot.ts (호출자 확인 — captureTarget 에서 isSensitive 호출 위치)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/runtime/page-agent-runtime.ts (isSensitive 호출 여부 확인 후 영향 평가)
    - /Users/chenjing/dev/agrune/agrune/packages/runtime/src/runtime/command-handlers.ts (isSensitive 호출 여부)
    - /Users/chenjing/dev/agrune/agrune/.planning/phases/11-manifest/11-RESEARCH.md (섹션 8)
  </read_first>
  <behavior>
    - Test: `isSensitive(input_password)` → `true` (DOM heuristic: type=password).
    - Test: `isSensitive(input_text, true)` → `true` (manifest OR-only).
    - Test: `isSensitive(input_text, undefined)` → `false`.
    - Test: `isSensitive(input_text)` → `false` (arity 1 호출 backward-compat).
    - Test: `isSensitive(input_agrune_legacy)` → `true` (data-agrune-sensitive='true' 레거시, Phase 17 전까지 유지).
    - Test: `isSensitive(autocomplete_new_password)` → `true` (autocomplete heuristic).
    - Test: `isSensitive(autocomplete_cc_number)` → `true`.
    - Test: `isSensitive(autocomplete_one_time_code)` → `true`.
    - Test: TS 타입: `isSensitive(el, false)` 는 @ts-expect-error.
  </behavior>
  <action>
    **1. `packages/runtime/src/runtime/dom-utils.ts` 의 `isSensitive` 함수 (line 308 주변) 를 다음으로 교체**:

    ```typescript
    /**
     * Sensitive detection — OR-only.
     *
     * Sensitive = manifest-flag OR DOM-heuristic. Once any source reports
     * sensitive, the field is sensitive. A manifest cannot force `sensitive:false`
     * to override a heuristic — the parameter type is `true | undefined`, not
     * `boolean`, and the schema (@agrune/manifest) rejects `sensitive: false`.
     *
     * MANIFEST-04. Phase 14 extends the DOM heuristic with word-boundary regex
     * on name/id/placeholder/aria-label and multilingual ARIA scanning. This
     * phase establishes the shape and the OR-only contract skeleton.
     */
    const AUTOCOMPLETE_SENSITIVE = new Set([
      'current-password',
      'new-password',
      'one-time-code',
      'cc-number',
      'cc-csc',
      'cc-exp',
      'cc-exp-month',
      'cc-exp-year',
    ])

    export function isSensitive(
      element: HTMLElement,
      manifestFlag?: true | undefined,
    ): boolean {
      // 1. Manifest flag — OR-only. `false` is not a valid argument.
      if (manifestFlag === true) return true

      // 2. DOM heuristic — input type=password
      if (element instanceof HTMLInputElement && element.type === 'password') {
        return true
      }

      // 3. DOM heuristic — autocomplete whitelist
      const autocomplete = element.getAttribute('autocomplete')
      if (autocomplete) {
        const normalized = autocomplete.toLowerCase().trim()
        if (AUTOCOMPLETE_SENSITIVE.has(normalized)) return true
      }

      // 4. Legacy inline annotation — maintained until Phase 17 REMOVE
      if (element.getAttribute('data-agrune-sensitive') === 'true') return true

      return false
    }
    ```

    **2. 호출자 확인**: `snapshot.ts` 의 `captureTarget` 내부 `isSensitive(element)` 호출은 파라미터 1개로 계속 동작해야 한다 (optional). 변경 불필요 — backward-compatible.

    `grep` 으로 모든 호출자 확인:
    ```bash
    grep -rn "isSensitive(" packages/runtime/src/ packages/browser/src/
    ```
    모든 호출이 `isSensitive(element)` 형태 (1-arity) 이면 변경 없이 backward-compat. 만약 호출이 있다면 그대로 둔다 — Plan 03 에서 manifest target 기반 호출을 추가한다.

    **3. `packages/runtime/tests/sensitive-or-only.spec.ts`**:
    ```typescript
    // @vitest-environment jsdom
    import { beforeEach, describe, it, expect } from 'vitest'
    import { isSensitive } from '../src/runtime/dom-utils'

    beforeEach(() => {
      document.body.innerHTML = ''
    })

    describe('isSensitive — OR-only contract', () => {
      it('returns true when manifest flag is true (overrides all)', () => {
        document.body.innerHTML = `<input type="text" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el, true)).toBe(true)
      })

      it('returns true when DOM has type=password (regardless of manifest)', () => {
        document.body.innerHTML = `<input type="password" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el)).toBe(true)
        expect(isSensitive(el, undefined)).toBe(true)
      })

      it('returns false for plain input when manifest flag absent', () => {
        document.body.innerHTML = `<input type="text" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el)).toBe(false)
        expect(isSensitive(el, undefined)).toBe(false)
      })

      it('backward-compat: single-arg call still works', () => {
        document.body.innerHTML = `<input type="text" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el)).toBe(false)
      })

      it('detects autocomplete=current-password', () => {
        document.body.innerHTML = `<input type="text" autocomplete="current-password" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el)).toBe(true)
      })

      it('detects autocomplete=new-password', () => {
        document.body.innerHTML = `<input type="text" autocomplete="new-password" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el)).toBe(true)
      })

      it('detects autocomplete=one-time-code', () => {
        document.body.innerHTML = `<input type="text" autocomplete="one-time-code" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el)).toBe(true)
      })

      it('detects autocomplete=cc-number', () => {
        document.body.innerHTML = `<input type="text" autocomplete="cc-number" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el)).toBe(true)
      })

      it('detects legacy data-agrune-sensitive=true (maintained until Phase 17)', () => {
        document.body.innerHTML = `<input type="text" data-agrune-sensitive="true" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el)).toBe(true)
      })

      it('type-level: manifestFlag: false is not allowed', () => {
        const el = document.createElement('input')
        // @ts-expect-error manifestFlag must be `true | undefined`, not `false`
        void isSensitive(el, false)
        expect(true).toBe(true)
      })

      it('OR combination: non-password input + manifest true = true', () => {
        document.body.innerHTML = `<input type="text" />`
        const el = document.querySelector('input')!
        expect(isSensitive(el, true)).toBe(true)
      })
    })
    ```

    **4. 테스트 실행**: `pnpm --filter @agrune/runtime run test -- sensitive-or-only` 통과 확인.

    **5. 기존 runtime 테스트 (`runtime.spec.ts`) 회귀 확인**: `pnpm --filter @agrune/runtime run test` 전체 실행 통과.
  </action>
  <verify>
    <automated>
      cd /Users/chenjing/dev/agrune/agrune &&
      grep -q "manifestFlag" packages/runtime/src/runtime/dom-utils.ts &&
      grep -q "AUTOCOMPLETE_SENSITIVE" packages/runtime/src/runtime/dom-utils.ts &&
      grep -q "if (manifestFlag === true) return true" packages/runtime/src/runtime/dom-utils.ts &&
      grep -q "manifestFlag?: true | undefined" packages/runtime/src/runtime/dom-utils.ts &&
      grep -q "@ts-expect-error" packages/runtime/tests/sensitive-or-only.spec.ts &&
      pnpm --filter @agrune/runtime run test
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "manifestFlag?: true | undefined" packages/runtime/src/runtime/dom-utils.ts` 성공 (signature 교체 확인)
    - `grep -q "if (manifestFlag === true) return true" packages/runtime/src/runtime/dom-utils.ts` 성공 (OR-only 구현)
    - `grep -q "AUTOCOMPLETE_SENSITIVE" packages/runtime/src/runtime/dom-utils.ts` 성공 (heuristic)
    - `grep -q "current-password" packages/runtime/src/runtime/dom-utils.ts` 성공
    - `grep -q "new-password" packages/runtime/src/runtime/dom-utils.ts` 성공
    - `grep -q "one-time-code" packages/runtime/src/runtime/dom-utils.ts` 성공
    - `grep -q "cc-number" packages/runtime/src/runtime/dom-utils.ts` 성공
    - `grep -q "cc-csc" packages/runtime/src/runtime/dom-utils.ts` 성공
    - `grep -q "data-agrune-sensitive" packages/runtime/src/runtime/dom-utils.ts` 성공 (legacy 유지)
    - `test -f packages/runtime/tests/sensitive-or-only.spec.ts` 성공
    - `grep -c "@ts-expect-error" packages/runtime/tests/sensitive-or-only.spec.ts` ≥ 1
    - `grep -c "it(" packages/runtime/tests/sensitive-or-only.spec.ts` ≥ 10
    - `pnpm --filter @agrune/runtime run test` exit 0 (전체 runtime 테스트 회귀 포함)
    - `pnpm --filter @agrune/runtime run typecheck` exit 0
  </acceptance_criteria>
  <done>
    `isSensitive` 가 `(element, manifestFlag?: true | undefined)` 시그니처로 변경되어 manifest flag 와 DOM heuristic 의 OR-only 결합을 구현한다. autocomplete whitelist (current-password, new-password, one-time-code, cc-*) + type=password + legacy data-agrune-sensitive 를 cover. 모든 기존 runtime 테스트가 통과하고 sensitive-or-only.spec.ts 가 녹색이다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest author → runtime resolver | Authored SelectorLadder 가 런타임에서 해석 — 악성/실수로 hash class·nth-child 포함 |
| Manifest flag → sensitive mask | `manifestFlag=true` 는 민감하게 만들 수만 있고, 해제할 수 없어야 한다 (OR-only) |
| DOM heuristic → password visibility | `type=password`, `autocomplete=*-password` 가 valuePreview 노출 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | SelectorLadder css/attr 필드에 hash class/:nth-child 주입 → 빌드 해시 바뀌면 selector 깨짐 | mitigate | `assertNoHashClass` / `assertNoNthChild` 가 런타임 resolve 시점에 `SelectorForbiddenError` throw |
| T-11-08 | Elevation of Privilege | 악성 manifest 가 password 필드에 `sensitive: false` 설정 (OR 우회 시도) | mitigate | TS signature `true \| undefined` + Plan 01 zod `z.literal(true).optional()` → manifest parsing 단계에서 이미 거부됨. 이 plan 에서는 runtime level OR (password type + autocomplete heuristic) 으로 2차 방어 |
| T-11-09 | Information Disclosure | manifest 없이 password 필드가 valuePreview 에 포함 | mitigate | autocomplete whitelist (current-password, new-password, one-time-code, cc-*) + type=password heuristic 이 manifest 주입 없어도 자동 감지 |
| T-11-10 | Information Disclosure | `resolveByLadder` 에 임의의 selector 전달 → SSRF/XSS? | accept | 이 함수는 순수 DOM 쿼리 — 네트워크/eval 없음. 쿼리 자체가 문제되지 않음. 금지 패턴은 selector stability 이슈일 뿐 보안 이슈 아님 |
| T-11-11 | Denial of Service | 매우 복잡한 css selector 로 resolver 성능 저하 | accept | `document.querySelectorAll` 은 브라우저 네이티브 — 악의적 selector 도 브라우저 파서가 조기 실패. 별도 timeout 불필요 |
</threat_model>

<verification>
독립 검증:
- `pnpm --filter @agrune/runtime run test` — target-resolver + sensitive-or-only + 기존 runtime 회귀 전부 녹색
- `pnpm --filter @agrune/runtime run typecheck` — strict 통과

Phase 통합 검증은 Plan 03/05 에서 담당.
</verification>

<success_criteria>
- RESOLVE-02: `resolveByLadder` 가 role > text > testId > attr > css 로 CSS fallback 해석, hash class/nth-child 차단, jsdom 단위 테스트 15+ 통과
- MANIFEST-04 (runtime layer): `isSensitive(element, manifestFlag?)` 가 OR-only 로 결합, 타입 레벨에서 `false` 불가
</success_criteria>

<output>
After completion, create `.planning/phases/11-manifest/11-02-SUMMARY.md`
</output>
