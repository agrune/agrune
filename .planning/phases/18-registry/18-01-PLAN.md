---
phase: 18-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/registry/package.json [NEW]
  - packages/registry/tsconfig.json [NEW]
  - packages/registry/tsup.config.ts [NEW]
  - packages/registry/src/index.ts [NEW]
  - packages/registry/src/schema.ts [NEW]
  - packages/registry/src/content-hash.ts [NEW]
  - packages/registry/src/cache.ts [NEW]
  - packages/registry/src/lockfile.ts [NEW]
  - packages/registry/src/registry-client.ts [NEW]
  - packages/registry/src/staleness.ts [NEW]
  - packages/registry/src/errors.ts [NEW]
  - packages/registry/tests/content-hash.test.ts [NEW]
  - packages/registry/tests/cache.test.ts [NEW]
  - packages/registry/tests/lockfile.test.ts [NEW]
  - packages/registry/tests/schema.test.ts [NEW]
  - packages/registry/tests/staleness.test.ts [NEW]
  - pnpm-workspace.yaml
  - scripts/regression-guard/data-agrune-allowlist.txt
autonomous: true
requirements:
  - REGISTRY-02
  - REGISTRY-03
  - REGISTRY-06
tags:
  - registry
  - scaffold
  - content-hash
  - cache
  - lockfile

must_haves:
  truths:
    - "`@agrune/registry` 패키지가 workspace 에 존재하고 `pnpm --filter @agrune/registry build` 로 dist 를 생성한다."
    - "`contentHash(manifest)` 가 같은 논리적 manifest 에 대해 키 순서와 무관하게 동일한 `sha256:<hex>` 를 반환한다."
    - "`readLockfile(cwd)` / `writeLockfile(cwd, lock)` 가 `agrune.maps.lock.json` 을 atomic write + host 정렬된 entries 로 왕복한다."
    - "`~/.agrune/maps/<host>@<ver>.json` 캐시 읽기/쓰기가 `mode: 0o700` 디렉토리 + `mode: 0o600` 파일로 생성된다."
    - "`RegistryEntrySchema.parse` 가 `ManifestSchema` 를 재사용해 registry entry 를 검증하고 unknown 필드를 거부한다."
    - "`classifyStaleness(entry, now)` 가 fresh/week_old/stale/auto_disabled 4 상태 중 하나를 반환한다."
  artifacts:
    - path: "packages/registry/package.json"
      provides: "@agrune/registry 패키지 manifest (workspace internal, publish 는 v0.5 말기/v0.6)"
      contains: "\"name\": \"@agrune/registry\""
    - path: "packages/registry/src/content-hash.ts"
      provides: "contentHash(manifest) → sha256:<hex> using fast-json-stable-stringify"
      exports: ["contentHash"]
    - path: "packages/registry/src/schema.ts"
      provides: "RegistryEntrySchema (wraps ManifestSchema + registry metadata: host/version/tier/author/submittedAt/reviewedBy/allowedEnvironments)"
      exports: ["RegistryEntrySchema", "RegistryEntry", "RegistryTier", "RegistryMetadata"]
    - path: "packages/registry/src/cache.ts"
      provides: "readCacheEntry / writeCacheEntry / clearCache — ~/.agrune/maps 디스크 캐시 (0700/0600 permission)"
      exports: ["readCacheEntry", "writeCacheEntry", "getCacheDir", "clearCache"]
    - path: "packages/registry/src/lockfile.ts"
      provides: "readLockfile / writeLockfile — agrune.maps.lock.json atomic read/write (host 정렬)"
      exports: ["readLockfile", "writeLockfile", "LockfileEntry", "Lockfile"]
    - path: "packages/registry/src/registry-client.ts"
      provides: "fetchRegistryEntry(host, version) — raw.githubusercontent.com 에서 JSON GET"
      exports: ["fetchRegistryEntry", "DEFAULT_REGISTRY_BASE_URL"]
    - path: "packages/registry/src/staleness.ts"
      provides: "classifyStaleness(entry, now) → 'fresh' | 'week_old' | 'stale' | 'auto_disabled' + threshold 상수"
      exports: ["classifyStaleness", "STALENESS_THRESHOLDS"]
    - path: "packages/registry/src/errors.ts"
      provides: "Registry error codes (REGISTRY_ENTRY_NOT_FOUND / CONTENT_HASH_MISMATCH / LOCKFILE_WRITE_FAILED / CACHE_PERMISSION_DENIED / REGISTRY_FETCH_FAILED)"
      exports: ["RegistryError", "REGISTRY_ERROR_CODES"]
  key_links:
    - from: "packages/registry/src/schema.ts"
      to: "packages/manifest/src/index.ts"
      via: "import { ManifestSchema } from '@agrune/manifest'"
      pattern: "from '@agrune/manifest'"
    - from: "packages/registry/src/content-hash.ts"
      to: "fast-json-stable-stringify"
      via: "import stableStringify from 'fast-json-stable-stringify'"
      pattern: "fast-json-stable-stringify"
    - from: "pnpm-workspace.yaml"
      to: "packages/registry/"
      via: "packages: packages/* (이미 glob 으로 포함)"
      pattern: "packages/\\*"
---

<objective>
`@agrune/registry` 신규 패키지를 workspace 에 추가하고 registry 의 비-CLI 순수 로직 (schema / content-hash / cache / lockfile / registry-client / staleness) 을 commit-sized 모듈로 scaffold 한다. CLI 서브커맨드 (Plan 02) 와 외부 repo seed (Plan 03) 둘 다 이 패키지의 내부 API 를 소비한다.

Purpose: Phase 18 의 "새 로직" 은 전부 표준 라이브러리 조립이므로 (RESEARCH "Don't Hand-Roll"), 먼저 library-grade 재사용 계층을 확정해 CLI 4 명령 (add/types/doctor/submit) 이 얇은 argparse wrapper 로 머무를 수 있게 한다. 내부 API 를 패키지로 분리하면 PR bot (Plan 04) 이 npm 으로 `@agrune/registry` 를 pull 해 schema validator 를 공유할 수 있다.

Output:
- `packages/registry/` — 신규 pnpm workspace 패키지 (`@agrune/registry`)
- Schema/hash/cache/lockfile/staleness 모듈 + vitest 단위 테스트
- `RegistryEntrySchema` (manifest v3 재사용 + registry 메타 래퍼)
- Staleness 임계값 상수 (weekly re-fetch / 4주 stale / 8주 auto-disable)
- Workspace integration (`pnpm-workspace.yaml` 이미 glob 이므로 추가 수정 없음 확인) + regression guard allow-list 갱신
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-registry/18-CONTEXT.md
@.planning/phases/18-registry/18-RESEARCH.md

@packages/manifest/src/index.ts
@packages/manifest/src/schema.ts
@packages/manifest/src/validator.ts
@packages/manifest/package.json
@pnpm-workspace.yaml
@scripts/regression-guard/data-agrune-allowlist.txt

<interfaces>
<!-- Plan 01 이 정의하는 public API. Plan 02 (CLI) 와 Plan 04 (PR bot scripts) 가 이 타입들을 consume. -->
<!-- 실행자는 아래 타입을 그대로 내보내고, downstream plan 들의 scavenger hunt 를 방지. -->

From @agrune/manifest (existing, re-used):
```typescript
// packages/manifest/src/schema.ts — 이미 존재하는 export
export const ManifestSchema: z.ZodSchema<AgruneManifest>
export type AgruneManifest = z.infer<typeof ManifestSchema>
// validateManifest(data) → ValidateResult = { ok: true; manifest } | { ok: false; errors }
```

New exports from @agrune/registry (Plan 01 이 정의):
```typescript
// packages/registry/src/schema.ts
export const RegistryEntrySchema: z.ZodSchema<RegistryEntry>
export type RegistryEntry = {
  registry: {
    host: string              // e.g. "news.ycombinator.com"
    version: string            // semver: "1.0.0"
    tier: 'verified' | 'community' | 'unlisted'
    author: string             // GitHub handle
    submittedAt: string        // ISO 8601
    reviewedBy?: string[]      // GitHub handles
    allowedEnvironments: ('dev' | 'prod')[]  // Pitfall 7 — default ['dev']
    seedUrl?: string           // optional for health check
    staleSince?: string        // ISO — bot 이 PR 로 추가
  }
  manifest: AgruneManifest      // Phase 11 schema 재사용
}
export type RegistryTier = RegistryEntry['registry']['tier']
export type RegistryMetadata = RegistryEntry['registry']

// packages/registry/src/content-hash.ts
export function contentHash(manifest: AgruneManifest): string  // "sha256:<64-hex>"

// packages/registry/src/lockfile.ts
export interface LockfileEntry {
  host: string
  version: string
  contentHash: string            // "sha256:..."
  tier: RegistryTier
  fetchedAt: string              // ISO 8601
  source: string                 // URL
  allowedEnvironments: ('dev' | 'prod')[]
  disabled?: { reason: 'stale' | 'revoked' | 'user'; at: string }
}
export interface Lockfile {
  version: 1
  entries: LockfileEntry[]       // sorted by host
}
export const LOCKFILE_NAME = 'agrune.maps.lock.json'
export async function readLockfile(cwd: string): Promise<Lockfile>
export async function writeLockfile(cwd: string, lock: Lockfile): Promise<void>

// packages/registry/src/cache.ts
export function getCacheDir(): string   // ~/.agrune/maps
export async function readCacheEntry(host: string, version: string): Promise<RegistryEntry | null>
export async function writeCacheEntry(entry: RegistryEntry): Promise<void>  // 0700 dir / 0600 file
export async function clearCache(): Promise<void>

// packages/registry/src/registry-client.ts
export const DEFAULT_REGISTRY_BASE_URL = 'https://raw.githubusercontent.com/agrune/maps/main'
export interface RegistryClientOptions { baseUrl?: string }
export async function fetchRegistryEntry(
  host: string,
  version: string,                // "latest" or "1.0.0" (정확 매칭만; v0.5 MVP)
  options?: RegistryClientOptions,
): Promise<RegistryEntry>

// packages/registry/src/staleness.ts
export type StalenessState = 'fresh' | 'week_old' | 'stale' | 'auto_disabled'
export const STALENESS_THRESHOLDS = {
  weekMs: 7 * 24 * 60 * 60 * 1000,
  staleMs: 28 * 24 * 60 * 60 * 1000,        // 4주
  autoDisableMs: 56 * 24 * 60 * 60 * 1000,  // 8주
} as const
export function classifyStaleness(
  entry: Pick<LockfileEntry, 'fetchedAt' | 'disabled'>,
  now?: Date,
): StalenessState

// packages/registry/src/errors.ts
export type RegistryErrorCode =
  | 'REGISTRY_ENTRY_NOT_FOUND'
  | 'CONTENT_HASH_MISMATCH'
  | 'LOCKFILE_WRITE_FAILED'
  | 'CACHE_PERMISSION_DENIED'
  | 'REGISTRY_FETCH_FAILED'
  | 'REGISTRY_SCHEMA_INVALID'
export class RegistryError extends Error {
  constructor(public readonly code: RegistryErrorCode, message: string)
}
export const REGISTRY_ERROR_CODES: readonly RegistryErrorCode[]
```

From packages/mcp/bin/agrune-mcp.ts (existing dispatch pattern — Plan 02 가 확장):
```typescript
if (args[0] === 'manifest') { ... }  // lines 66-82 — 동일 패턴으로 'maps' 블록 추가 예정
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: @agrune/registry 패키지 스캐폴드 + schema/content-hash 모듈</name>
  <files>
    packages/registry/package.json [NEW]
    packages/registry/tsconfig.json [NEW]
    packages/registry/tsup.config.ts [NEW]
    packages/registry/src/index.ts [NEW]
    packages/registry/src/schema.ts [NEW]
    packages/registry/src/content-hash.ts [NEW]
    packages/registry/src/errors.ts [NEW]
    packages/registry/tests/content-hash.test.ts [NEW]
    packages/registry/tests/schema.test.ts [NEW]
  </files>
  <behavior>
    content-hash.test.ts:
    - contentHash(manifest) returns 'sha256:<hex>' prefix
    - contentHash stable across key reorder: contentHash({a:1,b:2}) === contentHash({b:2,a:1}) (동일 논리 manifest)
    - contentHash deterministic: 두 번 호출 동일 결과
    - contentHash differs on any value change (sensitive guard)

    schema.test.ts:
    - RegistryEntrySchema.parse accepts minimal valid entry (registry + manifest with single group/target)
    - RegistryEntrySchema rejects missing registry.host
    - RegistryEntrySchema rejects unknown top-level fields (strict mode)
    - RegistryEntrySchema defaults allowedEnvironments to ['dev'] when omitted (Pitfall 7)
    - RegistryEntrySchema rejects tier='verified' + allowedEnvironments=['dev','prod'] when… (this is allowed — no rejection; Pitfall 7 restriction is "community tier 최대값 ['dev']" 만 검증)
    - RegistryEntrySchema rejects tier='community' with allowedEnvironments including 'prod'
  </behavior>
  <action>
    [D-N/A — discuss phase skipped. 모든 구현 선택은 RESEARCH 권고 + RESEARCH Architectural Responsibility Map 기준.]

    1. `packages/registry/package.json` 생성:
       - name: "@agrune/registry", version: "0.4.1" (workspace 과 정렬), private 아님 (publish 는 나중 milestone)
       - type: "module", main/module/types 는 `dist/index.js` / `dist/index.d.ts`
       - exports: `{ ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } }`
       - scripts: `"build": "tsup"`, `"typecheck": "tsc --noEmit -p tsconfig.json"`, `"test": "vitest run"`
       - dependencies:
         * `@agrune/manifest`: `workspace:*`
         * `@octokit/rest`: `^22.0.1` (RESEARCH 권고 — Plan 02 submit 에서 사용)
         * `@octokit/auth-oauth-device`: `^8.0.3` (Plan 02 device flow)
         * `fast-json-stable-stringify`: `^2.1.0`
         * `semver`: `^7.7.4`
         * `picocolors`: `^1.1.1`
         * `zod`: `^4.3.6`
       - devDependencies: `@types/node`, `@types/semver`, `tsup`, `typescript`, `vitest` (다른 패키지 devDep 과 동일 버전)
       - **금지:** `diff` — Plan 02 에서 필요하면 그 때 추가 (dependency 최소화 원칙)
       - 설치 명령은 사용자가 `pnpm install` 한 번으로 완료 (workspace 라 자동)

    2. `packages/registry/tsconfig.json` — `packages/manifest/tsconfig.json` 복사 + 필요 시 path 조정

    3. `packages/registry/tsup.config.ts` — `packages/manifest/tsup.config.ts` 미러. entry: `src/index.ts`, format: `['esm']`, dts: true, clean: true

    4. `packages/registry/src/errors.ts` — `RegistryError` 클래스 + 6 개 code (REGISTRY_ENTRY_NOT_FOUND / CONTENT_HASH_MISMATCH / LOCKFILE_WRITE_FAILED / CACHE_PERMISSION_DENIED / REGISTRY_FETCH_FAILED / REGISTRY_SCHEMA_INVALID). `REGISTRY_ERROR_CODES` readonly array 도 export.

    5. `packages/registry/src/content-hash.ts`:
       ```typescript
       import { createHash } from 'node:crypto'
       import stableStringify from 'fast-json-stable-stringify'
       import type { AgruneManifest } from '@agrune/manifest'

       export function contentHash(manifest: AgruneManifest): string {
         const canonical = stableStringify(manifest)
         const hex = createHash('sha256').update(canonical, 'utf-8').digest('hex')
         return `sha256:${hex}`
       }
       ```
       Pitfall 1 (whitespace / key reorder instability) 가 해결됨을 test 로 증명.

    6. `packages/registry/src/schema.ts`:
       - `RegistryTierSchema = z.enum(['verified', 'community', 'unlisted'])`
       - `AllowedEnvironmentsSchema = z.array(z.enum(['dev', 'prod'])).default(['dev']).superRefine(...)` — Pitfall 7 반영: tier='community' 일 때 'prod' 포함하면 `issue` 발생
       - `RegistryMetadataSchema = z.strictObject({ host, version, tier, author, submittedAt, reviewedBy?, allowedEnvironments, seedUrl?, staleSince? })`
       - `RegistryEntrySchema = z.strictObject({ registry: RegistryMetadataSchema, manifest: ManifestSchema })`
       - Cross-field refinement: `.superRefine((entry, ctx) => { if (entry.registry.tier === 'community' && entry.registry.allowedEnvironments.includes('prod')) ctx.addIssue({ code: 'custom', message: 'community tier 는 prod 환경 허용 불가', path: ['registry','allowedEnvironments'] }) })`
       - Re-export types: `RegistryEntry`, `RegistryTier`, `RegistryMetadata`

    7. `packages/registry/src/index.ts` — 모듈 barrel. Plan 01 에서는 `content-hash`, `schema`, `errors` 만 export (cache/lockfile/client/staleness 는 Task 2 에서 추가).

    8. 단위 테스트:
       - `tests/content-hash.test.ts`: 4 케이스 (prefix / key-reorder stability / determinism / value-change difference)
       - `tests/schema.test.ts`: 6 케이스 (minimal valid / missing host / unknown field strict / allowedEnvironments default / verified+prod allowed / community+prod rejected)

    9. `pnpm install` 로 workspace 링크 확인. `pnpm --filter @agrune/registry build` 가 dist 생성. `pnpm --filter @agrune/registry test` 가 green.

    10. `scripts/regression-guard/data-agrune-allowlist.txt` 확인: 본 패키지는 `data-agrune-` literal 을 도입하지 않으므로 allow-list 추가 불필요. `pnpm lint:no-legacy` 가 exit 0 유지됨을 Task 3 verify 에서 확인.

    [보안 — Pitfall 1 (content hash instability), Pitfall 7 (prod root-import abuse) 모두 schema/hash 레벨에서 구조적 차단. Threat T-18-01..T-18-03 참조.]
  </behavior>
  <action>위 behavior 블록에 정의된 테스트를 먼저 작성하고 RED 확인 (`pnpm --filter @agrune/registry test` 실패) → 구현 후 GREEN 확인. 커밋 단위: (1) 스캐폴드 + errors, (2) schema + test, (3) content-hash + test.</action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && pnpm install && pnpm --filter @agrune/registry build && pnpm --filter @agrune/registry test && pnpm --filter @agrune/registry typecheck</automated>
  </verify>
  <done>
    - `packages/registry/` 디렉토리 존재, `package.json` / `tsconfig.json` / `tsup.config.ts` / `src/index.ts` / `src/schema.ts` / `src/content-hash.ts` / `src/errors.ts` 전부 생성됨
    - `pnpm --filter @agrune/registry test` 가 10+ 테스트 모두 pass (4 content-hash + 6 schema)
    - `pnpm --filter @agrune/registry build` 가 `dist/index.js` + `dist/index.d.ts` 생성
    - `RegistryEntry` 타입이 export 되어 downstream import 가능 (typecheck 로 증명)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cache / lockfile / registry-client / staleness 모듈 + 단위 테스트</name>
  <files>
    packages/registry/src/cache.ts [NEW]
    packages/registry/src/lockfile.ts [NEW]
    packages/registry/src/registry-client.ts [NEW]
    packages/registry/src/staleness.ts [NEW]
    packages/registry/src/index.ts
    packages/registry/tests/cache.test.ts [NEW]
    packages/registry/tests/lockfile.test.ts [NEW]
    packages/registry/tests/staleness.test.ts [NEW]
  </files>
  <behavior>
    cache.test.ts (CACHE_DIR override via env AGRUNE_CACHE_DIR, tmp dir in test):
    - getCacheDir() returns ~/.agrune/maps (또는 override)
    - writeCacheEntry creates directory with mode 0o700 (stat 확인, POSIX 플랫폼만)
    - writeCacheEntry writes file with mode 0o600 (POSIX 플랫폼만)
    - readCacheEntry returns null if file not exists
    - readCacheEntry round-trips RegistryEntry
    - readCacheEntry throws CACHE_PERMISSION_DENIED (RegistryError) on EACCES
    - writeCacheEntry refuses symlink path (Pitfall T-18-08 cache dir symlink attack) — lstat 후 isSymbolicLink 이면 REGISTRY_ERROR
    - clearCache removes directory (사용자 opt-in, no auto)

    lockfile.test.ts:
    - readLockfile creates default { version: 1, entries: [] } when file missing
    - writeLockfile writes JSON (2-space indent, trailing newline)
    - writeLockfile sorts entries by host asciibetical (determinism — npm lockfile 패턴)
    - writeLockfile is atomic: write to tmp file + rename (확인: writeFile 실패 시 기존 파일 변경 없음 — mock fs.rename failure test)
    - Lockfile shape 유효 시: readLockfile → writeLockfile → readLockfile round-trip identity

    staleness.test.ts:
    - classifyStaleness({fetchedAt: now}) === 'fresh'
    - classifyStaleness({fetchedAt: now - 10 days}) === 'week_old'
    - classifyStaleness({fetchedAt: now - 30 days}) === 'stale'
    - classifyStaleness({fetchedAt: now - 60 days}) === 'auto_disabled'
    - classifyStaleness({fetchedAt: now, disabled: {reason:'revoked', at:...}}) === 'auto_disabled' (disabled 우선)
    - STALENESS_THRESHOLDS.weekMs / staleMs / autoDisableMs 상수 노출 + governance doc 값 (7/28/56 days) 과 일치
  </behavior>
  <action>
    1. `packages/registry/src/cache.ts`:
       - `getCacheDir()`: env `AGRUNE_CACHE_DIR` 우선, 없으면 `join(homedir(), '.agrune', 'maps')`. (Context 문서 default = `~/.agrune/maps/`)
       - `async function writeCacheEntry(entry)`:
         * `mkdir(dir, { recursive: true, mode: 0o700 })`
         * Pitfall T-18-08: `lstat(targetPath).catch(() => null)` → symbolic link 이면 `throw new RegistryError('CACHE_PERMISSION_DENIED', 'symlink detected')`
         * `writeFile(path, JSON.stringify(entry, null, 2), { mode: 0o600 })`
       - `async function readCacheEntry(host, version)`:
         * Path traversal 방어: host 는 `/[a-z0-9][a-z0-9\-\.]{0,253}$/i` + version 은 semver regex 로만 허용 (아니면 null — 또는 RegistryError)
         * `readFile` → `null` (ENOENT) 또는 RegistryError (EACCES)
         * `RegistryEntrySchema.parse(JSON.parse(raw))` 로 defense-in-depth 재검증 — Pitfall 5 (integrity check)
       - `async function clearCache()` — rm -r (user confirmation 은 CLI layer 에서)

    2. `packages/registry/src/lockfile.ts`:
       - `LOCKFILE_NAME = 'agrune.maps.lock.json'`
       - `async function readLockfile(cwd)`:
         * `readFile(join(cwd, LOCKFILE_NAME), 'utf8')` → parse → zod 검증 (간단 shape check)
         * ENOENT → `{ version: 1, entries: [] }` 반환 (no file == empty state)
       - `async function writeLockfile(cwd, lock)`:
         * entries 복제 후 `.sort((a,b) => a.host.localeCompare(b.host))`
         * `JSON.stringify(sorted, null, 2) + '\n'`
         * Atomic write: `writeFile(tmp)` + `rename(tmp, target)` (Node atomic rename)
         * 실패 시 RegistryError('LOCKFILE_WRITE_FAILED')

    3. `packages/registry/src/registry-client.ts`:
       - `DEFAULT_REGISTRY_BASE_URL = 'https://raw.githubusercontent.com/agrune/maps/main'` (env `AGRUNE_REGISTRY_BASE_URL` override 지원)
       - `async function fetchRegistryEntry(host, version, options?)`:
         * v0.5 MVP: `version === 'latest'` 이면 index.json fetch → 최신 version resolve (Plan 03 이 index.json 제공). 정확 semver 는 `manifests/<host>@<version>.json` 경로 직접 fetch.
         * `fetch(url)` Node 22 built-in, HTTPS only 강제 (URL.protocol === 'https:')
         * 200 → `RegistryEntrySchema.parse(await res.json())` + 반환
         * 404 → RegistryError('REGISTRY_ENTRY_NOT_FOUND', `${host}@${version} not found`)
         * 다른 실패 → RegistryError('REGISTRY_FETCH_FAILED')
       - v0.5 semver range (`^1.0.0` 등) 미지원 — RESEARCH Open Q 5 권고 (MVP: 정확 매칭 + latest)

    4. `packages/registry/src/staleness.ts`:
       - `STALENESS_THRESHOLDS = { weekMs, staleMs, autoDisableMs }` (governance doc 과 정확히 일치 — Plan 03 governance 에서 참조)
       - `classifyStaleness(entry, now = new Date())`:
         * `entry.disabled` 가 있으면 → 'auto_disabled'
         * `age = now.getTime() - new Date(entry.fetchedAt).getTime()`
         * `age < weekMs` → 'fresh'
         * `age < staleMs` → 'week_old'
         * `age < autoDisableMs` → 'stale'
         * else → 'auto_disabled'

    5. `src/index.ts` 재export — 모든 public API (위 `<interfaces>` 와 1:1 일치)

    6. 단위 테스트 (tmp dir 사용, `afterEach` 로 정리):
       - cache.test.ts 는 `os.tmpdir() + /agrune-test-<uuid>` 로 `AGRUNE_CACHE_DIR` override
       - staleness 는 pure function 이라 fixture 만
       - lockfile 은 tmp cwd

    [보안: Pitfall 6 (cache dir permission) 구조적 강제 + Pitfall 5 (staleness thrashing) threshold 명시 + Pitfall 8 (cache symlink attack) lstat check.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && pnpm --filter @agrune/registry test && pnpm --filter @agrune/registry build && pnpm --filter @agrune/registry typecheck</automated>
  </verify>
  <done>
    - `src/cache.ts` / `src/lockfile.ts` / `src/registry-client.ts` / `src/staleness.ts` 존재
    - `pnpm --filter @agrune/registry test` 20+ 테스트 모두 pass (기존 10 + 신규 ~15+)
    - `src/index.ts` 가 `<interfaces>` 블록의 모든 public API 를 export (barrel 완성)
    - `pnpm --filter @agrune/registry build` 성공, dist 에 모든 파일 포함
  </done>
</task>

<task type="auto">
  <name>Task 3: Workspace 통합 검증 + regression guard 재검증</name>
  <files>
    pnpm-workspace.yaml
    scripts/regression-guard/data-agrune-allowlist.txt
  </files>
  <action>
    1. `pnpm-workspace.yaml` 검토: `packages/*` glob 이미 `packages/registry/` 를 포함. **수정 불필요**. 만약 `onlyBuiltDependencies` 에 뭔가 추가되어야 한다면 (거의 없음) 검토.

    2. 루트에서 순차 실행 + 통과 확인:
       ```
       pnpm install                                  # workspace 재링크
       pnpm -r --filter "@agrune/*" run build        # 모든 패키지 build (registry 포함)
       pnpm -r --filter "@agrune/*" run typecheck    # typecheck
       pnpm -r --filter "@agrune/*" run test         # 모든 단위 테스트
       pnpm lint:no-legacy                           # Phase 17 regression guard
       ```

    3. `scripts/regression-guard/data-agrune-allowlist.txt` — 본 패키지는 `data-agrune-` literal 도입하지 않음. `pnpm lint:no-legacy` 가 exit 0 유지됨을 확인. 만약 예기치 않은 match 가 발생하면 allow-list 추가가 아니라 코드에서 literal 제거 (Phase 17 invariant 유지).

    4. `@agrune/registry` 가 `@agrune/manifest` 와 cross-package import 가 정상 동작함을 확인 (typecheck 가 `@agrune/manifest` 의 re-exported type 을 consume 가능해야 함).

    5. Build 순서 확인: `@agrune/manifest` 가 `@agrune/registry` 전에 build 되어야 함. `pnpm -r` 는 workspace deps 를 자동 해결하므로 `"@agrune/manifest": "workspace:*"` dep 이 순서 보장. CLAUDE.md 에 명시된 "manifest SDK 수정 후 의존 소비자 재빌드" 패턴 준수.

    6. 최종 smoke: Node REPL 에서 `import('@agrune/registry').then(m => console.log(Object.keys(m)))` 로 `<interfaces>` 블록의 12+ public export 가 노출됨을 확인 (실행자가 수동 또는 추가 스크립트로).

    [수정 없음 예상 파일: `pnpm-workspace.yaml` / allow-list. 양쪽 모두 "read + verify, write only if needed" 원칙.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && pnpm install && pnpm build && pnpm typecheck && pnpm test && pnpm lint:no-legacy</automated>
  </verify>
  <done>
    - `pnpm build` 가 9 개 패키지 (기존 8 + registry) 모두 성공
    - `pnpm test` 가 registry 신규 테스트 포함 전체 pass (pre-existing flaky 1 건은 baseline — STATE.md L92 에 이미 기록된 runtime full-suite order-dependent failure. 본 plan 으로 악화 없음이면 OK)
    - `pnpm lint:no-legacy` exit 0 (Phase 17 regression guard 유지)
    - `pnpm typecheck` exit 0 (registry 가 @agrune/manifest export 를 올바르게 consume)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry repo (external) → `@agrune/registry.fetchRegistryEntry` (local) | HTTPS 네트워크 경계. registry repo content 는 untrusted (compromised mirror / MITM 가능) — zod re-validation 필수 |
| `@agrune/registry.RegistryEntrySchema.parse` → in-memory RegistryEntry | schema-filtered 후 trusted (ManifestSchema + registry metadata shape 둘 다 검증됨) |
| in-memory RegistryEntry → local disk (`~/.agrune/maps/` + `agrune.maps.lock.json`) | local disk 경계. 파일 권한으로 multi-user 머신 시 유출 방어 (Pitfall 6) |
| local disk → `@agrune/registry.readCacheEntry` (재로드) | cache content 는 이전 write 시점 이후 변조 가능 (사용자 직접 편집, malicious local process) — 재parse + 재검증 필수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `contentHash` 입력 (manifest JSON) | mitigate | `fast-json-stable-stringify` canonical serialization + SHA-256 (Pitfall 1). 테스트로 key-reorder invariance 증명 |
| T-18-02 | Tampering | Registry 응답 body (registry-client.ts) | mitigate | `RegistryEntrySchema.parse` 강제 — unknown field rejection (z.strictObject), ManifestSchema 재사용 (defense-in-depth) |
| T-18-03 | Elevation of Privilege | RegistryEntry.registry.allowedEnvironments | mitigate | schema cross-field refinement: tier='community' + prod → schema error. 기본값 ['dev'] (Pitfall 7) |
| T-18-04 | Information Disclosure | 캐시 파일 권한 (`~/.agrune/maps/`) | mitigate | `mkdir({ mode: 0o700 })` + `writeFile({ mode: 0o600 })` — Pitfall 6 구조적 강제 |
| T-18-05 | Tampering | Cache 읽기 시 content | mitigate | readCacheEntry 에서도 `RegistryEntrySchema.parse` 재실행 (malicious 로컬 프로세스가 파일 교체 가능성) |
| T-18-06 | DoS (external) | `fetchRegistryEntry` 호출 빈도 | mitigate | v0.5 는 explicit opt-in: CLI add/submit 만 네트워크 호출. doctor 는 기본 offline (Plan 02 문서화) |
| T-18-07 | Spoofing | Registry URL | mitigate | HTTPS only (URL.protocol === 'https:' 강제), DEFAULT base URL 은 raw.githubusercontent.com 고정 |
| T-18-08 | Tampering | Cache dir symlink attack | mitigate | writeCacheEntry 가 lstat → isSymbolicLink 체크, symlink 이면 REGISTRY_ERROR |
| T-18-09 | Tampering | Lockfile atomic write 실패 | accept | writeLockfile 이 tmp+rename 패턴으로 partial write 방지. 실패 시 REGISTRY_ERROR 전파 (call site 가 roll-forward 재시도) |
| T-18-10 | Information Disclosure | Path traversal via host/version | mitigate | cache read 가 host `/[a-z0-9][a-z0-9\-\.]{0,253}$/i` + semver regex 화이트리스트 — 아니면 즉시 reject |
</threat_model>

<verification>
1. `pnpm --filter @agrune/registry build` 가 dist 생성 (src 8 개 파일 → esm + dts)
2. `pnpm --filter @agrune/registry test` 20+ tests pass (content-hash 4 / schema 6 / cache 6 / lockfile 5 / staleness 6)
3. `pnpm --filter @agrune/registry typecheck` exit 0
4. 전체 `pnpm build` 가 9 패키지 빌드 성공 (manifest → registry 순서 자동 해결)
5. `pnpm lint:no-legacy` exit 0 — Phase 17 regression guard 유지
6. `packages/registry/src/index.ts` 가 Interface 블록의 모든 public API export (grep 으로 `export` 헤드 12+ 확인)
7. Contract 증명: `@agrune/registry` 를 import 하는 최소 프로그램이 `@agrune/manifest` 의 `AgruneManifest` 를 타입 일치로 받음 (Plan 02 가 이 contract 에 의존)
</verification>

<success_criteria>
- ROADMAP Success Criteria #2 일부: "`add` → `~/.agrune/maps/<host>@<ver>.json` 캐시 + `agrune.maps.lock.json` content-hash 잠금" 에서 캐시/잠금 메커니즘의 라이브러리 층이 완성 (CLI wrapper 는 Plan 02)
- ROADMAP Success Criteria #5 일부: "staleness (weekly re-fetch 기준)" threshold 상수 + classifier 함수 완성 (CLI 표면은 Plan 02 doctor)
- REGISTRY-02 부분 달성: CLI 가 의존할 library 완비
- REGISTRY-03 완전 달성: 캐시 + lockfile 라이브러리 (CLI 는 Plan 02 에서 호출)
- REGISTRY-06 부분 달성: staleness classifier (CLI doctor 는 Plan 02)
- Phase 17 regression guard 무영향 유지 (pnpm lint:no-legacy 계속 exit 0)
</success_criteria>

<output>
완료 후 `.planning/phases/18-registry/18-01-SUMMARY.md` 를 템플릿대로 작성. key-files.created 에 `packages/registry/` 산하 10 개 파일 + `packages/registry/package.json` 등 명시. decisions 에 (a) zod superRefine 로 tier/allowedEnvironments cross-field 검증 선택, (b) semver range 미지원 (MVP: latest + 정확 매칭), (c) `AGRUNE_CACHE_DIR` / `AGRUNE_REGISTRY_BASE_URL` 환경 변수 이름 확정 기록.
</output>
