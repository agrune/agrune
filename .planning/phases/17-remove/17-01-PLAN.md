---
phase: "17-remove"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/dom-scanner.ts
  - packages/runtime/src/manifest-builder.ts
  - packages/runtime/src/page-runtime.ts
  - packages/runtime/src/index.ts
  - packages/runtime/src/types.ts
  - packages/runtime/src/runtime/snapshot.ts
  - packages/runtime/src/runtime/page-agent-runtime.ts
  - packages/runtime/src/runtime/dom-utils.ts
  - packages/runtime/src/runtime/command-handlers.ts
  - packages/browser/src/cdp-runtime-injector.ts
  - packages/runtime/tests/runtime-ignores-legacy.spec.ts
  - packages/runtime/tests/public-surface.spec.ts
  - packages/e2e/fixtures/runtime.bundle.js
autonomous: true
requirements:
  - REMOVE-01
tags:
  - runtime
  - legacy-removal
  - breaking-change
must_haves:
  truths:
    - "Runtime bundle에서 scanAnnotations/scanGroups/buildManifest 심볼이 더 이상 export되지 않는다"
    - "페이지에 data-agrune-action 등 legacy attribute가 박혀 있어도 runtime은 target으로 인식하지 않는다 (manifest-only)"
    - "CdpRuntimeInjector.resolveManifest는 window>preload>idle 3가지 source만 반환하고 'inline' source는 존재하지 않는다"
    - "data-agrune-aurora/data-agrune-pointer runtime 내부 마커는 온전히 남아 있다 (cursor animation non-negotiable)"
    - "data-agrune-canvas 경로(isInCanvasGroup/getCanvasGroupTransform/findCanvasGroupEl/drag auto-pan) dead code가 삭제됐다"
  artifacts:
    - path: "packages/runtime/src/dom-scanner.ts"
      provides: "DELETED — legacy inline-annotation scanner 제거"
      contains: "(file MUST NOT exist)"
    - path: "packages/runtime/src/manifest-builder.ts"
      provides: "DELETED — legacy data-agrune-* → manifest 빌더 제거"
      contains: "(file MUST NOT exist)"
    - path: "packages/runtime/src/page-runtime.ts"
      provides: "scanAnnotations/scanGroups/buildManifest re-export 제거됨"
      contains: "NO occurrence of scanAnnotations|scanGroups|buildManifest"
    - path: "packages/runtime/src/index.ts"
      provides: "public export surface에서 scanner 심볼 제거"
      contains: "NO occurrence of scanAnnotations|scanGroups|buildManifest|ScannedTarget|ScannedGroup"
    - path: "packages/runtime/src/runtime/snapshot.ts"
      provides: "LIVE_SCAN_* 상수 및 collectLiveDescriptors 제거"
      contains: "NO occurrence of LIVE_SCAN_ACTION_SELECTOR|LIVE_SCAN_GROUP_SELECTOR|collectLiveDescriptors|buildLiveSelector"
    - path: "packages/runtime/src/runtime/page-agent-runtime.ts"
      provides: "getDescriptors가 manifestDescriptors만 반환"
      contains: "const getDescriptors = () => manifestDescriptors"
    - path: "packages/runtime/src/runtime/dom-utils.ts"
      provides: "isSensitive legacy fallback(data-agrune-sensitive) 제거, buildLiveSelector 제거, AGRUNE_INTERNAL_SELECTOR/isAgruneInternalElement 유지"
      contains: "AGRUNE_INTERNAL_SELECTOR"
    - path: "packages/runtime/src/runtime/command-handlers.ts"
      provides: "detectMaskedInput의 data-agrune-masked 체크 제거 + canvas helper 3개 + drag auto-pan branch 삭제"
      contains: "NO occurrence of isInCanvasGroup|getCanvasGroupTransform|findCanvasGroupEl|data-agrune-masked"
    - path: "packages/browser/src/cdp-runtime-injector.ts"
      provides: "resolveManifest legacy inline-scan branch 삭제 — source union은 'window'|'preload'|'idle'"
      contains: "NO occurrence of 'inline'"
    - path: "packages/runtime/tests/public-surface.spec.ts"
      provides: "NEW — @agrune/runtime export surface negative test"
      contains: "public-surface"
    - path: "packages/runtime/tests/runtime-ignores-legacy.spec.ts"
      provides: "NEW — DOM에 data-agrune-* 있어도 target 무시 positive test"
      contains: "runtime ignores legacy"
  key_links:
    - from: "packages/browser/src/cdp-runtime-injector.ts"
      to: "packages/runtime/src/page-runtime.ts"
      via: "runtimeApi consumer — scan* / buildManifest 호출 부재"
      pattern: "resolveManifest.*window\\.__agrune_manifest__.*preload.*idle"
    - from: "packages/runtime/src/runtime/page-agent-runtime.ts"
      to: "packages/runtime/src/runtime/snapshot.ts"
      via: "getDescriptors — manifestDescriptors 단독 반환"
      pattern: "getDescriptors = \\(\\) => manifestDescriptors"
    - from: "packages/runtime/tests/runtime-ignores-legacy.spec.ts"
      to: "packages/runtime/src/runtime/page-agent-runtime.ts"
      via: "신규 regression spec이 manifest-only 계약을 검증"
      pattern: "data-agrune-action.*NOT.*target"
---

<objective>
**Runtime + Browser src에서 legacy `data-agrune-*` bootstrap 경로를 물리 삭제한다.**

RESEARCH.md §3 Runtime Inventory 9개 call-site 그룹 전부를 처리한다: 2개 src 파일 삭제(dom-scanner.ts, manifest-builder.ts), 3개 runtime 파일에서 live-scan 경로 제거(snapshot.ts LIVE_SCAN_* 전부, page-agent-runtime.ts getDescriptors 축소, dom-utils.ts isSensitive legacy fallback 삭제), 1개 browser 파일에서 resolveManifest legacy branch 제거(cdp-runtime-injector.ts), canvas 관련 dead code 삭제(command-handlers.ts canvas helper 3개 + drag auto-pan branch, masked-input legacy check). Wave 0 regression spec 2개 신규 작성. 마지막에 runtime 패키지 재빌드 + e2e bundle 재생성.

**단일 파일 삭제만으로는 충분하지 않다** (RESEARCH.md Pitfall 1). Success Criteria 1의 "신규 페이지 로드에서 `data-agrune-*` 속성은 runtime이 무시한다"는 snapshot.ts live-scan 경로까지 제거해야 구조적으로 성립한다.

Purpose: 단일 authoring 경로(manifest + root-import)를 구조적으로 확정. 이후 wave들이 문서/fixture를 의미 반전시킬 수 있는 전제.
Output: runtime src 레벨에서 data-agrune 의존 0, export surface 축소, 새 regression spec 2개, 재빌드된 dist + e2e runtime bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-remove/17-CONTEXT.md
@.planning/phases/17-remove/17-RESEARCH.md
@packages/runtime/src/dom-scanner.ts
@packages/runtime/src/manifest-builder.ts
@packages/runtime/src/page-runtime.ts
@packages/runtime/src/index.ts
@packages/runtime/src/runtime/snapshot.ts
@packages/runtime/src/runtime/page-agent-runtime.ts
@packages/runtime/src/runtime/dom-utils.ts
@packages/runtime/src/runtime/command-handlers.ts
@packages/browser/src/cdp-runtime-injector.ts

<interfaces>
<!-- Key call-sites extracted from RESEARCH.md §3 Runtime Inventory. -->
<!-- Executor should use these as the deletion/modification target list. -->

Files to DELETE entirely:
- packages/runtime/src/dom-scanner.ts  (82 lines — scanAnnotations, scanGroups)
- packages/runtime/src/manifest-builder.ts  (69 lines — buildManifest)

Re-exports to REMOVE from packages/runtime/src/page-runtime.ts (L19-20):
- scanAnnotations, scanGroups, buildManifest

Exports to REMOVE from packages/runtime/src/index.ts (L19-22):
- scanAnnotations, scanGroups, buildManifest (values)
- ScannedTarget, ScannedGroup (types) — likely defined in packages/runtime/src/types.ts; remove from there too if only used by scanner

Symbols to REMOVE from packages/runtime/src/runtime/snapshot.ts:
- LIVE_SCAN_ACTION_SELECTOR (L~93)
- LIVE_SCAN_GROUP_SELECTOR  (L~94)
- LIVE_SCAN_DEFAULT_GROUP_ID (L~95)
- LIVE_SCAN_DEFAULT_GROUP_NAME (L~96)
- collectLiveDescriptors() function (L~203-240)
- data-agrune-* entries from SNAPSHOT_RELEVANT_ATTRIBUTES (L~100-116, 10 entries)
- Any fallback paths in getTargetAttributes (L~522-523) or resolveMeta (L~584) that read data-agrune-* attributes

Change in packages/runtime/src/runtime/page-agent-runtime.ts:
- L27: remove `collectLiveDescriptors` / `mergeDescriptors` imports (delete mergeDescriptors if no other caller)
- L205 BEFORE: const getDescriptors = () => mergeDescriptors(manifestDescriptors, collectLiveDescriptors())
- L205 AFTER:  const getDescriptors = () => manifestDescriptors

Symbols to REMOVE from packages/runtime/src/runtime/dom-utils.ts:
- L393-394: isSensitive legacy fallback reading `data-agrune-sensitive` attribute (keep type=password / autocomplete / word-boundary regex / ARIA label heuristics)
- L505-520: buildLiveSelector function (and any remaining caller in snapshot.ts L~231)
MUST KEEP:
- L8:   AGRUNE_INTERNAL_SELECTOR constant (covers data-agrune-aurora, data-agrune-pointer)
- L39-40: isAgruneInternalElement helper

Changes in packages/runtime/src/runtime/command-handlers.ts:
- L319 detectMaskedInput: remove `el.hasAttribute('data-agrune-masked')` branch (keep type=password / autocomplete / sensitive heuristic path)
- L921-926: isInCanvasGroup — DELETE
- L928-958: getCanvasGroupTransform — DELETE
- L960-970: findCanvasGroupEl — DELETE
- L1174-1220 (approx): drag auto-pan canvas branch — DELETE (the non-canvas drag path stays)
- Remove unused imports `canvasToViewport`, `viewportToCanvas` if they become orphaned
- CANVAS_PAN_FAILED error-code emission site: if unreachable after removal, keep the enum entry but delete the throw site (safer than removing the error-code enum in this wave)

Changes in packages/browser/src/cdp-runtime-injector.ts (L125-144):
BEFORE:
  const resolveManifest = () => {
    if (window.__agrune_manifest__)          return { manifest: ..., hasManifest: true, source: 'window' };
    if (window.__agrune_preload_manifest__)  return { manifest: ..., hasManifest: true, source: 'preload' };
    // Legacy inline-scan path — maintained until Phase 17 (REMOVE-01).
    if (typeof runtimeApi.scanAnnotations === 'function' && ...) { ... source: 'inline' ... }
    return { manifest: runtimeApi.buildEmptyManifest(), hasManifest: false, source: 'idle' };
  };
AFTER:
  const resolveManifest = () => {
    if (window.__agrune_manifest__)          return { manifest: window.__agrune_manifest__,          hasManifest: true, source: 'window' };
    if (window.__agrune_preload_manifest__)  return { manifest: window.__agrune_preload_manifest__,  hasManifest: true, source: 'preload' };
    return { manifest: runtimeApi.buildEmptyManifest(), hasManifest: false, source: 'idle' };
  };

Also:
- L126 (comment "legacy inline scan") — DELETE
- L91, L212, L296 — terminology comments referencing 'annotation' / 'inline': audit and rewrite neutral
- `source` TypeScript union: narrow from `'window'|'preload'|'inline'|'idle'` to `'window'|'preload'|'idle'`
- Any consumer of RuntimeState['source'] that switches on 'inline' must be handled (will surface via `pnpm typecheck` — follow the errors)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Regression spec scaffolding (public surface + runtime-ignores-legacy)</name>
  <files>
    packages/runtime/tests/public-surface.spec.ts,
    packages/runtime/tests/runtime-ignores-legacy.spec.ts
  </files>
  <action>
  Create two new vitest specs **before** modifying src. These anchor the REMOVE-01 contract.

  **packages/runtime/tests/public-surface.spec.ts** (negative export surface test):
  - `import * as runtime from '../src'` (or '../src/index')
  - Assertions (each `expect(...).toBeUndefined()`):
    - `runtime.scanAnnotations`
    - `runtime.scanGroups`
    - `runtime.buildManifest`
  - Assert type re-exports absent: use `expectTypeOf` or a compile-time marker. If simpler, just assert the runtime values. Note in a comment: "Type-level absence enforced by tsc at build time."
  - Keep spec LEAN — this is a shape check, not behavior.

  **packages/runtime/tests/runtime-ignores-legacy.spec.ts** (positive regression):
  - Import PageAgentRuntime bootstrap helper (follow the pattern used by `bootstrap-gate.spec.ts` / `runtime.spec.ts`).
  - Set up a DOM with `document.body.innerHTML = '<button data-agrune-action="submit" data-agrune-key="x">go</button>'`
  - Bootstrap runtime with `buildEmptyManifest()` (no window manifest, no preload).
  - Wait for runtime to reach idle.
  - Assert:
    - `getDescriptors()` length === 0 (or whatever the manifest-empty baseline is)
    - No descriptor's `id` / `selector` references `data-agrune-action` or `data-agrune-key`
    - Optional: assert `source === 'idle'` if the test harness exposes it (otherwise skip).
  - These specs MUST fail in this task (source still uses live-scan) — that's Wave 0 RED by construction. We will turn them green in Tasks 2-4.

  **Verification strategy:** initial run shows both specs FAIL (expected). After Tasks 2-4 complete, they PASS. Commit separately so history shows RED→GREEN explicitly.

  Commit message: `test(17-01): add REMOVE-01 regression specs (public-surface + runtime-ignores-legacy)`
  </action>
  <verify>
    <automated>pnpm --filter @agrune/runtime test -- public-surface runtime-ignores-legacy --run 2>&1 | tail -40</automated>
  </verify>
  <done>
  두 spec 파일이 존재하고, **이 task 시점에서는 FAIL** (Red phase). FAIL 사유가 "runtime.scanAnnotations is not undefined" + "descriptors include data-agrune-action" 같은 **source-removal 미완료** 관련이어야 하며, import error나 문법 오류로 인한 실패가 아니어야 한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete scanner/builder files + clean page-runtime & index re-exports + cdp-runtime-injector legacy branch</name>
  <files>
    packages/runtime/src/dom-scanner.ts,
    packages/runtime/src/manifest-builder.ts,
    packages/runtime/src/page-runtime.ts,
    packages/runtime/src/index.ts,
    packages/runtime/src/types.ts,
    packages/browser/src/cdp-runtime-injector.ts
  </files>
  <action>
  Physical deletion + export cleanup + injector contract narrowing. Order matters — follow strictly.

  **Step A: Delete files**
  - `rm packages/runtime/src/dom-scanner.ts`
  - `rm packages/runtime/src/manifest-builder.ts`

  **Step B: Clean re-exports**
  - `packages/runtime/src/page-runtime.ts`: remove the `export { scanAnnotations, scanGroups, buildManifest } from './dom-scanner'` and `./manifest-builder` lines (L19-20).
  - `packages/runtime/src/index.ts` (L19-22): remove value exports `scanAnnotations|scanGroups|buildManifest` AND type exports `ScannedTarget|ScannedGroup`.
  - `packages/runtime/src/types.ts`: if `ScannedTarget`/`ScannedGroup` are defined here and ONLY consumed by the deleted scanner/builder, delete the type definitions. Use `pnpm --filter @agrune/runtime typecheck` to confirm no dangling reference.

  **Step C: Simplify injector (packages/browser/src/cdp-runtime-injector.ts)**
  - Locate `resolveManifest` (L~125-144). Replace legacy inline-scan branch per <interfaces> AFTER block. Final shape is 3 returns only (window / preload / idle).
  - Delete the L126 "Legacy inline-scan path — maintained until Phase 17 (REMOVE-01)" comment.
  - Find the `source` union type. If declared as `'window' | 'preload' | 'inline' | 'idle'`, narrow to `'window' | 'preload' | 'idle'`.
  - Audit L91, L212, L296 comments/strings for legacy-annotation language — rewrite neutrally (e.g., "inline scan" → "manifest resolution", "annotations" → "manifest targets" where appropriate per Pitfall 4 — do NOT touch `data-agrune-aurora`/`data-agrune-pointer` or internal cursor marker).

  **Step D: Drive typecheck errors to ground zero**
  Run `pnpm --filter @agrune/runtime typecheck && pnpm --filter @agrune/browser typecheck`. Each error points to a still-importing consumer. Fix bottom-up:
  - Unused imports → remove
  - `source === 'inline'` checks → remove that branch
  - `ScannedTarget`/`ScannedGroup` type references → remove the parameter/return type annotation (replace with a manifest-native type if genuinely needed)

  **Commit boundary**
  - Commit A: `refactor(17-01): delete dom-scanner + manifest-builder + runtime re-exports`
  - Commit B: `refactor(17-01): drop inline-scan branch from CdpRuntimeInjector.resolveManifest`

  Keep the two commits separate so bisect is cheap.
  </action>
  <verify>
    <automated>pnpm --filter @agrune/runtime typecheck && pnpm --filter @agrune/browser typecheck && pnpm --filter @agrune/runtime test -- public-surface --run 2>&1 | tail -30</automated>
  </verify>
  <done>
  - `packages/runtime/src/dom-scanner.ts` and `manifest-builder.ts` do NOT exist.
  - `pnpm --filter @agrune/runtime typecheck` exits 0.
  - `pnpm --filter @agrune/browser typecheck` exits 0.
  - `public-surface.spec.ts` from Task 1 now PASSES.
  - `grep -rn "scanAnnotations\\|scanGroups\\|buildManifest\\|ScannedTarget\\|ScannedGroup" packages/runtime/src packages/browser/src` prints 0 lines (outside of deleted files).
  - `grep -n "'inline'" packages/browser/src/cdp-runtime-injector.ts` prints 0 lines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Excise live-scan from snapshot.ts + collapse page-agent-runtime.getDescriptors + prune dom-utils legacy fallbacks</name>
  <files>
    packages/runtime/src/runtime/snapshot.ts,
    packages/runtime/src/runtime/page-agent-runtime.ts,
    packages/runtime/src/runtime/dom-utils.ts
  </files>
  <action>
  Remove the **post-bootstrap** live-scan path. This is the task that actually makes "runtime ignores data-agrune-*" true.

  **Step A: packages/runtime/src/runtime/snapshot.ts**
  - Delete constants (L~93-96): `LIVE_SCAN_ACTION_SELECTOR`, `LIVE_SCAN_GROUP_SELECTOR`, `LIVE_SCAN_DEFAULT_GROUP_ID`, `LIVE_SCAN_DEFAULT_GROUP_NAME`.
  - Delete function `collectLiveDescriptors()` (L~203-240) entirely, including any helpers it depends on that become orphaned (e.g. a local `isLiveActionCandidate` if one exists).
  - In `SNAPSHOT_RELEVANT_ATTRIBUTES` (L~100-116), remove all 10 `data-agrune-*` entries. Keep non-agrune entries (aria-*, role, etc.). Verify final list is the non-legacy subset.
  - In `getTargetAttributes` (~L522-523): remove any fallback that reads `data-agrune-*`. The manifest provides everything needed.
  - In `resolveMeta` (~L584): remove any `data-agrune-meta` parsing.
  - Around L~231: remove the call to `buildLiveSelector(...)` (which we'll delete from dom-utils in Step C). If `buildLiveSelector` is also called elsewhere in snapshot.ts (e.g. L~608-617, L~715), delete those call-sites too.
  - Audit imports at top of snapshot.ts — remove now-unused imports (`buildLiveSelector` from dom-utils, anything from dom-scanner/manifest-builder).

  **Step B: packages/runtime/src/runtime/page-agent-runtime.ts**
  - L27: remove `collectLiveDescriptors` and `mergeDescriptors` imports (use `pnpm typecheck` to confirm `mergeDescriptors` has no other caller; if it does, leave it; if not, delete its definition too).
  - L~205:
    BEFORE: `const getDescriptors = () => mergeDescriptors(manifestDescriptors, collectLiveDescriptors())`
    AFTER:  `const getDescriptors = () => manifestDescriptors`
  - If `manifestDescriptors` is recomputed on mutation anywhere (search for MutationObserver / `onDescriptorsChange`), ensure the mutation path still works — but the live-scan side is gone.

  **Step C: packages/runtime/src/runtime/dom-utils.ts**
  - L~393-394: delete the `isSensitive` legacy fallback that reads `el.hasAttribute('data-agrune-sensitive')` or `el.dataset.agruneSensitive === 'true'`. Keep the type=password / autocomplete whitelist / word-boundary regex / ARIA label heuristics — those are the OR-only sensitive mask from Phase 14.
  - L~505-520: delete `buildLiveSelector` function entirely.
  - **KEEP** `AGRUNE_INTERNAL_SELECTOR` (L8) and `isAgruneInternalElement` (L39-40). These cover cursor animation markers (`data-agrune-aurora`, `data-agrune-pointer`) which are **non-negotiable** per PROJECT.md / MEMORY. Do NOT touch these.

  **Step D: typecheck + spec**
  - `pnpm --filter @agrune/runtime typecheck` — fix any dangling reference.
  - `pnpm --filter @agrune/runtime test -- runtime-ignores-legacy --run` — the spec from Task 1 should now PASS.
  - Full runtime suite: `pnpm --filter @agrune/runtime test --run`. Some existing specs (runtime.spec.ts, fill-cdp.spec.ts, macro-runner.spec.ts, sensitive-or-only.spec.ts) use `data-agrune-key` as a CSS selector inside a manifest — that is **fixture-only CSS usage**, allowed by Success Criteria 4. Those specs should still PASS. If any fail because they depend on live-scan side-effects, that indicates a legitimate behavior change — update the spec to use manifest-native approach.

  **Commit boundary**
  - Commit A: `refactor(17-01): excise LIVE_SCAN_* + collectLiveDescriptors from snapshot.ts`
  - Commit B: `refactor(17-01): collapse getDescriptors to manifest-only in page-agent-runtime`
  - Commit C: `refactor(17-01): drop isSensitive legacy attribute fallback + buildLiveSelector`
  </action>
  <verify>
    <automated>pnpm --filter @agrune/runtime typecheck && pnpm --filter @agrune/runtime test --run 2>&1 | tail -60</automated>
  </verify>
  <done>
  - `grep -n "LIVE_SCAN_\\|collectLiveDescriptors\\|buildLiveSelector" packages/runtime/src` prints 0 lines.
  - `grep -n "data-agrune-sensitive\\|data-agrune-meta" packages/runtime/src/runtime/dom-utils.ts packages/runtime/src/runtime/snapshot.ts` prints 0 lines.
  - `grep -n "AGRUNE_INTERNAL_SELECTOR" packages/runtime/src/runtime/dom-utils.ts` prints at least one line (NOT deleted).
  - Runtime full suite green, including the new `runtime-ignores-legacy.spec.ts` from Task 1.
  - page-agent-runtime.ts line has exactly `const getDescriptors = () => manifestDescriptors` (or semantically equivalent — no `collectLiveDescriptors` call).
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove canvas + masked-input legacy branches from command-handlers + rebuild dist + regenerate e2e runtime bundle</name>
  <files>
    packages/runtime/src/runtime/command-handlers.ts,
    packages/e2e/fixtures/runtime.bundle.js
  </files>
  <action>
  Final src cleanup (canvas dead code, masked-input legacy) plus mandatory rebuild to avoid Pitfall 2 (E2E bundle staleness).

  **Step A: packages/runtime/src/runtime/command-handlers.ts**
  - L~319 (`detectMaskedInput`): remove the `hasAttribute('data-agrune-masked')` / `dataset.agruneMasked` branch. Keep type=password / autocomplete-based detection.
  - L~921-926: DELETE `isInCanvasGroup`.
  - L~928-958: DELETE `getCanvasGroupTransform`.
  - L~960-970: DELETE `findCanvasGroupEl`.
  - L~1174-1220 (drag auto-pan): DELETE the canvas branch. The non-canvas drag path stays. If the function becomes straight-through, simplify.
  - L~1175, 1201, 1249, 1269 (per RESEARCH.md): scan for other `canvas` / `data-agrune-canvas` references and remove.
  - Remove now-unused imports `canvasToViewport`, `viewportToCanvas` (if they exist in this file and become orphaned).
  - `CANVAS_PAN_FAILED` error-code enum entry: leave the **enum definition** alone (do NOT alter error-code schema in this task — that's out of scope). Remove only the `throw new Error({ code: 'CANVAS_PAN_FAILED' })` call-sites that are now unreachable.

  **Step B: Build runtime + regenerate E2E bundle**
  - `pnpm --filter @agrune/runtime build` — regenerates `packages/runtime/dist/*`.
  - Determine how `packages/e2e/fixtures/runtime.bundle.js` is produced:
    - Check `packages/e2e/package.json` for a `build:fixtures` or similar script.
    - Check `packages/e2e/scripts/` for a build entry.
    - If a script exists: `pnpm --filter @agrune/e2e run build:fixtures` (or the correct name).
    - If no script exists: copy/bundle the runtime dist output into `packages/e2e/fixtures/runtime.bundle.js` following the pattern already in that file (inspect first line — tsup/esbuild banner reveals the producer).
  - `git status packages/e2e/fixtures/runtime.bundle.js` — if modified, the bundle was commit-tracked (per RESEARCH.md A2 assumption). Commit the regenerated bundle.
  - `pnpm -r build` — ensure browser/mcp dist consumers rebuild too.

  **Step C: Run full suite (unit + e2e smoke)**
  - `pnpm --filter @agrune/runtime test --run`  — must stay green.
  - `pnpm --filter @agrune/browser test --run` (if suite exists) — must stay green.
  - Optional: `pnpm --filter @agrune/e2e exec playwright test bootstrap-idle.spec.ts --project=chromium` — may fail on `legacy-annotated` expectation (we will fix in Wave 2, 17-02). If the FAIL reason is "expected source=inline but got source=idle", that's **correct for Phase 17** — log it and let Wave 2 handle the fixture reversal. If it fails for any other reason, investigate.

  **Commit boundary**
  - Commit A: `refactor(17-01): delete canvas helpers + drag auto-pan branch + masked-input legacy check`
  - Commit B: `chore(17-01): rebuild runtime dist + regenerate e2e runtime.bundle.js`
  </action>
  <verify>
    <automated>pnpm --filter @agrune/runtime test --run && grep -rn "data-agrune-" packages/runtime/src packages/browser/src 2>&1 | tail -40</automated>
  </verify>
  <done>
  - `grep -rn "data-agrune-" packages/runtime/src packages/browser/src` prints 0 lines **except** permitted internal markers in `dom-utils.ts` (`data-agrune-aurora`, `data-agrune-pointer` referenced by `AGRUNE_INTERNAL_SELECTOR`). If AGRUNE_INTERNAL_SELECTOR is a string literal containing those attributes, it counts as allowed — verify the grep matches are exactly those two markers and nothing else.
  - `grep -n "isInCanvasGroup\\|getCanvasGroupTransform\\|findCanvasGroupEl" packages/runtime/src` prints 0 lines.
  - `grep -n "data-agrune-masked" packages/runtime/src` prints 0 lines.
  - `pnpm -r build` exits 0.
  - `packages/e2e/fixtures/runtime.bundle.js` regenerated — no occurrence of `scanAnnotations` / `LIVE_SCAN_` / `collectLiveDescriptors` in the bundle.
  - Runtime full suite green.
  - `bootstrap-idle.spec.ts` may fail with `legacy-annotated: expected source=inline, got source=idle` — acceptable, documented for Wave 2 (17-02) to fix the fixture.
  </done>
</task>

</tasks>

<verification>
**Gate checks (run in order, all must pass for this plan to be DONE):**

1. `find packages/runtime/src -name 'dom-scanner.ts' -o -name 'manifest-builder.ts' | wc -l` → `0`
2. `pnpm --filter @agrune/runtime typecheck` exits 0
3. `pnpm --filter @agrune/browser typecheck` exits 0
4. `pnpm --filter @agrune/runtime test --run` exits 0
5. `pnpm -r build` exits 0
6. `grep -rn "data-agrune-" packages/runtime/src packages/browser/src` matches ONLY internal cursor marker references (`data-agrune-aurora`, `data-agrune-pointer` in dom-utils.ts)
7. `grep -rn "scanAnnotations\\|scanGroups\\|buildManifest\\|LIVE_SCAN_\\|collectLiveDescriptors\\|buildLiveSelector" packages/runtime/src packages/browser/src` prints 0 lines
8. New specs `public-surface.spec.ts` and `runtime-ignores-legacy.spec.ts` exist and PASS

**Known acceptable regression (to be fixed in 17-02):** `packages/e2e/tests/bootstrap-idle.spec.ts`의 `legacy-annotated.html` expectation이 `source=inline`을 요구하는 경우 FAIL. Wave 2에서 fixture 의미 반전으로 해결.

**Non-goal for this plan (explicit):**
- E2E fixtures `legacy-annotated.html` / `idle-boot.html` / user-flow helpers의 inline bootstrap script rewrite — Wave 2 (17-02)
- README/AGENTS/docs 재작성 — Wave 3 (17-03)
- External repo sync 및 regression-guard script 작성 — Wave 4 (17-04)
- `lint:annotations` script 삭제 — Wave 4 (17-04)
</verification>

<success_criteria>
- Physical deletion: `dom-scanner.ts`, `manifest-builder.ts` 부재
- Export surface: `scanAnnotations/scanGroups/buildManifest/ScannedTarget/ScannedGroup` 공개 API 부재
- Runtime behavior: `getDescriptors()` = `manifestDescriptors` (live-scan 경로 0)
- Injector: `resolveManifest`의 source union = `'window'|'preload'|'idle'` (3가지)
- DOM에 `data-agrune-action/key/group/meta/sensitive/masked/canvas` 있어도 target 0 (regression spec으로 증명)
- `data-agrune-aurora/pointer` 내부 마커는 유지 (cursor animation 보존)
- `pnpm -r build` + `pnpm --filter @agrune/runtime test` 전부 green
- E2E runtime bundle 재생성되어 legacy symbol 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-remove/17-01-SUMMARY.md` documenting:
- 삭제된 파일 목록
- src에서 제거된 심볼 inventory
- new spec 2개의 PASS 증거
- E2E bundle 재생성 확인 (git status 결과)
- Wave 2에 인계되는 known regression (bootstrap-idle.spec legacy-annotated expectation)
- Wave 2/3/4 handoff 체크리스트 (fixture 의미 반전 + doc rewrite + regression guard)
</output>
</content>
</invoke>