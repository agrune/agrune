---
phase: "17-remove"
plan: "04"
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - scripts/regression-guard/no-legacy-data-agrune.sh
  - scripts/regression-guard/data-agrune-allowlist.txt
  - package.json
  - README.md
  - AGENTS.md
  - .github/workflows/ci.yml
  - /Users/chenjing/dev/agrune/.github/profile/README.md
  - .planning/phases/17-remove/external-sync-instructions.md
autonomous: true
requirements:
  - REMOVE-02
  - REMOVE-03
tags:
  - regression-guard
  - external-sync
  - ci
must_haves:
  truths:
    - "`scripts/regression-guard/no-legacy-data-agrune.sh` 가 존재하고, allow-list 에 명시된 경로 외에서 `data-agrune-`이 발견되면 non-zero로 종료한다"
    - "`scripts/regression-guard/data-agrune-allowlist.txt` 가 존재하며 annotation-lint 내부 파일 + 허용된 fixture 경로 + runtime 내부 cursor 마커 주석을 line-by-line으로 나열한다"
    - "루트 `package.json` 의 scripts 중 `lint:annotations` 엔트리가 삭제되고 `lint:no-legacy` 가 추가되어 있다 (RESEARCH.md Open Q 1 권고)"
    - "README.md / AGENTS.md 의 script 목록이 `lint:annotations` 대신 `lint:no-legacy` 를 설명한다"
    - "CI workflow가 `pnpm lint:no-legacy` 단계를 포함한다 (legacy `lint:annotations` job은 교체됨)"
    - "외부 `/Users/chenjing/dev/agrune/.github/profile/README.md` 가 'annotation' → 'target mapping' 용어로 sync되어 있고 manifest pivot 메시지를 반영한다"
    - "`/Users/chenjing/dev/agrune/skills/skills/annotate/` 폐기 및 외부 `.github` push 지침이 `external-sync-instructions.md` 에 구체 diff로 문서화되어 있다"
    - "`pnpm lint:no-legacy` 를 repo 루트에서 실행하면 exit 0 으로 통과한다"
  artifacts:
    - path: "scripts/regression-guard/no-legacy-data-agrune.sh"
      provides: "POSIX bash regression guard script — allow-list 외 `data-agrune-` 매치 시 exit 1"
      contains: "data-agrune-allowlist.txt"
    - path: "scripts/regression-guard/data-agrune-allowlist.txt"
      provides: "allow-list of permitted paths (test fixtures, annotation-lint package)"
      contains: "packages/core/src/annotation-lint"
    - path: "package.json"
      provides: "`lint:annotations` 제거 + `lint:no-legacy` 추가"
      contains: "lint:no-legacy"
    - path: ".github/workflows/ci.yml"
      provides: "CI가 `pnpm lint:no-legacy` 단계 실행"
      contains: "lint:no-legacy"
    - path: "/Users/chenjing/dev/agrune/.github/profile/README.md"
      provides: "외부 조직 프로필 README — manifest pivot 반영, target mapping 용어"
      contains: "target mapping"
    - path: ".planning/phases/17-remove/external-sync-instructions.md"
      provides: "외부 repo (`.github` / `skills`) sync 절차 — 사용자 수동 push 가이드"
      contains: "git push"
  key_links:
    - from: "package.json"
      to: "scripts/regression-guard/no-legacy-data-agrune.sh"
      via: "npm script `lint:no-legacy` 가 스크립트 실행"
      pattern: "bash \\./scripts/regression-guard/no-legacy-data-agrune\\.sh"
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts.lint:no-legacy"
      via: "CI job이 pnpm lint:no-legacy 호출"
      pattern: "pnpm lint:no-legacy"
    - from: "scripts/regression-guard/no-legacy-data-agrune.sh"
      to: "scripts/regression-guard/data-agrune-allowlist.txt"
      via: "script가 allow-list를 읽어 false positive 필터링"
      pattern: "ALLOWLIST=.*data-agrune-allowlist\\.txt"
---

<objective>
**Regression guard script + CI 배선 + `lint:annotations` script 폐기 + 외부 repo sync 지침 + 외부 `.github/profile/README.md` 수정.**

Wave 4는 Phase 17 종료 gate. 다음 4가지가 동시에 닫힌다:

1. **Regression guard 도입** — RESEARCH.md §Example 3 의 POSIX bash script + 별도 allow-list 파일. CI에 wire.
2. **`lint:annotations` script 제거** — RESEARCH.md Open Q 1 권고(삭제) 실행. `package.json` / `README.md` / `AGENTS.md` / `.github/workflows/*.yml` script list 정리. **annotation-lint 패키지 자체는 유지** (Success Criteria 4 예외).
3. **외부 `.github/profile/README.md` sync** — REMOVE-03 성공 기준. "annotation" → "target mapping" 용어 + manifest pivot 반영. 본 plan 은 **파일 수정만 수행**, push는 사용자 수동(Pitfall 5).
4. **외부 sync 지침 문서** — `external-sync-instructions.md` 작성. `.github/profile/` push + `/Users/chenjing/dev/agrune/skills/skills/annotate/` 폐기 절차 + 사용자가 실행할 git commands + 예상 diff 요약.

Final gate: `pnpm lint:no-legacy && pnpm -r build && pnpm -r test && pnpm test:e2e` 전부 green.

Purpose: Phase 17의 성공 기준 4(`grep -r 'data-agrune-' packages/` ≤ allow-list)를 구조적으로 보장 + Phase 18 공개 전제 확정.
Output: regression script/allowlist 2개 파일, package.json/README/AGENTS/ci.yml 수정, 외부 profile README 수정, 외부 sync 지침 문서.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-remove/17-CONTEXT.md
@.planning/phases/17-remove/17-RESEARCH.md
@.planning/phases/17-remove/17-01-SUMMARY.md
@.planning/phases/17-remove/17-02-SUMMARY.md
@.planning/phases/17-remove/17-03-SUMMARY.md
@package.json
@README.md
@AGENTS.md
@/Users/chenjing/dev/agrune/.github/profile/README.md

<interfaces>
<!-- Reference shapes for this wave's deliverables. -->

Regression script contract (RESEARCH.md §Example 3):
```bash
#!/usr/bin/env bash
# scripts/regression-guard/no-legacy-data-agrune.sh
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
ALLOWLIST="$ROOT/scripts/regression-guard/data-agrune-allowlist.txt"

MATCHES=$(grep -rEn 'data-agrune-' "$ROOT/packages" \
  --include='*.ts' --include='*.tsx' --include='*.js' --include='*.html' --include='*.md' \
  --exclude-dir=node_modules --exclude-dir=dist || true)

# Filter allow-listed paths out
while IFS= read -r allow; do
  [[ -z "$allow" || "$allow" == \#* ]] && continue
  MATCHES=$(echo "$MATCHES" | grep -vF "$allow" || true)
done < "$ALLOWLIST"

if [[ -n "$MATCHES" ]]; then
  echo "Phase 17 regression: legacy 'data-agrune-' found outside allow-list:"
  echo "$MATCHES"
  exit 1
fi
echo "OK - No legacy 'data-agrune-' outside allow-list."
```

Allow-list contract (text file, one relative path prefix per line, `#` comments):
```
# scripts/regression-guard/data-agrune-allowlist.txt
# Phase 17 REMOVE: paths permitted to reference 'data-agrune-'.
# Changes to this list require explicit phase-level review.
packages/core/src/annotation-lint
packages/core/tests/annotation-lint.spec.ts
packages/runtime/tests/runtime.spec.ts
packages/runtime/tests/fill-cdp.spec.ts
packages/runtime/tests/macro-runner.spec.ts
packages/runtime/tests/sensitive-or-only.spec.ts
packages/runtime/tests/runtime-ignores-legacy.spec.ts
packages/runtime/src/runtime/dom-utils.ts   # AGRUNE_INTERNAL_SELECTOR (aurora/pointer cursor markers)
packages/e2e/fixtures/legacy-annotated.html
packages/e2e/fixtures/tricky-inputs.html
packages/e2e/fixtures/overlay-modal.html
packages/e2e/tests/annotation-scan.spec.ts
packages/e2e/tests/overlay-modal.spec.ts
```

package.json scripts diff:
```diff
-"lint:annotations": "node ./packages/core/bin/agrune-lint.js packages apps",
+"lint:no-legacy":   "bash ./scripts/regression-guard/no-legacy-data-agrune.sh",
```

External profile README rewrite (high-level):
- "annotation" 기반 제품 서술 → "manifest-based target mapping"
- `data-agrune-*` 예제 섹션 (있다면) → `defineManifest` 예제
- "Chrome extension" 표현이 남아있다면 "MCP server + CDP" 로 교정 (이미 v1.1 DOCS-02 작업에서 일부 했지만 push가 미뤄진 상태 — Pending todo)
- 핵심 메시지: "Manifest-driven, CDP-only, local-first browser automation with AI agent control"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create regression-guard script + allow-list + wire into package.json</name>
  <files>
    scripts/regression-guard/no-legacy-data-agrune.sh,
    scripts/regression-guard/data-agrune-allowlist.txt,
    package.json,
    README.md,
    AGENTS.md
  </files>
  <action>
  Core infrastructure task — regression script + allow-list + script wiring.

  **Step A: Create script + allow-list**
  - `mkdir -p scripts/regression-guard`
  - Create `scripts/regression-guard/no-legacy-data-agrune.sh` per <interfaces> contract. Make it executable: `chmod +x scripts/regression-guard/no-legacy-data-agrune.sh`.
  - Create `scripts/regression-guard/data-agrune-allowlist.txt` per <interfaces> contract.
    - 시작 상태는 RESEARCH.md §3 Component 표 기준 + 17-01~17-03 진행 중 실제로 남은 매치를 **실측으로** 검증해 curating. 즉: 스크립트 먼저 완성 → allow-list 빈 상태에서 실행 → 매치 목록 → 정당한 경로만 allow-list에 추가.
    - 각 항목에 대해 trailing comment로 reason 명시 (예: `# AGRUNE_INTERNAL_SELECTOR cursor markers`).
  - allow-list 매칭은 line-by-line `grep -vF` 기반이므로 prefix match가 아니라 substring match. 따라서 경로가 유일하게 식별 가능하도록 충분히 구체적으로 기재.

  **Step B: Dry-run iterate**
  - `bash scripts/regression-guard/no-legacy-data-agrune.sh || true`
  - 출력된 매치들을 분류:
    - 유지돼야 할 legitimate reference (annotation-lint package, fixture, internal cursor marker, test) → allow-list에 추가
    - 놓친 제거 대상 → 해당 wave(1/2/3)로 돌아가 fix (이 플랜에서 직접 고쳐도 되지만 small scope면 적절한 wave의 후속 커밋으로 핸들)
    - WORKFLOW.md의 "Deprecated workflows" historical 문단에 literal attribute가 없도록 17-03이 phrasing으로 회피했는지 확인 (17-03 summary 참고)
  - 반복 실행해 exit 0 달성.

  **Step C: Wire into package.json**
  - `package.json` scripts 섹션:
    - `lint:annotations` 엔트리 삭제 (RESEARCH.md Open Q 1 권고 a, L10).
    - `lint:no-legacy` 엔트리 추가: `"lint:no-legacy": "bash ./scripts/regression-guard/no-legacy-data-agrune.sh"`
    - 기존 `lint` 또는 `verify` aggregate script가 있으면 `lint:no-legacy`를 거기에 포함.

  **Step D: Update README.md + AGENTS.md script list**
  - 17-03에서 본문 설명은 정리했지만 script list 엔트리 자체는 건드리지 않았다 (W4 담당으로 명시). 본 task에서:
    - README.md L209 근처 script 표/목록에서 `lint:annotations` 라인 삭제, `lint:no-legacy` 라인 추가 (설명: "CI guard — fails on legacy `data-agrune-` references outside allow-list").
    - AGENTS.md L28 근처 동일 처리.
    - 다른 문서(docs/agent-setup.md 등)에 `lint:annotations` 언급이 남아있는지 `grep -rn "lint:annotations" --exclude-dir=node_modules --exclude-dir=dist` 로 확인. 남아있으면 삭제/교체.

  **Step E: Run and commit**
  - `pnpm lint:no-legacy` exit 0 확인.
  - Commit boundary:
    - A: `chore(17-04): add regression-guard script + allow-list for data-agrune-`
    - B: `chore(17-04): drop lint:annotations script, add lint:no-legacy + docs`

  **Rationale reminder (RESEARCH.md Pitfall 6)**: `@agrune/core/annotation-lint` 패키지는 그대로 publish (외부 소비자용), 루트의 `lint:annotations` script만 제거.
  </action>
  <verify>
    <automated>bash scripts/regression-guard/no-legacy-data-agrune.sh && pnpm lint:no-legacy && grep -n "lint:annotations" package.json README.md AGENTS.md 2>&1 | tail -10</automated>
  </verify>
  <done>
  - `scripts/regression-guard/no-legacy-data-agrune.sh` + `.../data-agrune-allowlist.txt` 존재, script는 실행 가능.
  - `bash scripts/regression-guard/no-legacy-data-agrune.sh` exit 0.
  - `pnpm lint:no-legacy` exit 0.
  - `grep -n "lint:annotations" package.json README.md AGENTS.md` 0 matches.
  - `package.json` scripts에 `lint:no-legacy` 엔트리 존재.
  - allow-list 각 줄에 trailing comment / 카테고리 구분이 있어 리뷰 시 이유 즉시 파악 가능.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire regression guard into CI workflow</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
  CI가 regression guard를 강제하도록 job 수정.

  **Step A: Locate CI workflow file**
  - `ls -la .github/workflows/` 로 파일명 확인. 보통 `ci.yml` 또는 여러 개 (`test.yml`, `lint.yml`).
  - 기존 `lint:annotations` step이 있는 workflow 식별 (17-01~17-03 진행 중 CI가 이미 이 step으로 fail 하고 있을 가능성 — 본 단계에서 최종 정리).

  **Step B: Edit workflow**
  - 기존 `run: pnpm lint:annotations` 단계를 `run: pnpm lint:no-legacy` 로 교체.
    - Step name도 업데이트 (예: `Legacy data-agrune regression guard`).
  - 만약 별도 step이 없었다면 `lint` job 또는 `test` job에 새 step 추가:
    ```yaml
    - name: Legacy data-agrune regression guard
      run: pnpm lint:no-legacy
    ```
  - `bash`가 기본 shell이 아닐 경우(Windows runner 등) `shell: bash` 명시. 본 repo CI는 현재 macOS/Linux 가정이므로 기본으로 OK.
  - 여러 workflow 파일에 흩어져 있다면 일관되게 처리.

  **Step C: Verify locally (best-effort)**
  - CI yaml syntax check: `yq` 또는 간단한 `cat` 후 시각 검토.
  - 가능하면 `act` (GitHub Actions local runner)로 dry-run. 없으면 skip.

  Commit: `ci(17-04): replace lint:annotations with lint:no-legacy in CI workflow`
  </action>
  <verify>
    <automated>grep -rn "lint:annotations\\|lint:no-legacy" .github/workflows/ 2>&1 | tail -10</automated>
  </verify>
  <done>
  - `grep -rn "lint:annotations" .github/workflows/` 0 matches.
  - `grep -rn "lint:no-legacy" .github/workflows/` 최소 1 match.
  - CI workflow yaml이 문법적으로 valid (시각 검토 또는 `yq` 파싱 통과).
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite external /Users/chenjing/dev/agrune/.github/profile/README.md + create external-sync-instructions.md</name>
  <files>
    /Users/chenjing/dev/agrune/.github/profile/README.md,
    .planning/phases/17-remove/external-sync-instructions.md
  </files>
  <action>
  외부 repo 두 개의 sync 작업. **push는 사용자 수동** (Pitfall 5) — 본 task는 파일 수정 + 지침 문서만.

  **Step A: Read current external profile README**
  - `cat /Users/chenjing/dev/agrune/.github/profile/README.md` 로 현재 내용 확인.
  - 2026-04-15 CDP-only 피봇과 2026-04-19 manifest 피봇 반영 상태 확인:
    - "extension" / "Chrome extension" 표현
    - `data-agrune-*` annotation 언급
    - manifest / `defineTarget` / `defineMacro` 언급 부재
    - "annotation" 단어의 제품 표면 사용

  **Step B: Rewrite /Users/chenjing/dev/agrune/.github/profile/README.md**
  재작성 원칙:
  - 제품 정체성: "Manifest-driven, CDP-only, local-first browser automation for AI agents"
  - 핵심 value prop 3줄:
    1. AI agents control web apps via MCP (11 tools)
    2. Targets defined in typed manifest (`@agrune/manifest` SDK) + optional root-import React integration
    3. Local + deterministic — CDP session on user-launched Chrome, no cloud, no vision fallback
  - 용어: "annotation" → "target mapping" (제품 표면 맥락 한정 — 일반적 annotation 단어가 있으면 보존).
  - 예제 코드 블록이 있다면 `defineManifest({ targets: [defineTarget({...})] })` 형태.
  - `data-agrune-*` 언급 전부 제거.
  - v0.5 milestone 상태 선언 문단 추가 (optional but recommended):
    "Current milestone: **v0.5 Manifest Pivot** (branch `feat/v0.5-manifest`). Tracking ships of `@agrune/manifest`, `@agrune/react`, and registry governance."
  - 링크: agrune main repo (`github.com/<org>/agrune` — 실제 org 명 확인), future registry (`github.com/agrune/maps` — Phase 18).

  **Step C: Create .planning/phases/17-remove/external-sync-instructions.md**
  본 plan이 외부 repo에 직접 push 불가하므로 사용자에게 수동 실행 지침 제공.

  문서 구조:
  ```markdown
  # Phase 17 External Repository Sync Instructions

  **Generated:** 2026-04-19 (Phase 17 Wave 4)
  **Status:** Awaiting user manual push

  Two external repositories need manual sync after Phase 17 completion.
  Agrune main repo has no push permission to these repos.

  ## 1. /Users/chenjing/dev/agrune/.github/

  **File edited by Phase 17-04:**
  - `/Users/chenjing/dev/agrune/.github/profile/README.md`

  **Diff summary:**
  [fill with actual diff summary — compare before/after narrative, list rewritten sections]

  **User action:**
  ```bash
  cd /Users/chenjing/dev/agrune/.github
  git status   # confirm only profile/README.md changed
  git diff profile/README.md
  git add profile/README.md
  git commit -m "docs(profile): manifest pivot — annotation→target mapping terminology"
  git push origin main   # or appropriate branch
  ```

  ## 2. /Users/chenjing/dev/agrune/skills/

  **Action required:** retire `skills/skills/annotate/` directory.

  **Rationale:**
  - v0.5 Manifest Pivot removed the inline `data-agrune-*` path.
  - `.agents/skills/manifest/` in main agrune repo is the authoritative replacement (Phase 16-04).
  - Phase 16-04 SUMMARY L149 declared annotate skill "retires once manifest skill reaches feature parity — this plan is the evidence".

  **User action:**
  ```bash
  cd /Users/chenjing/dev/agrune/skills
  git rm -r skills/annotate/
  # Optionally update skills/README.md to point to .agents/skills/manifest/ in the main agrune repo
  git status
  git commit -m "chore: retire skills/annotate per v0.5 manifest pivot (Phase 17)"
  git push origin main
  ```

  Optional: before `git rm`, copy any still-useful references into `.agents/skills/manifest/references/`
  in the main agrune repo (if not already done by Phase 16-04).

  ## Verification after manual push

  1. Visit github.com/<org> and confirm org profile card reflects new messaging.
  2. Confirm skills repo no longer shows `annotate/` directory.
  3. Mark Phase 17 complete in `.planning/ROADMAP.md` progress table.
  ```

  RESEARCH.md §Open Q 6 ("외부 skills repo 폐기 확정") 권고대로 구체 diff + 명령어 제공.

  **Step D: Commit**
  - 외부 `.github/profile/README.md` 수정은 **외부 repo** 작업이므로 agrune main repo에서는 commit 대상 아님. 다만 파일 자체는 Phase 17-04 실행 시점에 이미 편집된 상태여야 함.
  - `external-sync-instructions.md`는 agrune main repo에 commit: `docs(17-04): add external repository sync instructions for Phase 17`
  </action>
  <verify>
    <automated>grep -En "data-agrune-(action|key|group)|Chrome extension|browser extension" /Users/chenjing/dev/agrune/.github/profile/README.md 2>&1 | tail -10 && ls -la .planning/phases/17-remove/external-sync-instructions.md</automated>
  </verify>
  <done>
  - `/Users/chenjing/dev/agrune/.github/profile/README.md` 재작성 완료. `grep` 결과 legacy pattern 0 matches.
  - 해당 README가 "manifest" 또는 "target mapping" 또는 "@agrune/manifest" 언급 최소 1회.
  - `.planning/phases/17-remove/external-sync-instructions.md` 생성 + 두 repo에 대한 구체적 git commands + 예상 diff summary 포함.
  - Instructions가 명시적으로 "user manual push" 경계를 선언해 본 plan 범위 혼동 방지.
  </done>
</task>

<task type="auto">
  <name>Task 4: Full phase gate verification (build + test + e2e + lint:no-legacy)</name>
  <files></files>
  <action>
  Phase 17 종료 gate — 전체 suite 실행해 최종 green 확인.

  **Step A: Build all packages**
  ```bash
  pnpm -r build
  ```
  Exit 0 확인. `packages/*/dist/*` 가 최신 src를 반영.

  **Step B: Unit tests**
  ```bash
  pnpm -r test
  ```
  모든 패키지 suite green. 17-01의 신규 spec 2개(public-surface, runtime-ignores-legacy) PASS 포함.

  **Step C: E2E tests**
  ```bash
  pnpm test:e2e
  ```
  17-02가 해소한 bootstrap-idle legacy-annotated spec 및 전체 E2E suite PASS.

  **Step D: Regression guard**
  ```bash
  pnpm lint:no-legacy
  ```
  Exit 0 확인. (Task 1에서 이미 확인했지만 src/doc 최종 상태에서 재검증.)

  **Step E: Sanity grep**
  ```bash
  grep -rEn "scanAnnotations|scanGroups|buildManifest|LIVE_SCAN_|collectLiveDescriptors|buildLiveSelector" packages/runtime/src packages/browser/src
  ```
  0 lines.

  ```bash
  grep -rEn "data-agrune-(action|key|group|canvas|meta|masked|sensitive)" README.md AGENTS.md PRIVACY.md workflows/annotate/WORKFLOW.md docs/agent-setup.md docs/improvement-notes.md packages/mcp/README.md
  ```
  0 lines.

  **Step F: ROADMAP.md status update**
  - `.planning/ROADMAP.md` Phase 17 항목의 `Plans: TBD` → `Plans: 4 plans` 업데이트.
  - `[ ] Phase 17: REMOVE` → 각 plan `[x]` 체크 (실행 완료 상태는 `/gsd-execute-phase` orchestrator가 표시하지만, 본 task가 plan 인벤토리를 업데이트).
  - Progress 표 `17. REMOVE | v0.5 | 0/?` → `4/4 | Complete | 2026-04-19` (또는 실행일).

  **Step G: Commit (ROADMAP 변경만)**
  - `docs(17-04): mark Phase 17 REMOVE complete in ROADMAP + phase gate verification`

  **If any gate fails:**
  - Root-cause the failure.
  - Fix in the appropriate wave's follow-up commit (W1/W2/W3) — don't patch from W4.
  - Rerun gates until green.
  </action>
  <verify>
    <automated>pnpm -r build && pnpm -r test && pnpm test:e2e && pnpm lint:no-legacy 2>&1 | tail -40</automated>
  </verify>
  <done>
  - `pnpm -r build` exit 0
  - `pnpm -r test` exit 0
  - `pnpm test:e2e` exit 0
  - `pnpm lint:no-legacy` exit 0
  - Sanity greps (Step E) 모두 0 matches
  - ROADMAP.md Phase 17 항목이 `4 plans` + plan 목록 기록
  - Progress 표 Phase 17 항목 업데이트
  </done>
</task>

</tasks>

<verification>
**Phase 17 final gate:**

1. `bash scripts/regression-guard/no-legacy-data-agrune.sh` exit 0
2. `pnpm lint:no-legacy` exit 0
3. `pnpm -r build && pnpm -r test && pnpm test:e2e` all exit 0
4. `grep -n "lint:annotations"` 루트 + docs에서 0 matches
5. `grep -rEn "data-agrune-"` `packages/` 에서 allow-list 외 0 matches
6. `scripts/regression-guard/no-legacy-data-agrune.sh` + `data-agrune-allowlist.txt` 존재
7. CI workflow에 `pnpm lint:no-legacy` step 존재
8. `/Users/chenjing/dev/agrune/.github/profile/README.md` 수정 완료 (push는 사용자 수동)
9. `.planning/phases/17-remove/external-sync-instructions.md` 존재 + 두 외부 repo 대상 git 명령 포함
10. ROADMAP.md Phase 17 entry가 `Plans: 4 plans` 반영

**Non-goal for this plan (explicit):**
- 외부 `.github` repo에 직접 push — 사용자 수동 (Pitfall 5)
- 외부 `/Users/chenjing/dev/agrune/skills/skills/annotate/` 디렉터리 삭제 — 외부 repo 작업, instructions만 제공
- annotation-lint 패키지 삭제 — Success Criteria 4 명시 예외
- Phase 18 REGISTRY 준비 — 별도 phase

**Handoff to Phase 18:**
- schema stable 상태 확인 (manifest v3 확정)
- breaking surface 정리 완료 (inline 경로 0)
- regression guard가 CI로 wire되어 drift 감지 활성
</verification>

<success_criteria>
- Regression guard script + allow-list 존재 + CI wire + `pnpm lint:no-legacy` green
- 루트 `lint:annotations` script 삭제 + README/AGENTS/CI script list 정리
- 외부 `.github/profile/README.md` manifest pivot + target mapping 용어 반영
- `external-sync-instructions.md`로 사용자 수동 push 경로 문서화
- 전체 build + unit + e2e + lint suite green
- Phase 17 REMOVE-01/02/03 requirements 전부 완료
- Phase 18 REGISTRY 공개 전제 확정
</success_criteria>

<output>
After completion, create `.planning/phases/17-remove/17-04-SUMMARY.md` documenting:
- regression guard script + allow-list 구성 (경로별 이유 카탈로그)
- `lint:annotations` → `lint:no-legacy` 전환 결과
- CI workflow diff 요약
- 외부 `.github/profile/README.md` before/after 핵심 narrative diff
- `external-sync-instructions.md` 경로 + 사용자 다음 step 요약
- Phase 17 전체 4 wave 종합 verification 결과 (build/test/e2e/lint 모두 green)
- Phase 18 REGISTRY 인계 사항:
  - schema stable 확인
  - breaking surface 정리 완료
  - regression guard로 drift 감지 구조적 보장
- 사용자 pending: 두 외부 repo에 대한 수동 git push
</output>
</content>
</invoke>