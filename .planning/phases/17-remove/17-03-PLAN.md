---
phase: "17-remove"
plan: "03"
type: execute
wave: 3
depends_on: ["17-01"]
files_modified:
  - README.md
  - AGENTS.md
  - PRIVACY.md
  - workflows/annotate/WORKFLOW.md
  - docs/agent-setup.md
  - docs/improvement-notes.md
  - packages/mcp/README.md
autonomous: true
requirements:
  - REMOVE-02
tags:
  - documentation
  - terminology-rewrite
must_haves:
  truths:
    - "루트 README가 inline `data-agrune-*` 어노테이션 섹션을 포함하지 않고 manifest + `defineTarget`/`defineMacro` 중심으로 재작성되어 있다"
    - "AGRUNE_README의 예제·튜토리얼이 inline annotation을 보여주지 않는다 (manifest.ts 예제 또는 defineTarget 호출 형태)"
    - "PRIVACY.md 첫 줄의 `browser extension` 표현이 `MCP server via CDP`로 교정되어 있다 (2026-04-15 CDP-only 피봇 반영)"
    - "`docs/agent-setup.md`의 extension 관련 설치 안내가 MCP 서버 기반으로 재작성되어 있다"
    - "`workflows/annotate/WORKFLOW.md`가 manifest 기반(`agrune manifest dev` watcher + recorder)으로 재작성되어 있고, 상단에 `.agents/skills/manifest/SKILL.md`를 authoritative source로 지정하는 노트를 포함한다"
    - "`packages/mcp/README.md`의 extension mode backend/native-host 언급이 제거되어 있다"
    - "`annotation-lint` 패키지명·경로·심볼은 온전히 유지되어 있다 (Success Criteria 4 명시 예외)"
    - "`docs/notes/` 아카이브 (v1.0 시점 historical record)는 수정되지 않았다"
  artifacts:
    - path: "README.md"
      provides: "루트 README — manifest 중심 quickstart, 예제, 아키텍처 다이어그램"
      contains: "defineManifest"
    - path: "AGENTS.md"
      provides: "AI 에이전트 가이드 — manifest 작성 흐름, agrune_manifest_load / agrune_macro_run 도구 소개"
      contains: "manifest"
    - path: "PRIVACY.md"
      provides: "privacy statement — MCP server via CDP 정체성 반영"
      contains: "MCP server"
    - path: "workflows/annotate/WORKFLOW.md"
      provides: "manifest authoring workflow (recorder + manifest dev watcher) + SKILL.md 포인터"
      contains: "agrune manifest dev"
    - path: "docs/agent-setup.md"
      provides: "MCP server 기반 설정 가이드 — extension 설치 단계 제거"
      contains: "@agrune/mcp"
    - path: "docs/improvement-notes.md"
      provides: "개선 노트 — manifest pivot 반영, legacy 언급 정리"
      contains: "manifest"
    - path: "packages/mcp/README.md"
      provides: "MCP server 패키지 문서 — extension backend/native-host 섹션 제거"
      contains: "stdio MCP server"
  key_links:
    - from: "README.md"
      to: ".agents/skills/manifest/SKILL.md"
      via: "Getting Started 섹션이 manifest skill을 authoritative source로 지시"
      pattern: "\\.agents/skills/manifest"
    - from: "workflows/annotate/WORKFLOW.md"
      to: ".agents/skills/manifest/SKILL.md"
      via: "workflow 상단의 SOT pointer"
      pattern: "authoritative"
    - from: "AGENTS.md"
      to: "packages/manifest/README.md"
      via: "manifest SDK 레퍼런스 링크"
      pattern: "@agrune/manifest"
---

<objective>
**루트 문서 7개를 manifest 중심으로 재작성하고 "annotation" 용어를 선별 치환한다.**

RESEARCH.md §4 Documentation Surface에서 식별된 8개 문서 중 **현재 repo 안에 있는 7개**를 대상으로 한다 (외부 `.github/profile/README.md` 및 외부 skills repo sync는 Wave 4 담당).

**용어 전환의 원칙** (Pitfall 4 — 맹목적 치환 금지):
- **치환 대상**: "legacy `data-agrune-*` inline 어노테이션을 지칭하는 문맥"에 한정
  - 예: "annotate your HTML with data-agrune-action" → "define targets in a manifest via defineTarget"
- **유지 대상**:
  - `annotation-lint` 패키지명 / 경로 / 심볼 (외부 소비자 API)
  - `KNOWN_AGRUNE_ATTRS` 같은 내부 lint 상수
  - "annotation" 단어가 일반적 의미(예: "TypeScript type annotation", "function annotation")로 쓰인 곳
  - historical archive (`docs/notes/[통합됨] *.md` 등) — README L215 명시

**문서별 이슈 요약** (RESEARCH.md 기반):
- `README.md` L155-157 annotation 섹션 + L209의 `lint:annotations` 언급 (삭제는 W4 plan 04가 수행하므로 W3에서는 설명 표현만 정리)
- `AGENTS.md` L28의 `lint:annotations` script 언급 (동일 — W4 대기)
- `PRIVACY.md` L1-2 "agrune is a browser extension" — 2026-04-15 피봇 이후 drift
- `workflows/annotate/WORKFLOW.md` 전체 — manifest 버전으로 재작성 + SOT pointer
- `docs/agent-setup.md` L8-16 `@agrune/cli` / `Chrome Extension` 표현 — 재작성
- `docs/improvement-notes.md` — manifest pivot 반영
- `packages/mcp/README.md` L24 "extension mode backend/native-host" — 제거

Purpose: 제품 표면 문서가 현재 아키텍처(CDP-only + manifest + root-import)를 정확히 반영하고, 새 사용자가 legacy 경로로 오인 유도되지 않음.
Output: 7개 문서 재작성, 용어 전환, SOT pointer 배치. `data-agrune-*` 예제는 전부 제거 (allow-list 대상 외).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-remove/17-CONTEXT.md
@.planning/phases/17-remove/17-RESEARCH.md
@README.md
@AGENTS.md
@PRIVACY.md
@workflows/annotate/WORKFLOW.md
@docs/agent-setup.md
@docs/improvement-notes.md
@packages/mcp/README.md
@packages/manifest/README.md
@.agents/skills/manifest/SKILL.md
@packages/e2e/fixtures/todomvc/manifest.ts

<interfaces>
<!-- Source-of-truth documents that rewrites should link to. -->
<!-- Use these as authoritative examples for manifest usage. -->

Canonical manifest example (cite / link, don't reproduce inline):
- `packages/e2e/fixtures/todomvc/manifest.ts` — Phase 16-04 reference TodoMVC fixture, known working

Canonical SDK docs:
- `packages/manifest/README.md` — @agrune/manifest SDK documentation
- `.agents/skills/manifest/SKILL.md` — AI authoring skill (authoritative for workflow)

Canonical tool docs:
- `packages/mcp/README.md` will become the MCP tool reference after this plan's rewrite

Key vocabulary map (annotate-context only, NOT global):
| Old phrase | New phrase |
|---|---|
| "annotate with data-agrune-*" | "define targets in a manifest" |
| "inline annotation" | "target mapping" |
| "annotate the HTML" | "author a manifest.ts" |
| "scan annotations" | "resolve targets" |
| "Chrome extension that…" | "MCP server that launches Chrome via CDP to…" |
| "extension panel" | "devtools webapp" |
| "install the extension" | "run `agrune` / connect via MCP" |
| "annotation-lint" (package name) | UNCHANGED — keep as-is |
| "TypeScript annotation" | UNCHANGED — general term, not legacy |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md + AGENTS.md + PRIVACY.md (product-surface trio)</name>
  <files>
    README.md,
    AGENTS.md,
    PRIVACY.md
  </files>
  <action>
  루트 3개 문서 — 제품 표면 first-impression. 각 문서의 현재 상태를 먼저 읽고, 목표 shape으로 재작성.

  **README.md**
  - L155-157 (approx) `data-agrune-*` 어노테이션 섹션 — 전부 교체:
    - Quickstart / "How to use" 섹션에서 "annotate your HTML with data-agrune-action=..." 같은 예제 → "author a manifest using @agrune/manifest SDK" 예제로 교체.
    - 코드 스니펫은 `packages/e2e/fixtures/todomvc/manifest.ts`를 참조 (혹은 해당 파일에서 간결한 `defineManifest({ targets: [defineTarget({ id: 'submit', selector: [...] })] })` 일부 발췌).
    - 필요하면 architecture diagram block을 추가 (manifest → TargetResolver → CDP act/fill).
  - L209 근처 `lint:annotations` script 언급 — **이 plan에서는 내용 재작성만**. 실제 script 삭제는 Wave 4 (17-04)에서 수행. 현 단계에서는:
    - "lint:annotations checks legacy data-agrune-* HTML" 같은 설명은 삭제하고, 대신 "`agrune manifest validate` validates targets against live DOM" 안내로 교체.
    - script list의 `lint:annotations` 엔트리 자체는 Wave 4에서 삭제되므로 **건드리지 않음** — 본 task는 본문 설명만 정리.
  - L215 historical notes 지칭 (`docs/notes/`) — 유지. "archived" 라벨 정확성만 확인.
  - "Chrome extension" / "browser extension" 언급이 있으면 전부 "MCP server + CDP-managed Chrome" 으로 교정.
  - SOT pointer 추가: Getting Started 섹션 하단 "For AI agent authoring, see `.agents/skills/manifest/SKILL.md`".

  **AGENTS.md**
  - `data-agrune-*` 어노테이션 설명 / 예제 전부 manifest 기반으로 재작성.
  - MCP 도구 목록에서 `agrune_manifest_load`, `agrune_macro_run`이 등장하는지 확인 — Phase 12/14에서 이미 추가됐을 것. 만약 legacy 경로 설명이 남아있다면 정리.
  - L28 `lint:annotations` 언급 — Wave 4에서 삭제될 예정이므로 README와 동일한 전략: 설명만 정리, script 엔트리 자체는 건드리지 않음.
  - "어노테이션 워크플로" 같은 섹션 제목이 있으면 "manifest authoring workflow"로 rename.
  - SOT pointer 추가: "See `.agents/skills/manifest/SKILL.md` for the authoring skill."

  **PRIVACY.md**
  - L1-2 첫 문단: "agrune is a browser extension that enables AI agents to interact with web pages"
    → "agrune is an MCP server that enables AI agents to interact with web pages through locally-installed Chrome via the Chrome DevTools Protocol (CDP)."
  - 나머지 privacy 원칙(로컬 우선, 데이터 전송 없음 등)은 내용상 동일하므로 보존. 다만 extension의 권한 모델을 언급하는 bullet이 있으면 CDP의 권한 모델로 교체:
    - "Chrome extension permissions" → "CDP session scoped to user-launched Chrome instance, no system-wide install".
  - 파일 끝 날짜 / revision history 필드가 있으면 "Updated YYYY-MM-DD: rephrased post-2026-04-15 CDP-only pivot" 추가.

  **Commits (3 separate):**
  - `docs(17-03): rewrite README annotation section to manifest-centric`
  - `docs(17-03): rewrite AGENTS manifest authoring surface`
  - `docs(17-03): update PRIVACY to CDP MCP server identity`
  </action>
  <verify>
    <automated>grep -En "data-agrune-(action|key|group|canvas|meta|masked|sensitive)" README.md AGENTS.md PRIVACY.md 2>&1 | tail -20 && grep -cn "browser extension" PRIVACY.md</automated>
  </verify>
  <done>
  - `grep -En "data-agrune-(action|key|group|canvas|meta|masked|sensitive)" README.md AGENTS.md PRIVACY.md` → 0 matches (cursor markers aurora/pointer는 이들 3개 파일에 등장하지 않을 것이지만, 만약 있으면 정당한 cursor-anim 컨텍스트인지 확인).
  - `grep "browser extension" PRIVACY.md` → 0 matches.
  - README와 AGENTS에 `defineManifest` 또는 `@agrune/manifest` 언급이 최소 1회씩 존재.
  - README / AGENTS가 `.agents/skills/manifest/SKILL.md`를 pointer로 포함.
  - `lint:annotations` script 라인은 **건드리지 않음** (W4 담당).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite workflows/annotate/WORKFLOW.md + docs/agent-setup.md</name>
  <files>
    workflows/annotate/WORKFLOW.md,
    docs/agent-setup.md
  </files>
  <action>
  두 문서 모두 harness integration / setup 경로를 설명. RESEARCH.md Open Question 3과 5 해소.

  **workflows/annotate/WORKFLOW.md**
  - 현재는 "harness 중립 어노테이션 워크플로 원본" (PROJECT.md L50에서 validated된 원칙).
  - 디렉터리명(`annotate/`)과 파일명(`WORKFLOW.md`)은 유지 — 외부 하네스가 이미 링크해뒀을 가능성 높음 (RESEARCH.md Open Q 3 권고).
  - 최상단에 authoritative source pointer 삽입:
    ```markdown
    > **v0.5 이후 authoritative source**: `.agents/skills/manifest/SKILL.md` 가 manifest authoring
    > 의 공식 매뉴얼입니다. 이 파일은 하네스 호환을 위해 유지되는 요약판입니다.
    ```
  - 본문을 manifest workflow로 재작성. 핵심 경로 3단계:
    1. `defineManifest` / `defineTarget` / `defineMacro` / `defineRepeat`로 `manifest.ts` 작성
    2. `agrune manifest validate <file> --url <URL>` 로 live DOM 매칭 검증
    3. `agrune manifest dev` watcher + DevTools recorder(Phase 16)로 iterative 편집
  - 기존 `data-agrune-*` 섹션은 전체 삭제. Harness별 step-by-step (Claude Code / Codex) 은 agrune CLI 호출 + MCP 연결 flow로 재작성.
  - TodoMVC reference fixture (`packages/e2e/fixtures/todomvc/manifest.ts`)를 "minimal complete example"로 링크.
  - 마지막 섹션에 "Deprecated workflows" block을 추가해 v1.1까지 사용됐던 inline-annotation 방식을 **역사 기록으로만** 한 문단 남긴다: "Prior to v0.5, agrune used inline `data-agrune-*` HTML attributes. This path was removed in Phase 17 (v0.5). Upgrade path: rewrite your annotations into a manifest using the `@agrune/manifest` SDK." — 이 문단이 legacy attribute를 literal string으로 쓰면 regression guard에 걸리므로 `` `data-agrune-*` `` 패턴을 회피: "inline HTML data attributes with the `data-agrune-` prefix (now removed)" 정도로 phrasing. **중요**: code block 안에 실제 legacy attribute 예시는 넣지 않음. 역사적 맥락만 기술.

  **docs/agent-setup.md**
  - L8-16 `@agrune/cli` installer + `Chrome Extension` 표현 → 재작성:
    - 설치: `npm i -g @agrune/mcp` (또는 monorepo dev setup)
    - 실행: `agrune` CLI로 Chrome launch/attach/headless 모드 선택
    - Harness 연결: MCP stdio 설정 (Claude Code / Codex 예제)
    - DevTools UI: `http://localhost:PORT/devtools` (MCP 서버가 serve)
  - Extension 설치 단계, Chrome Web Store 링크 등 전부 삭제.
  - 기존 troubleshooting 섹션이 extension 관련이면 CDP 관련(`--remote-debugging-port` 충돌, user-data-dir headful 가시성 팁 등)으로 교체. MEMORY의 "Headful Chrome 가시성" 노트 참고 ("`--user-data-dir` Chrome이 headful로 안 보이면 `--new-window` 플래그부터 의심").

  **Commits:**
  - `docs(17-03): rewrite WORKFLOW.md to manifest-centric + SOT pointer`
  - `docs(17-03): rewrite agent-setup to MCP server + CDP (drop extension installer)`
  </action>
  <verify>
    <automated>grep -En "data-agrune-(action|key|group|canvas|meta|masked|sensitive)|browser extension|Chrome Extension|@agrune/cli" workflows/annotate/WORKFLOW.md docs/agent-setup.md 2>&1 | tail -20</automated>
  </verify>
  <done>
  - WORKFLOW.md 최상단에 `.agents/skills/manifest/SKILL.md` authoritative 선언 존재.
  - WORKFLOW.md 본문의 핵심 flow가 manifest 3단계 (`defineManifest` → `manifest validate` → `manifest dev` watcher).
  - docs/agent-setup.md 설치 경로가 MCP server 중심이며 extension 설치 단계 부재.
  - `grep -En "data-agrune-(action|key|group|canvas|meta|masked|sensitive)"` → 0 matches in both files.
  - `grep "browser extension\\|Chrome Extension\\|@agrune/cli"` → 0 matches in both files.
  - TodoMVC fixture 링크가 WORKFLOW.md에 최소 1회 존재.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tidy docs/improvement-notes.md + packages/mcp/README.md</name>
  <files>
    docs/improvement-notes.md,
    packages/mcp/README.md
  </files>
  <action>
  보조 문서 2개 — legacy drift 제거.

  **docs/improvement-notes.md**
  - 현재 내용을 읽고 manifest pivot 이전 관점에서 작성된 항목 식별.
  - 만약 "annotation validation", "data-agrune-* ergonomics" 같은 bullet이 있으면:
    - 이미 v0.5에서 해결됐으면 → "Resolved in v0.5 (Phase 11-16)" 주석 붙여 보존 or 이동.
    - 여전히 valid한 개선 포인트면 → manifest 맥락으로 rephrase.
  - Legacy inline attribute 언급은 전부 manifest 경로로 맞춤. 예: "improve annotation ergonomics" → "improve manifest authoring ergonomics via recorder precision / selector heuristics".
  - 날짜 스탬프가 파일 상단에 있으면 "Updated 2026-04-19 (v0.5 Manifest Pivot — Phase 17)" 추가.

  **packages/mcp/README.md**
  - L24 근처 "extension mode backend/native-host entrypoints" → 전부 삭제 (RESEARCH.md Open Q 4).
  - MCP 서버의 현재 정체성 반영:
    - stdio MCP server, 11 tools (v1.1 확정)
    - devtools webapp HTTP/WebSocket server (Phase 8)
    - `agrune` / `agrune-mcp` CLI binary
  - Tool 목록에 Phase 12-14에서 추가된 `agrune_manifest_load`, `agrune_macro_run`, `recorder_*` (Phase 16) 포함.
  - "Usage" / "Architecture" 섹션에 `data-agrune-*` 언급이 남아있으면 manifest 경로로 교체.
  - 외부 소비자 breaking change note block 추가:
    ```markdown
    ## v0.5 Breaking Changes
    - Removed inline `data-agrune-*` bootstrap scanner. Use `@agrune/manifest` SDK.
    - PageSnapshot v2 → v3 (selector field now a ladder, not a string).
    - Runtime boots to idle even when page has `data-agrune-*` attributes.
    See `.planning/phases/17-remove/` for migration details.
    ```
    (v0.5 breaking change 라인 전체가 이미 Phase 11-12에서 선언됐으면 중복 방지 — 기존 섹션에 Phase 17 한 bullet만 추가해도 무방.)

  **Commits:**
  - `docs(17-03): refresh improvement-notes for v0.5 manifest context`
  - `docs(17-03): remove extension backend language from packages/mcp/README`
  </action>
  <verify>
    <automated>grep -En "data-agrune-(action|key|group|canvas|meta|masked|sensitive)|extension mode|native-host|native messaging" docs/improvement-notes.md packages/mcp/README.md 2>&1 | tail -20</automated>
  </verify>
  <done>
  - `grep` 결과 0 matches for 위 pattern in both files.
  - `packages/mcp/README.md`가 `agrune_manifest_load` 또는 `agrune_macro_run` 을 언급 (최신 tool 목록 반영).
  - `docs/improvement-notes.md`가 manifest 맥락으로 rephrase됨.
  </done>
</task>

</tasks>

<verification>
**Gate checks:**

1. 7개 문서에서 `grep -En "data-agrune-(action|key|group|canvas|meta|masked|sensitive)"` → 0 matches (cursor 마커 `data-agrune-aurora`/`data-agrune-pointer`는 이 7개 문서에 등장 없을 것이지만, 있다면 cursor-anim 컨텍스트 정당성 확인)
2. `grep -En "browser extension|Chrome Extension|extension mode|native-host|native messaging"` 위 7개 문서에서 0 matches
3. README + AGENTS + WORKFLOW.md 각각에 `defineManifest` 또는 `@agrune/manifest` 1회 이상 존재
4. WORKFLOW.md 최상단에 `.agents/skills/manifest/SKILL.md` pointer 존재
5. PRIVACY.md 첫 문단이 "MCP server" 기반으로 교정됨
6. `annotation-lint` 패키지명 / `KNOWN_AGRUNE_ATTRS` 상수명은 **유지**되어야 함 (grep으로 sanity check)
7. `docs/notes/` 아카이브 파일들은 **수정되지 않음** (`git status docs/notes/` empty 확인)

**Non-goal for this plan (explicit):**
- `package.json`의 `lint:annotations` script 자체 삭제 — Wave 4 (17-04). 본 plan은 설명 표현만 정리.
- 외부 repo `.github/profile/README.md` 또는 외부 `skills/` repo sync — Wave 4 (17-04)
- `packages/core/src/annotation-lint/**` 내부 파일 수정 — Out of scope (Success Criteria 4 명시 예외)
- Source code 수정 — Wave 1 (17-01) 담당
- `docs/superpowers/specs/*` 파일 — Tertiary sources, plan이 없어서 수정 불필요 (RESEARCH.md §Sources)
- `docs/notes/[통합됨] *.md` 아카이브 — README L215 명시 historical record

**Terminology rewrite scope reminder (Pitfall 4 재강조):**
- 치환 대상: `data-agrune-*` inline 어노테이션 지칭 맥락
- 유지 대상: annotation-lint 패키지 API, 일반적 "annotation" 용례, historical archive
</verification>

<success_criteria>
- 7개 문서 전체가 manifest 중심으로 재작성됨
- 제품 표면 first-impression (README/AGENTS/PRIVACY)에서 legacy 경로 노출 0
- `workflows/annotate/WORKFLOW.md`가 `.agents/skills/manifest/SKILL.md`를 SOT로 지정
- 설치 가이드(agent-setup)가 MCP server 기반
- MCP README가 최신 tool 집합 (manifest_load, macro_run 등) 반영
- `annotation-lint` 패키지 API는 온전히 유지 (Success Criteria 4 예외)
- `docs/notes/` 아카이브 비침
</success_criteria>

<output>
After completion, create `.planning/phases/17-remove/17-03-SUMMARY.md` documenting:
- 7개 문서별 변경 요약 (before/after 핵심 diff)
- 치환된 용어 vs 유지된 용어 결정 로그 (Pitfall 4 원칙 반영)
- SOT pointer 배치 위치 (README, AGENTS, WORKFLOW.md)
- Wave 4 (17-04) 인계 체크리스트:
  - `package.json`의 `lint:annotations` script 삭제 + README/AGENTS script list 정리
  - 외부 `.github/profile/README.md` sync
  - 외부 skills repo `annotate/` 폐기 지침
  - Regression guard script 추가
</output>
</content>
</invoke>