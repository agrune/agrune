---
phase: 07-multi-tab-session-ux
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/browser/src/session-manager.ts
  - packages/browser/tests/session-manager.spec.ts
autonomous: true
user_setup: []
requirements_addressed:
  - SESS-01
must_haves:
  truths:
    - SessionManager가 activeSessionId 필드를 갖고 getActiveSessionId로 노출한다
    - setActiveSession(tabId)은 존재하지 않는 tab에 대해 false를 반환하고 상태를 바꾸지 않는다
    - touchSession(tabId)은 lastInteractionAt을 현재 시각으로 업데이트하고 activeSessionId를 해당 tab으로 갱신한다
    - closeSession(tabId)은 그 tab이 active였으면 activeSessionId를 null로 만든다
    - clear()는 activeSessionId를 null로 만든다
  artifacts:
    - packages/browser/src/session-manager.ts (active tracking API)
    - packages/browser/tests/session-manager.spec.ts (active state test suite)
  key_links:
    - Plan 02의 CdpDriver.resolveTabId와 focusSession이 이 API를 호출한다
    - Plan 03의 MCP layer가 listSessions 결과에 active 플래그를 합친다
---

# Phase 7 Plan 01: Active Session State in SessionManager

## Objective
- **What:** `SessionManager`에 active session 개념과 최근 상호작용 타임스탬프를 도입하고, `setActiveSession`·`touchSession`·`getActiveSessionId` API를 추가한다.
- **Why:** SESS-01을 이 단계에서 독립적으로 닫아야 Plan 02(`resolveTabId`·`focusSession`)가 orchestration에만 집중하고, Plan 03(MCP layer)이 `active` 플래그를 전달하는 데 필요한 ground truth가 확보된다.
- **Output:** 변경된 `session-manager.ts`와 active 동작을 커버하는 단위 테스트.

## Context
@.planning/PROJECT.md
@.planning/phases/07-multi-tab-session-ux/07-CONTEXT.md
@.planning/phases/07-multi-tab-session-ux/07-RESEARCH.md
@packages/browser/src/session-manager.ts
@packages/browser/tests/session-manager.spec.ts

## Tasks

<task type="execute">
  <name>Add active-session tracking API to SessionManager</name>
  <files>packages/browser/src/session-manager.ts</files>
  <read_first>
    - packages/browser/src/session-manager.ts (현재 구현 83줄 전체 — Session 인터페이스 3-9줄, SessionManager 클래스 11-83줄)
    - .planning/phases/07-multi-tab-session-ux/07-RESEARCH.md §3.1
  </read_first>
  <action>
    `packages/browser/src/session-manager.ts`를 다음과 같이 변경한다. 기존 public method(openSession, closeSession, clear, getSession, getSessions, updateSnapshot, getSnapshot, hasReadySession, waitForSnapshot)는 시그니처를 모두 유지한다.

    1. `Session` 인터페이스(3-9줄)에 `lastInteractionAt?: number` 필드를 추가한다. 최종:
    ```ts
    export interface Session {
      tabId: number
      url: string
      title: string
      snapshot: PageSnapshot | null
      openedAt: number
      lastInteractionAt?: number
    }
    ```

    2. 클래스 필드 블록(`private sessions = new Map<number, Session>()` 아래)에 다음 한 줄을 추가한다:
    ```ts
    private activeSessionId: number | null = null
    ```

    3. `openSession` 내부 로직은 그대로 두되 `lastInteractionAt`은 upsert 과정에서 기존 값을 유지한다(`existing?.lastInteractionAt`). 최종:
    ```ts
    openSession(tabId: number, url: string, title: string): void {
      const existing = this.sessions.get(tabId)
      this.sessions.set(tabId, {
        tabId,
        url,
        title,
        snapshot: existing?.url === url ? existing.snapshot : null,
        openedAt: existing?.openedAt ?? Date.now(),
        lastInteractionAt: existing?.lastInteractionAt,
      })
    }
    ```

    4. `closeSession(tabId)` 내부를 다음으로 교체한다(기존 한 줄 대신):
    ```ts
    closeSession(tabId: number): void {
      this.sessions.delete(tabId)
      if (this.activeSessionId === tabId) {
        this.activeSessionId = null
      }
    }
    ```

    5. `clear()` 내부에 active 초기화 한 줄을 추가한다:
    ```ts
    clear(): void {
      this.sessions.clear()
      this.activeSessionId = null
    }
    ```

    6. `getSessions()` 바로 아래(또는 파일 하단 private 영역 직전)에 세 개의 public 메서드를 추가한다:
    ```ts
    getActiveSessionId(): number | null {
      if (this.activeSessionId !== null && !this.sessions.has(this.activeSessionId)) {
        this.activeSessionId = null
      }
      return this.activeSessionId
    }

    setActiveSession(tabId: number): boolean {
      if (!this.sessions.has(tabId)) {
        return false
      }
      this.activeSessionId = tabId
      return true
    }

    touchSession(tabId: number): boolean {
      const session = this.sessions.get(tabId)
      if (!session) return false
      session.lastInteractionAt = Date.now()
      this.activeSessionId = tabId
      return true
    }
    ```

    다른 메서드(`updateSnapshot`, `getSnapshot`, `hasReadySession`, `waitForSnapshot`, `notifyWaiters`)는 건드리지 않는다.
  </action>
  <acceptance_criteria>
    - packages/browser/src/session-manager.ts contains `private activeSessionId: number | null = null`
    - packages/browser/src/session-manager.ts contains `getActiveSessionId(): number | null`
    - packages/browser/src/session-manager.ts contains `setActiveSession(tabId: number): boolean`
    - packages/browser/src/session-manager.ts contains `touchSession(tabId: number): boolean`
    - packages/browser/src/session-manager.ts contains `lastInteractionAt?: number`
    - packages/browser/src/session-manager.ts contains `if (this.activeSessionId === tabId)`
    - packages/browser/src/session-manager.ts still contains `openSession(tabId: number, url: string, title: string): void`
    - packages/browser/src/session-manager.ts still contains `waitForSnapshot(timeoutMs: number): Promise<boolean>`
    - `pnpm --filter @agrune/browser build` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Extend session-manager test suite with active-state coverage</name>
  <files>packages/browser/tests/session-manager.spec.ts</files>
  <read_first>
    - packages/browser/tests/session-manager.spec.ts (기존 suite 구조 1-165줄)
    - packages/browser/src/session-manager.ts (Plan 01 task 1의 결과물)
  </read_first>
  <action>
    `packages/browser/tests/session-manager.spec.ts` 파일 끝에 새로운 `describe('active session tracking', ...)` 블록을 **append** 한다. 기존 describe 블록(SessionManager, hasReadySession, waitForSnapshot)과 import는 변경하지 않는다. 추가할 블록:

    ```ts
    describe('active session tracking', () => {
      it('getActiveSessionId returns null by default', () => {
        const mgr = new SessionManager()
        expect(mgr.getActiveSessionId()).toBeNull()
      })

      it('setActiveSession returns false for unknown tab and leaves state untouched', () => {
        const mgr = new SessionManager()
        mgr.openSession(1, 'https://a.com', 'A')
        expect(mgr.setActiveSession(999)).toBe(false)
        expect(mgr.getActiveSessionId()).toBeNull()
      })

      it('setActiveSession marks a known tab as active', () => {
        const mgr = new SessionManager()
        mgr.openSession(1, 'https://a.com', 'A')
        mgr.openSession(2, 'https://b.com', 'B')
        expect(mgr.setActiveSession(2)).toBe(true)
        expect(mgr.getActiveSessionId()).toBe(2)
      })

      it('touchSession updates lastInteractionAt and marks the tab active', () => {
        const mgr = new SessionManager()
        mgr.openSession(1, 'https://a.com', 'A')
        expect(mgr.getSession(1)!.lastInteractionAt).toBeUndefined()
        const before = Date.now() - 1
        expect(mgr.touchSession(1)).toBe(true)
        expect(mgr.getActiveSessionId()).toBe(1)
        const lastAt = mgr.getSession(1)!.lastInteractionAt
        expect(typeof lastAt).toBe('number')
        expect(lastAt!).toBeGreaterThanOrEqual(before)
      })

      it('touchSession returns false for unknown tab', () => {
        const mgr = new SessionManager()
        expect(mgr.touchSession(42)).toBe(false)
        expect(mgr.getActiveSessionId()).toBeNull()
      })

      it('closeSession clears active when closing the active tab', () => {
        const mgr = new SessionManager()
        mgr.openSession(1, 'https://a.com', 'A')
        mgr.openSession(2, 'https://b.com', 'B')
        mgr.setActiveSession(2)
        mgr.closeSession(2)
        expect(mgr.getActiveSessionId()).toBeNull()
        expect(mgr.getSession(1)).not.toBeNull()
      })

      it('closeSession does not clear active when closing a different tab', () => {
        const mgr = new SessionManager()
        mgr.openSession(1, 'https://a.com', 'A')
        mgr.openSession(2, 'https://b.com', 'B')
        mgr.setActiveSession(1)
        mgr.closeSession(2)
        expect(mgr.getActiveSessionId()).toBe(1)
      })

      it('clear resets activeSessionId', () => {
        const mgr = new SessionManager()
        mgr.openSession(1, 'https://a.com', 'A')
        mgr.setActiveSession(1)
        mgr.clear()
        expect(mgr.getActiveSessionId()).toBeNull()
      })

      it('openSession preserves lastInteractionAt when the URL is unchanged', () => {
        const mgr = new SessionManager()
        mgr.openSession(1, 'https://a.com', 'A')
        mgr.touchSession(1)
        const firstTouch = mgr.getSession(1)!.lastInteractionAt!
        mgr.openSession(1, 'https://a.com', 'A (updated)')
        expect(mgr.getSession(1)!.lastInteractionAt).toBe(firstTouch)
      })
    })
    ```
  </action>
  <acceptance_criteria>
    - packages/browser/tests/session-manager.spec.ts contains `describe('active session tracking'`
    - packages/browser/tests/session-manager.spec.ts contains `mgr.touchSession(1)`
    - packages/browser/tests/session-manager.spec.ts contains `mgr.setActiveSession(2)`
    - packages/browser/tests/session-manager.spec.ts contains `mgr.getActiveSessionId()`
    - `pnpm --filter @agrune/browser test -- session-manager` exits 0
  </acceptance_criteria>
</task>

## Verification
- `pnpm --filter @agrune/browser build`가 green.
- `pnpm --filter @agrune/browser test`가 green (특히 `session-manager.spec.ts`의 기존 테스트 전부 + 새 `active session tracking` 블록).
- `grep -n "activeSessionId\|touchSession\|setActiveSession" packages/browser/src/session-manager.ts`가 각 최소 2 매치.

## Success Criteria
- [ ] `SessionManager`가 `activeSessionId`·`lastInteractionAt`을 관리한다
- [ ] `setActiveSession`/`touchSession`/`getActiveSessionId`가 노출된다
- [ ] `closeSession`·`clear`가 active 상태를 정리한다
- [ ] 새 단위 테스트 + 기존 테스트 모두 green
