---
phase: 07-multi-tab-session-ux
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - packages/mcp/src/tools.ts
  - packages/mcp/src/mcp-tools.ts
  - packages/mcp/src/public-shapes.ts
  - packages/mcp/src/index.ts
  - packages/mcp/tests/tools.spec.ts
  - packages/mcp/tests/public-shapes.spec.ts
autonomous: true
user_setup: []
requirements_addressed:
  - SESS-02
  - SESS-03
must_haves:
  truths:
    - getToolDefinitions가 agrune_focus를 포함해 10개 tool을 반환한다
    - registerAgruneTools가 agrune_focus를 등록한다
    - toPublicSession이 active boolean 필드를 포함한다
    - 도구 응답 JSON에 session meta (tabId, url, title, wasActive, becameActive)가 포함된다
    - agrune_focus 핸들러가 driver.focusSession(tabId)을 호출하고 결과를 JSON으로 반환한다
    - 존재하지 않는 tabId로 agrune_focus를 부르면 TAB_NOT_FOUND isError 응답이 반환된다
    - agrune_sessions 응답의 각 session에 active 플래그가 들어있다
  artifacts:
    - packages/mcp/src/tools.ts (agrune_focus schema)
    - packages/mcp/src/mcp-tools.ts (register agrune_focus)
    - packages/mcp/src/public-shapes.ts (PublicSession.active, toPublicSessionMeta helper)
    - packages/mcp/src/index.ts (focus 핸들러, session meta injection)
    - packages/mcp/tests/tools.spec.ts (10 tools, agrune_focus schema)
    - packages/mcp/tests/public-shapes.spec.ts (active 필드 검증)
  key_links:
    - Plan 02의 driver.focusSession / listSessions.active를 소비한다
    - SESS-04 devtools UI는 Phase 8에서 별도 plan으로 진행
---

# Phase 7 Plan 03: MCP `agrune_focus` Tool and Session Meta Wiring

## Objective
- **What:** `agrune_focus` MCP 도구를 추가하고, 모든 command-style 도구 응답에 active session meta를 주입한다. `agrune_sessions` 결과에 `active` 플래그를 노출한다.
- **Why:** SESS-03의 외부 표면과 SESS-02의 "호출자가 어느 세션에 액션이 갔는지 추적 가능" 부분을 MCP 경계에서 완성한다. Plan 02의 driver API를 얇은 래퍼로 노출하고, 호환성을 위해 기존 도구의 응답 구조는 동일하게 유지하면서 optional `session` 필드만 추가한다.
- **Output:** 확장된 MCP 레이어 + 기존 스펙(`toEqual` 등)을 고려해 업데이트된 테스트.

## Context
@.planning/PROJECT.md
@.planning/phases/07-multi-tab-session-ux/07-CONTEXT.md
@.planning/phases/07-multi-tab-session-ux/07-RESEARCH.md
@packages/mcp/src/tools.ts
@packages/mcp/src/mcp-tools.ts
@packages/mcp/src/public-shapes.ts
@packages/mcp/src/index.ts
@packages/mcp/tests/tools.spec.ts
@packages/mcp/tests/public-shapes.spec.ts

## Tasks

<task type="execute">
  <name>Add agrune_focus to getToolDefinitions()</name>
  <files>packages/mcp/src/tools.ts</files>
  <read_first>
    - packages/mcp/src/tools.ts (현재 163줄 전체)
    - .planning/phases/07-multi-tab-session-ux/07-RESEARCH.md §3.4
  </read_first>
  <action>
    `packages/mcp/src/tools.ts`의 `return [...]` 배열 마지막 항목(`agrune_read` block, 150-160줄) 바로 뒤에 `agrune_focus` 정의를 **append** 한다. 기존 9개 정의와 순서는 유지한다. 추가 항목:
    ```ts
    {
      name: 'agrune_focus',
      description:
        'Switch the active browser session and best-effort bring the underlying tab to the front. Use before a series of tool calls to ensure subsequent calls target a specific tab. Requires tabId from agrune_sessions.',
      inputSchema: {
        type: 'object',
        properties: {
          tabId: { type: 'number', description: 'Browser tab ID to focus.' },
          sessionId: {
            type: 'string',
            description: 'Reserved for future string session IDs. If present and parseable as a number, treated as tabId.',
          },
        },
      },
    },
    ```

    (required 배열은 생략 — tabId 또는 sessionId 중 하나가 있는지 런타임에서 검증한다.)
  </action>
  <acceptance_criteria>
    - packages/mcp/src/tools.ts contains `name: 'agrune_focus'`
    - packages/mcp/src/tools.ts contains `Switch the active browser session`
    - packages/mcp/src/tools.ts still contains `name: 'agrune_sessions'`
    - packages/mcp/src/tools.ts still contains `name: 'agrune_read'`
    - `pnpm --filter @agrune/mcp build` exits 0 (빌드 실패가 사전 pnpm install 상태에 의한 ws 이슈인 경우 plan 전체의 실패 조건에서 제외 — type check 통과만 요구)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Register agrune_focus via registerAgruneTools()</name>
  <files>packages/mcp/src/mcp-tools.ts</files>
  <read_first>
    - packages/mcp/src/mcp-tools.ts (현재 190줄 전체)
    - packages/mcp/src/tools.ts (앞 task 결과)
  </read_first>
  <action>
    `packages/mcp/src/mcp-tools.ts`의 `mcp.tool('agrune_read', ...)` 등록(180-188줄) 뒤에 `agrune_focus` 등록 블록을 추가한다. 기존 도구 등록은 그대로 둔다. 추가:
    ```ts
    mcp.tool(
      'agrune_focus',
      'Switch the active browser session (tab). Subsequent tool calls without tabId target this tab. Best-effort brings the underlying browser window to the foreground. Get tabIds from agrune_sessions.',
      {
        tabId: z.number().optional().describe('Tab ID to focus (preferred)'),
        sessionId: z.string().optional().describe('Reserved — pass numeric-string sessionId for future compatibility'),
      },
      async (args) => toMcpToolResult(await handleToolCall('agrune_focus', args)),
    )
    ```
  </action>
  <acceptance_criteria>
    - packages/mcp/src/mcp-tools.ts contains `mcp.tool(` with `'agrune_focus'`
    - packages/mcp/src/mcp-tools.ts contains `Switch the active browser session`
    - packages/mcp/src/mcp-tools.ts still contains `mcp.tool('agrune_read'`
    - packages/mcp/src/mcp-tools.ts still contains `mcp.tool('agrune_sessions'`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Add active flag to PublicSession and a session-meta helper</name>
  <files>packages/mcp/src/public-shapes.ts</files>
  <read_first>
    - packages/mcp/src/public-shapes.ts (현재 197줄 전체, PublicSession 10-16, toPublicSession 70-85)
    - packages/core/src/driver.ts (Plan 02 task 1 결과물)
    - .planning/phases/07-multi-tab-session-ux/07-RESEARCH.md §3.5
  </read_first>
  <action>
    `packages/mcp/src/public-shapes.ts`를 다음 최소 변경으로 수정한다.

    **1. `PublicSession` 인터페이스**(10-16줄)에 `active` 필드 추가:
    ```ts
    export interface PublicSession {
      tabId: number
      url: string
      title: string
      hasSnapshot: boolean
      snapshotVersion: number | null
      active: boolean
    }
    ```

    **2. `toPublicSession`**(70-85줄) 구현을 `active` 반영하도록 교체:
    ```ts
    export function toPublicSession(
      session: Session & { snapshot?: PageSnapshot | null },
    ): PublicSession {
      const snapshot =
        'snapshot' in session && session.snapshot
          ? session.snapshot
          : null

      return {
        tabId: session.tabId,
        url: session.url,
        title: session.title || snapshot?.title || '',
        hasSnapshot: session.hasSnapshot ?? snapshot !== null,
        snapshotVersion: session.snapshotVersion ?? snapshot?.version ?? null,
        active: session.active ?? false,
      }
    }
    ```

    **3. 새 export: `PublicSessionMeta` 인터페이스와 `toPublicSessionMeta` 헬퍼**를 파일 하단(마지막 closing brace 위)에 추가한다:
    ```ts
    export interface PublicSessionMeta {
      tabId: number
      url: string
      title: string
      wasActive: boolean
      becameActive: boolean
    }

    export function toPublicSessionMeta(
      session: Session & { snapshot?: PageSnapshot | null },
      opts: { wasActive: boolean; becameActive: boolean },
    ): PublicSessionMeta {
      const snapshot =
        'snapshot' in session && session.snapshot
          ? session.snapshot
          : null
      return {
        tabId: session.tabId,
        url: session.url,
        title: session.title || snapshot?.title || '',
        wasActive: opts.wasActive,
        becameActive: opts.becameActive,
      }
    }
    ```

    다른 export(`toPublicSnapshot`, `toPublicCommandResult`, `getActiveContext`, `toPublicGroups`, `toPublicTarget`)는 건드리지 않는다.
  </action>
  <acceptance_criteria>
    - packages/mcp/src/public-shapes.ts contains `active: boolean`
    - packages/mcp/src/public-shapes.ts contains `active: session.active ?? false`
    - packages/mcp/src/public-shapes.ts contains `export interface PublicSessionMeta`
    - packages/mcp/src/public-shapes.ts contains `export function toPublicSessionMeta`
    - packages/mcp/src/public-shapes.ts still contains `export function toPublicSnapshot`
    - packages/mcp/src/public-shapes.ts still contains `export function toPublicCommandResult`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Route agrune_focus and inject session meta in handleToolCall</name>
  <files>packages/mcp/src/index.ts</files>
  <read_first>
    - packages/mcp/src/index.ts (현재 101줄 전체)
    - packages/mcp/src/public-shapes.ts (앞 task 결과 — toPublicSessionMeta)
    - packages/core/src/index.ts (Plan 02 결과 — TAB_NOT_FOUND)
    - packages/browser/src/cdp-driver.ts (Plan 02 결과 — focusSession, listSessions.active)
  </read_first>
  <action>
    `packages/mcp/src/index.ts`를 아래와 같이 수정한다.

    **1. import 블록 교체**(1-6줄):
    ```ts
    import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'
    import type { AgruneRuntimeConfig, BrowserDriver, CommandErrorShape, Session } from '@agrune/core'
    import { registerAgruneTools } from './mcp-tools.js'
    import type { ToolHandlerResult } from './mcp-tools.js'
    import {
      toPublicCommandResult,
      toPublicSession,
      toPublicSessionMeta,
      toPublicSnapshot,
    } from './public-shapes.js'
    import type { PublicSessionMeta, PublicSnapshotOptions } from './public-shapes.js'
    ```

    **2. `handleToolCall` 안의 switch 블록을 교체**: command tool들의 응답에 session meta를 주입하고, `agrune_focus` 케이스를 추가한다. `case 'agrune_config':`와 `default:`는 현재 코드 그대로 유지한다.

    구체적으로: 현재 `case 'agrune_act': case 'agrune_fill': ... case 'agrune_read':` 합쳐진 블록을 다음으로 교체:
    ```ts
      case 'agrune_act':
      case 'agrune_fill':
      case 'agrune_drag':
      case 'agrune_pointer':
      case 'agrune_wait':
      case 'agrune_guide':
      case 'agrune_read': {
        if (tabId == null) return { text: 'No active sessions.', isError: true }
        const wasActive = driver.listSessions().find(s => s.tabId === tabId)?.active === true
        const command: Record<string, unknown> & { kind: string } = {
          kind: name.replace('agrune_', ''), ...args,
        }
        delete command.tabId
        const result = await driver.execute(tabId, command)
        const publicResult = toPublicCommandResult(result)
        const after = driver.listSessions().find(s => s.tabId === tabId)
        const sessionMeta = after
          ? toPublicSessionMeta(after as Session, {
              wasActive,
              becameActive: after.active === true && !wasActive,
            })
          : undefined
        const payload = sessionMeta ? { ...publicResult, session: sessionMeta } : publicResult
        return { text: JSON.stringify(payload, null, 2) }
      }
    ```

    Also, `case 'agrune_snapshot':`는 해당 응답에도 session meta를 붙인다. 현재 블록을 교체:
    ```ts
      case 'agrune_snapshot': {
        if (tabId == null) return { text: 'No active sessions.', isError: true }
        const snapshot = driver.getSnapshot(tabId)
        if (!snapshot) return { text: `No snapshot available for tab ${tabId}.`, isError: true }
        const payload = {
          ...toPublicSnapshot(snapshot, resolveSnapshotOptions(args)),
          session: buildSessionMeta(driver, tabId, false),
        }
        return { text: JSON.stringify(payload, null, 2) }
      }
    ```

    (snapshot은 상태를 바꾸지 않으므로 `becameActive`는 false.)

    `case 'agrune_sessions':` 블록은 아래로 교체 — `toPublicSession`이 이미 `active` 플래그를 내려주므로 본질은 동일하나 명시적으로 유지:
    ```ts
      case 'agrune_sessions': {
        return { text: JSON.stringify(driver.listSessions().map(toPublicSession), null, 2) }
      }
    ```
    (변경 없음 — 이미 위 형태였다면 그대로.)

    **3. `case 'agrune_focus':`를 `default:` 앞에 추가한다:**
    ```ts
      case 'agrune_focus': {
        const focusArg = resolveFocusTabId(args)
        if (focusArg == null) {
          return errorText('TAB_NOT_FOUND', 'agrune_focus requires tabId or numeric sessionId.')
        }
        const target = driver.listSessions().find(s => s.tabId === focusArg)
        if (!target) {
          return errorText('TAB_NOT_FOUND', `No session exists for tabId ${focusArg}.`, { tabId: focusArg })
        }
        const wasActive = target.active === true
        try {
          const focusResult = await driver.focusSession(focusArg)
          const refreshed = driver.listSessions().find(s => s.tabId === focusArg) ?? target
          const sessionMeta = toPublicSessionMeta(refreshed as Session, {
            wasActive,
            becameActive: focusResult.becameActive && !wasActive,
          })
          const payload: Record<string, unknown> = {
            ok: true,
            session: sessionMeta,
          }
          if (focusResult.cdpFocusError) {
            payload.cdpFocusError = focusResult.cdpFocusError
          }
          return { text: JSON.stringify(payload, null, 2) }
        } catch (error) {
          const shape = error as Partial<CommandErrorShape>
          if (shape && typeof shape.code === 'string' && shape.code === 'TAB_NOT_FOUND') {
            return errorText('TAB_NOT_FOUND', shape.message ?? 'Tab not found.', shape.details)
          }
          return errorText('INVALID_COMMAND', error instanceof Error ? error.message : String(error))
        }
      }
    ```

    **4. 파일 맨 아래 `resolveSnapshotOptions` 함수 뒤에 helper 함수들을 추가한다:**
    ```ts
    function buildSessionMeta(
      driver: BrowserDriver,
      tabId: number,
      becameActive: boolean,
    ): PublicSessionMeta | undefined {
      const session = driver.listSessions().find(s => s.tabId === tabId)
      if (!session) return undefined
      return toPublicSessionMeta(session as Session, {
        wasActive: session.active === true,
        becameActive,
      })
    }

    function resolveFocusTabId(args: Record<string, unknown>): number | null {
      if (typeof args.tabId === 'number' && Number.isFinite(args.tabId)) return args.tabId
      if (typeof args.sessionId === 'string') {
        const parsed = Number(args.sessionId)
        if (Number.isFinite(parsed)) return parsed
      }
      return null
    }

    function errorText(
      code: string,
      message: string,
      details?: Record<string, unknown>,
    ): ToolHandlerResult {
      return {
        text: JSON.stringify(
          { ok: false, error: { code, message, ...(details ? { details } : {}) } },
          null,
          2,
        ),
        isError: true,
      }
    }
    ```
  </action>
  <acceptance_criteria>
    - packages/mcp/src/index.ts contains `case 'agrune_focus':`
    - packages/mcp/src/index.ts contains `driver.focusSession(focusArg)`
    - packages/mcp/src/index.ts contains `toPublicSessionMeta`
    - packages/mcp/src/index.ts contains `function resolveFocusTabId`
    - packages/mcp/src/index.ts contains `function buildSessionMeta`
    - packages/mcp/src/index.ts contains `TAB_NOT_FOUND`
    - packages/mcp/src/index.ts still contains `case 'agrune_config':`
    - packages/mcp/src/index.ts still contains `driver.execute(tabId, command)`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Update tools.spec.ts for 10 tools including agrune_focus</name>
  <files>packages/mcp/tests/tools.spec.ts</files>
  <read_first>
    - packages/mcp/tests/tools.spec.ts (94줄 전체, 특히 `defines all 9 required tools` 7-20줄)
    - packages/mcp/src/tools.ts (Plan 03 task 1 결과)
  </read_first>
  <action>
    `packages/mcp/tests/tools.spec.ts`에서 다음만 바꾼다. 다른 describe/it는 그대로 둔다.

    **1. `it('defines all 9 required tools', ...)`(7-20줄)을 교체:**
    ```ts
    it('defines all 10 required tools', () => {
      const names = tools.map((t) => t.name)
      expect(names).toEqual([
        'agrune_sessions',
        'agrune_snapshot',
        'agrune_act',
        'agrune_fill',
        'agrune_drag',
        'agrune_wait',
        'agrune_guide',
        'agrune_config',
        'agrune_read',
        'agrune_focus',
      ])
    })
    ```

    **2. 파일 끝(`})` 위)에 `agrune_focus` 전용 it 블록 두 개 추가:**
    ```ts
    it('agrune_focus exposes tabId and sessionId as optional properties', () => {
      const focus = tools.find((t) => t.name === 'agrune_focus')!
      expect(focus.inputSchema.properties).toHaveProperty('tabId')
      expect(focus.inputSchema.properties).toHaveProperty('sessionId')
      expect(focus.inputSchema.required ?? []).toEqual([])
    })

    it('agrune_focus description mentions switching the active session', () => {
      const focus = tools.find((t) => t.name === 'agrune_focus')!
      expect(focus.description.toLowerCase()).toContain('active')
    })
    ```
  </action>
  <acceptance_criteria>
    - packages/mcp/tests/tools.spec.ts contains `defines all 10 required tools`
    - packages/mcp/tests/tools.spec.ts contains `'agrune_focus',`
    - packages/mcp/tests/tools.spec.ts contains `agrune_focus exposes tabId`
    - packages/mcp/tests/tools.spec.ts contains `agrune_focus description mentions`
    - `pnpm --filter @agrune/mcp test -- tools` exits 0 (해당 spec 단독 실행)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Update public-shapes.spec.ts to include active in expected PublicSession</name>
  <files>packages/mcp/tests/public-shapes.spec.ts</files>
  <read_first>
    - packages/mcp/tests/public-shapes.spec.ts (256줄 전체, `sessions omit embedded snapshots` 10-33줄)
    - packages/mcp/src/public-shapes.ts (Plan 03 task 3 결과)
  </read_first>
  <action>
    `packages/mcp/tests/public-shapes.spec.ts`를 최소 수정한다. 기존 다른 it은 건드리지 않는다.

    **1. `'sessions omit embedded snapshots and expose summary fields only'` 블록의 `expect(toPublicSession(session)).toEqual({...})` 기대값**을 교체해 `active: false`를 추가:
    ```ts
    expect(toPublicSession(session)).toEqual({
      tabId: 42,
      url: 'http://localhost:5173',
      title: 'Project Management Tool',
      hasSnapshot: true,
      snapshotVersion: 3,
      active: false,
    })
    ```

    **2. 같은 describe 안 끝부분(`command results omit embedded snapshots` 블록 앞)에 새 it 두 개를 추가:**
    ```ts
    it('passes through session.active when provided', () => {
      const session = {
        tabId: 7,
        url: 'https://a.com',
        title: 'A',
        hasSnapshot: false,
        snapshotVersion: null,
        active: true,
      }
      expect(toPublicSession(session)).toEqual({
        tabId: 7,
        url: 'https://a.com',
        title: 'A',
        hasSnapshot: false,
        snapshotVersion: null,
        active: true,
      })
    })

    it('toPublicSessionMeta carries wasActive/becameActive', async () => {
      const { toPublicSessionMeta } = await import('../src/public-shapes.js')
      const session = {
        tabId: 11,
        url: 'https://b.com',
        title: 'B',
        hasSnapshot: false,
        snapshotVersion: null,
      }
      expect(toPublicSessionMeta(session, { wasActive: false, becameActive: true })).toEqual({
        tabId: 11,
        url: 'https://b.com',
        title: 'B',
        wasActive: false,
        becameActive: true,
      })
    })
    ```
  </action>
  <acceptance_criteria>
    - packages/mcp/tests/public-shapes.spec.ts contains `active: false`
    - packages/mcp/tests/public-shapes.spec.ts contains `passes through session.active when provided`
    - packages/mcp/tests/public-shapes.spec.ts contains `toPublicSessionMeta`
    - `pnpm --filter @agrune/mcp test -- public-shapes` exits 0
  </acceptance_criteria>
</task>

## Verification
- `pnpm --filter @agrune/core build` && `pnpm --filter @agrune/browser build` 모두 green.
- `pnpm --filter @agrune/mcp test -- tools public-shapes`가 green.
- `grep -n "agrune_focus" packages/mcp/src/tools.ts packages/mcp/src/mcp-tools.ts packages/mcp/src/index.ts`가 3개 파일 모두에서 매치.
- `grep -n "active" packages/mcp/src/public-shapes.ts`가 최소 2 매치.
- **주의**: `pnpm --filter @agrune/mcp build` 전체가 pre-existing `ws` 의존성 이슈로 실패할 수 있음(phase 외부 문제, Phase 9 scope). `tsc` type check가 통과하는지를 성공 기준으로 한다.

## Success Criteria
- [ ] `agrune_focus` 도구가 `getToolDefinitions()`에 추가되어 총 10개
- [ ] `registerAgruneTools`가 `agrune_focus`를 등록한다
- [ ] `PublicSession`이 `active: boolean`을 갖는다
- [ ] `toPublicSessionMeta` 헬퍼가 wasActive/becameActive를 전달한다
- [ ] command-style 도구 응답에 `session` 메타 필드가 포함된다
- [ ] `agrune_focus` 핸들러가 alive session에 대해 active를 전환하고, unknown tabId에 TAB_NOT_FOUND 에러를 반환한다
- [ ] 기존 테스트(9-tool 테스트 → 10-tool, `toPublicSession` 기대값) 전부 green
