---
phase: 09-quality-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - packages/e2e/package.json
  - packages/e2e/tsconfig.json
  - packages/e2e/playwright.config.ts
  - packages/e2e/fixtures/overlay-modal.html
  - packages/e2e/fixtures/tricky-inputs.html
  - packages/e2e/tests/annotation-scan.spec.ts
  - packages/e2e/tests/overlay-modal.spec.ts
  - packages/e2e/tests/hitl-toolbar.spec.ts
  - .github/workflows/ci.yml
autonomous: true
user_setup:
  - "On CI, Playwright downloads Chromium. Locally this may be skipped — run `pnpm test:e2e:install` once to enable local runs."
requirements_addressed:
  - QUAL-01
must_haves:
  truths:
    - "루트 `pnpm test:e2e` 명령이 존재하고 Playwright 기반 E2E 스위트를 실행한다"
    - "overlay/modal·tricky-input 시나리오를 실제 브라우저에서 돌린다"
    - "`.github/workflows/ci.yml`이 PR/main push에서 빌드·typecheck·unit·E2E를 모두 실행하고 실패 시 PR을 블록한다"
  artifacts:
    - packages/e2e (신규 워크스페이스)
    - .github/workflows/ci.yml
  key_links:
    - Plan 02가 추가하는 `pnpm lint:annotations`를 CI build-test job에 묶는다
---

# Phase 9 Plan 01: Playwright E2E harness + CI

## Objective
- **What:** `packages/e2e` 워크스페이스를 추가하고 Playwright 기반 overlay/modal·tricky-inputs·devtools HITL E2E 스펙을 배선한다. `pnpm test:e2e` 명령을 루트에 노출한다. 신규 GitHub Actions 워크플로 `.github/workflows/ci.yml`로 PR에서 lint/unit/E2E 모두를 강제한다.
- **Why:** QUAL-01은 overlay/modal 시나리오가 CI에서 자동 검증되는 것이 기준이다. 단위 테스트로는 드러나지 않는 z-index/포인터 이벤트 문제를 실 브라우저로 잡아야 한다.
- **Output:** `pnpm test:e2e`(로컬 설치 후) + `ci.yml` job `e2e`.

## Context
@.planning/PROJECT.md
@.planning/phases/09-quality-infrastructure/09-CONTEXT.md
@.planning/phases/09-quality-infrastructure/09-RESEARCH.md
@packages/runtime/src/dom-scanner.ts
@packages/mcp/src/devtools-server.ts
@packages/devtools/src/panel.ts
@pnpm-workspace.yaml
@package.json

## Tasks

<task type="execute">
  <name>Create @agrune/e2e workspace package.json</name>
  <files>packages/e2e/package.json</files>
  <read_first>
    - packages/mcp/package.json (reference scripts shape)
    - pnpm-workspace.yaml
  </read_first>
  <action>
Write exactly this file (overwrite if exists):

```json
{
  "name": "@agrune/e2e",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest:e2e": "pnpm -w -r --filter @agrune/runtime --filter @agrune/devtools --filter @agrune/mcp --filter @agrune/core --filter @agrune/browser run build",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@agrune/core": "workspace:*",
    "@agrune/devtools": "workspace:*",
    "@agrune/mcp": "workspace:*",
    "@agrune/runtime": "workspace:*",
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "ws": "^8.20.0"
  }
}
```
  </action>
  <acceptance_criteria>
    - File `packages/e2e/package.json` exists
    - File contains `"name": "@agrune/e2e"`
    - File contains `"test:e2e": "playwright test"`
    - File contains `"test:e2e:install": "playwright install --with-deps chromium"`
    - File contains `"@playwright/test"`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Create e2e tsconfig</name>
  <files>packages/e2e/tsconfig.json</files>
  <read_first>
    - packages/mcp/tsconfig.json
    - tsconfig.base.json
  </read_first>
  <action>
Write:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "target": "ES2022",
    "types": ["node"],
    "rootDir": ".",
    "outDir": "dist",
    "noEmit": true,
    "skipLibCheck": true
  },
  "include": ["tests/**/*.ts", "playwright.config.ts"]
}
```
  </action>
  <acceptance_criteria>
    - File `packages/e2e/tsconfig.json` exists
    - File contains `"extends": "../../tsconfig.base.json"`
    - File contains `"include"` with `"tests/**/*.ts"`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Create Playwright config</name>
  <files>packages/e2e/playwright.config.ts</files>
  <read_first>
    - packages/e2e/package.json
  </read_first>
  <action>
Write:

```ts
import { defineConfig, devices } from '@playwright/test'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'

const __dirname = dirname(fileURLToPath(import.meta.url))
const fixturesDir = join(__dirname, 'fixtures')

// If Playwright's chromium binary cannot be downloaded (sandboxed local env),
// the user can export PLAYWRIGHT_SKIP_E2E=1 to have each test soft-skip itself.
// CI never sets this flag — ci.yml always runs the full suite.

export default defineConfig({
  testDir: './tests',
  timeout: 30_000,
  expect: { timeout: 5_000 },
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? [['github'], ['list']] : [['list']],
  use: {
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: `node --experimental-strip-types -e "import('node:http').then(({createServer}) => import('node:fs/promises').then(async fs => { const srv = createServer(async (req, res) => { const p = new URL(req.url ?? '/', 'http://x').pathname; const rel = p === '/' ? '/index.html' : p; try { const data = await fs.readFile('${fixturesDir}' + rel); const ct = rel.endsWith('.html') ? 'text/html' : rel.endsWith('.js') ? 'text/javascript' : 'text/plain'; res.writeHead(200, {'content-type': ct}); res.end(data); } catch { res.writeHead(404); res.end('not found'); } }); srv.listen(5555, '127.0.0.1', () => console.log('fixtures on 5555')); })); "`,
    url: 'http://127.0.0.1:5555/overlay-modal.html',
    reuseExistingServer: !process.env.CI,
    timeout: 10_000,
  },
})
```
  </action>
  <acceptance_criteria>
    - File `packages/e2e/playwright.config.ts` exists
    - File contains `defineConfig(`
    - File contains `testDir: './tests'`
    - File contains `'Desktop Chrome'`
    - File contains `webServer`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Create overlay-modal fixture</name>
  <files>packages/e2e/fixtures/overlay-modal.html</files>
  <read_first>
    - packages/runtime/src/dom-scanner.ts
  </read_first>
  <action>
Write an HTML fixture that contains:
- A toolbar group with two annotated click buttons ("Save" and "Cancel").
- A modal overlay (z-index:9999, `role="dialog"`) that covers the toolbar. When the modal is open, the Cancel button must be `covered` by the modal per the scanner's intersection rules. The modal itself has its own annotated Confirm button.

Exact content:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>overlay-modal fixture</title>
    <style>
      body { font-family: sans-serif; margin: 0; padding: 16px; }
      #toolbar { display: flex; gap: 8px; padding: 12px; background: #eee; }
      #toolbar button { padding: 8px 12px; }
      .modal-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,0.5); display: none; align-items: center; justify-content: center; z-index: 9999; }
      .modal-backdrop.open { display: flex; }
      .modal { background: white; padding: 24px; border-radius: 8px; min-width: 280px; }
      .modal button { padding: 8px 12px; margin-right: 8px; }
    </style>
  </head>
  <body>
    <main data-agrune-group="toolbar" data-agrune-group-name="Toolbar" data-agrune-group-desc="Document toolbar">
      <div id="toolbar">
        <button
          data-agrune-action="click"
          data-agrune-name="Save"
          data-agrune-desc="Save the document"
          data-agrune-key="save-btn"
        >Save</button>
        <button
          data-agrune-action="click"
          data-agrune-name="Cancel"
          data-agrune-desc="Cancel pending edits"
          data-agrune-key="cancel-btn"
          id="cancel"
        >Cancel</button>
      </div>
    </main>

    <div class="modal-backdrop" id="backdrop" role="presentation">
      <div class="modal" role="dialog" aria-modal="true" data-agrune-group="confirm-modal" data-agrune-group-name="Confirm Modal" data-agrune-group-desc="Discard confirmation">
        <p>Discard your changes?</p>
        <button
          data-agrune-action="click"
          data-agrune-name="Discard"
          data-agrune-desc="Confirm discard and close modal"
          data-agrune-key="modal-discard"
          id="discard"
        >Discard</button>
        <button
          data-agrune-action="click"
          data-agrune-name="Keep editing"
          data-agrune-desc="Cancel discard and close modal"
          data-agrune-key="modal-keep"
          id="keep"
        >Keep editing</button>
      </div>
    </div>

    <script>
      const backdrop = document.getElementById('backdrop')
      document.getElementById('cancel').addEventListener('click', () => backdrop.classList.add('open'))
      document.getElementById('keep').addEventListener('click', () => backdrop.classList.remove('open'))
      document.getElementById('discard').addEventListener('click', () => { backdrop.classList.remove('open'); document.body.setAttribute('data-discarded', '1') })
    </script>
  </body>
</html>
```
  </action>
  <acceptance_criteria>
    - File `packages/e2e/fixtures/overlay-modal.html` exists
    - File contains `data-agrune-key="modal-discard"`
    - File contains `role="dialog"`
    - File contains `data-agrune-group="confirm-modal"`
    - File contains `id="cancel"`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Create tricky-inputs fixture</name>
  <files>packages/e2e/fixtures/tricky-inputs.html</files>
  <read_first>
    - packages/runtime/src/dom-scanner.ts
  </read_first>
  <action>
Write:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>tricky-inputs fixture</title>
  </head>
  <body data-agrune-group="form" data-agrune-group-name="Signup form" data-agrune-group-desc="Credit card + bio form">
    <label>Card number:
      <input
        data-agrune-action="fill"
        data-agrune-name="Card number"
        data-agrune-desc="Credit card number — 16 digits"
        data-agrune-key="cc-number"
        inputmode="numeric"
        maxlength="19"
        id="cc"
      >
    </label>

    <label>Bio:
      <div
        contenteditable="true"
        data-agrune-action="fill"
        data-agrune-name="Bio"
        data-agrune-desc="Short bio — rich text"
        data-agrune-key="bio"
        id="bio"
        style="border:1px solid #ccc; min-height:40px; padding:4px;"
      ></div>
    </label>

    <label>Password:
      <input
        type="password"
        data-agrune-action="fill"
        data-agrune-name="Password"
        data-agrune-desc="Account password — sensitive"
        data-agrune-key="pw"
        data-agrune-sensitive
        id="pw"
      >
    </label>
  </body>
</html>
```
  </action>
  <acceptance_criteria>
    - File `packages/e2e/fixtures/tricky-inputs.html` exists
    - File contains `contenteditable="true"`
    - File contains `data-agrune-sensitive`
    - File contains `data-agrune-key="cc-number"`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Write annotation-scan E2E spec</name>
  <files>packages/e2e/tests/annotation-scan.spec.ts</files>
  <read_first>
    - packages/runtime/src/dom-scanner.ts
    - packages/e2e/fixtures/overlay-modal.html
    - packages/e2e/fixtures/tricky-inputs.html
  </read_first>
  <action>
Write:

```ts
import { test, expect } from '@playwright/test'

const SKIP = process.env.PLAYWRIGHT_SKIP_E2E === '1'

test.describe('annotation scan — real browser', () => {
  test.skip(SKIP, 'PLAYWRIGHT_SKIP_E2E=1 set — run `pnpm test:e2e:install` to enable locally')

  test('overlay-modal fixture yields 4 targets and 2 groups', async ({ page }) => {
    await page.goto('http://127.0.0.1:5555/overlay-modal.html')

    const result = await page.evaluate(() => {
      const actions = Array.from(document.querySelectorAll('[data-agrune-action]'))
      const groups = Array.from(document.querySelectorAll('[data-agrune-group]'))
      return { actionCount: actions.length, groupCount: groups.length }
    })

    expect(result.actionCount).toBe(4)
    expect(result.groupCount).toBe(2)
  })

  test('opening modal covers the cancel button', async ({ page }) => {
    await page.goto('http://127.0.0.1:5555/overlay-modal.html')
    await page.click('#cancel')

    // Backdrop is now visible and covers the toolbar.
    const backdrop = page.locator('#backdrop')
    await expect(backdrop).toBeVisible()

    // elementFromPoint over the cancel button should be the backdrop, not the button.
    const elementUnderCancel = await page.evaluate(() => {
      const cancel = document.getElementById('cancel')!
      const r = cancel.getBoundingClientRect()
      const el = document.elementFromPoint(r.x + r.width / 2, r.y + r.height / 2)
      return el?.id ?? el?.className ?? el?.tagName
    })

    expect(elementUnderCancel).not.toBe('cancel')
  })

  test('discard closes modal and marks document', async ({ page }) => {
    await page.goto('http://127.0.0.1:5555/overlay-modal.html')
    await page.click('#cancel')
    await page.click('#discard')
    await expect(page.locator('#backdrop')).not.toBeVisible()
    const flag = await page.evaluate(() => document.body.getAttribute('data-discarded'))
    expect(flag).toBe('1')
  })

  test('tricky-inputs fixture exposes contenteditable + sensitive + masked', async ({ page }) => {
    await page.goto('http://127.0.0.1:5555/tricky-inputs.html')

    const bioEditable = await page.evaluate(() =>
      document.getElementById('bio')?.getAttribute('contenteditable'),
    )
    expect(bioEditable).toBe('true')

    const sensitive = await page.evaluate(() =>
      document.getElementById('pw')?.hasAttribute('data-agrune-sensitive'),
    )
    expect(sensitive).toBe(true)

    const cc = page.locator('#cc')
    await cc.fill('1234123412341234')
    await expect(cc).toHaveValue('1234123412341234')
  })
})
```
  </action>
  <acceptance_criteria>
    - File `packages/e2e/tests/annotation-scan.spec.ts` exists
    - File contains `PLAYWRIGHT_SKIP_E2E`
    - File contains `document.querySelectorAll('[data-agrune-action]')`
    - File contains `elementFromPoint`
    - File contains 4 `test(` calls
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Write overlay-modal interaction spec</name>
  <files>packages/e2e/tests/overlay-modal.spec.ts</files>
  <read_first>
    - packages/e2e/fixtures/overlay-modal.html
  </read_first>
  <action>
Write:

```ts
import { test, expect } from '@playwright/test'

const SKIP = process.env.PLAYWRIGHT_SKIP_E2E === '1'

test.describe('overlay-modal interactions', () => {
  test.skip(SKIP, 'PLAYWRIGHT_SKIP_E2E=1 set — run `pnpm test:e2e:install` to enable locally')

  test('save click does not open modal', async ({ page }) => {
    await page.goto('http://127.0.0.1:5555/overlay-modal.html')
    await page.click('[data-agrune-key="save-btn"]')
    await expect(page.locator('#backdrop')).not.toBeVisible()
  })

  test('keep-editing closes modal without discard flag', async ({ page }) => {
    await page.goto('http://127.0.0.1:5555/overlay-modal.html')
    await page.click('[data-agrune-key="cancel-btn"]')
    await expect(page.locator('#backdrop')).toBeVisible()
    await page.click('[data-agrune-key="modal-keep"]')
    await expect(page.locator('#backdrop')).not.toBeVisible()
    const flag = await page.evaluate(() => document.body.getAttribute('data-discarded'))
    expect(flag).toBeNull()
  })

  test('modal keyboard focus trapping smoke', async ({ page }) => {
    await page.goto('http://127.0.0.1:5555/overlay-modal.html')
    await page.click('[data-agrune-key="cancel-btn"]')
    const dialog = page.locator('[role="dialog"]')
    await expect(dialog).toBeVisible()
  })
})
```
  </action>
  <acceptance_criteria>
    - File `packages/e2e/tests/overlay-modal.spec.ts` exists
    - File contains 3 `test(` calls
    - File contains `data-agrune-key="cancel-btn"`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Write HITL devtools spec</name>
  <files>packages/e2e/tests/hitl-toolbar.spec.ts</files>
  <read_first>
    - packages/mcp/src/devtools-server.ts
    - packages/mcp/src/command-broker.ts
    - packages/mcp/src/hitl-controller.ts
  </read_first>
  <action>
Write:

```ts
import { test, expect } from '@playwright/test'
import { WebSocket } from 'ws'

const SKIP = process.env.PLAYWRIGHT_SKIP_E2E === '1'

// This spec spins up the real @agrune/mcp devtools-server against a mock driver
// and asserts the protocol used by the devtools webapp. It's a pure Node WS check
// wrapped in Playwright's runner for parity with the rest of the E2E suite.

test.describe('HITL toolbar WebSocket contract', () => {
  test.skip(SKIP, 'PLAYWRIGHT_SKIP_E2E=1 set — run `pnpm test:e2e:install` to enable locally')

  test('server emits hitl_state + accepts pause/resume', async () => {
    const { startDevtoolsServer, stopDevtoolsServer } = await import('@agrune/mcp/devtools-server')
    const { HitlController } = await import('@agrune/mcp')
    const { CommandBroker } = await import('@agrune/mcp')

    const driver = {
      listSessions: () => [{ tabId: 1, url: 'https://x', title: 'x', hasSnapshot: false, snapshotVersion: null }],
      getSnapshot: () => null,
      onSnapshotUpdate: () => {},
      onSessionOpen: () => {},
      onSessionClose: () => {},
      execute: async () => ({ ok: true }),
    }

    const hitl = new HitlController()
    const broker = new CommandBroker()
    const port = await startDevtoolsServer(driver as never, 0, { hitl, commandBroker: broker })

    try {
      const ws = new WebSocket(`ws://127.0.0.1:${port}/devtools/ws`)
      const messages: Array<{ type: string; data?: unknown }> = []
      ws.on('message', (raw) => { messages.push(JSON.parse(raw.toString())) })

      await new Promise<void>((resolve, reject) => {
        ws.once('open', () => resolve())
        ws.once('error', reject)
      })

      // Wait for hitl_state broadcast after connect.
      await expect.poll(() => messages.find(m => m.type === 'hitl_state')?.data ?? null, { timeout: 2000 })
        .toMatchObject({ paused: false })

      ws.send(JSON.stringify({ type: 'hitl', action: 'pause' }))

      await expect.poll(
        () => messages.filter(m => m.type === 'hitl_state').pop()?.data ?? null,
        { timeout: 2000 },
      ).toMatchObject({ paused: true })

      hitl.resume()
      ws.close()
    } finally {
      await stopDevtoolsServer()
    }
  })
})
```
  </action>
  <acceptance_criteria>
    - File `packages/e2e/tests/hitl-toolbar.spec.ts` exists
    - File contains `startDevtoolsServer`
    - File contains `new HitlController()`
    - File contains `action: 'pause'`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Root package.json — wire e2e + lint scripts</name>
  <files>package.json</files>
  <read_first>
    - package.json
  </read_first>
  <action>
Replace the `"scripts"` block with:

```json
  "scripts": {
    "build": "pnpm -r --filter \"@agrune/*\" run build",
    "typecheck": "pnpm -r --filter \"@agrune/*\" --if-present run typecheck && pnpm -r --filter \"@agrune/*\" --if-present run build",
    "test": "pnpm -r --filter \"@agrune/*\" --if-present run test",
    "lint:annotations": "node ./packages/core/bin/agrune-lint.js packages apps",
    "test:e2e": "pnpm --filter @agrune/e2e run test:e2e",
    "test:e2e:install": "pnpm --filter @agrune/e2e run test:e2e:install"
  }
```

Keep all other fields (name, private, version, packageManager) untouched.
  </action>
  <acceptance_criteria>
    - `package.json` contains `"lint:annotations"`
    - `package.json` contains `"test:e2e"`
    - `package.json` contains `"test:e2e:install"`
    - `package.json` contains `"packageManager": "pnpm@10.23.0"` (unchanged)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Confirm pnpm-workspace covers packages/e2e</name>
  <files>pnpm-workspace.yaml</files>
  <read_first>
    - pnpm-workspace.yaml
  </read_first>
  <action>
File already says `packages/*` so `packages/e2e` is covered. Verify no change needed — but add a comment line to confirm intent:

```yaml
packages:
  # includes packages/e2e (added in Phase 9 for Playwright harness)
  - packages/*

onlyBuiltDependencies:
  - '@swc/core'
  - esbuild
```
  </action>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` contains `packages/*`
    - `pnpm-workspace.yaml` contains `Phase 9`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>CI workflow — build-test + e2e jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/release.yml
    - package.json
  </read_first>
  <action>
Write:

```yaml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: build + typecheck + unit + lint:annotations
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      - name: Annotation lint
        run: pnpm lint:annotations

  e2e:
    name: playwright e2e
    runs-on: ubuntu-latest
    needs: build-test
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build upstream packages
        run: pnpm build

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install Playwright Chromium
        run: pnpm test:e2e:install

      - name: Run E2E
        run: pnpm test:e2e

      - name: Upload Playwright traces on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-trace
          path: packages/e2e/test-results/
          retention-days: 7
```
  </action>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` exists
    - File contains `pull_request:`
    - File contains `needs: build-test`
    - File contains `pnpm test:e2e`
    - File contains `pnpm lint:annotations`
    - File contains `playwright install --with-deps chromium` OR `pnpm test:e2e:install`
    - YAML parseable: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>pnpm install to pick up new workspace + Playwright</name>
  <files></files>
  <read_first>
    - packages/e2e/package.json
  </read_first>
  <action>
Run `pnpm install` in the repo root so pnpm adds `@playwright/test` and hoists the new workspace.

Expected: new lockfile entry for `@playwright/test`, `packages/e2e/node_modules` created, no other package-side regressions.

If the sandbox blocks network → mark the task incomplete in a comment but do not fail the whole phase: CI will run `pnpm install --frozen-lockfile` fresh. The pre-existing `pnpm install` worked in Phase 8 per `08-01-SUMMARY.md`, so offline regressions are unlikely.

Command:
```bash
pnpm install
```
  </action>
  <acceptance_criteria>
    - `pnpm-lock.yaml` contains `@playwright/test` OR install was skipped with logged reason
    - `packages/e2e/node_modules` directory exists after install OR logged reason
  </acceptance_criteria>
</task>

## Verification
- `pnpm install` succeeds OR failure logged as sandbox-only.
- `pnpm --filter @agrune/e2e run typecheck` exits 0.
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (YAML valid).
- Local `PLAYWRIGHT_SKIP_E2E=1 pnpm test:e2e` runs in "all tests skipped" mode so Plan 03 can still validate the rest. CI runs the real suite.

## Rollback
- Delete `packages/e2e/`.
- Remove the two scripts from `package.json`.
- Delete `.github/workflows/ci.yml`.
