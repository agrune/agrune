---
phase: 08-devtools-webapp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/devtools-server.ts
  - packages/mcp/src/command-broker.ts
  - packages/mcp/src/hitl-controller.ts
  - packages/mcp/src/index.ts
  - packages/mcp/tests/devtools-server.spec.ts
  - packages/mcp/tests/command-broker.spec.ts
  - packages/mcp/tests/hitl-controller.spec.ts
autonomous: true
user_setup: []
requirements_addressed:
  - DEVT-01
  - DEVT-02
  - DEVT-03
must_haves:
  truths:
    - devtools-server가 MCP tool 호출마다 `command_event` 메시지(start/end/error phase)를 모든 연결된 WebSocket client에 브로드캐스트한다
    - devtools-server가 `hitl` 인바운드 명령(pause/resume/skip/step)과 `focus_session` 인바운드 명령을 수락하고, 현재 HITL 상태를 `hitl_state` 메시지로 브로드캐스트한다
    - HITL controller가 도구 실행 직전 gate를 걸어 paused 상태에서는 resume/skip/step 신호가 올 때까지 기다린다
    - 기존 `sessions_update`·`snapshot_update`·`subscribe`·`highlight` 메시지와 동작은 바뀌지 않는다(backward compatible)
    - 실패 payload에는 최소 `tool`, `sessionId`, `error.code`, `error.message`, `durationMs`가 포함된다
  artifacts:
    - packages/mcp/src/command-broker.ts (새 파일, in-memory pub/sub)
    - packages/mcp/src/hitl-controller.ts (새 파일, pause gate state machine)
    - packages/mcp/src/devtools-server.ts (확장: new event types, driver 추가 훅)
    - packages/mcp/src/index.ts (handleToolCall 래핑: 이벤트 emit + HITL gate)
    - packages/mcp/tests/command-broker.spec.ts
    - packages/mcp/tests/hitl-controller.spec.ts
    - packages/mcp/tests/devtools-server.spec.ts (확장)
  key_links:
    - Plan 02의 devtools 프론트엔드가 여기서 추가한 `command_event`·`hitl_state` 메시지를 구독하고 `hitl`·`focus_session` 메시지를 전송한다
    - focusSession은 기존 `driver.focusSession(tabId)`를 그대로 호출한다 (Phase 7 산출물 재사용)
---

# Phase 8 Plan 01: Devtools Server — Command Broker & HITL Controller

## Objective
- **What:** `@agrune/mcp` 서버에 (1) 모든 MCP 도구 호출을 관찰 가능한 이벤트(`command_event`) 스트림으로 바꿔 주는 **CommandBroker**, (2) pause/resume/skip/step 상태 머신을 가진 **HitlController**, (3) devtools-server가 이 두 모듈의 이벤트를 WebSocket으로 브로드캐스트하고 역방향 `hitl`·`focus_session` 명령을 수락하도록 하는 확장을 추가한다.
- **Why:** DEVT-01 (명령 로그 뷰), DEVT-02 (HITL 제어), DEVT-03 (실패 진단)의 프론트엔드는 **서버 쪽 이벤트 스트림이 없으면 표시할 데이터가 없다.** 이 plan은 UI가 의존하는 프로토콜을 먼저 열어 놓는다. Plan 02(프론트엔드)가 이 채널을 소비한다.
- **Output:** 새 모듈 2개, devtools-server 확장, `createMcpServer`의 handleToolCall wrap, 단위 테스트.

## Context
@.planning/PROJECT.md
@.planning/phases/08-devtools-webapp/08-CONTEXT.md
@.planning/phases/08-devtools-webapp/08-UI-SPEC.md
@packages/mcp/src/index.ts
@packages/mcp/src/devtools-server.ts
@packages/mcp/src/mcp-tools.ts
@packages/mcp/src/public-shapes.ts

## Tasks

<task type="execute">
  <name>Create CommandBroker pub/sub module</name>
  <files>packages/mcp/src/command-broker.ts</files>
  <read_first>
    - packages/mcp/src/index.ts (handleToolCall 위치, 31-134줄)
    - packages/mcp/src/public-shapes.ts (error shape 모양 — PublicCommandResult 59-69줄)
  </read_first>
  <action>
    새 파일 `packages/mcp/src/command-broker.ts`를 다음과 같이 작성한다:

    ```ts
    export type CommandEventPhase = 'start' | 'end' | 'error'

    export interface CommandEvent {
      id: string
      ts: number
      sessionId: number | null
      tool: string
      phase: CommandEventPhase
      durationMs?: number
      args?: Record<string, unknown>
      error?: {
        code: string
        message: string
      }
    }

    export type CommandEventListener = (event: CommandEvent) => void

    /**
     * In-memory pub/sub for MCP command lifecycle events.
     * Also retains the last N events so a newly connected devtools client
     * can render a backfill on subscribe.
     */
    export class CommandBroker {
      private readonly listeners = new Set<CommandEventListener>()
      private readonly buffer: CommandEvent[] = []
      private counter = 0

      constructor(private readonly bufferSize = 500) {}

      nextId(): string {
        this.counter += 1
        return `cmd-${Date.now()}-${this.counter}`
      }

      emit(event: CommandEvent): void {
        this.buffer.push(event)
        if (this.buffer.length > this.bufferSize) {
          this.buffer.splice(0, this.buffer.length - this.bufferSize)
        }
        for (const listener of this.listeners) {
          try {
            listener(event)
          } catch {
            // Never let a rogue listener crash the tool call.
          }
        }
      }

      getBuffered(): CommandEvent[] {
        return [...this.buffer]
      }

      subscribe(listener: CommandEventListener): () => void {
        this.listeners.add(listener)
        return () => {
          this.listeners.delete(listener)
        }
      }

      clear(): void {
        this.buffer.length = 0
      }
    }
    ```

    Stdio 밖으로는 아무것도 내보내지 않는다. 외부 import 없음.
  </action>
  <acceptance_criteria>
    - packages/mcp/src/command-broker.ts exists
    - packages/mcp/src/command-broker.ts contains `export class CommandBroker`
    - packages/mcp/src/command-broker.ts contains `phase: CommandEventPhase`
    - packages/mcp/src/command-broker.ts contains `this.bufferSize = 500`
    - packages/mcp/src/command-broker.ts contains `subscribe(listener: CommandEventListener)`
    - `pnpm --filter @agrune/mcp typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Create HitlController pause-gate module</name>
  <files>packages/mcp/src/hitl-controller.ts</files>
  <read_first>
    - packages/mcp/src/index.ts (handleToolCall 전체 구조)
    - .planning/phases/08-devtools-webapp/08-UI-SPEC.md §"HITL state machine"
  </read_first>
  <action>
    새 파일 `packages/mcp/src/hitl-controller.ts`를 다음과 같이 작성한다:

    ```ts
    export interface HitlState {
      paused: boolean
      pausedAt: number | null
      pendingTool: string | null
    }

    export type HitlStateListener = (state: HitlState) => void

    export class HitlSkipError extends Error {
      code = 'HITL_SKIPPED' as const
      constructor(tool: string) {
        super(`HITL: call to ${tool} was skipped by the operator.`)
        this.name = 'HitlSkipError'
      }
    }

    type GateResolver = (decision: 'resume' | 'skip') => void

    /**
     * Pause gate for MCP tool calls. UI sends pause/resume/skip/step commands
     * over the devtools WebSocket. When `paused` is true, incoming tool calls
     * block at `awaitGate()` until the operator resumes, steps once, or skips.
     */
    export class HitlController {
      private state: HitlState = { paused: false, pausedAt: null, pendingTool: null }
      private readonly listeners = new Set<HitlStateListener>()
      private waiters: GateResolver[] = []
      private stepPending = false

      getState(): HitlState {
        return { ...this.state }
      }

      onChange(listener: HitlStateListener): () => void {
        this.listeners.add(listener)
        return () => {
          this.listeners.delete(listener)
        }
      }

      pause(): void {
        if (this.state.paused) return
        this.state = { paused: true, pausedAt: Date.now(), pendingTool: null }
        this.broadcast()
      }

      resume(): void {
        if (!this.state.paused) return
        this.state = { paused: false, pausedAt: null, pendingTool: null }
        this.flush('resume')
        this.broadcast()
      }

      step(): void {
        // "Step" = let the next pending (or upcoming) call through once, then re-pause.
        this.stepPending = true
        if (this.waiters.length > 0) {
          const [next, ...rest] = this.waiters
          this.waiters = rest
          this.stepPending = false // consumed
          next('resume')
        }
      }

      skip(): void {
        if (this.waiters.length === 0) return
        const [next, ...rest] = this.waiters
        this.waiters = rest
        next('skip')
        // Remain paused; pendingTool cleared, next call will pause again.
        this.state = { paused: true, pausedAt: this.state.pausedAt ?? Date.now(), pendingTool: null }
        this.broadcast()
      }

      /**
       * Gate a tool call. Resolves immediately when not paused.
       * Throws HitlSkipError if the operator chose "skip current call".
       */
      async awaitGate(tool: string): Promise<void> {
        if (this.stepPending) {
          // One-shot step: allow this call, then re-pause.
          this.stepPending = false
          this.state = { paused: true, pausedAt: Date.now(), pendingTool: null }
          this.broadcast()
          return
        }
        if (!this.state.paused) return
        this.state = { ...this.state, pendingTool: tool }
        this.broadcast()
        const decision = await new Promise<'resume' | 'skip'>((resolve) => {
          this.waiters.push(resolve)
        })
        if (decision === 'skip') {
          throw new HitlSkipError(tool)
        }
      }

      private flush(decision: 'resume' | 'skip'): void {
        const pending = this.waiters.splice(0)
        for (const w of pending) w(decision)
      }

      private broadcast(): void {
        const snapshot = this.getState()
        for (const listener of this.listeners) {
          try {
            listener(snapshot)
          } catch {
            // Ignore bad listeners.
          }
        }
      }
    }
    ```
  </action>
  <acceptance_criteria>
    - packages/mcp/src/hitl-controller.ts exists
    - packages/mcp/src/hitl-controller.ts contains `export class HitlController`
    - packages/mcp/src/hitl-controller.ts contains `export class HitlSkipError`
    - packages/mcp/src/hitl-controller.ts contains `awaitGate(tool: string)`
    - packages/mcp/src/hitl-controller.ts contains `pause()` and `resume()` and `step()` and `skip()`
    - `pnpm --filter @agrune/mcp typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Wire CommandBroker and HitlController into createMcpServer</name>
  <files>packages/mcp/src/index.ts</files>
  <read_first>
    - packages/mcp/src/index.ts (전체 파일 191줄)
    - packages/mcp/src/command-broker.ts (task 1 결과)
    - packages/mcp/src/hitl-controller.ts (task 2 결과)
  </read_first>
  <action>
    `packages/mcp/src/index.ts`에 CommandBroker와 HitlController를 합류시킨다.

    1. import 섹션(1-12줄) 아래에 다음을 추가:
    ```ts
    import { CommandBroker, type CommandEvent } from './command-broker.js'
    import { HitlController, HitlSkipError } from './hitl-controller.js'

    export type { CommandEvent } from './command-broker.js'
    export type { HitlState } from './hitl-controller.js'
    export { CommandBroker } from './command-broker.js'
    export { HitlController, HitlSkipError } from './hitl-controller.js'
    ```

    2. `createMcpServer` 함수 시그니처(22-24줄)는 유지한다. 함수 본문 시작부(McpServer 생성 전)에 다음 두 줄을 추가한다:
    ```ts
      const commandBroker = new CommandBroker()
      const hitl = new HitlController()
    ```

    3. `handleToolCall` 함수(31-133줄)를 wrap한다. 기존 함수는 `innerHandleToolCall` 로 이름을 바꾸고, 새 외부 `handleToolCall`이 gating + event emission을 담당한다. 최종 구조:
    ```ts
      const innerHandleToolCall = async (
        name: string,
        args: Record<string, unknown>,
      ): Promise<ToolHandlerResult> => {
        /* 기존 31-133줄 본문 그대로 */
      }

      const handleToolCall = async (
        name: string,
        args: Record<string, unknown>,
      ): Promise<ToolHandlerResult> => {
        const tool = name
        const id = commandBroker.nextId()
        const start = Date.now()
        const sessionId = typeof args.tabId === 'number' ? (args.tabId as number) : null

        try {
          await hitl.awaitGate(tool)
        } catch (err) {
          if (err instanceof HitlSkipError) {
            const event: CommandEvent = {
              id,
              ts: start,
              sessionId,
              tool,
              phase: 'error',
              durationMs: 0,
              error: { code: err.code, message: err.message },
            }
            commandBroker.emit(event)
            return {
              text: JSON.stringify({ ok: false, error: { code: err.code, message: err.message } }, null, 2),
              isError: true,
            }
          }
          throw err
        }

        commandBroker.emit({ id, ts: start, sessionId, tool, phase: 'start' })

        try {
          const result = await innerHandleToolCall(name, args)
          const durationMs = Date.now() - start
          if (result.isError) {
            const parsed = safeParseJson(result.text)
            const err = extractError(parsed)
            commandBroker.emit({
              id, ts: Date.now(), sessionId, tool, phase: 'error', durationMs,
              error: err,
            })
          } else {
            commandBroker.emit({ id, ts: Date.now(), sessionId, tool, phase: 'end', durationMs })
          }
          return result
        } catch (error) {
          const message = error instanceof Error ? error.message : String(error)
          commandBroker.emit({
            id, ts: Date.now(), sessionId, tool,
            phase: 'error',
            durationMs: Date.now() - start,
            error: { code: 'INTERNAL_ERROR', message },
          })
          throw error
        }
      }
    ```

    4. 파일 하단(errorText 함수 근처)에 헬퍼 두 개를 추가:
    ```ts
    function safeParseJson(text: string): unknown {
      try { return JSON.parse(text) } catch { return null }
    }

    function extractError(parsed: unknown): { code: string; message: string } {
      if (parsed && typeof parsed === 'object' && 'error' in parsed) {
        const err = (parsed as { error?: unknown }).error
        if (err && typeof err === 'object') {
          const code = typeof (err as { code?: unknown }).code === 'string' ? (err as { code: string }).code : 'UNKNOWN_ERROR'
          const message = typeof (err as { message?: unknown }).message === 'string' ? (err as { message: string }).message : 'Unknown error'
          return { code, message }
        }
      }
      return { code: 'UNKNOWN_ERROR', message: 'Unknown error' }
    }
    ```

    5. `createMcpServer`의 return 객체에 `commandBroker`, `hitl`을 추가한다. 최종:
    ```ts
      registerAgruneTools(mcp, handleToolCall)

      return { server: mcp, driver, handleToolCall, commandBroker, hitl }
    ```

    다른 export/선언은 모두 유지한다. `driver.onActivity?.()` 호출은 `innerHandleToolCall` 안에 그대로 남겨둔다.
  </action>
  <acceptance_criteria>
    - packages/mcp/src/index.ts contains `import { CommandBroker, type CommandEvent } from './command-broker.js'`
    - packages/mcp/src/index.ts contains `import { HitlController, HitlSkipError } from './hitl-controller.js'`
    - packages/mcp/src/index.ts contains `const commandBroker = new CommandBroker()`
    - packages/mcp/src/index.ts contains `const hitl = new HitlController()`
    - packages/mcp/src/index.ts contains `await hitl.awaitGate(tool)`
    - packages/mcp/src/index.ts contains `commandBroker.emit({`
    - packages/mcp/src/index.ts contains `return { server: mcp, driver, handleToolCall, commandBroker, hitl }`
    - `pnpm --filter @agrune/mcp typecheck` exits 0
    - `pnpm --filter @agrune/mcp build` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Extend devtools-server with command_event, hitl_state, focus_session, hitl messages</name>
  <files>packages/mcp/src/devtools-server.ts</files>
  <read_first>
    - packages/mcp/src/devtools-server.ts (현재 271줄 전체)
    - packages/mcp/src/command-broker.ts (task 1)
    - packages/mcp/src/hitl-controller.ts (task 2)
  </read_first>
  <action>
    `packages/mcp/src/devtools-server.ts`를 다음과 같이 확장한다. **기존 message type(`sessions_update`, `snapshot_update`, `subscribe`, `highlight`, `clear_highlight`)과 동작은 바꾸지 않는다.**

    1. import 섹션(1-7줄) 아래에 다음을 추가:
    ```ts
    import type { CommandBroker, CommandEvent } from './command-broker.js'
    import type { HitlController, HitlState } from './hitl-controller.js'
    ```

    2. `DevtoolsDriver` interface(10-17줄) 뒤에 새 interface를 추가(driver 자체는 바꾸지 않는다 — server signature를 통해 주입):
    ```ts
    export interface DevtoolsServerOptions {
      commandBroker?: CommandBroker
      hitl?: HitlController
      onFocusSession?: (tabId: number) => Promise<void> | void
    }
    ```

    3. `startDevtoolsServer` 시그니처를 다음으로 변경한다:
    ```ts
    export async function startDevtoolsServer(
      driver: DevtoolsDriver,
      port = 0,
      options: DevtoolsServerOptions = {},
    ): Promise<number>
    ```

    4. `wss.on('connection', ...)` 콜백 안에서, 기존 `sendToClient(ws, { type: 'sessions_update', ... })` 바로 뒤에, HITL state snapshot과 command buffer backfill을 한 번씩 내려보낸다:
    ```ts
    if (options.hitl) {
      sendToClient(ws, { type: 'hitl_state', data: options.hitl.getState() })
    }
    if (options.commandBroker) {
      const buffered = options.commandBroker.getBuffered()
      if (buffered.length > 0) {
        sendToClient(ws, { type: 'command_backfill', data: buffered })
      }
    }
    ```

    5. `// --- Driver event subscriptions ---` 블록 뒤(기존 onSessionOpen/Close 블록 뒤)에 다음 두 구독을 추가한다:
    ```ts
    if (options.commandBroker) {
      options.commandBroker.subscribe((event: CommandEvent) => {
        const payload = JSON.stringify({ type: 'command_event', data: event })
        for (const client of clients) {
          if (client.ws.readyState === client.ws.OPEN) {
            client.ws.send(payload)
          }
        }
      })
    }
    if (options.hitl) {
      options.hitl.onChange((state: HitlState) => {
        const payload = JSON.stringify({ type: 'hitl_state', data: state })
        for (const client of clients) {
          if (client.ws.readyState === client.ws.OPEN) {
            client.ws.send(payload)
          }
        }
      })
    }
    ```

    6. `handleClientMessage` 함수의 signature를 확장한다:
    ```ts
    function handleClientMessage(
      client: ConnectedClient,
      message: { type: string; tabId?: number; targetId?: string; action?: string; sessionId?: number },
      driver: DevtoolsDriver,
      clients: ConnectedClient[],
      options: DevtoolsServerOptions,
    ): void
    ```
    그리고 switch 문에 두 개의 새 case를 추가(기본값 분기 전에 삽입):
    ```ts
        case 'hitl': {
          if (!options.hitl) return
          if (message.action === 'pause') options.hitl.pause()
          else if (message.action === 'resume') options.hitl.resume()
          else if (message.action === 'step') options.hitl.step()
          else if (message.action === 'skip') options.hitl.skip()
          return
        }
        case 'focus_session': {
          if (typeof message.sessionId !== 'number') return
          if (!options.onFocusSession) return
          void Promise.resolve(options.onFocusSession(message.sessionId)).catch(() => {
            // Swallow — onFocusSession is best-effort.
          })
          return
        }
    ```

    7. `ws.on('message', ...)` 안에서 `handleClientMessage(client, message, driver, clients)` 호출부를 `handleClientMessage(client, message, driver, clients, options)`로 바꾼다.

    다른 코드는 모두 유지한다.
  </action>
  <acceptance_criteria>
    - packages/mcp/src/devtools-server.ts contains `export interface DevtoolsServerOptions`
    - packages/mcp/src/devtools-server.ts contains `options: DevtoolsServerOptions = {}`
    - packages/mcp/src/devtools-server.ts contains `case 'hitl':`
    - packages/mcp/src/devtools-server.ts contains `case 'focus_session':`
    - packages/mcp/src/devtools-server.ts contains `type: 'command_event'`
    - packages/mcp/src/devtools-server.ts contains `type: 'hitl_state'`
    - packages/mcp/src/devtools-server.ts contains `type: 'command_backfill'`
    - packages/mcp/src/devtools-server.ts still contains `type: 'sessions_update'`
    - packages/mcp/src/devtools-server.ts still contains `type: 'snapshot_update'`
    - packages/mcp/src/devtools-server.ts still contains `case 'subscribe':`
    - packages/mcp/src/devtools-server.ts still contains `case 'highlight':`
    - `pnpm --filter @agrune/mcp typecheck` exits 0
    - `pnpm --filter @agrune/mcp build` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Wire CommandBroker, HitlController, and focus_session into agrune-mcp bin</name>
  <files>packages/mcp/bin/agrune-mcp.ts</files>
  <read_first>
    - packages/mcp/bin/agrune-mcp.ts (현재 77줄 전체)
    - packages/mcp/src/index.ts (task 3 결과: createMcpServer return shape)
    - packages/mcp/src/devtools-server.ts (task 4 결과: options shape)
  </read_first>
  <action>
    `packages/mcp/bin/agrune-mcp.ts`의 단일 변경: `createMcpServer(driver)`의 구조분해에서 `commandBroker`, `hitl`을 꺼내고, `startDevtoolsServer(driver, port)` 호출을 options와 함께 하도록 바꾼다.

    1. `const { server } = createMcpServer(driver)`를 다음으로 교체한다(20줄):
    ```ts
    const { server, commandBroker, hitl } = createMcpServer(driver)
    ```

    2. `const devtoolsPort = await startDevtoolsServer(driver, devtoolsPortArg ? Number(devtoolsPortArg) : 47654)`를 다음으로 교체한다(36줄):
    ```ts
    const devtoolsPort = await startDevtoolsServer(
      driver,
      devtoolsPortArg ? Number(devtoolsPortArg) : 47654,
      {
        commandBroker,
        hitl,
        onFocusSession: async (tabId: number) => {
          try {
            await driver.focusSession(tabId)
          } catch {
            // devtools-initiated focus is best-effort; ignore errors here
            // since the UI will re-render session list on next sessions_update.
          }
        },
      },
    )
    ```

    다른 줄은 모두 유지.
  </action>
  <acceptance_criteria>
    - packages/mcp/bin/agrune-mcp.ts contains `const { server, commandBroker, hitl } = createMcpServer(driver)`
    - packages/mcp/bin/agrune-mcp.ts contains `onFocusSession: async (tabId: number)`
    - packages/mcp/bin/agrune-mcp.ts contains `await driver.focusSession(tabId)`
    - `pnpm --filter @agrune/mcp build` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Add unit tests for CommandBroker</name>
  <files>packages/mcp/tests/command-broker.spec.ts</files>
  <read_first>
    - packages/mcp/src/command-broker.ts (task 1 결과)
    - packages/mcp/tests/tools.spec.ts (vitest 패턴 확인용, import 스타일)
  </read_first>
  <action>
    새 파일 `packages/mcp/tests/command-broker.spec.ts`:

    ```ts
    import { describe, it, expect } from 'vitest'
    import { CommandBroker, type CommandEvent } from '../src/command-broker.js'

    describe('CommandBroker', () => {
      it('nextId returns monotonically unique strings', () => {
        const broker = new CommandBroker()
        const a = broker.nextId()
        const b = broker.nextId()
        expect(a).not.toBe(b)
        expect(a.startsWith('cmd-')).toBe(true)
      })

      it('emits events to active subscribers', () => {
        const broker = new CommandBroker()
        const received: CommandEvent[] = []
        broker.subscribe((e) => received.push(e))
        broker.emit({ id: '1', ts: 0, sessionId: null, tool: 'agrune_act', phase: 'start' })
        expect(received).toHaveLength(1)
        expect(received[0].tool).toBe('agrune_act')
      })

      it('unsubscribe removes listener', () => {
        const broker = new CommandBroker()
        let count = 0
        const off = broker.subscribe(() => { count += 1 })
        broker.emit({ id: '1', ts: 0, sessionId: null, tool: 'x', phase: 'start' })
        off()
        broker.emit({ id: '2', ts: 0, sessionId: null, tool: 'x', phase: 'start' })
        expect(count).toBe(1)
      })

      it('retains at most bufferSize events (FIFO)', () => {
        const broker = new CommandBroker(3)
        for (let i = 0; i < 5; i += 1) {
          broker.emit({ id: `${i}`, ts: i, sessionId: null, tool: 't', phase: 'start' })
        }
        const buffered = broker.getBuffered()
        expect(buffered).toHaveLength(3)
        expect(buffered[0].id).toBe('2')
        expect(buffered[2].id).toBe('4')
      })

      it('isolates listener errors from other listeners', () => {
        const broker = new CommandBroker()
        let secondCalled = false
        broker.subscribe(() => { throw new Error('boom') })
        broker.subscribe(() => { secondCalled = true })
        broker.emit({ id: '1', ts: 0, sessionId: null, tool: 't', phase: 'start' })
        expect(secondCalled).toBe(true)
      })

      it('clear empties the buffer', () => {
        const broker = new CommandBroker()
        broker.emit({ id: '1', ts: 0, sessionId: null, tool: 't', phase: 'start' })
        broker.clear()
        expect(broker.getBuffered()).toHaveLength(0)
      })
    })
    ```
  </action>
  <acceptance_criteria>
    - packages/mcp/tests/command-broker.spec.ts exists
    - packages/mcp/tests/command-broker.spec.ts contains `describe('CommandBroker'`
    - packages/mcp/tests/command-broker.spec.ts contains `new CommandBroker(3)`
    - `pnpm --filter @agrune/mcp test -- command-broker` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Add unit tests for HitlController</name>
  <files>packages/mcp/tests/hitl-controller.spec.ts</files>
  <read_first>
    - packages/mcp/src/hitl-controller.ts (task 2 결과)
  </read_first>
  <action>
    새 파일 `packages/mcp/tests/hitl-controller.spec.ts`:

    ```ts
    import { describe, it, expect } from 'vitest'
    import { HitlController, HitlSkipError } from '../src/hitl-controller.js'

    describe('HitlController', () => {
      it('starts unpaused and awaitGate resolves immediately', async () => {
        const hitl = new HitlController()
        expect(hitl.getState().paused).toBe(false)
        await expect(hitl.awaitGate('agrune_act')).resolves.toBeUndefined()
      })

      it('pause then resume emits state changes', async () => {
        const hitl = new HitlController()
        const states: boolean[] = []
        hitl.onChange((s) => states.push(s.paused))
        hitl.pause()
        hitl.resume()
        expect(states).toEqual([true, false])
      })

      it('blocks awaitGate while paused, unblocks on resume', async () => {
        const hitl = new HitlController()
        hitl.pause()
        let settled = false
        const p = hitl.awaitGate('agrune_fill').then(() => { settled = true })
        await Promise.resolve()
        expect(settled).toBe(false)
        hitl.resume()
        await p
        expect(settled).toBe(true)
      })

      it('sets pendingTool while a call is waiting', async () => {
        const hitl = new HitlController()
        hitl.pause()
        const p = hitl.awaitGate('agrune_click')
        await Promise.resolve()
        expect(hitl.getState().pendingTool).toBe('agrune_click')
        hitl.resume()
        await p
      })

      it('skip rejects the current call with HitlSkipError and remains paused', async () => {
        const hitl = new HitlController()
        hitl.pause()
        const p = hitl.awaitGate('agrune_fill')
        await Promise.resolve()
        hitl.skip()
        await expect(p).rejects.toBeInstanceOf(HitlSkipError)
        expect(hitl.getState().paused).toBe(true)
      })

      it('step lets exactly one call through then re-pauses', async () => {
        const hitl = new HitlController()
        hitl.pause()
        const p1 = hitl.awaitGate('agrune_act')
        await Promise.resolve()
        hitl.step()
        await p1
        // second call should still be blocked
        let second = false
        const p2 = hitl.awaitGate('agrune_act').then(() => { second = true })
        await Promise.resolve()
        expect(second).toBe(false)
        hitl.resume()
        await p2
      })

      it('HitlSkipError carries code HITL_SKIPPED', async () => {
        const hitl = new HitlController()
        hitl.pause()
        const p = hitl.awaitGate('agrune_drag')
        await Promise.resolve()
        hitl.skip()
        try {
          await p
        } catch (err) {
          expect(err).toBeInstanceOf(HitlSkipError)
          expect((err as HitlSkipError).code).toBe('HITL_SKIPPED')
        }
      })
    })
    ```
  </action>
  <acceptance_criteria>
    - packages/mcp/tests/hitl-controller.spec.ts exists
    - packages/mcp/tests/hitl-controller.spec.ts contains `describe('HitlController'`
    - packages/mcp/tests/hitl-controller.spec.ts contains `HitlSkipError`
    - packages/mcp/tests/hitl-controller.spec.ts contains `hitl.step()`
    - packages/mcp/tests/hitl-controller.spec.ts contains `hitl.skip()`
    - `pnpm --filter @agrune/mcp test -- hitl-controller` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Extend devtools-server test suite with command_event / hitl_state / focus_session coverage</name>
  <files>packages/mcp/tests/devtools-server.spec.ts</files>
  <read_first>
    - packages/mcp/tests/devtools-server.spec.ts (현재 273줄 전체)
    - packages/mcp/src/devtools-server.ts (task 4 결과)
    - packages/mcp/src/command-broker.ts (task 1)
    - packages/mcp/src/hitl-controller.ts (task 2)
  </read_first>
  <action>
    `packages/mcp/tests/devtools-server.spec.ts`를 다음과 같이 확장한다. 기존 describe 블록 전체는 **변경하지 않는다**.

    1. 파일 상단 `import type { PageSnapshot, Session } from '@agrune/core'` 바로 아래에 다음 import를 추가한다:
    ```ts
    import { CommandBroker } from '../src/command-broker.js'
    import { HitlController } from '../src/hitl-controller.js'
    ```

    2. 기존 `describe('devtools-server', ...)` 블록 바로 다음에(파일 마지막, 272줄 `})` 뒤) 새 describe 블록을 append한다. 이 블록은 **별도의 server 인스턴스**를 띄워 기존 테스트를 건드리지 않는다. 먼저 `import WebSocket from 'ws'` 가 이미 import되어 있고 helper 함수들도 모듈-scope에 있으므로 그대로 재사용할 수 있다.

    ```ts
    describe('devtools-server — phase 8 extensions', () => {
      let port2: number
      let driver2: ReturnType<typeof createMockDriver>
      let broker: CommandBroker
      let hitl: HitlController
      let start2: typeof import('../src/devtools-server.js').startDevtoolsServer
      let stop2: typeof import('../src/devtools-server.js').stopDevtoolsServer
      let createdTestDist2 = false
      let focusCalls: number[]

      beforeAll(async () => {
        // Stop the prior singleton server (shared module-level state) before starting a fresh one.
        const mod = await import('../src/devtools-server.js')
        await mod.stopDevtoolsServer().catch(() => {})
        start2 = mod.startDevtoolsServer
        stop2 = mod.stopDevtoolsServer

        try {
          mkdirSync(DEVTOOLS_DIST, { recursive: true })
          writeFileSync(
            join(DEVTOOLS_DIST, 'index.html'),
            '<!DOCTYPE html><html><head><title>Agrune DevTools</title></head><body></body></html>',
          )
          createdTestDist2 = true
        } catch {
          // already exists
        }

        driver2 = createMockDriver()
        broker = new CommandBroker()
        hitl = new HitlController()
        focusCalls = []
        port2 = await start2(driver2, 0, {
          commandBroker: broker,
          hitl,
          onFocusSession: (tabId) => { focusCalls.push(tabId) },
        })
      })

      afterAll(async () => {
        await stop2()
        if (createdTestDist2) {
          try {
            rmSync(DEVTOOLS_DIST, { recursive: true, force: true })
          } catch { /* ignore */ }
        }
      })

      it('sends hitl_state on connect', async () => {
        const { ws, waitForMessage } = await connectWs(port2)
        // First message is sessions_update
        await waitForMessage()
        // Next should be hitl_state
        const msg = await waitForMessage() as { type: string; data: { paused: boolean } }
        expect(msg.type).toBe('hitl_state')
        expect(msg.data.paused).toBe(false)
        ws.close()
      })

      it('broadcasts command_event when broker emits', async () => {
        const { ws, waitForMessage } = await connectWs(port2)
        await waitForMessage() // sessions_update
        await waitForMessage() // hitl_state
        broker.emit({
          id: 'test-1', ts: Date.now(), sessionId: 1,
          tool: 'agrune_act', phase: 'start',
        })
        const msg = await waitForMessage() as { type: string; data: { tool: string; phase: string } }
        expect(msg.type).toBe('command_event')
        expect(msg.data.tool).toBe('agrune_act')
        expect(msg.data.phase).toBe('start')
        ws.close()
      })

      it('broadcasts hitl_state on pause/resume', async () => {
        const { ws, waitForMessage } = await connectWs(port2)
        await waitForMessage() // sessions_update
        await waitForMessage() // initial hitl_state
        hitl.pause()
        const paused = await waitForMessage() as { type: string; data: { paused: boolean } }
        expect(paused.type).toBe('hitl_state')
        expect(paused.data.paused).toBe(true)
        hitl.resume()
        const resumed = await waitForMessage() as { type: string; data: { paused: boolean } }
        expect(resumed.data.paused).toBe(false)
        ws.close()
      })

      it('accepts hitl pause action from client', async () => {
        const { ws, waitForMessage } = await connectWs(port2)
        await waitForMessage() // sessions_update
        await waitForMessage() // hitl_state
        ws.send(JSON.stringify({ type: 'hitl', action: 'pause' }))
        const msg = await waitForMessage() as { type: string; data: { paused: boolean } }
        expect(msg.type).toBe('hitl_state')
        expect(msg.data.paused).toBe(true)
        hitl.resume() // cleanup
        ws.close()
      })

      it('routes focus_session to onFocusSession callback', async () => {
        const { ws, waitForMessage } = await connectWs(port2)
        await waitForMessage()
        await waitForMessage()
        const before = focusCalls.length
        ws.send(JSON.stringify({ type: 'focus_session', sessionId: 7 }))
        // give event loop a tick to route
        await new Promise(r => setTimeout(r, 30))
        expect(focusCalls.length).toBe(before + 1)
        expect(focusCalls[focusCalls.length - 1]).toBe(7)
        ws.close()
      })

      it('sends command_backfill on connect when broker has buffered events', async () => {
        broker.emit({ id: 'bf-1', ts: 1, sessionId: null, tool: 'agrune_wait', phase: 'start' })
        const { ws, waitForMessage } = await connectWs(port2)
        let seen: string[] = []
        for (let i = 0; i < 5; i += 1) {
          try {
            const msg = await waitForMessage(300) as { type: string }
            seen.push(msg.type)
            if (msg.type === 'command_backfill') break
          } catch { break }
        }
        expect(seen).toContain('command_backfill')
        ws.close()
      })
    })
    ```
  </action>
  <acceptance_criteria>
    - packages/mcp/tests/devtools-server.spec.ts contains `describe('devtools-server — phase 8 extensions'`
    - packages/mcp/tests/devtools-server.spec.ts contains `type: 'command_event'`
    - packages/mcp/tests/devtools-server.spec.ts contains `type: 'hitl_state'`
    - packages/mcp/tests/devtools-server.spec.ts contains `type: 'focus_session'`
    - packages/mcp/tests/devtools-server.spec.ts contains `type: 'command_backfill'`
    - `pnpm --filter @agrune/mcp test -- devtools-server` exits 0
  </acceptance_criteria>
</task>

## Verification
- `pnpm install` exits 0 (syncs `ws` into `packages/mcp/node_modules`).
- `pnpm --filter @agrune/mcp typecheck` exits 0.
- `pnpm --filter @agrune/mcp build` exits 0.
- `pnpm --filter @agrune/mcp test` exits 0 (기존 + 새 3개 spec 모두 green).
- `grep -n "command_event\|hitl_state\|focus_session\|command_backfill" packages/mcp/src/devtools-server.ts`가 각 최소 1 매치.
- 기존 `sessions_update`·`snapshot_update`·`subscribe`·`highlight` 매치가 여전히 존재한다 (backward compatibility).

## Success Criteria
- [ ] `CommandBroker`가 생기고 `CommandEvent`를 발행/구독/버퍼링한다
- [ ] `HitlController`가 pause/resume/step/skip 상태 머신을 제공한다
- [ ] `createMcpServer`가 handleToolCall 래핑을 통해 gating + 이벤트 emit을 수행한다
- [ ] `startDevtoolsServer`가 `command_event`·`hitl_state`·`command_backfill`을 브로드캐스트하고 `hitl`·`focus_session` 인바운드를 수락한다
- [ ] `agrune-mcp.ts`가 broker/hitl/focus callback을 서버에 전달한다
- [ ] 기존 WebSocket 메시지 3종은 타입·의미 모두 변경되지 않는다
- [ ] CommandBroker·HitlController·devtools-server 확장 모두 단위 테스트 green
