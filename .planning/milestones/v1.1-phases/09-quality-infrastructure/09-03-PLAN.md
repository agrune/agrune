---
phase: 09-quality-infrastructure
plan: "03"
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/mcp/tests/devtools-server.spec.ts
  - packages/mcp/tests/devtools-server-extensions.spec.ts
  - packages/browser/tsconfig.json
  - packages/devtools/src/panel.css
autonomous: true
user_setup: []
requirements_addressed:
  - QUAL-01
must_haves:
  truths:
    - "`pnpm --filter @agrune/mcp test`가 전부 통과 (기존 4건 race 제거)"
    - "`pnpm --filter @agrune/browser typecheck`가 `noUnusedLocals`/`noUnusedParameters`와 함께 exit 0"
    - "panel.css 토큰 정리로 Phase 8 UI-review 잔여 지적 해소 (font-weight, 그리고 toolbar/target-row padding이 4의 배수)"
  artifacts:
    - packages/mcp/tests/devtools-server-extensions.spec.ts (신규)
  key_links:
    - QUAL-01 E2E가 먼저 돌아야 기존 MCP unit 경로가 안정 baseline 위에 돌아감
---

# Phase 9 Plan 03: Tech debt cleanup

## Objective
- **What:** 3가지 기술부채를 한꺼번에 닫는다: (1) `devtools-server.spec.ts`의 race 4건, (2) `@agrune/browser`의 unused 변수/파라미터, (3) `panel.css`의 UI 규약 위반 잔여 토큰.
- **Why:** 다음 phase(10 Docs & Distribution) 시작 전 품질 baseline을 정상화한다. CI를 추가해도 tech debt가 있으면 빨간불이 고정된다.
- **Output:** 모든 기존 테스트 녹색, 새 lint 경로 녹색, UI-review 22/24 → 24/24 (재리뷰 시).

## Context
@.planning/PROJECT.md
@.planning/phases/09-quality-infrastructure/09-CONTEXT.md
@.planning/phases/09-quality-infrastructure/09-RESEARCH.md
@.planning/phases/08-devtools-webapp/08-UI-REVIEW.md
@packages/mcp/tests/devtools-server.spec.ts
@packages/browser/tsconfig.json
@packages/devtools/src/panel.css

## Tasks

<task type="execute">
  <name>Split devtools-server spec into two files</name>
  <files>
    packages/mcp/tests/devtools-server.spec.ts
    packages/mcp/tests/devtools-server-extensions.spec.ts
  </files>
  <read_first>
    - packages/mcp/tests/devtools-server.spec.ts
    - .planning/phases/08-devtools-webapp/08-01-SUMMARY.md
  </read_first>
  <action>
Goal: the original `describe('devtools-server', ...)` block must live in `devtools-server.spec.ts` alone; the `describe('devtools-server — phase 8 extensions', ...)` block moves to a new file `devtools-server-extensions.spec.ts`. Each file gets its own `beforeAll/afterAll` so vitest runs them in a separate process (vitest default: each spec file is a worker-level isolate).

Additionally, tighten `connectWs` and the first spec's expectations so that inserting extra lifecycle messages (`hitl_state`, `command_backfill`) does not break ordering checks.

Concrete steps:

1. Create new file `packages/mcp/tests/devtools-server-extensions.spec.ts` that contains:
   - The helper `connectWs` (copy of the one in the original file).
   - The `createMockDriver` helper (copy).
   - The `DEVTOOLS_DIST` constant (copy).
   - The `nextOfType` helper (copy — already present in the original).
   - Just the `describe('devtools-server — phase 8 extensions', ...)` block from the original, exactly as-is.
   - At the top of `beforeAll`, call `await mod.stopDevtoolsServer().catch(() => {})` (already present).

2. In the old `packages/mcp/tests/devtools-server.spec.ts`:
   - DELETE the `describe('devtools-server — phase 8 extensions', ...)` block (lines 276–410 per current file).
   - Rewrite the top `describe('devtools-server', ...)` so that every test that previously depended on "first message is sessions_update, second is snapshot_update" uses a new helper `nextOfType` instead. Copy `nextOfType` from the extensions describe block into the top of the file.
   - For each test that does `await waitForMessage()` positionally, replace with `await nextOfType(waitForMessage, 'sessions_update')` or `'snapshot_update'` accordingly.

Example rewrite of the `'sends snapshot_update on subscribe when snapshot is available'` test:

```ts
it('sends snapshot_update on subscribe when snapshot is available', async () => {
  const { ws, waitForMessage } = await connectWs(port)
  ws.send(JSON.stringify({ type: 'subscribe', tabId: 1 }))

  // Any ordering is acceptable — the server is allowed to emit sessions_update,
  // snapshot_update, hitl_state, command_backfill in any order. Grab the one we care about.
  const snapshotMsg = await nextOfType<{ tabId: number; snapshot: PageSnapshot }>(
    waitForMessage, 'snapshot_update'
  )
  expect(snapshotMsg.data.tabId).toBe(1)
  expect(snapshotMsg.data.snapshot.url).toBe('https://example.com')

  ws.close()
})
```

Apply the analogous change to:
- `'sends sessions_update on subscribe'`
- `'broadcasts snapshot_update when driver emits'`
- `'broadcasts sessions_update when a session opens'`
- `'does not receive snapshot_update for unsubscribed tabs'` (this one uses a timeout path — keep timeout approach but scope it to `snapshot_update` type only: drain `sessions_update`/`hitl_state` with `nextOfType` briefly, then assert no `snapshot_update` arrives within 300ms)

The `'serves devtools HTML at /devtools'` and `'accepts WebSocket connection at /devtools/ws'` tests do not use `waitForMessage` positionally; leave them as-is.

Expose `nextOfType` by copying its definition above `describe(...)` in both files — do not try to share it; each file is an isolate.

Write all of this as a single coordinated edit. When done, `packages/mcp/tests/devtools-server.spec.ts` should end at the close of the first `describe` block. The new extensions file should be complete and runnable on its own.
  </action>
  <acceptance_criteria>
    - File `packages/mcp/tests/devtools-server-extensions.spec.ts` exists
    - `packages/mcp/tests/devtools-server.spec.ts` does NOT contain `'phase 8 extensions'`
    - `packages/mcp/tests/devtools-server.spec.ts` contains `nextOfType`
    - `packages/mcp/tests/devtools-server-extensions.spec.ts` contains `'phase 8 extensions'`
    - `pnpm --filter @agrune/mcp test` exits 0
    - Test count in `pnpm --filter @agrune/mcp test` output is at least 45 and 0 failures
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Enable strict unused checks in @agrune/browser</name>
  <files>packages/browser/tsconfig.json</files>
  <read_first>
    - packages/browser/tsconfig.json
  </read_first>
  <action>
Replace the entire file with:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "noUnusedLocals": true,
    "noUnusedParameters": true
  },
  "include": ["src"],
  "exclude": ["dist", "tests", "node_modules"]
}
```

Then run `pnpm --filter @agrune/browser typecheck`. If it reports unused locals/parameters, fix each by:
- Prefixing unused function parameters with `_` (TypeScript treats `_`-prefixed params as intentionally unused).
- Deleting unused local declarations.
- Replacing genuinely-unused but API-contracted parameters with `_` rename.

Do NOT suppress violations with `@ts-expect-error`. Do NOT disable the new flags. Do NOT introduce new exports to placate the checker.

If the original tsconfig did not extend the base config, the replacement here now does. Make sure `src` still has correct `rootDir` semantics and nothing else relied on the old non-extending shape (verify by running build after typecheck: `pnpm --filter @agrune/browser build`).
  </action>
  <acceptance_criteria>
    - `packages/browser/tsconfig.json` contains `"noUnusedLocals": true`
    - `packages/browser/tsconfig.json` contains `"noUnusedParameters": true`
    - `pnpm --filter @agrune/browser typecheck` exits 0
    - `pnpm --filter @agrune/browser build` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Resolve panel.css UI-review deductions</name>
  <files>packages/devtools/src/panel.css</files>
  <read_first>
    - packages/devtools/src/panel.css
    - .planning/phases/08-devtools-webapp/08-UI-REVIEW.md
  </read_first>
  <action>
Apply three surgical edits, preserving everything else.

Edit 1 — `#toolbar` padding (was `6px 10px`, must be multiples of 4):
Replace line `  padding: 6px 10px;` under `#toolbar {` with `  padding: 8px 12px;`.

Edit 2 — `.group-header` font-weight (was `600`, collapse to 2-weight contract):
Replace line `  font-weight: 600;` under `.group-header {` with `  font-weight: 700;`.

Edit 3 — `.target-row` padding (was `4px 10px 4px 20px`, use 4-multiple grid):
Replace line `  padding: 4px 10px 4px 20px;` under `.target-row {` with `  padding: 8px 12px 8px 20px;`.

Do not touch `.target-row.selected { padding-left: 18px; }` — the visual effect (border-left: 2px then 18px padding) deliberately aligns to the `.target-row`'s 20px left edge and must be kept. Update to `padding-left: 18px` as before — no change needed.

Verify by `pnpm --filter @agrune/devtools build` — should still produce `dist/index.html` + assets.
  </action>
  <acceptance_criteria>
    - `packages/devtools/src/panel.css` contains `padding: 8px 12px;` for `#toolbar`
    - `packages/devtools/src/panel.css` contains `font-weight: 700;` under `.group-header`
    - `packages/devtools/src/panel.css` contains `padding: 8px 12px 8px 20px;` under `.target-row`
    - `packages/devtools/src/panel.css` does NOT contain `font-weight: 600`
    - `packages/devtools/src/panel.css` does NOT contain `padding: 6px 10px`
    - `packages/devtools/src/panel.css` does NOT contain `padding: 4px 10px 4px 20px`
    - `pnpm --filter @agrune/devtools build` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Final smoke</name>
  <files></files>
  <read_first></read_first>
  <action>
Run, in order:
1. `pnpm build` — all packages clean.
2. `pnpm typecheck` — all packages clean (strict flags on browser respected).
3. `pnpm test` — all unit tests green.
4. `pnpm lint:annotations` — exits 0.
5. `PLAYWRIGHT_SKIP_E2E=1 pnpm test:e2e` (optional; only to confirm harness files import cleanly — tests will skip).

If any step fails, diagnose and fix before marking the task complete.
  </action>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm test` exits 0
    - `pnpm lint:annotations` exits 0
  </acceptance_criteria>
</task>

## Verification
- MCP test count: ≥ 45, failures = 0.
- Browser typecheck green with strict flags.
- Devtools panel.css has zero UI-review violation strings.

## Rollback
- Revert the two MCP spec files to single-file form.
- Revert `packages/browser/tsconfig.json`.
- Revert `packages/devtools/src/panel.css`.
