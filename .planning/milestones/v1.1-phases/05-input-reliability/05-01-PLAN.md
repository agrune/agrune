---
phase: 05-input-reliability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/index.ts
  - packages/runtime/src/runtime/event-sequences.ts
  - packages/runtime/src/runtime/dom-utils.ts
autonomous: true
user_setup: []
requirements_addressed:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - INPUT-04
must_haves:
  truths:
    - eventSequences가 CDP Input 도메인 기반의 text input 프리미티브를 제공한다
    - FillCommandRequest가 clear·strategy 옵션을 포함한다
    - contenteditable 요소를 fill 대상으로 인식할 수 있다
  artifacts:
    - packages/runtime/src/runtime/event-sequences.ts (extended)
    - packages/core/src/index.ts (FillCommandRequest updated)
    - packages/runtime/src/runtime/dom-utils.ts (canReceiveTextInput helper)
  key_links:
    - handleFill이 이 프리미티브를 호출하도록 Plan 02에서 연결
---

# Phase 5 Plan 01: CDP Input Primitives & Core Types

## Objective
- **What:** `eventSequences`에 `Input.insertText`/`Input.dispatchKeyEvent` 기반의 text input 프리미티브를 추가하고, `FillCommandRequest` 타입에 `clear`/`strategy` 옵션을, `dom-utils`에 contenteditable 판별 헬퍼를 추가한다.
- **Why:** handleFill 재작성(Plan 02)이 쓸 low-level building block을 한 번에 확정해 Plan 02가 순수하게 "조합"만 하도록 만든다.
- **Output:** 새 이벤트 프리미티브 + 확장된 타입 + helper — 단위 테스트로 검증.

## Context
@.planning/PROJECT.md
@.planning/phases/05-input-reliability/05-CONTEXT.md
@.planning/phases/05-input-reliability/05-RESEARCH.md
@packages/runtime/src/runtime/event-sequences.ts
@packages/runtime/src/runtime/cdp-client.ts
@packages/runtime/src/runtime/dom-utils.ts
@packages/core/src/index.ts

## Tasks

<task type="execute">
  <name>Extend FillCommandRequest with clear/strategy fields</name>
  <files>packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/index.ts (FillCommandRequest 현 정의는 lines 134-139)
    - .planning/phases/05-input-reliability/05-RESEARCH.md §5
  </read_first>
  <action>
    `FillCommandRequest` 인터페이스(lines 134-139)에 두 개 optional 필드를 추가:
    ```ts
    export type FillStrategy = 'insert' | 'keystroke' | 'auto'

    export interface FillCommandRequest extends BaseCommandRequest {
      kind: 'fill'
      targetId: string
      value: string
      clear?: boolean
      strategy?: FillStrategy
      expectedVersion?: number
    }
    ```
    `FillStrategy` 타입 export를 같은 파일 상단 import 블록 아래, 다른 union 타입들(ActionKind, CommandKind)과 같은 섹션에 추가.
    다른 파일 import 영향 없음(필드가 optional).
  </action>
  <acceptance_criteria>
    - packages/core/src/index.ts contains `export type FillStrategy = 'insert' | 'keystroke' | 'auto'`
    - packages/core/src/index.ts contains `clear?: boolean` inside the FillCommandRequest interface body
    - packages/core/src/index.ts contains `strategy?: FillStrategy` inside the FillCommandRequest interface body
    - `pnpm --filter @agrune/core build` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Add contenteditable helper to dom-utils</name>
  <files>packages/runtime/src/runtime/dom-utils.ts</files>
  <read_first>
    - packages/runtime/src/runtime/dom-utils.ts (isFillableElement은 lines 335-343)
  </read_first>
  <action>
    `isFillableElement` 바로 아래에 새 helper 두 개 추가:
    ```ts
    export function isContentEditableElement(element: Element): element is HTMLElement {
      return element instanceof HTMLElement && element.isContentEditable === true
    }

    export function canReceiveTextInput(
      element: Element,
    ): element is HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement | HTMLElement {
      return isFillableElement(element) || isContentEditableElement(element)
    }
    ```
    기존 `isFillableElement` 시그니처/동작은 그대로 둔다 (snapshot 쪽에서 select/input/textarea만 value 수집하기 때문).
  </action>
  <acceptance_criteria>
    - packages/runtime/src/runtime/dom-utils.ts contains `export function isContentEditableElement`
    - packages/runtime/src/runtime/dom-utils.ts contains `export function canReceiveTextInput`
    - packages/runtime/src/runtime/dom-utils.ts still contains original `export function isFillableElement(`
    - `pnpm --filter @agrune/runtime build` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Extend EventSequences with text-input primitives</name>
  <files>packages/runtime/src/runtime/event-sequences.ts</files>
  <read_first>
    - packages/runtime/src/runtime/event-sequences.ts (전체, 현 96 lines)
    - packages/runtime/src/runtime/cdp-client.ts (sendCdpEvent 시그니처 reference)
    - .planning/phases/05-input-reliability/05-RESEARCH.md §2, §4
  </read_first>
  <action>
    `EventSequences` interface에 네 개 메서드 추가(파일 상단 interface block):
    ```ts
    insertText(text: string): Promise<void>
    typeText(text: string, options?: { delayMs?: number }): Promise<void>
    pressKey(key: string, options?: { modifiers?: number; code?: string; text?: string }): Promise<void>
    selectAllAndDelete(): Promise<void>
    ```
    `createEventSequences` 구현(return object) 안에 구현 추가:
    ```ts
    async insertText(text) {
      await send('Input.insertText', { text })
    },
    async typeText(text, options) {
      const delay = options?.delayMs ?? 0
      for (const ch of Array.from(text)) {
        const keyInfo = keyFromChar(ch)
        await send('Input.dispatchKeyEvent', {
          type: 'keyDown',
          key: keyInfo.key,
          code: keyInfo.code,
          text: ch,
          unmodifiedText: ch,
          ...(keyInfo.windowsVirtualKeyCode ? { windowsVirtualKeyCode: keyInfo.windowsVirtualKeyCode } : {}),
        })
        await send('Input.dispatchKeyEvent', {
          type: 'keyUp',
          key: keyInfo.key,
          code: keyInfo.code,
          ...(keyInfo.windowsVirtualKeyCode ? { windowsVirtualKeyCode: keyInfo.windowsVirtualKeyCode } : {}),
        })
        if (delay > 0) await sleep(delay)
      }
    },
    async pressKey(key, options) {
      const modifiers = options?.modifiers ?? 0
      const code = options?.code ?? key
      const text = options?.text
      await send('Input.dispatchKeyEvent', {
        type: 'keyDown',
        key,
        code,
        modifiers,
        ...(text ? { text, unmodifiedText: text } : {}),
      })
      await send('Input.dispatchKeyEvent', {
        type: 'keyUp',
        key,
        code,
        modifiers,
      })
    },
    async selectAllAndDelete() {
      // Use Chromium CDP "selectAll" command hint — works on both macOS/Linux/Win at the CDP layer.
      await send('Input.dispatchKeyEvent', {
        type: 'rawKeyDown',
        key: 'a',
        code: 'KeyA',
        modifiers: 4, // Meta — macOS
        commands: ['selectAll'],
      })
      await send('Input.dispatchKeyEvent', {
        type: 'keyUp',
        key: 'a',
        code: 'KeyA',
        modifiers: 4,
      })
      await send('Input.dispatchKeyEvent', {
        type: 'keyDown',
        key: 'Delete',
        code: 'Delete',
      })
      await send('Input.dispatchKeyEvent', {
        type: 'keyUp',
        key: 'Delete',
        code: 'Delete',
      })
    },
    ```
    `keyFromChar(ch)` helper를 같은 파일 top-level에 정의 — printable ASCII·한글 문자를 `{ key, code, windowsVirtualKeyCode? }`로 매핑. 단순 구현:
    ```ts
    function keyFromChar(ch: string): { key: string; code: string; windowsVirtualKeyCode?: number } {
      if (ch.length === 1 && /[a-zA-Z]/.test(ch)) {
        return { key: ch, code: `Key${ch.toUpperCase()}`, windowsVirtualKeyCode: ch.toUpperCase().charCodeAt(0) }
      }
      if (ch.length === 1 && /[0-9]/.test(ch)) {
        return { key: ch, code: `Digit${ch}`, windowsVirtualKeyCode: ch.charCodeAt(0) }
      }
      if (ch === ' ') return { key: ' ', code: 'Space', windowsVirtualKeyCode: 32 }
      return { key: ch, code: '' }
    }
    ```
    기존 click/drag/wheel 메서드는 전부 그대로 유지. `sleep` 함수는 파일 상단에 이미 존재.
  </action>
  <acceptance_criteria>
    - packages/runtime/src/runtime/event-sequences.ts contains `insertText(text: string): Promise<void>` in the EventSequences interface
    - packages/runtime/src/runtime/event-sequences.ts contains `typeText(text: string, options?: { delayMs?: number }): Promise<void>` in the interface
    - packages/runtime/src/runtime/event-sequences.ts contains `pressKey(key: string,` in the interface
    - packages/runtime/src/runtime/event-sequences.ts contains `selectAllAndDelete(): Promise<void>` in the interface
    - packages/runtime/src/runtime/event-sequences.ts contains `Input.insertText` string literal
    - packages/runtime/src/runtime/event-sequences.ts contains `Input.dispatchKeyEvent` string literal
    - packages/runtime/src/runtime/event-sequences.ts contains `function keyFromChar(`
    - `pnpm --filter @agrune/runtime build` exits 0
    - Existing tests in packages/runtime (vitest) still pass
  </acceptance_criteria>
</task>

## Verification
- `pnpm build` 전체가 green.
- `pnpm --filter @agrune/runtime test`가 기존 테스트를 통과한다.
- `grep -n "Input.insertText\\|Input.dispatchKeyEvent" packages/runtime/src/runtime/event-sequences.ts`가 최소 5개의 매치를 반환한다.

## Success Criteria
- [ ] `FillCommandRequest`가 `clear?` `strategy?` 필드를 가진다
- [ ] `eventSequences`가 `insertText`/`typeText`/`pressKey`/`selectAllAndDelete`를 노출한다
- [ ] `canReceiveTextInput` helper가 contenteditable을 인식한다
- [ ] 기존 단위 테스트가 모두 green
