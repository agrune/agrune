---
phase: 10-docs-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agrune/README.md
  - agrune/AGENTS.md
  - agrune/docs/improvement-notes.md
autonomous: true
requirements:
  - DOCS-01
user_setup: []

must_haves:
  truths:
    - "agrune/README.md describes CDP-only architecture without recommending extension install/build"
    - "agrune/AGENTS.md reflects the CDP-only repo structure and drops the extension dist/reload workflow"
    - "agrune/docs/improvement-notes.md removes `extension + native host + backend daemon` language from design principles"
    - "README bilingual tone (ko + en where applicable) is preserved; headline/tagline remains Korean"
  artifacts:
    - path: "agrune/README.md"
      provides: "CDP-only product overview, package table, flag reference link"
      contains: "CDP"
      min_lines: 60
    - path: "agrune/AGENTS.md"
      provides: "Dev guide for CDP-only monorepo"
      contains: "pnpm build"
      min_lines: 10
    - path: "agrune/docs/improvement-notes.md"
      provides: "Design principles aligned to CDP-only runtime"
      contains: "CDP"
      min_lines: 30
  key_links:
    - from: "agrune/README.md"
      to: "agrune/docs/notes/"
      via: "archive pointer"
      pattern: "docs/notes"
---

<objective>
`agrune/README.md`, `agrune/AGENTS.md`, `agrune/docs/improvement-notes.md` 세 문서를 CDP-only 현실에 맞추어 재작성한다. extension/native messaging/backend daemon 표현을 제거하고, 현재의 `@agrune/mcp` + DevTools 웹앱 + Playwright E2E 분위기로 맞춘다.

Purpose: 외부 사용자가 README만 보고 CDP-only agrune를 정확히 이해할 수 있도록 core 서술을 정리.
Output: 세 파일 수정. CLI flag 표나 automation profile 섹션은 후속 plan (03/04) 에서 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-docs-distribution/10-CONTEXT.md
@agrune/README.md
@agrune/AGENTS.md
@agrune/docs/improvement-notes.md
@agrune/packages/mcp/bin/agrune-mcp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite agrune/README.md for CDP-only</name>
  <files>agrune/README.md</files>
  <read_first>agrune/README.md, agrune/packages/mcp/bin/agrune-mcp.ts, .planning/phases/10-docs-distribution/10-CONTEXT.md, agrune/packages/mcp/README.md</read_first>
  <action>
Rewrite `agrune/README.md` end-to-end while keeping the Korean headline/tagline and the bilingual tone. Apply the following concrete changes:

1. Tagline (line 3) stays Korean but drop any extension mention. Replace with: "AI 에이전트가 어노테이션된 웹 앱을 브라우저에서 직접 조작할 수 있게 해주는 CDP 기반 브라우저 자동화 도구입니다." (keep as-is if already matches).

2. "현재 구조" diagram MUST remove the `extension/` and the mention of `extension devtools panel`. New tree:

```text
packages/
  core/        shared types, contracts, annotation linter
  runtime/     page runtime, scanner, manifest builder
  browser/     CdpDriver, Chrome launcher, CDP transport
  mcp/         publish target: @agrune/mcp (MCP server + DevTools webapp)
  devtools/    DevTools webapp (command log, HITL toolbar, sessions, failures)

workflows/
  annotate/    harness-neutral annotation workflow source of truth
```

3. "배포 모델" section: keep `@agrune/mcp` as canonical; remove any line implying `extension` is part of the deploy model. Add: "DevTools 웹앱은 `@agrune/mcp` 프로세스가 기본 포트 47654에서 `http://localhost:47654/devtools` 로 제공합니다."

4. "패키지" table: remove the `@agrune/extension` row entirely. Update the `@agrune/browser` description to "`CdpDriver`, Chrome launcher, CDP 전송 계층 (extension/native messaging 경로 없음)". Update `@agrune/devtools` description to "MCP 서버가 내장 서빙하는 DevTools 웹앱 (command log, HITL toolbar, sessions panel, failure diagnostics)".

5. "실행 방식" section: ensure the text explicitly says "agrune은 CDP(Chrome DevTools Protocol) 모드만 지원합니다. extension mode는 v1.1에서 제거되었습니다." Keep the code block with `agrune`, `agrune --headless`, `agrune --attach ws://...`. Add one more line referencing `agrune --port 47655` and `agrune --no-devtools`. A detailed flag table will be added by plan 03 — for this plan just make sure the four flags are mentioned by name.

6. Replace "### 어노테이션" section: keep reference to `workflows/annotate/WORKFLOW.md`. Remove any implication that annotations are harness-specific; keep the existing Korean sentence about harness-neutral adapters.

7. "MCP 도구" table: ensure it lists the v1.1 tool set. Add `agrune_focus` (explicit multi-tab session focus) row between `agrune_sessions` and `agrune_snapshot`. Keep the existing rows; add one more row `agrune_lint` only if it exists — check `packages/mcp/src/mcp-tools.ts` or `tools.ts`. If it does not exist, do NOT add it.

8. "개발" section: keep Node 22 + pnpm 10.23.0. In "로컬 quick mode 확인" replace the command with `pnpm --filter @agrune/mcp start` or `node packages/mcp/dist/bin/agrune-mcp.js` (drop the `--mode cdp` flag which no longer exists). Add a new subsection "### 품질 검증" with:
   - `pnpm test` — unit tests
   - `pnpm test:e2e` — Playwright E2E harness (added in v1.1)
   - `pnpm lint:annotations` — annotation linter for data-agrune-* attributes

9. DELETE the entire "### 확장 프로그램 개발" subsection (lines 99-106 in current file) — it references `packages/extension` which is no longer part of the CDP-only deploy model.

10. "관련 디렉터리" list: keep `packages/mcp/README.md` and `workflows/annotate/WORKFLOW.md`. Add `docs/notes/` with a one-line note: "v1.0 시점의 아카이브 문서 (extension/native messaging 경로 포함) — 현재 아키텍처는 본 README 기준".

11. "개인정보 처리방침" section: remove the line "quick mode는 브라우저와 직접 CDP로 통신합니다" and "extension mode는 extension과 로컬 MCP/runtime 사이에서만 통신합니다" — replace with a single bullet: "agrune은 Chrome 본체와 로컬 CDP 연결만 사용하며, 외부 서버로 사용자 페이지 데이터를 전송하지 않습니다."

12. The new file MUST NOT contain the literal strings "extension mode", "native messaging", or "backend daemon" anywhere (verify with grep after write). It MAY contain `data-agrune-*` (annotation attribute) and `Chrome extension` only if referencing the BROWSER (not our product) — prefer to avoid the word "extension" entirely where possible.
  </action>
  <verify>
grep -nE "extension mode|native messaging|backend daemon" agrune/README.md   # must print nothing
grep -q "CDP" agrune/README.md
grep -q "47654" agrune/README.md
grep -q "DevTools" agrune/README.md
grep -q "pnpm test:e2e" agrune/README.md
  </verify>
  <acceptance_criteria>
    - `agrune/README.md` contains "CDP" and "47654" and "DevTools"
    - `agrune/README.md` contains "pnpm test:e2e" and "pnpm lint:annotations"
    - `agrune/README.md` does NOT contain the literal strings "extension mode", "native messaging", or "backend daemon"
    - `agrune/README.md` does NOT contain "packages/extension"
    - `agrune/README.md` does NOT contain "--mode cdp"
    - `agrune/README.md` Korean headline/tagline preserved (first non-heading line is Korean)
  </acceptance_criteria>
  <done>README rewritten CDP-only; no extension-mode legacy strings present.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite agrune/AGENTS.md for CDP-only dev workflow</name>
  <files>agrune/AGENTS.md</files>
  <read_first>agrune/AGENTS.md, agrune/README.md, agrune/packages/mcp/bin/agrune-mcp.ts, agrune/package.json</read_first>
  <action>
Replace `agrune/AGENTS.md` contents with a CDP-only dev guide. Keep the Korean tone. Concrete new contents (overwrite file):

```markdown
# agrune 개발 가이드

## 빌드 주의사항

- 브랜치 전환 후 반드시 `pnpm build` 실행할 것. `@agrune/mcp` 실행 바이너리는 빌드된 `packages/mcp/dist/` 기준으로 동작한다.
- CDP 경로만 사용하므로 Chrome 확장 설치/리로드 과정은 필요 없다. `agrune` 실행 시 Chrome이 자동 런치되거나 `--attach` 로 기존 인스턴스에 붙는다.
- DevTools 웹앱(`packages/devtools`)을 수정했으면 `pnpm --filter @agrune/devtools build` 후 `pnpm --filter @agrune/mcp build` 로 MCP 서버 번들을 재생성해야 `http://localhost:47654/devtools` 에 반영된다.

## 프로젝트 구조

- `agrune` — 메인 모노레포 (`@agrune/core`, `@agrune/runtime`, `@agrune/browser`, `@agrune/mcp`, `@agrune/devtools`)
- `workflows/annotate` — 하네스 중립 어노테이션 워크플로 원본
- 하네스별 플러그인/스킬 저장소 — core repo 밖에서 별도로 관리하는 어댑터 레이어

## 실행 모드

- 기본 `agrune` — Chrome 런치 + DevTools 웹앱 (port 47654)
- `agrune --headless` — UI 없이 실행
- `agrune --attach ws://...` — 이미 실행 중인 Chrome (--remote-debugging-port) 에 붙기
- `agrune --port 47655` — DevTools 웹앱 포트 오버라이드
- `agrune --no-devtools` — DevTools 웹앱 비활성화 (MCP stdio 만 사용)

## 테스트 시 체크리스트

1. `pnpm build` 실행 (monorepo 전체)
2. `pnpm test` — 유닛/통합 테스트
3. `pnpm test:e2e` — Playwright E2E 하네스 (v1.1 phase 9 에서 추가)
4. `pnpm lint:annotations` — `data-agrune-*` 어노테이션 linter (`@agrune/core`)
5. `agrune` 수동 실행 → Chrome 상단에 "Chrome is being controlled by automated test software" 디버깅 툴바가 떠야 정상
6. `http://localhost:47654/devtools` 를 열어 command log / HITL toolbar / sessions panel / failure diagnostics 가 동작하는지 확인

## 역사적 참고

- extension/native messaging 경로와 backend daemon 은 v1.0 시점 구현이며 v1.1 에서 제거되었다. 관련 설계 메모는 `docs/notes/` 아카이브 참고.
```

Ensure the file ends with a single trailing newline. Do NOT reintroduce the `feat/cdp-migration` branch note (it was merged long ago) or the old `~/.agrune/mcp-server/` daemon sync step (the MCP deploy automation path is no longer the primary flow).
  </action>
  <verify>
grep -nE "extension mode|native messaging|backend daemon" agrune/AGENTS.md   # must print nothing
grep -q "pnpm test:e2e" agrune/AGENTS.md
grep -q "47654" agrune/AGENTS.md
grep -q "--attach" agrune/AGENTS.md
grep -q "--no-devtools" agrune/AGENTS.md
  </verify>
  <acceptance_criteria>
    - `agrune/AGENTS.md` contains "pnpm test:e2e", "47654", "--attach", "--no-devtools", "--port"
    - `agrune/AGENTS.md` does NOT contain the literal strings "extension mode", "native messaging", "backend daemon"
    - `agrune/AGENTS.md` does NOT contain "feat/cdp-migration" or "synthetic MouseEvent"
    - `agrune/AGENTS.md` contains a "## 역사적 참고" or equivalent section that links archived notes
  </acceptance_criteria>
  <done>AGENTS.md rewritten for CDP-only flow.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite agrune/docs/improvement-notes.md to drop extension-mode language</name>
  <files>agrune/docs/improvement-notes.md</files>
  <read_first>agrune/docs/improvement-notes.md, agrune/README.md, agrune/packages/mcp/bin/agrune-mcp.ts</read_first>
  <action>
Edit `agrune/docs/improvement-notes.md` in place, preserving the overall document structure (제품 방향, 스냅샷 상태 변화, 모달/오버레이, 액션 타입, 페이지 콘텐츠 읽기). Concrete changes:

1. In "## 제품 방향" section, DELETE the bullet that says "브라우저 조작 능력은 `extension + native host + backend daemon + MCP` 조합으로 제공한다." and REPLACE it with: "브라우저 조작 능력은 `CdpDriver + @agrune/mcp` 조합으로 제공한다. Chrome 을 직접 CDP 로 제어하며 extension/native messaging 경로는 사용하지 않는다."

2. Keep every other bullet in that section unchanged (MCP as primary surface, QA automation long-term direction, agrune_read visible-content rule).

3. Keep "## 스냅샷 상태 변화", "## 모달/오버레이", "## 액션 타입", "## 페이지 콘텐츠 읽기" sections as-is — those are CDP-agnostic design rules and remain correct.

4. Add a new first section or header block at the top (after the `# 설계 원칙` H1) containing a one-line italic note: `_v1.1 기준. 2026-04 CDP-only 피봇 이후 갱신._`

5. After edits, run grep and confirm no line contains the three forbidden phrases.
  </action>
  <verify>
grep -nE "extension \+ native host|backend daemon|extension \+ native" agrune/docs/improvement-notes.md   # must print nothing
grep -q "CdpDriver" agrune/docs/improvement-notes.md
grep -q "v1.1" agrune/docs/improvement-notes.md
  </verify>
  <acceptance_criteria>
    - `agrune/docs/improvement-notes.md` contains "CdpDriver"
    - `agrune/docs/improvement-notes.md` contains "v1.1"
    - `agrune/docs/improvement-notes.md` does NOT contain "extension + native host" or "backend daemon"
    - File retains the five design-principle subheadings (제품 방향, 스냅샷 상태 변화, 모달/오버레이, 액션 타입, 페이지 콘텐츠 읽기)
  </acceptance_criteria>
  <done>improvement-notes reflect CDP-only architecture with a dated pivot note at the top.</done>
</task>

</tasks>

<verification>
- [ ] `grep -rnE "extension mode|native messaging|backend daemon" agrune/README.md agrune/AGENTS.md agrune/docs/improvement-notes.md` is empty
- [ ] `grep -q "47654" agrune/README.md && grep -q "pnpm test:e2e" agrune/README.md` succeeds
- [ ] `pnpm -w -r build` still succeeds (no code files touched but sanity check)
- [ ] README Korean tagline preserved
</verification>

<success_criteria>
- DOCS-01 partially satisfied (remaining docs/notes cleanup handled by plan 02)
- Three core doc surfaces (README, AGENTS, improvement-notes) CDP-only
- No extension-mode legacy strings in the modified files
- Bilingual tone retained
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-distribution/10-01-SUMMARY.md`
</output>
