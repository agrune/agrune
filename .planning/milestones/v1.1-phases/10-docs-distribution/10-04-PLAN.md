---
phase: 10-docs-distribution
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - agrune/README.md
autonomous: true
requirements:
  - DOCS-04
user_setup: []

must_haves:
  truths:
    - "agrune/README.md describes a copy-and-reuse workflow for Chrome automation profiles using --user-data-dir"
    - "The workflow covers: (a) creating a profile by running agrune once and keeping the user-data dir, (b) copying an existing Chrome profile directory, (c) reusing via --user-data-dir"
    - "The README points the user at --attach as an alternative for users who want to reuse their real Chrome profile without copying"
  artifacts:
    - path: "agrune/README.md"
      provides: "Automation profile import/copy how-to"
      contains: "automation profile"
      min_lines: 100
  key_links:
    - from: "agrune/README.md"
      to: "agrune/README.md"
      via: "same-file anchor — CLI flag table → profile section"
      pattern: "--user-data-dir"
---

<objective>
README 에 사용자가 CDP-only agrune 의 automation profile 을 import/copy/reuse 할 수 있는 단계별 절차를 문서화한다. Phase 10 CONTEXT.md 결정에 따라 별도 `agrune profile import` 서브커맨드는 만들지 않고, `--user-data-dir` 플래그(10-03 에서 추가) 기반의 가벼운 파일-복사 워크플로만 문서화한다.

Purpose: DOCS-04 요구사항 — 사용자가 README 만 보고 automation profile 을 import/복제하는 단계를 따라할 수 있어야 한다.
Output: README 의 신규 "### Automation profile" 섹션.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-docs-distribution/10-CONTEXT.md
@agrune/README.md
@agrune/packages/browser/src/chrome-launcher.ts
@agrune/packages/mcp/bin/agrune-mcp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Automation profile section to README</name>
  <files>agrune/README.md</files>
  <read_first>agrune/README.md, agrune/packages/browser/src/chrome-launcher.ts, agrune/packages/mcp/bin/agrune-mcp.ts</read_first>
  <action>
Insert a new H3 section `### Automation profile` in `agrune/README.md` immediately after the `### CLI 플래그` section added by plan 03. If plan 03's table has not yet been added, anchor on the `## 실행 방식` subsection instead.

Exact Markdown to insert:

```markdown
### Automation profile

agrune 은 기본적으로 Chrome 을 실행할 때마다 임시 user-data 디렉터리를 새로 만들고, 종료
시 삭제합니다. 자동화에서 **로그인 상태·쿠키·세션 저장소·확장 프로그램 설정**을 유지하고
싶다면 user-data 디렉터리를 고정해 재사용해야 합니다. agrune 은 `--user-data-dir` 플래그
하나로 이를 지원하며, 별도 `profile import` 서브커맨드는 제공하지 않습니다. 표준 Chrome
user-data 디렉터리 레이아웃을 그대로 사용합니다.

#### 1. 새 profile 생성

```bash
mkdir -p ~/.agrune/profiles/default
agrune --user-data-dir ~/.agrune/profiles/default
```

Chrome 이 뜬 뒤 원하는 사이트에 수동 로그인하거나 확장 프로그램을 설치합니다.
`Ctrl+C` 로 agrune 을 종료하면, 해당 디렉터리에 profile 이 그대로 보존됩니다.

다음 실행부터는 같은 경로를 재지정하면 동일한 로그인 상태로 시작합니다:

```bash
agrune --user-data-dir ~/.agrune/profiles/default
```

#### 2. 기존 Chrome profile 복제

이미 로그인해둔 평소 쓰는 Chrome profile 을 자동화 용도로 복제해서 사용할 수도 있습니다.
**원본 profile 을 직접 쓰지 말고 반드시 복사본을 만드세요** — agrune 이 쓰는 동안 원본
Chrome 을 열면 Chrome 이 "프로필이 이미 사용 중" 오류를 띄웁니다.

macOS:

```bash
# 1. Chrome 을 완전히 종료
# 2. 자동화 전용 복사본 생성
cp -R "$HOME/Library/Application Support/Google/Chrome/Default" \
      "$HOME/.agrune/profiles/imported"
agrune --user-data-dir ~/.agrune/profiles/imported
```

Linux:

```bash
cp -R "$HOME/.config/google-chrome/Default" ~/.agrune/profiles/imported
agrune --user-data-dir ~/.agrune/profiles/imported
```

Windows (PowerShell):

```powershell
Copy-Item -Recurse `
  "$Env:LOCALAPPDATA\Google\Chrome\User Data\Default" `
  "$Env:USERPROFILE\.agrune\profiles\imported"
agrune --user-data-dir "$Env:USERPROFILE\.agrune\profiles\imported"
```

> **주의**: Chrome user-data 디렉터리는 프로필 상위 폴더가 기준입니다. `Default/` 하위
> 디렉터리 **하나만 복사**해서 그 복사본을 `--user-data-dir` 에 지정하면 됩니다. 여러 프로
> 필을 쓰고 있다면 Chrome `User Data/` 루트 전체를 복사한 뒤 `--user-data-dir` 로 그
> 루트를 지정하면 기존과 동일한 profile selector 가 나타납니다.

#### 3. profile 없이 실제 Chrome 에 붙기

복사본을 만들지 않고 *현재 열려 있는* Chrome 에 직접 붙고 싶다면 `--attach` 를 씁니다.
이 경우 Chrome 을 `--remote-debugging-port=9222` 옵션으로 먼저 실행해두어야 합니다:

```bash
# Chrome 을 remote-debugging 모드로 실행 (별도 터미널)
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --remote-debugging-port=9222

# agrune 이 동일 Chrome 에 붙기
agrune --attach ws://127.0.0.1:9222/devtools/browser/<id>
```

`<id>` 는 `http://127.0.0.1:9222/json/version` 응답의 `webSocketDebuggerUrl` 에서 확인
할 수 있습니다. 이 방식은 profile 복사본을 만들지 않고도 실제 로그인 상태를 쓸 수 있지만,
agrune 이 종료된 뒤에도 Chrome 은 계속 열려 있습니다.

#### 정리

| 목표 | 추천 방법 |
|------|-----------|
| 전용 자동화 profile 을 한 번 만들고 매번 같은 상태로 시작 | `--user-data-dir` (방법 1) |
| 지금 Chrome 에 저장된 로그인·쿠키를 자동화로 그대로 옮기고 싶다 | `--user-data-dir` 복제 (방법 2) |
| 평소 Chrome 에 그대로 붙어서 자동화하고 싶다 | `--attach` (방법 3) |
```

After insertion, verify the section is present and flows naturally after the CLI 플래그 table. Do NOT reorder other sections.

Additionally: scan `agrune/README.md` one more time with grep for the forbidden strings to confirm nothing regressed:

```
grep -nE "extension mode|native messaging|backend daemon" agrune/README.md
```

must return nothing.
  </action>
  <verify>
grep -q "### Automation profile" agrune/README.md
grep -q "user-data-dir" agrune/README.md
grep -q "~/.agrune/profiles" agrune/README.md
grep -q "profile 이 이미 사용 중\|프로필이 이미 사용 중" agrune/README.md
grep -nE "extension mode|native messaging|backend daemon" agrune/README.md   # must print nothing
  </verify>
  <acceptance_criteria>
    - `agrune/README.md` contains section `### Automation profile`
    - `agrune/README.md` contains at least 3 code blocks in the Automation profile section (new profile, clone existing, attach)
    - `agrune/README.md` contains `cp -R` command referencing `Google/Chrome/Default`
    - `agrune/README.md` contains `--attach ws://127.0.0.1:9222`
    - `agrune/README.md` still has no forbidden strings
  </acceptance_criteria>
  <done>Automation profile section published in README covering all three UX flows.</done>
</task>

</tasks>

<verification>
- [ ] README Automation profile section present, with copy-clone-attach subsections
- [ ] No regressions to forbidden strings
- [ ] Readers with only README can follow profile workflow end-to-end
</verification>

<success_criteria>
- DOCS-04 satisfied: README documents automation profile import/copy workflow using existing `--user-data-dir` + `--attach` flags
- No new CLI surface added (per CONTEXT.md: "기능 폭발이 생기면 문서 단위만 정리")
- Cross-platform instructions (macOS, Linux, Windows PowerShell)
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-distribution/10-04-SUMMARY.md`
</output>
