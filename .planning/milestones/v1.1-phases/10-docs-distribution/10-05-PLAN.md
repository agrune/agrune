---
phase: 10-docs-distribution
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-03"]
files_modified:
  - .planning/phases/10-docs-distribution/PROFILE-README.draft.md
autonomous: true
requirements:
  - DOCS-02
user_setup: []

must_haves:
  truths:
    - "A draft org-profile README is written to .planning/phases/10-docs-distribution/PROFILE-README.draft.md"
    - "The draft removes 'Native Messaging' / 'Chrome Extension' architecture language and describes the CDP + @agrune/mcp + DevTools webapp model"
    - "An attempt is made to commit the draft to the external repo at /Users/chenjing/dev/agrune/.github"
    - "If the external-repo commit fails (any reason), the draft remains in .planning/ and a note in the PLAN summary marks human_needed"
  artifacts:
    - path: ".planning/phases/10-docs-distribution/PROFILE-README.draft.md"
      provides: "Updated org-profile README draft (CDP-only)"
      contains: "CDP"
      min_lines: 60
---

<objective>
조직 프로필 `.github/profile/README.md` (별도 git repo `/Users/chenjing/dev/agrune/.github/.git`) 를 CDP-only + `@agrune/mcp` + DevTools 웹앱 메시지로 업데이트한다. CONTEXT.md 방침에 따라 다음 2단계로 진행: (1) 먼저 현재 agrune repo 에 `PROFILE-README.draft.md` 를 작성, (2) 외부 repo 에 한 번 commit 시도. 실패하면 draft 만 남기고 `human_needed`.

Purpose: DOCS-02 — 조직 프로필이 v1.1 CDP-only 현실을 반영하도록 한다.
Output: 내부 draft 파일 + (시도가 성공하면) 외부 `.github/profile/README.md` 업데이트 커밋.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-docs-distribution/10-CONTEXT.md
@agrune/README.md
@/Users/chenjing/dev/agrune/.github/profile/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author updated org-profile README draft</name>
  <files>.planning/phases/10-docs-distribution/PROFILE-README.draft.md</files>
  <read_first>/Users/chenjing/dev/agrune/.github/profile/README.md, agrune/README.md, .planning/phases/10-docs-distribution/10-CONTEXT.md</read_first>
  <action>
Create `.planning/phases/10-docs-distribution/PROFILE-README.draft.md` with the updated content for the org-profile README. Keep the same visual structure (centered header, badges, hero placeholder, Use Cases table, Key Features grid, Why agrune comparison table, Architecture diagram, Quick Start, footer links). Apply these concrete content changes:

1. Keep the centered header, the `# agrune` H1, and the tagline. CHANGE the English tagline line from:
   - Old: "Browser automation for AI agents via annotated DOM elements"
   - New: "Browser automation for AI agents — CDP-native, 100% local, works with any MCP harness"

2. Keep the badges as-is (Version/Chrome Web Store/Downloads/License). *Note*: the Chrome Web Store badge points to an extension that no longer ships in v1.1; this plan does NOT touch badges — keep them so the human reviewer can decide whether to remove the store link when they merge the draft externally. Add a 4-line HTML comment above the badge block: `<!-- TODO 2026-04-18: Chrome Web Store 배지는 v1.1 CDP-only 피봇 이후 더 이상 canonical 배포 채널이 아닙니다. 재설치/링크 삭제 판단은 org maintainer 수동 결정. -->`

3. Keep hero demo GIF placeholder as-is.

4. Replace the "What is agrune?" paragraph with:

```
**agrune** lets AI agents see and control web pages — directly in a real Chrome browser, over CDP. Add simple `data-agrune-*` annotations to your UI, run `@agrune/mcp`, and watch your MCP-compatible agent (Claude, GPT, Gemini, …) interact with the page with live command logs and a smooth Aurora pointer.

Everything runs **100% locally**. No cloud. No data leaves your machine.
```

5. Use Cases table: leave the three rows unchanged. Status column remains `Coming Soon`.

6. Replace the "Key Features" 3×2 grid cells so none of them read "9 MCP Tools" or reference extensions. New cells:

```
| :dart: **10 MCP Tools** | :lock: **100% Local** | :sparkles: **Live DevTools Webapp** |
| Click, fill, drag, wait, read, focus — full browser control via MCP | Zero cloud. CDP straight to your own Chrome | Command log, HITL toolbar, session routing, failure diagnostics at http://localhost:47654/devtools |
| :label: **Simple Annotations** | :robot: **Agent Agnostic** | :zap: **Self-Healing Sessions** |
| Just add `data-agrune-*` attributes to your HTML. No SDK | Works with Claude, GPT, Gemini — any MCP-speaking agent | Auto-reattach on tab crashes, supervisor-driven recovery, `recovered` flag on tool responses |
```

7. Replace the "Why agrune?" comparison table row for "Real browser". Change the "Native Messaging" architecture diagram (currently in the ```Architecture``` code block) from:

```
┌──────────┐     ┌────────────┐     ┌──────────────────┐     ┌──────────┐
│ AI Agent │◄───►│ MCP Server │◄───►│ Chrome Extension │◄───►│ Web Page │
└──────────┘     └────────────┘     └──────────────────┘     └──────────┘
                  Native Messaging    Manifest V3              data-agrune-*
```

to:

```
┌──────────┐     ┌──────────────────────┐     ┌────────┐     ┌──────────┐
│ AI Agent │◄───►│ @agrune/mcp (stdio)  │◄───►│ Chrome │◄───►│ Web Page │
└──────────┘     └──────────────────────┘     └────────┘     └──────────┘
                  + DevTools webapp :47654     CDP            data-agrune-*
```

8. Replace the Quick Start section (currently plugin-install flow) with:

```
**1. Install the MCP server**

```sh
npm install -g @agrune/mcp
```

**2. Launch agrune**

```sh
agrune                     # Chrome launch + DevTools webapp on :47654
agrune --attach ws://...   # attach to an existing Chrome (--remote-debugging-port)
agrune --help              # full flag reference
```

**3. Connect your MCP-compatible agent** to the stdio server and start interacting.

See the monorepo [README](https://github.com/agrune/agrune#readme) for annotation examples, DevTools webapp features, and automation profile setup.
```

9. Footer links block: keep Documentation / Chrome Web Store / npm / Contributing links but annotate the Chrome Web Store link with a `<!-- deprecated -->` HTML comment next to it (so GitHub renders the link but an org maintainer can remove it without code review).

10. End file with a single trailing newline.

Do NOT prettify or collapse the badge block — GitHub org-profile rendering is sensitive to whitespace in the centered `<div>` wrapper.
  </action>
  <verify>
test -f ".planning/phases/10-docs-distribution/PROFILE-README.draft.md"
grep -q "CDP-native" .planning/phases/10-docs-distribution/PROFILE-README.draft.md
grep -q "@agrune/mcp (stdio)" .planning/phases/10-docs-distribution/PROFILE-README.draft.md
grep -q "47654" .planning/phases/10-docs-distribution/PROFILE-README.draft.md
grep -qvE "Chrome Extension" .planning/phases/10-docs-distribution/PROFILE-README.draft.md || \
  grep -q "<!-- deprecated -->" .planning/phases/10-docs-distribution/PROFILE-README.draft.md
  </verify>
  <acceptance_criteria>
    - Draft file exists with CDP + @agrune/mcp + DevTools webapp messaging
    - Architecture diagram shows `@agrune/mcp (stdio)` and CDP, NOT "Native Messaging"
    - Port 47654 present in Key Features grid
    - Chrome Web Store badge/link present but flagged with `<!-- deprecated -->` or explicit TODO comment
    - Bilingual original tone preserved (English body, optional Korean comments OK)
  </acceptance_criteria>
  <done>Draft org-profile README authored inside agrune repo.</done>
</task>

<task type="auto">
  <name>Task 2: Attempt one-shot commit to external .github repo</name>
  <files>/Users/chenjing/dev/agrune/.github/profile/README.md</files>
  <read_first>/Users/chenjing/dev/agrune/.github/profile/README.md, .planning/phases/10-docs-distribution/PROFILE-README.draft.md</read_first>
  <action>
Attempt ONE commit to the external repo. Steps:

1. Check writability: `test -w /Users/chenjing/dev/agrune/.github/profile/README.md && test -d /Users/chenjing/dev/agrune/.github/.git`. If either check fails, STOP and record the reason in the plan summary; leave the draft in place.

2. Check that the external repo is clean: run `git -C /Users/chenjing/dev/agrune/.github status --porcelain`. If output is NON-EMPTY (dirty working tree), STOP: do not mix agrune work with pre-existing changes. Record `external repo dirty — human review needed` in the summary.

3. If checks pass: copy the draft over the live file:
   ```bash
   cp .planning/phases/10-docs-distribution/PROFILE-README.draft.md \
      /Users/chenjing/dev/agrune/.github/profile/README.md
   ```

4. Commit using the external repo's `.git` directory, via `git -C` so the current working directory stays inside agrune:
   ```bash
   git -C /Users/chenjing/dev/agrune/.github add profile/README.md
   git -C /Users/chenjing/dev/agrune/.github commit -m "$(cat <<'EOF'
docs(profile): update org README for v1.1 CDP-only architecture

- Drop Chrome Extension + Native Messaging architecture references
- Describe @agrune/mcp stdio + DevTools webapp (:47654) + self-healing
- Mark Chrome Web Store badge/link with deprecation TODO for maintainer review

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
   ```

5. Do NOT push. The human will review and push manually.

6. If ANY step fails (non-zero exit, error output, hook rejection), do NOT retry. Leave the draft in place and record the failure mode in the SUMMARY.

7. ONLY attempt this ONCE. If the working tree is dirty after step 3 for any reason other than the expected single-file add, run `git -C /Users/chenjing/dev/agrune/.github restore profile/README.md` to revert the file and then bail.
  </action>
  <verify>
# Either the commit happened OR the draft exists and we noted the failure
test -f .planning/phases/10-docs-distribution/PROFILE-README.draft.md
# If commit succeeded, the external repo should have a new commit mentioning v1.1 CDP-only
git -C /Users/chenjing/dev/agrune/.github log -1 --pretty=%s | grep -q "v1.1\|CDP\|profile" || \
  echo "external commit skipped — draft retained"
  </verify>
  <acceptance_criteria>
    - Draft file present in agrune repo at `.planning/phases/10-docs-distribution/PROFILE-README.draft.md`
    - Either the external repo has a new commit touching `profile/README.md`, OR the SUMMARY explicitly records the failure mode and leaves the draft for manual promotion
    - External repo NOT pushed (only committed locally, if at all)
    - No force-push, no hook skipping
  </acceptance_criteria>
  <done>External-repo commit either succeeded (single local commit, no push) or deferred with draft retained + clear reason.</done>
</task>

</tasks>

<verification>
- [ ] Draft file present in agrune repo
- [ ] External repo commit attempted once
- [ ] If attempt failed, SUMMARY includes the failure reason and next manual step
- [ ] No push to external repo
</verification>

<success_criteria>
- DOCS-02 satisfied (either directly via committed update OR via a fully-prepared draft awaiting human promotion)
- External-repo contract honored: single attempt, no push, clean-tree precondition
- All forbidden strings absent from the draft
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-distribution/10-05-SUMMARY.md`
</output>
