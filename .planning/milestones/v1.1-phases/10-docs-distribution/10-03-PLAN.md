---
phase: 10-docs-distribution
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - agrune/packages/mcp/bin/agrune-mcp.ts
  - agrune/README.md
autonomous: true
requirements:
  - DOCS-03
user_setup: []

must_haves:
  truths:
    - "`agrune --help` prints a flag table including --headless, --attach, --port (default 47654), --no-devtools, --url, --user-data-dir, --version"
    - "`agrune --version` prints the agrune version string and exits 0 without launching Chrome"
    - "`agrune --help` exits 0 without launching Chrome, without starting DevTools server, without connecting stdio MCP transport"
    - "agrune/README.md has a flag reference table matching the --help output (same flag names, same defaults)"
  artifacts:
    - path: "agrune/packages/mcp/bin/agrune-mcp.ts"
      provides: "CLI entrypoint with --help / --version short-circuit and argument parsing"
      contains: "--help"
      min_lines: 80
    - path: "agrune/README.md"
      provides: "Flag reference table"
      contains: "--headless"
      min_lines: 60
  key_links:
    - from: "agrune/packages/mcp/bin/agrune-mcp.ts"
      to: "agrune/README.md"
      via: "flag table consistency"
      pattern: "--headless"
---

<objective>
`agrune-mcp` CLI 에 `--help` 와 `--version` 출력을 추가하고, README 에 같은 내용을 그대로 반영하는 flag reference 표를 만든다. DOCS-03 요구사항: "`agrune --help` 실행 시 `--headless`·`--attach`·`--port`·`--no-devtools` 와 기본 포트 47654 가 문서화되어 보인다".

Purpose: 사용자가 CLI 와 README 두 표면 어디에서도 동일한 플래그 정보를 얻을 수 있게 한다.
Output: `agrune-mcp.ts` 에 help/version 처리 + README flag table 섹션.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-docs-distribution/10-CONTEXT.md
@agrune/packages/mcp/bin/agrune-mcp.ts
@agrune/packages/mcp/bin/version.ts
@agrune/packages/browser/src/cdp-driver.ts
@agrune/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --help and --version handling to agrune-mcp CLI</name>
  <files>agrune/packages/mcp/bin/agrune-mcp.ts</files>
  <read_first>agrune/packages/mcp/bin/agrune-mcp.ts, agrune/packages/mcp/bin/version.ts, agrune/packages/browser/src/cdp-driver.ts, agrune/packages/browser/src/chrome-launcher.ts</read_first>
  <action>
Modify `agrune/packages/mcp/bin/agrune-mcp.ts` so that:

1. Immediately after `const args = process.argv.slice(2)` (currently line 6), add a short-circuit block that handles `--help`, `-h`, `--version`, `-v` BEFORE any driver construction or MCP transport connect. The block must `process.exit(0)` after writing to stdout (NOT stderr) so pipelines like `agrune --help | less` work.

2. Import `AGRUNE_VERSION` (or whatever the version export is) from `./version.js`. If `version.ts` exports something else, adapt the import.

3. The `--help` text MUST be the exact following (Korean + English bilingual, matching project tone):

```
agrune — AI 에이전트용 CDP 기반 브라우저 자동화 MCP 서버

Usage:
  agrune [options]

Options:
  --headless              Chrome 을 headless 모드로 실행 (UI 없음)
  --attach <ws>           이미 실행 중인 Chrome 의 CDP WebSocket endpoint 에 연결
                          (예: --attach ws://127.0.0.1:9222/devtools/browser/...)
  --port <n>              DevTools 웹앱 포트 (default: 47654)
  --no-devtools           DevTools 웹앱을 서빙하지 않음 (MCP stdio 만 사용)
  --url <url>             Chrome 기동 시 열 초기 URL (default: about:blank)
  --user-data-dir <path>  Chrome user-data 디렉터리 지정. 기존 automation profile 을
                          그대로 사용하려면 여기에 profile 경로를 지정한다.
                          (미지정 시 agrune 이 임시 디렉터리를 생성·삭제)
  -h, --help              이 도움말 출력 후 종료
  -v, --version           버전 출력 후 종료

DevTools 웹앱: http://localhost:<port>/devtools  (기본 포트 47654)

예시:
  agrune                                   # Chrome 런치 + DevTools 웹앱
  agrune --headless                        # headless 모드
  agrune --attach ws://127.0.0.1:9222/...  # 기존 Chrome 에 연결
  agrune --port 47655 --no-devtools        # 포트 변경 + DevTools 비활성화
  agrune --user-data-dir ~/.agrune/profile # automation profile 재사용

문서: https://github.com/agrune/agrune
```

4. The `--version` output MUST be a single line `agrune vX.Y.Z` where X.Y.Z is imported from `version.ts`. If `version.ts` does not yet exist or does not export a version constant, create/fix it to read from `packages/mcp/package.json` (use `createRequire` + `import { createRequire } from 'node:module'` if needed, or use `fs.readFile` with `fileURLToPath(import.meta.url)` to locate package.json relative to the bundled bin).

5. Also add `--user-data-dir <path>` parsing. Pass it as `userDataDir` to `CdpDriver` options (the CdpDriver already accepts `userDataDir`; verify by reading the file). When `--user-data-dir` is present AND `--attach` is also present, print a warning to stderr: `[agrune] --user-data-dir is ignored when --attach is set` and continue.

6. Keep the existing flow intact: stdio transport only when `!process.stdin.isTTY`, DevTools server unless `--no-devtools`, eager `driver.connect()` when standalone. The help/version short-circuit must run BEFORE any of this.

7. Keep `getArgValue()` helper. Do NOT rewrite it.

8. Output of `--help` must go to `process.stdout.write(...)`. Output of errors or warnings stays on `process.stderr.write(...)`.

Concrete code shape:

```ts
#!/usr/bin/env node
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js'
import { CdpDriver } from '@agrune/browser'
import { createMcpServer } from '../src/index.js'
import { AGRUNE_VERSION } from './version.js'

const args = process.argv.slice(2)

if (args.includes('--help') || args.includes('-h')) {
  process.stdout.write(HELP_TEXT)
  process.exit(0)
}
if (args.includes('--version') || args.includes('-v')) {
  process.stdout.write(`agrune v${AGRUNE_VERSION}\n`)
  process.exit(0)
}

const attachEndpoint = getArgValue('--attach')
const headless = args.includes('--headless')
const noDevtools = args.includes('--no-devtools')
const devtoolsPortArg = getArgValue('--port')
const startUrl = getArgValue('--url')
const userDataDir = getArgValue('--user-data-dir')

if (userDataDir && attachEndpoint) {
  process.stderr.write('[agrune] --user-data-dir is ignored when --attach is set\n')
}

const driver = new CdpDriver({
  mode: attachEndpoint ? 'attach' : 'launch',
  ...(attachEndpoint ? { wsEndpoint: attachEndpoint } : {}),
  headless,
  startUrl,
  ...(userDataDir && !attachEndpoint ? { userDataDir } : {}),
})

// ... rest of existing file unchanged

const HELP_TEXT = `agrune — AI 에이전트용 CDP 기반 브라우저 자동화 MCP 서버
... (full help text from step 3 above) ...
`
```

Place `HELP_TEXT` at the bottom of the file (after the SIGTERM handler and `getArgValue` helper) so the top of the file remains readable.

9. If `packages/mcp/bin/version.ts` does NOT exist, create it with:

```ts
import { createRequire } from 'node:module'
const require = createRequire(import.meta.url)
const pkg = require('../package.json') as { version: string }
export const AGRUNE_VERSION = pkg.version
```

If it DOES exist, verify it exports `AGRUNE_VERSION` (or adapt the import name to what it actually exports).
  </action>
  <verify>
# Type check
pnpm --filter @agrune/mcp build
# Help works without launching Chrome or opening stdio
node agrune/packages/mcp/dist/bin/agrune-mcp.js --help | grep -q -- "--headless"
node agrune/packages/mcp/dist/bin/agrune-mcp.js --help | grep -q -- "47654"
node agrune/packages/mcp/dist/bin/agrune-mcp.js --help | grep -q -- "--attach"
node agrune/packages/mcp/dist/bin/agrune-mcp.js --help | grep -q -- "--no-devtools"
node agrune/packages/mcp/dist/bin/agrune-mcp.js --help | grep -q -- "--user-data-dir"
node agrune/packages/mcp/dist/bin/agrune-mcp.js --version | grep -qE "^agrune v[0-9]+"
  </verify>
  <acceptance_criteria>
    - Running `agrune --help` exits 0 and stdout contains `--headless`, `--attach`, `--port`, `--no-devtools`, `--user-data-dir`, `47654`
    - Running `agrune --version` exits 0 and prints `agrune v<semver>`
    - `--help` short-circuit happens BEFORE `new CdpDriver(...)` (no Chrome launched, no server started)
    - `packages/mcp/bin/agrune-mcp.ts` contains the string `HELP_TEXT` and `AGRUNE_VERSION`
    - File compiles with `pnpm --filter @agrune/mcp build` (no TS errors)
  </acceptance_criteria>
  <done>CLI `--help` and `--version` work end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI flag reference table to agrune/README.md</name>
  <files>agrune/README.md</files>
  <read_first>agrune/README.md, agrune/packages/mcp/bin/agrune-mcp.ts</read_first>
  <action>
Insert a new subsection "### CLI 플래그" directly after the "### 어노테이션" subsection (or if not present, after the `## 실행 방식` code block) in `agrune/README.md`. The table must match the help text verbatim in flag names, defaults, and descriptions.

Exact Markdown to insert:

```markdown
### CLI 플래그

| 플래그 | 기본값 | 설명 |
|--------|--------|------|
| `--headless` | `false` | Chrome 을 headless 모드로 실행 (UI 없음) |
| `--attach <ws>` | (미설정) | 이미 실행 중인 Chrome 의 CDP WebSocket endpoint 에 연결 |
| `--port <n>` | `47654` | DevTools 웹앱 포트 |
| `--no-devtools` | `false` | DevTools 웹앱을 서빙하지 않음 (MCP stdio 만 사용) |
| `--url <url>` | `about:blank` | Chrome 기동 시 열 초기 URL |
| `--user-data-dir <path>` | (임시 디렉터리) | Chrome user-data 디렉터리. automation profile 재사용 시 사용 |
| `-h`, `--help` | — | 도움말 출력 후 종료 |
| `-v`, `--version` | — | 버전 출력 후 종료 |

DevTools 웹앱은 `http://localhost:<port>/devtools` (기본 포트 47654) 에서 제공됩니다.
전체 도움말은 `agrune --help` 로 볼 수 있습니다.
```

Verify by grepping for the table header row after write. Do NOT duplicate existing flag descriptions elsewhere in README (plan 01 already mentioned the flags by name without defaults — this plan owns the canonical table).
  </action>
  <verify>
grep -q "### CLI 플래그" agrune/README.md
grep -q "47654" agrune/README.md
grep -q "agrune --help" agrune/README.md
grep -q "| \`--user-data-dir <path>\` |" agrune/README.md
  </verify>
  <acceptance_criteria>
    - `agrune/README.md` contains section heading `### CLI 플래그`
    - `agrune/README.md` table row `| \`--headless\` | \`false\` |` present
    - `agrune/README.md` table row `| \`--port <n>\` | \`47654\` |` present
    - `agrune/README.md` mentions `agrune --help` as authoritative source
  </acceptance_criteria>
  <done>README CLI flag table in sync with CLI help output.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @agrune/mcp build` succeeds
- [ ] `node agrune/packages/mcp/dist/bin/agrune-mcp.js --help` exits 0 and contains all required flags
- [ ] README flag table matches CLI help flag names (diff of flag names is empty)
</verification>

<success_criteria>
- DOCS-03 satisfied: `agrune --help` documents `--headless`, `--attach`, `--port` (default 47654), `--no-devtools`
- README flag table is canonical and consistent with `--help`
- `--help`/`--version` short-circuit never launches Chrome (fast, safe)
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-distribution/10-03-SUMMARY.md`
</output>
