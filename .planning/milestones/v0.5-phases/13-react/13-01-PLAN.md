---
phase: "13"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/manifest/src/schema.ts
  - packages/manifest/src/index.ts
  - packages/manifest/tests/builders.spec.ts
  - packages/manifest/tests/validator.spec.ts
  - packages/runtime/src/runtime/target-resolver.ts
  - packages/runtime/tests/target-resolver.spec.ts
autonomous: true
requirements: [REACT-01]
tags: [schema, selector-ladder, fiber-path, resolver, tdd]

must_haves:
  truths:
    - "@agrune/manifest `SelectorLadder`에 optional `fiber: { path: FiberIdentityPath }` 필드가 추가되어도 기존 5개 분기(role/text/testId/attr/css)만 사용하는 모든 manifest는 여전히 유효하다"
    - "`FiberIdentityPath` 타입(`{ componentName: string; key: string | null; index: number }[]`)이 `@agrune/manifest`에서 export 되어 `@agrune/react`·`@agrune/runtime` 모두가 공통 값 타입으로 참조할 수 있다"
    - "zod `SelectorLadderSchema`가 `fiber` 필드 유효성(길이 1-8, segment shape, index non-negative integer)을 거부/통과 케이스 모두에서 올바르게 검증한다"
    - "runtime `resolveByLadder`가 `ladder.fiber` 가 있고 `window.__agrune_identity__` 가 존재하면 fiber path를 **최우선**으로 시도하고, 결과가 null 이면 기존 role/text/testId/attr/css ladder로 fallback 한다"
    - "`window.__agrune_identity__` 가 없는(external site, no React) 환경에서 `ladder.fiber` 가 있어도 fallback ladder가 정상 동작한다"
  artifacts:
    - path: packages/manifest/src/schema.ts
      provides: "FiberIdentityPath interface + FiberPathSegment + SelectorLadder.fiber 추가 + SelectorLadderSchema fiber branch"
      contains: "FiberIdentityPath"
    - path: packages/manifest/src/index.ts
      provides: "FiberIdentityPath / FiberPathSegment re-export"
      contains: "FiberIdentityPath"
    - path: packages/runtime/src/runtime/target-resolver.ts
      provides: "resolveByLadder fiber-first branch via window.__agrune_identity__"
      contains: "ladder.fiber"
    - path: packages/runtime/tests/target-resolver.spec.ts
      provides: "fiber-first priority + fallback tests"
      contains: "window.__agrune_identity__"
  key_links:
    - from: packages/runtime/src/runtime/target-resolver.ts
      to: "window.__agrune_identity__.resolve(path)"
      via: "runtime 전역 bridge (Plan 02에서 게시)"
      pattern: "window.*__agrune_identity__"
    - from: packages/manifest/src/schema.ts
      to: packages/manifest/src/index.ts
      via: "public barrel export"
      pattern: "FiberIdentityPath"

user_setup: []
---

<objective>
`SelectorLadder`를 6번째 단계(`fiber`)로 확장하고 runtime resolver에 fiber-first 우선순위를 심어, Plan 02에서 만드는 `window.__agrune_identity__` bridge가 활성화되면 refactor에 내성 있는 component-identity selector가 CSS ladder보다 먼저 동작하도록 경로를 연다.

Purpose:
- REACT-01 의 기반 contract(fiber path descriptor 타입 + resolver 우선순위)를 Plan 02의 `@agrune/react` 패키지 구현 이전에 단독으로 확정해 parallel/후속 작업이 계약 기반으로 진행되도록 한다.
- SelectorLadder 변경은 optional 필드 추가이므로 Phase 11/12에서 생성된 기존 manifest/스냅샷/fixture와 100% backward-compatible 해야 한다 (non-breaking).

Output:
- `FiberIdentityPath` / `FiberPathSegment` 값 타입 + `SelectorLadder.fiber` optional 필드 (schema + zod + builders 투과적 통과).
- `resolveByLadder` fiber branch — `window.__agrune_identity__.resolve(path)` 조회 + 실패 시 기존 ladder fallback.
- `@agrune/manifest` barrel 재export + `@agrune/runtime` 회귀 테스트 통과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-react/13-CONTEXT.md
@.planning/phases/13-react/13-RESEARCH.md
@.planning/phases/11-manifest/11-01-SUMMARY.md
@.planning/phases/11-manifest/11-02-SUMMARY.md
@.planning/phases/11-manifest/11-03-SUMMARY.md
@.planning/phases/12-inject/12-01-SUMMARY.md

@packages/manifest/src/schema.ts
@packages/manifest/src/index.ts
@packages/manifest/src/validator.ts
@packages/manifest/src/builders.ts
@packages/runtime/src/runtime/target-resolver.ts
@packages/runtime/tests/target-resolver.spec.ts

<interfaces>
<!-- Key types and contracts the executor needs. Do NOT re-explore the codebase. -->

From packages/manifest/src/schema.ts (existing, before this plan):
```typescript
export type ActionKind = 'click' | 'fill' | 'dblclick' | 'contextmenu' | 'hover' | 'longpress'

type AtLeastOne<T> = { [K in keyof T]: Pick<T, K> & Partial<Omit<T, K>> }[keyof T]

export type SelectorLadder = AtLeastOne<{
  role: { name: string; level?: string }
  text: string
  testId: string
  attr: string
  css: string
}>

export const SelectorLadderSchema = z
  .object({
    role: z.object({ name: z.string(), level: z.string().optional() }).optional(),
    text: z.string().optional(),
    testId: z.string().optional(),
    attr: z.string().optional(),
    css: z.string().optional(),
  })
  .refine(
    (v) => Boolean(v.role || v.text || v.testId || v.attr || v.css),
    { message: 'SelectorLadder must define at least one of: role, text, testId, attr, css' },
  )
```

From packages/runtime/src/runtime/target-resolver.ts (existing SelectorLadder local copy, to be extended):
```typescript
export interface SelectorLadder {
  role?: { name: string; level?: string }
  text?: string
  testId?: string
  attr?: string
  css?: string
}

export function resolveByLadder(
  ladder: SelectorLadder,
  doc: Document = document,
): HTMLElement[] {
  if (ladder.role) { ... }
  if (ladder.text !== undefined && ladder.text !== '') { ... }
  if (ladder.testId) { ... }
  if (ladder.attr) { ... }
  if (ladder.css) { ... }
  return []
}
```

New types to define in packages/manifest/src/schema.ts:
```typescript
export interface FiberPathSegment {
  /** getDisplayName(fiber.type) — empty string if anonymous */
  componentName: string
  /** fiber.key (React key prop) — null if not keyed */
  key: string | null
  /** fiber.index (sibling 순서) — non-negative integer */
  index: number
}

/** DOM 노드를 식별하는 직렬화 가능한 경로 (컴포넌트 → root 방향). Max 8 segments. */
export type FiberIdentityPath = FiberPathSegment[]
```

`window.__agrune_identity__` bridge shape (Plan 02가 publish — Plan 01은 read-only consumer):
```typescript
interface AgruneIdentityBridge {
  resolve(path: FiberIdentityPath): HTMLElement | null
  version: '1'
}
declare global {
  interface Window {
    __agrune_identity__?: AgruneIdentityBridge
  }
}
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FiberIdentityPath 타입 + SelectorLadder.fiber 필드 확장 (manifest schema)</name>
  <files>
    packages/manifest/src/schema.ts,
    packages/manifest/src/index.ts,
    packages/manifest/tests/builders.spec.ts,
    packages/manifest/tests/validator.spec.ts
  </files>

  <read_first>
    packages/manifest/src/schema.ts (전체),
    packages/manifest/src/index.ts (전체 — 기존 barrel export),
    packages/manifest/src/validator.ts (`assertNoHashClass`/`assertNoNthChild` 현 구현),
    packages/manifest/tests/builders.spec.ts (기존 `@ts-expect-error` 패턴 — sensitive:false 차단 방식),
    packages/manifest/tests/validator.spec.ts (기존 SelectorLadder min-1 거부 테스트)
  </read_first>

  <behavior>
    - 기존 manifest 43 tests 전부 회귀 없이 통과
    - `SelectorLadder` TS 타입에 `fiber?: { path: FiberIdentityPath }` 가 추가되어도 role-only / css-only / testId-only manifest는 여전히 컴파일 통과
    - `defineTarget({ targetId, actionKinds, selector: { fiber: { path: [{componentName: 'Button', key: null, index: 0}] } } })` 가 typecheck 통과
    - `defineTarget({ selector: {} })` 는 여전히 `@ts-expect-error` — 6개 중 최소 1개 요구
    - zod `SelectorLadderSchema` refine이 fiber 단독 manifest도 통과 (최소 1필드 요건)
    - zod가 `fiber.path` 빈 배열, segment의 잘못된 shape(index negative, componentName non-string, key가 boolean) 전부 거부
    - zod가 `fiber.path.length > 8` 거부
    - `FiberIdentityPath` / `FiberPathSegment` 가 `@agrune/manifest` barrel에서 export — `import { FiberIdentityPath } from '@agrune/manifest'` 가능
  </behavior>

  <action>
    1. `packages/manifest/src/schema.ts` 수정:
       - 파일 상단(types 섹션 맨 위, `ActionKind` 바로 아래)에 `FiberPathSegment` interface + `FiberIdentityPath` type 추가.
         - `componentName: string`, `key: string | null`, `index: number` 필드 3개 (위 interfaces 블록의 정확한 주석 유지).
       - `SelectorLadder` 의 `AtLeastOne<{...}>` 맵에 `fiber: { path: FiberIdentityPath }` 6번째 필드 추가.
       - `SelectorLadderSchema` 의 `z.object({...})` 에 `fiber` 필드 추가:
         ```typescript
         fiber: z.object({
           path: z.array(z.object({
             componentName: z.string(),
             key: z.string().nullable(),
             index: z.number().int().nonnegative(),
           })).min(1).max(8),
         }).optional(),
         ```
       - `.refine()` predicate에 `|| v.fiber` 추가 — 메시지는 기존 문자열에 `, fiber` 포함해 갱신:
         `'SelectorLadder must define at least one of: role, text, testId, attr, css, fiber'`.
       - 이 필드 구조는 D-2026-04-19(bippy) + RESEARCH Pattern 2 에 근거.

    2. `packages/manifest/src/index.ts` 수정:
       - `FiberIdentityPath`, `FiberPathSegment` type export 추가 (기존 `SelectorLadder`/`AgruneManifest` barrel 라인 근처).

    3. `packages/manifest/tests/builders.spec.ts` 수정 — 새 positive + negative 테스트 추가:
       - positive: `defineTarget({ ..., selector: { fiber: { path: [{ componentName: 'Button', key: null, index: 0 }] } } })` 가 typecheck+runtime 통과.
       - `@ts-expect-error` negative: `fiber.path` 에 `{ componentName: 'X', key: true, index: 0 }` (boolean key) 전달 — 기존 `sensitive: false` 차단 패턴과 동일한 `@ts-expect-error` 한 줄 위 배치 사용.
       - **회귀:** `defineTarget({ selector: {} })` `@ts-expect-error` 그대로 유지 확인 (fiber 추가 후에도).

    4. `packages/manifest/tests/validator.spec.ts` 수정 — zod 분기 테스트 추가:
       - fiber-only manifest가 `validateManifest()` 통과.
       - `fiber.path = []` 거부 (empty array).
       - `fiber.path.length = 9` 거부.
       - `fiber.path[0].index = -1` 거부.
       - `fiber.path[0].componentName = 42` (number) 거부.
       - JSON round-trip: `JSON.parse(JSON.stringify(manifestWithFiber))` 이후 재검증 통과 — path가 문자열/null/number만 포함.

    TDD 순서: spec에 failing 테스트 작성 → RED 커밋 → schema 확장 → GREEN 커밋. 기존 43/43 테스트는 전 과정에서 실패하면 안 됨 (기존 manifest shape는 변경 없음).
  </action>

  <verify>
    <automated>pnpm --filter @agrune/manifest run typecheck && pnpm --filter @agrune/manifest run test && pnpm --filter @agrune/manifest run build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "FiberPathSegment" packages/manifest/src/schema.ts` → 1+ 매치 (interface 선언)
    - `grep -n "FiberIdentityPath" packages/manifest/src/schema.ts` → 1+ 매치 (type alias)
    - `grep -n "fiber:" packages/manifest/src/schema.ts` → 2+ 매치 (타입 블록 + zod 블록)
    - `grep -n "FiberIdentityPath" packages/manifest/src/index.ts` → 1+ 매치 (barrel re-export)
    - `grep -n "SelectorLadder must define at least one of: role, text, testId, attr, css, fiber" packages/manifest/src/schema.ts` → 1 매치 (refine 메시지 갱신)
    - `pnpm --filter @agrune/manifest run test` exit 0 — 최소 48 tests (기존 43 + 신규 ≥ 5), 전부 PASS.
    - `pnpm --filter @agrune/manifest run typecheck` exit 0.
    - `pnpm --filter @agrune/manifest run build` — `dist/index.d.ts` 안에 `FiberIdentityPath` 포함.
  </acceptance_criteria>

  <done>
    - FiberPathSegment / FiberIdentityPath 타입 + SelectorLadder.fiber optional 필드가 schema.ts 와 barrel에 추가됨.
    - 48+ manifest 테스트 전부 통과, 기존 43 tests 회귀 없음.
    - TDD RED → GREEN 2 커밋 기록됨.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runtime resolveByLadder 에 fiber-first branch 추가</name>
  <files>
    packages/runtime/src/runtime/target-resolver.ts,
    packages/runtime/tests/target-resolver.spec.ts
  </files>

  <read_first>
    packages/runtime/src/runtime/target-resolver.ts (전체 — 특히 `SelectorLadder` local interface + `resolveByLadder` branch 순서),
    packages/runtime/tests/target-resolver.spec.ts (28 tests 기존 패턴),
    packages/manifest/src/schema.ts (Task 1 이후의 `FiberIdentityPath` shape)
  </read_first>

  <behavior>
    - `resolveByLadder({ fiber: { path }, role: { name: 'button' } }, doc)`:
      - `window.__agrune_identity__.resolve(path)` 가 HTMLElement 반환 → 그 element만 `[el]` 리턴 (role 분기 호출 안 함).
      - `window.__agrune_identity__` 가 `undefined` → role 분기로 fallback.
      - `window.__agrune_identity__.resolve(path)` 가 `null` 반환 → role 분기로 fallback.
    - `resolveByLadder({ fiber: { path } })` fiber-only:
      - bridge 존재 + resolve 성공 → `[el]`.
      - bridge 존재 + resolve null → `[]`.
      - bridge 없음 → `[]` (다른 분기 없음).
    - 기존 28 tests 그대로 통과 (ladder 순서 변경 없음 — fiber는 role 앞에 최우선 삽입, fiber 없으면 기존 경로 그대로).
    - `window.__agrune_identity__` 가 존재하지만 `resolve` 가 함수가 아닌 경우 throw 하지 않고 fallback.
  </behavior>

  <action>
    1. `packages/runtime/src/runtime/target-resolver.ts` 수정:
       - 파일 상단 SelectorLadder local interface(line 20-26)에 `fiber?: { path: FiberIdentityPath }` 필드 추가.
       - **임시 로컬 type 복제 금지**: `FiberIdentityPath` 를 `import type { FiberIdentityPath } from '@agrune/manifest'` 로 가져온다 — Plan 11-03 SUMMARY 의 `@agrune/manifest` re-export 통합 방향과 일치.
         - 만약 `@agrune/runtime` 가 아직 `@agrune/manifest` workspace dep를 가지지 않으면 `packages/runtime/package.json` 의 dependencies에 `"@agrune/manifest": "workspace:*"` 추가 + `pnpm install`.
         - `@agrune/core` 가 이미 `@agrune/manifest` 를 re-export하므로 `import type { FiberIdentityPath } from '@agrune/core'` 경로도 허용 (실제 선택은 executor 재량).
       - `resolveByLadder` 본문 맨 앞에 fiber branch 추가 (role 위):
         ```typescript
         if (ladder.fiber) {
           const bridge = (globalThis as { __agrune_identity__?: { resolve?: (p: FiberIdentityPath) => HTMLElement | null } }).__agrune_identity__
           if (bridge && typeof bridge.resolve === 'function') {
             try {
               const el = bridge.resolve(ladder.fiber.path)
               if (el) return [el]
             } catch { /* fallback */ }
           }
           // bridge 없음/null/throw → 아래 role/text/... 분기 fallback
         }
         ```
       - `globalThis` 접근은 `window` 와 달리 Node/test 환경에서도 안전 — jsdom이 `window === globalThis` 로 설정.

    2. `packages/runtime/tests/target-resolver.spec.ts` 수정 — `describe('fiber-first branch')` 추가:
       - 각 테스트 `beforeEach` 에서 `(globalThis as any).__agrune_identity__` 를 설정, `afterEach` 에서 delete.
       - Test A: bridge + resolve → matched element → `resolveByLadder({ fiber: { path: [...] }, role: {...} })` 가 fiber element만 반환, role 분기 호출 안 됨 (jsdom에 role 대응 DOM이 있어도 fiber element가 우선).
       - Test B: bridge undefined → fiber branch 건너뜀, role fallback 정상 반환.
       - Test C: bridge.resolve = null → role fallback.
       - Test D: fiber-only ladder + bridge 없음 → `[]`.
       - Test E: `resolve` 가 throw → fallback 정상.
       - Test F: 기존 28 tests 회귀 확인 (fiber 필드 없이 ladder 호출).

    TDD 순서: spec 실패 테스트 → RED 커밋 → resolver 수정 → GREEN 커밋.
  </action>

  <verify>
    <automated>pnpm --filter @agrune/runtime run typecheck && pnpm --filter @agrune/runtime run test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "ladder.fiber" packages/runtime/src/runtime/target-resolver.ts` → 1+ 매치
    - `grep -n "__agrune_identity__" packages/runtime/src/runtime/target-resolver.ts` → 1+ 매치
    - `grep -n "FiberIdentityPath" packages/runtime/src/runtime/target-resolver.ts` → 1+ 매치 (import type)
    - `grep -nE "fiber.+first|fiber-first branch|fiber-first priority" packages/runtime/tests/target-resolver.spec.ts` → 1+ 매치 (describe block)
    - `pnpm --filter @agrune/runtime run test` exit 0 — 기존 28 target-resolver tests + 신규 ≥ 5 tests, 132+ 전체 tests PASS (회귀 없음).
    - `pnpm --filter @agrune/runtime run typecheck` exit 0.
    - `resolveByLadder({ fiber: {path:[...]} })` 가 bridge 없을 때 throw 하지 않음 (Test D 확인).
  </acceptance_criteria>

  <done>
    - fiber branch가 `resolveByLadder` 맨 앞에 배치되어 bridge 존재 시 CSS ladder보다 먼저 동작.
    - bridge 부재/null/throw 모두 fallback으로 처리, 기존 `@agrune/runtime` 132+ 테스트 회귀 없음.
    - TDD RED → GREEN 커밋 기록.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted manifest JSON → runtime resolver | 외부에서 로드된 `FiberIdentityPath` 가 resolver 경로로 흐름. path 값이 악의적이면 bridge.resolve가 임의 DOM 반환 시도 가능 |
| runtime `globalThis.__agrune_identity__` consumer | 이 plan은 read-only consumer — 악의적 코드가 bridge.resolve 를 덮어쓰면 resolver가 잘못된 element를 받음. lock은 Plan 02 책임 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `SelectorLadderSchema.fiber` zod validation | mitigate | zod `z.array(...).min(1).max(8)` + segment shape 검증으로 악성 path (과도한 depth / 잘못된 타입) 거부. `validateManifest` 경로에서 차단 |
| T-13-02 | Info Disclosure | `resolveByLadder` fiber branch 예외 전파 | mitigate | `try { bridge.resolve(...) } catch { /* fallback */ }` — resolve 내부 에러를 상위로 전파하지 않아 page internal 정보 유출 방지 |
| T-13-03 | EoP | `bridge.resolve` 반환 element를 resolver가 무조건 신뢰 | accept | 이 plan은 consumer only. bridge 자체 보안(lock, prod guard)은 Plan 02 `threat_model` 담당. 본 plan은 resolve 결과를 다른 DOM으로 해석하지 않고 그대로 반환만 함 |
| T-13-04 | DoS | `fiber.path.length` 과대 | mitigate | zod `.max(8)` 로 직렬화/순회 비용 상한 고정. RESEARCH Pattern 2 "max 8 levels (deep tree 방어)" 일치 |
| T-13-05 | Tampering | `globalThis.__agrune_identity__` 가 non-object로 재정의되어 `.resolve` 접근 시 throw | mitigate | `typeof bridge.resolve === 'function'` guard + try/catch 로 non-function value 안전 처리 |
</threat_model>

<verification>
## Phase-Level Checks

- `pnpm --filter @agrune/manifest run test` — 48+ tests PASS (기존 43 + 신규).
- `pnpm --filter @agrune/runtime run test` — 132+ tests PASS (기존 132 + 신규 ≥ 5, 회귀 0).
- `pnpm --filter @agrune/core run test` — 29 tests PASS (v3 re-export 경로 영향 없음 확인).
- `pnpm --filter @agrune/mcp run test` — 기존 MCP tool snapshot shape 회귀 없음 (PageSnapshot v3 selector 필드 타입 변화 없음 — optional 필드 추가이므로).
- `pnpm typecheck` workspace 전체 통과.
- `grep -rn "SelectorLadder must define at least one of:" packages/ --include='*.ts'` 결과 메시지가 전부 새 문자열로 갱신.
</verification>

<success_criteria>
- [ ] `FiberIdentityPath` / `FiberPathSegment` 타입이 `@agrune/manifest` barrel에서 export.
- [ ] `SelectorLadder` TS 타입 + zod schema 모두 `fiber` 필드 허용.
- [ ] 기존 manifest(5 필드만 사용) 전부 회귀 없이 유효.
- [ ] `resolveByLadder` 가 fiber-first branch로 동작하고 bridge 없을 때 기존 ladder로 fallback.
- [ ] 4개 워크스페이스(`@agrune/manifest`, `@agrune/runtime`, `@agrune/core`, `@agrune/mcp`) typecheck + test 통과.
- [ ] TDD gate: 각 task 별 RED test commit + GREEN impl commit.
</success_criteria>

<output>
After completion, create `.planning/phases/13-react/13-01-SUMMARY.md` using the standard template — include tests added, files modified, TDD gate commit hashes, and note that Plan 02 consumes these types via `@agrune/manifest` barrel import.
</output>
