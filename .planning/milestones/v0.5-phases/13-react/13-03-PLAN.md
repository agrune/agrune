---
phase: "13"
plan: "03"
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - packages/react/tests/fixtures/react17-fixture.tsx
  - packages/react/tests/fixtures/react18-fixture.tsx
  - packages/react/tests/fixtures/react19-fixture.tsx
  - packages/react/tests/fixtures/memo-forwardref.fixture.tsx
  - packages/react/tests/fixtures/portal.fixture.tsx
  - packages/react/tests/fixtures/suspense.fixture.tsx
  - packages/react/tests/fixtures/compound.fixture.tsx
  - packages/react/tests/edge-cases.spec.tsx
  - packages/react/package.json
  - .github/workflows/react-matrix.yml
autonomous: true
requirements: [REACT-05]
tags: [react, matrix-ci, fixtures, memo, forwardref, portal, suspense, compound, integration]

must_haves:
  truths:
    - "React 17 / 18 / 19 세 버전 matrix에서 `@agrune/react` 의 unit test + edge-case fixture test가 모두 통과한다"
    - "memo(forwardRef(Button)) 래핑된 컴포넌트의 `FiberIdentityPath.componentName` 이 내부 `Button` 으로 추출된다 (bippy `getDisplayName` unwrap 확인)"
    - "Portal로 렌더된 DOM 노드의 fiber path가 **DOM 위치가 아닌 fiber tree 논리적 위치** 기반으로 생성되어, portal container 밖에서도 resolve 가능"
    - "Suspense fallback → content 전환 시 FiberIdentityIndex가 unmount(fallback) + mount(content) 순으로 update되어 stale entry 남지 않음"
    - "Compound component (`Modal.Header`, `Select.Option`) 에서 `displayName` 이 명시 설정된 경우 그것을 우선 사용, 없으면 함수명 반환"
    - "GitHub Actions `react-matrix.yml` job이 react ['17.0.2', '18.3.1', '19.2.5'] 3개 branch로 fan-out되어 각각 build+test를 수행한다"
    - "CI matrix 실패 시 어떤 React 버전에서 어떤 테스트가 실패했는지 `name: react matrix (React ${{ matrix.react }})` 로 식별 가능"
  artifacts:
    - path: packages/react/tests/fixtures/react17-fixture.tsx
      provides: "React 17 ReactDOM.render + act 헬퍼 (RTL 의존 없음)"
      contains: "ReactDOM.render"
    - path: packages/react/tests/fixtures/react18-fixture.tsx
      provides: "React 18 createRoot + RTL render 헬퍼"
      contains: "createRoot"
    - path: packages/react/tests/fixtures/react19-fixture.tsx
      provides: "React 19 createRoot + RTL render 헬퍼 (forwardRef 선택적)"
      contains: "createRoot"
    - path: packages/react/tests/fixtures/memo-forwardref.fixture.tsx
      provides: "React.memo(React.forwardRef(Button)) 컴포넌트 + displayName 설정"
      contains: "React.memo"
    - path: packages/react/tests/fixtures/portal.fixture.tsx
      provides: "createPortal 래퍼 + separate container"
      contains: "createPortal"
    - path: packages/react/tests/fixtures/suspense.fixture.tsx
      provides: "Suspense + fallback + lazy content"
      contains: "Suspense"
    - path: packages/react/tests/fixtures/compound.fixture.tsx
      provides: "Modal.Header / Select.Option compound pattern"
      contains: "Modal.Header"
    - path: packages/react/tests/edge-cases.spec.tsx
      provides: "edge-case integration tests — FiberIdentityIndex + bridge 전 체인"
      contains: "describe"
    - path: .github/workflows/react-matrix.yml
      provides: "GitHub Actions matrix job [17.0.2, 18.3.1, 19.2.5]"
      contains: "matrix"
  key_links:
    - from: .github/workflows/react-matrix.yml
      to: packages/react/tests/edge-cases.spec.tsx
      via: "pnpm --filter @agrune/react run test (matrix-overridden react version)"
      pattern: "pnpm.*--filter.*@agrune/react.*test"
    - from: packages/react/tests/edge-cases.spec.tsx
      to: packages/react/src/index.ts
      via: "import { AgruneDevtools, FiberIdentityIndex } from '@agrune/react'"
      pattern: "@agrune/react"

user_setup: []
---

<objective>
React 17 / 18 / 19 세 메이저 버전 matrix CI 와 memo/forwardRef/portal/Suspense/compound 엣지케이스 fixture를 추가해 REACT-05 를 완결한다. Plan 02 가 만든 `@agrune/react` 가 세 React 버전 + 네 가지 특수 컴포넌트 패턴 모두에서 정상 동작함을 CI가 강제하도록 만든다.

Purpose:
- REACT-05: React 17/18/19 matrix CI fixture + memo/forwardRef/portal/Suspense 엣지케이스 전부 pass.
- RESEARCH Open Question #1 해결: React 17 은 RTL 없이 `ReactDOM.render` + `act()` 직접 사용 (RTL 16.x는 React 17 미지원, 13.x와 vitest 조합은 불안정).
- RESEARCH Pattern 3, Pitfall 3 (portal), Pitfall 4 (SSR) 엣지케이스를 CI 회귀 테스트로 고정.

Output:
- `packages/react/tests/fixtures/*.tsx` — 3개 React 버전 렌더 헬퍼 + 4개 엣지케이스 fixture.
- `packages/react/tests/edge-cases.spec.tsx` — FiberIdentityIndex + activateBridge + `_fiberRoots` 실제 동작을 엣지케이스 fixture로 통합 검증 (local React 18 default).
- `.github/workflows/react-matrix.yml` — 3-way matrix build + test job.
- `packages/react/package.json` — scripts (`test:react17`, `test:react18`, `test:react19`) + `@testing-library/react` 16.x (18/19) + 13.x fallback 의존성 (optional) 등록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-react/13-CONTEXT.md
@.planning/phases/13-react/13-RESEARCH.md
@.planning/phases/13-react/13-01-PLAN.md
@.planning/phases/13-react/13-02-PLAN.md

@.github/workflows/ci.yml
@packages/react/package.json
@packages/react/src/index.ts
@packages/react/src/fiber/identity-index.ts
@packages/react/src/components/AgruneDevtools.tsx

<interfaces>
<!-- Key types from Plans 01 + 02 (already in place when Plan 03 runs). -->

From @agrune/react (Plan 02 barrel):
```typescript
export { AgruneDevtools } from './components/AgruneDevtools.js'
export { FiberIdentityIndex } from './fiber/identity-index.js'
export { isProdEnabled } from './guard/prod-guard.js'
export { waitForHydration } from './guard/ssr-barrier.js'
export type { AgruneDevtoolsProps, AgruneIdentityBridge } from '...'
export type { FiberIdentityPath, FiberPathSegment } from '@agrune/manifest'
```

Matrix versions (VERIFIED 2026-04-19):
- React 17.0.2 (latest 17.x)
- React 18.3.1 (latest 18.x)
- React 19.2.5 (latest 19.x)
- @testing-library/react 16.3.2 (React 18/19)
- @testing-library/react 13.4.0 (React 17 — vitest compat uncertain, fallback plan: skip RTL for 17)

CI job name pattern (from .github/workflows/ci.yml):
- Existing jobs: `build-test` (build+typecheck+unit+lint:annotations), `e2e` (playwright)
- New job: `react-matrix` — fan-out strategy, independent of `build-test`
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: React 17/18/19 렌더 fixture + memo/forwardRef/portal/Suspense/compound 엣지케이스 fixture + edge-cases spec</name>
  <files>
    packages/react/tests/fixtures/react17-fixture.tsx,
    packages/react/tests/fixtures/react18-fixture.tsx,
    packages/react/tests/fixtures/react19-fixture.tsx,
    packages/react/tests/fixtures/memo-forwardref.fixture.tsx,
    packages/react/tests/fixtures/portal.fixture.tsx,
    packages/react/tests/fixtures/suspense.fixture.tsx,
    packages/react/tests/fixtures/compound.fixture.tsx,
    packages/react/tests/edge-cases.spec.tsx,
    packages/react/package.json
  </files>

  <read_first>
    packages/react/src/index.ts (Plan 02 barrel),
    packages/react/src/fiber/identity-index.ts (Plan 02 FiberIdentityIndex API),
    packages/react/src/components/AgruneDevtools.tsx (Plan 02 컴포넌트),
    .planning/phases/13-react/13-RESEARCH.md (§Pattern 3, §Pitfall 2, §Pitfall 3, Open Question #1),
    packages/react/package.json (현 devDependencies — React 18 default)
  </read_first>

  <action>
    **1. 버전별 렌더 헬퍼 (Open Q #1 대응):**

    `tests/fixtures/react17-fixture.tsx` — ReactDOM.render + act (RTL 불사용):
    ```tsx
    import { createElement, type ReactElement } from 'react'
    // React 17: default import ReactDOM, act from 'react-dom/test-utils'
    import ReactDOM from 'react-dom'
    // @ts-expect-error React 17 test-utils shape
    import { act } from 'react-dom/test-utils'

    export function renderReact17(ui: ReactElement, container: HTMLElement): () => void {
      act(() => {
        ReactDOM.render(ui, container)
      })
      return () => {
        act(() => {
          ReactDOM.unmountComponentAtNode(container)
        })
      }
    }
    ```
    주의: React 18/19 로 build하면 `react-dom/test-utils` + `ReactDOM.render` 경로가 deprecated/removed. 이 fixture는 **React 17 matrix job에서만 import되는 파일** — React 18/19 job에서는 `react17-fixture.spec.tsx` 자체를 skip (`if (React.version.startsWith('17'))` runtime guard).

    `tests/fixtures/react18-fixture.tsx`:
    ```tsx
    import { createRoot, type Root } from 'react-dom/client'
    import type { ReactElement } from 'react'
    import { act } from 'react'

    export function renderReact18(ui: ReactElement, container: HTMLElement): { unmount: () => void } {
      let root: Root
      act(() => {
        root = createRoot(container)
        root.render(ui)
      })
      return {
        unmount() {
          act(() => root.unmount())
        },
      }
    }
    ```

    `tests/fixtures/react19-fixture.tsx` — 구조 React 18 과 동일하나 주석으로 "React 19는 forwardRef 불필요, ref를 prop으로 전달 가능" 표시.
    ```tsx
    import { createRoot } from 'react-dom/client'
    // React 19: same createRoot API; forwardRef wrapper가 선택사항 (ref가 일반 prop)
    export { renderReact18 as renderReact19 } from './react18-fixture.js'
    ```

    **2. 엣지케이스 fixture 컴포넌트 4개:**

    `tests/fixtures/memo-forwardref.fixture.tsx`:
    ```tsx
    import { memo, forwardRef, type ReactElement, type Ref } from 'react'

    interface ButtonProps { label: string }
    const ButtonInner = forwardRef<HTMLButtonElement, ButtonProps>(function ButtonInner(
      { label }, ref,
    ) {
      return <button ref={ref} data-testid="memo-btn">{label}</button>
    })
    // displayName 명시 — bippy getDisplayName이 inner 이름 추출하는지 검증
    ButtonInner.displayName = 'Button'

    export const MemoButton = memo(ButtonInner)
    MemoButton.displayName = 'Button'  // memo 래퍼에도 설정 (React 19 호환)

    export function MemoForwardRefApp(): ReactElement {
      return <div><MemoButton label="Click me" /></div>
    }
    ```

    `tests/fixtures/portal.fixture.tsx`:
    ```tsx
    import { useEffect, useState, type ReactElement, type ReactNode } from 'react'
    import { createPortal } from 'react-dom'

    function Modal({ children }: { children: ReactNode }): ReactElement | null {
      const [container, setContainer] = useState<HTMLElement | null>(null)
      useEffect(() => {
        const el = document.createElement('div')
        el.id = 'portal-root'
        document.body.appendChild(el)
        setContainer(el)
        return () => { document.body.removeChild(el) }
      }, [])
      if (!container) return null
      return createPortal(
        <div data-testid="portal-content">{children}</div>,
        container,
      )
    }

    export function PortalApp(): ReactElement {
      return (
        <div>
          <header data-testid="header">In tree</header>
          <Modal>
            <button data-testid="portal-btn">Close</button>
          </Modal>
        </div>
      )
    }
    ```

    `tests/fixtures/suspense.fixture.tsx`:
    ```tsx
    import { Suspense, lazy, type ReactElement } from 'react'

    const LazyContent = lazy(() =>
      Promise.resolve({
        default: function Content() {
          return <div data-testid="suspense-content">Loaded</div>
        },
      }),
    )

    export function SuspenseApp(): ReactElement {
      return (
        <Suspense fallback={<div data-testid="suspense-fallback">Loading</div>}>
          <LazyContent />
        </Suspense>
      )
    }
    ```

    `tests/fixtures/compound.fixture.tsx`:
    ```tsx
    import type { ReactElement, ReactNode } from 'react'

    function ModalHeader({ children }: { children: ReactNode }): ReactElement {
      return <header data-testid="modal-header">{children}</header>
    }
    ModalHeader.displayName = 'Modal.Header'

    function ModalBody({ children }: { children: ReactNode }): ReactElement {
      return <div data-testid="modal-body">{children}</div>
    }
    ModalBody.displayName = 'Modal.Body'

    export const Modal = Object.assign(
      function ModalRoot({ children }: { children: ReactNode }) {
        return <div data-testid="modal-root">{children}</div>
      },
      { Header: ModalHeader, Body: ModalBody },
    )

    export function CompoundApp(): ReactElement {
      return (
        <Modal>
          <Modal.Header>Title</Modal.Header>
          <Modal.Body>Content</Modal.Body>
        </Modal>
      )
    }
    ```

    **3. `tests/edge-cases.spec.tsx` — 통합 테스트 (React 18 default, matrix가 version 교체):**

    `describe('memo + forwardRef unwrapping')`:
    - render MemoForwardRefApp → bippy `instrument` 활성화 → FiberIdentityIndex 에 `data-testid="memo-btn"` element 등록.
    - `getByPath` 에 `[{componentName: 'Button', key: null, index: 0}, ...]` path 로 resolve → element 반환.
    - **assertion:** component path segment 중 `componentName === 'Button'` 매치 존재 (memo/forwardRef 래퍼 이름 아님).

    `describe('Portal — fiber tree 위치 기반')`:
    - render PortalApp → Modal 내부 portal 렌더.
    - portal 안 button의 fiber path에 `Modal` 컴포넌트 포함 확인 (DOM 위치는 portal-root 밖이지만 fiber tree는 Modal 아래).
    - path의 마지막 segment가 root 방향이고 중간에 `Modal` 가 존재함을 assertion.

    `describe('Suspense — fallback→content 전환')`:
    - render SuspenseApp → 초기에 fallback fiber.
    - `await waitFor(() => screen.getByTestId('suspense-content'))` → content 로드 후 fiber commit.
    - FiberIdentityIndex 에 fallback element가 남아있지 않음 확인 (`getByPath` 로 fallback path 조회 → null).

    `describe('Compound component displayName')`:
    - render CompoundApp → Modal.Header / Modal.Body 두 sub-컴포넌트.
    - 각 component의 fiber path에 `componentName === 'Modal.Header'` / `'Modal.Body'` segment 존재 확인.

    `describe('React version matrix smoke')`:
    - `expect(React.version).toMatch(/^(17|18|19)\./)` 확인 — matrix 가 실제로 override 됐는지 보증.
    - `const major = React.version.split('.')[0]`. Test가 `major === '17'` 일 때 `ReactDOM.render` 경로 사용, 그 외는 `createRoot`.

    테스트 보조:
    - 각 테스트 top-level 에서 `render(<AgruneDevtools manifest={{version:3,groups:[]}} mode="dev" />)` 를 함께 render 해 bippy instrument + bridge 활성화.
    - `await waitFor(() => expect(window.__agrune_identity__).toBeDefined())` 로 bridge 게시 대기.
    - `afterEach`: `cleanup()` + `delete (window as any).__agrune_identity__` 시도 (configurable:false 때문에 실패할 수 있음 — 이 경우 **spec 파일 전체를 순차 실행**하거나 vitest `poolOptions.threads.singleThread: true` + file-level isolation).
    - vitest isolation: `vitest.config.ts` 에 `test.isolate: true` (기본값) + `test.pool: 'forks'` (fork 격리) 설정. 이미 Plan 02 vitest.config.ts에 `environment: 'jsdom'` 포함 — `test.pool: 'forks'` 추가.

    **4. package.json scripts 추가:**
    ```json
    "scripts": {
      "build": "tsup",
      "typecheck": "tsc --noEmit -p tsconfig.json",
      "test": "vitest run",
      "test:react17": "vitest run --pool forks --poolOptions.forks.singleFork",
      "test:react18": "vitest run",
      "test:react19": "vitest run"
    }
    ```

    **주의 React 17 compatibility:**
    - React 17 에서는 `react/jsx-runtime` 이 존재하지 않아 tsconfig `jsx: 'react-jsx'` 빌드가 실패 — matrix CI가 React 17 package를 override하기 전에 본 패키지의 `jsx-runtime` 참조가 문제. 대응:
      - `src/*.tsx` 는 `import { createElement } from 'react'` 로 작성하지 말고 `jsx: 'react-jsx'` 유지 (최신 React 18 기준). 그러나 test 파일은 matrix 에서 React 17로 실행될 때 compile 에러 위험.
      - **해결:** `tsconfig.json` 에 `"jsx": "react-jsx"` 유지 + matrix CI 의 React 17 job 에서 tsconfig override 또는 `@vitejs/plugin-react` 사용 + `babel-preset-react` 설정. 하지만 vitest는 esbuild 기본 — esbuild의 `jsx: "automatic"` 은 React 17.0.0+ 지원 (`react/jsx-runtime` 필요). React 17.0.2 는 `jsx-runtime` 포함 확인됨.
      - **실행자에게 남기는 세부 재량:** 만약 matrix 돌릴 때 jsx-runtime 문제 발견되면 `vitest.config.ts` 에 `esbuild: { jsx: 'automatic', jsxImportSource: 'react' }` 명시. 이후 React 17 에서도 동작.

    **5. `@testing-library/react` 버전 처리:**
    - Plan 02에서 16.3.2 (React 18/19용) 설치됨.
    - React 17 matrix: RTL 13.x 설치는 **CI workflow에서 on-the-fly override** (Task 2) — package.json 레벨은 18/19 기준 유지.
    - 또는 edge-cases.spec.tsx 에서 RTL 사용하지 않고 Task 1 React 17 헬퍼로 우회 → **이 경로 채택**. RTL dependency를 React 17 job에 추가하지 않아도 됨.

  </action>

  <verify>
    <automated>pnpm --filter @agrune/react run typecheck && pnpm --filter @agrune/react run test</automated>
  </verify>

  <acceptance_criteria>
    - `test -f packages/react/tests/fixtures/react17-fixture.tsx && test -f packages/react/tests/fixtures/react18-fixture.tsx && test -f packages/react/tests/fixtures/react19-fixture.tsx`
    - `test -f packages/react/tests/fixtures/memo-forwardref.fixture.tsx && test -f packages/react/tests/fixtures/portal.fixture.tsx && test -f packages/react/tests/fixtures/suspense.fixture.tsx && test -f packages/react/tests/fixtures/compound.fixture.tsx`
    - `grep -n "React.memo\|memo(" packages/react/tests/fixtures/memo-forwardref.fixture.tsx` → 1+ 매치
    - `grep -n "createPortal" packages/react/tests/fixtures/portal.fixture.tsx` → 1+ 매치
    - `grep -n "Suspense\|lazy" packages/react/tests/fixtures/suspense.fixture.tsx` → 2+ 매치
    - `grep -n "Modal.Header\|displayName" packages/react/tests/fixtures/compound.fixture.tsx` → 2+ 매치
    - `grep -nE "describe\\(.*(memo|Portal|Suspense|Compound|matrix)" packages/react/tests/edge-cases.spec.tsx` → 5+ 매치 (5 describe block)
    - `pnpm --filter @agrune/react run test` exit 0 — Plan 02 tests (26) + 새 edge-case tests (≥ 8) = 34+ PASS (React 18 default)
    - `pnpm --filter @agrune/react run typecheck` exit 0
    - `grep -n "React.version" packages/react/tests/edge-cases.spec.tsx` → 1+ 매치 (matrix smoke test)
  </acceptance_criteria>

  <done>
    - 3개 버전 렌더 헬퍼 + 4개 엣지케이스 fixture 파일 생성.
    - `edge-cases.spec.tsx` 가 `@agrune/react` barrel import 로 실제 pipeline (AgruneDevtools mount → bippy instrument → bridge 활성 → FiberIdentityIndex.getByPath) 을 통합 테스트.
    - React 18 default에서 34+ tests PASS.
    - package.json scripts가 test:react17/18/19 분기 준비.
  </done>
</task>

<task type="auto">
  <name>Task 2: .github/workflows/react-matrix.yml — React 17/18/19 matrix CI job</name>
  <files>
    .github/workflows/react-matrix.yml
  </files>

  <read_first>
    .github/workflows/ci.yml (기존 node setup, pnpm setup pattern),
    .planning/phases/13-react/13-RESEARCH.md (§"React 17/18/19 matrix CI 설계" 섹션 코드 예제),
    packages/react/package.json (Task 1 scripts)
  </read_first>

  <action>
    **`.github/workflows/react-matrix.yml` — 신규 workflow:**

    ```yaml
    name: React Matrix

    on:
      pull_request:
        branches: [main]
        paths:
          - 'packages/react/**'
          - 'packages/manifest/**'
          - '.github/workflows/react-matrix.yml'
      push:
        branches: [main]
        paths:
          - 'packages/react/**'
          - 'packages/manifest/**'
          - '.github/workflows/react-matrix.yml'

    concurrency:
      group: react-matrix-${{ github.ref }}
      cancel-in-progress: true

    jobs:
      react-matrix:
        name: react matrix (React ${{ matrix.react }})
        runs-on: ubuntu-latest
        timeout-minutes: 15
        strategy:
          fail-fast: false
          matrix:
            react:
              - '17.0.2'
              - '18.3.1'
              - '19.2.5'
            include:
              - react: '17.0.2'
                types-react: '17'
              - react: '18.3.1'
                types-react: '18'
              - react: '19.2.5'
                types-react: '19'
        steps:
          - uses: actions/checkout@v4

          - uses: pnpm/action-setup@v4

          - uses: actions/setup-node@v4
            with:
              node-version: 22
              cache: pnpm

          - name: Install dependencies
            run: pnpm install --frozen-lockfile

          - name: Build upstream workspace packages
            run: |
              pnpm --filter @agrune/manifest run build
              pnpm --filter @agrune/react run build

          - name: Override React version to ${{ matrix.react }}
            run: |
              pnpm --filter @agrune/react add -D \
                react@${{ matrix.react }} \
                react-dom@${{ matrix.react }} \
                @types/react@^${{ matrix.types-react }}.0.0 \
                @types/react-dom@^${{ matrix.types-react }}.0.0 \
                --ignore-workspace-root-check --no-save

          - name: Typecheck @agrune/react with React ${{ matrix.react }}
            run: pnpm --filter @agrune/react run typecheck

          - name: Test @agrune/react with React ${{ matrix.react }}
            run: pnpm --filter @agrune/react run test

          - name: Upload test results on failure
            if: failure()
            uses: actions/upload-artifact@v4
            with:
              name: react-matrix-${{ matrix.react }}-failure
              path: packages/react/test-results/
              retention-days: 7
              if-no-files-found: ignore
    ```

    **핵심 결정:**
    - `paths` 필터: `packages/react/**` 또는 `packages/manifest/**` 변경 시만 트리거 — 불필요한 matrix 비용 방지.
    - `fail-fast: false`: 한 버전 실패해도 나머지 버전 결과 확인 가능.
    - `--no-save`: matrix override가 package.json / lockfile 을 변경하지 않도록 (commit dirty 방지, CI 격리 보장).
    - `--ignore-workspace-root-check`: pnpm이 workspace root가 아닌 하위 패키지에서 add 를 허용하도록.
    - React 17 job 에서 `@testing-library/react` 는 devDep 그대로 16.x 유지 — edge-cases.spec.tsx 가 RTL 의존 없이 fixture 헬퍼 사용하도록 Task 1에서 설계했으므로 override 불필요.
    - 만약 React 17 job 에서 jsx-runtime 호환 문제 발생 시 Task 1의 `vitest.config.ts` esbuild 옵션으로 해결 (RESEARCH Open Q 주의사항).

    **CI 연계:** `ci.yml` 의 `build-test` 와 독립적으로 실행. `react-matrix.yml` 성공을 main branch protection required check 로 설정할지는 PR 레벨 결정 (이 plan 범위 밖 — STATE blockers "branch-protection required-check 토글 — 레포 외부 설정" 이미 pending).
  </action>

  <verify>
    <automated>test -f .github/workflows/react-matrix.yml && grep -c "matrix" .github/workflows/react-matrix.yml | awk '$1 >= 3'</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .github/workflows/react-matrix.yml` → exit 0
    - `grep -n "react matrix (React \${{ matrix.react }})" .github/workflows/react-matrix.yml` → 1 매치 (job name)
    - `grep -cE "'17\\.0\\.2'|'18\\.3\\.1'|'19\\.2\\.5'" .github/workflows/react-matrix.yml` → 3 매치 (세 버전 모두 존재)
    - `grep -n "fail-fast: false" .github/workflows/react-matrix.yml` → 1 매치
    - `grep -n "paths:" .github/workflows/react-matrix.yml` → 2+ 매치 (pull_request + push)
    - `grep -n "pnpm --filter @agrune/react run test" .github/workflows/react-matrix.yml` → 1 매치
    - `grep -n "\\-\\-no-save" .github/workflows/react-matrix.yml` → 1 매치 (lockfile dirty 방지)
    - Local `gh workflow run react-matrix --ref HEAD -f dryrun=true` 또는 `actionlint .github/workflows/react-matrix.yml` (lint 도구 있으면) PASS. 없으면 YAML syntax check `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/react-matrix.yml'))"`.
  </acceptance_criteria>

  <done>
    - `.github/workflows/react-matrix.yml` 파일 존재.
    - 3개 React 버전 matrix 구성 + paths 필터 + fail-fast false + no-save override 전부 포함.
    - YAML lint / syntax check 통과.
    - 이 workflow가 main branch push + PR 트리거에 자동으로 활성화 (실제 첫 실행은 커밋 후 GitHub Actions 로그에서 확인 — 본 plan 범위 밖 post-merge observation).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → pnpm registry | React/RTL version override 가 pnpm registry에서 signed package만 받음. `--frozen-lockfile` 은 초기 install에만 적용, override는 `--no-save` 로 lockfile 불변 |
| CI artifact → repository | 실패 시 test-results upload — 7일 retention. 민감 정보 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-20 | Tampering | matrix job 이 package.json / lockfile 오염 | mitigate | `pnpm add --no-save` 로 파일 변경 없음. install은 `--frozen-lockfile`. 두 패턴이 override 와 lockfile 불변 양립 |
| T-13-21 | DoS | matrix 3 fan-out * 전체 workspace build 로 CI 시간 폭증 | mitigate | `paths` 필터로 관련 파일 변경 시만 트리거. matrix 안에서는 `pnpm --filter @agrune/manifest --filter @agrune/react run build` 만 실행 — 전체 workspace build 회피 |
| T-13-22 | Info Disclosure | 실패 아티팩트에 env/secret 포함 | accept | test-results 디렉토리는 Playwright trace 용 — 이 plan의 vitest은 test-results 미생성. `if-no-files-found: ignore` 로 상시 no-op |
</threat_model>

<verification>
## Phase-Level Checks

- `pnpm --filter @agrune/react run test` exit 0 (local default React 18) — 34+ tests PASS
- `pnpm --filter @agrune/react run typecheck` exit 0
- `pnpm --filter @agrune/react run build` exit 0
- `test -f .github/workflows/react-matrix.yml`
- YAML syntax valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/react-matrix.yml'))"`
- `grep -rn "describe.*memo\|describe.*Portal\|describe.*Suspense\|describe.*Compound" packages/react/tests/edge-cases.spec.tsx` → 4+ 매치 (4 엣지케이스 커버)
- 전체 workspace 회귀: `pnpm typecheck && pnpm test` PASS (Plan 01+02 영향 없음 확인)
</verification>

<success_criteria>
- [ ] React 17/18/19 렌더 헬퍼 fixture 3개 존재.
- [ ] memo/forwardRef/portal/Suspense/compound 엣지케이스 fixture 4개 존재.
- [ ] `edge-cases.spec.tsx` 가 `@agrune/react` barrel에서 AgruneDevtools + FiberIdentityIndex + activateBridge 통합 동작을 검증.
- [ ] React 18 default 로컬 실행 시 Plan 02 (26) + edge-cases (8+) = 34+ tests PASS.
- [ ] `.github/workflows/react-matrix.yml` 가 fail-fast false + 3-way matrix + no-save override + paths 필터 전부 포함.
- [ ] Plan 01/02 회귀 없음 (@agrune/manifest, @agrune/runtime, @agrune/core, @agrune/mcp 영향 없음).
- [ ] REACT-05 (React 17/18/19 matrix CI + edge-cases fixture pass) 요건 전체 충족.
</success_criteria>

<output>
After completion, create `.planning/phases/13-react/13-03-SUMMARY.md` using the standard template — include:
- 7 fixture files + edge-cases.spec.tsx breakdown (test count per edge case).
- react-matrix.yml YAML syntax validation proof.
- Confirmation that React 18 default local run produces 34+ PASS.
- Note: Actual React 17 / 19 CI runs happen on first push after merge — this plan only configures the workflow, not the first execution observation. Add a ROADMAP followup item if matrix fails on first run.
- If esbuild jsx-runtime tweak was needed for React 17 compatibility, record the exact `vitest.config.ts` diff applied.
</output>
