---
phase: 15-repeat
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/manifest/src/schema.ts
  - packages/manifest/tests/builders.spec.ts
  - packages/manifest/tests/schema.spec.ts
  - packages/core/src/index.ts
  - packages/core/tests/native-messages.spec.ts
autonomous: true
requirements:
  - REPEAT-01
  - REPEAT-02
  - REPEAT-03

user_setup: []

must_haves:
  truths:
    - "Author가 `defineRepeat({ ..., containerSelector: { css: '.post-row' } })` 를 TypeScript에서 작성하면 `containerSelector`가 타입 레벨에서 optional SelectorLadder로 검증된다"
    - "ManifestRepeat 타입에 `containerSelector?: SelectorLadder` 필드가 존재한다"
    - "PageTarget 타입에 `repeatInstance?: { repeatId, index, key }` 필드가 존재한다"
    - "PageSnapshotGroup 타입에 `repeats?: Array<{ repeatId, strategy, instanceCount, logicalSize }>` 필드가 존재한다"
    - "COMMAND_ERROR_CODES 배열에 `REPEAT_INDEX_OUT_OF_RANGE` 코드가 존재한다"
    - "RepeatSchema zod가 containerSelector를 optional SelectorLadderSchema로 검증한다"
  artifacts:
    - path: "packages/manifest/src/schema.ts"
      provides: "ManifestRepeat.containerSelector optional + RepeatSchema zod 확장"
      contains: "containerSelector"
    - path: "packages/core/src/index.ts"
      provides: "PageTarget.repeatInstance + PageSnapshotGroup.repeats + REPEAT_INDEX_OUT_OF_RANGE"
      contains: "repeatInstance"
    - path: "packages/manifest/tests/schema.spec.ts"
      provides: "containerSelector 타입 + 런타임 zod 테스트"
      exports: []
  key_links:
    - from: "packages/manifest/src/schema.ts (ManifestRepeat)"
      to: "packages/runtime/src/runtime/snapshot.ts (Plan 15-02)"
      via: "RepeatExpander가 containerSelector를 소비"
      pattern: "containerSelector"
    - from: "packages/core/src/index.ts (PageTarget/PageSnapshotGroup)"
      to: "packages/runtime/src/runtime/snapshot.ts (captureTarget, makeSnapshot)"
      via: "snapshot builder가 repeatInstance / group.repeats 필드를 emit"
      pattern: "repeatInstance|repeats:"
---

<objective>
Phase 15의 타입 계약을 닫는다. `@agrune/manifest`의 `ManifestRepeat` 스키마에 **Decision A**(containerSelector) 필드를 추가하고, `@agrune/core`의 `PageTarget`/`PageSnapshotGroup`/`COMMAND_ERROR_CODES`를 repeat 소비에 필요한 필드·에러 코드로 확장한다. 이 plan은 **순수 타입 + zod + 에러코드 확장**만 수행한다 — 런타임 동작 변경 없음. Plan 15-02/15-03가 이 타입을 consume한다.

Purpose: 병렬화는 불가능(2개 패키지의 타입이 서로 독립이지만 15-02/15-03의 의존 기반). 모든 field를 additive optional로 추가해 기존 132개 runtime 테스트가 그대로 통과되게 한다 — breaking change 없음.
Output: `ManifestRepeat.containerSelector?: SelectorLadder`, `PageTarget.repeatInstance?: {...}`, `PageSnapshotGroup.repeats?: [...]`, `REPEAT_INDEX_OUT_OF_RANGE` 에러 코드, zod 스키마 확장, 음성 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-repeat/15-CONTEXT.md
@.planning/phases/15-repeat/15-RESEARCH.md
@.planning/phases/11-manifest/11-01-SUMMARY.md
@.planning/phases/12-inject/12-01-SUMMARY.md

<interfaces>
<!-- 기존 코드 핵심 계약 — executor는 codebase exploration 없이 이 계약을 그대로 사용 -->

From packages/manifest/src/schema.ts (기존, 현재 상태):
```typescript
export type SelectorLadder = AtLeastOne<{
  role: { name: string; level?: string }
  text: string
  testId: string
  attr: string
  css: string
  fiber: { path: FiberIdentityPath }
}>

export interface ManifestRepeat {
  repeatId: string
  template: string        // "post_${key}" 형태
  keyFrom: string         // "el.dataset.postId" 형태
  nameFrom?: string
  strategy: 'dom' | 'virtualized'
  targets: ManifestTarget[]
  // Phase 15-01 추가: containerSelector?: SelectorLadder
}

export const SelectorLadderSchema = z.object({
  role: z.object({ name: z.string(), level: z.string().optional() }).optional(),
  text: z.string().optional(),
  testId: z.string().optional(),
  attr: z.string().optional(),
  css: z.string().optional(),
  fiber: z.object({ path: z.array(...).min(1).max(8) }).optional(),
}).refine((v) => Boolean(v.role || v.text || v.testId || v.attr || v.css || v.fiber), ...)

export const RepeatSchema = z.object({
  repeatId: z.string().min(1),
  template: z.string(),
  keyFrom: z.string(),
  nameFrom: z.string().optional(),
  strategy: z.enum(['dom', 'virtualized']),
  targets: z.array(TargetSchema),
  // Phase 15-01 추가: containerSelector: SelectorLadderSchema.optional()
})
```

From packages/core/src/index.ts (기존):
```typescript
export const COMMAND_ERROR_CODES = [
  'STALE_SNAPSHOT', 'TARGET_NOT_FOUND', 'NOT_VISIBLE', 'DISABLED',
  'FLOW_BLOCKED', 'TIMEOUT', 'SESSION_NOT_ACTIVE', 'AGENT_STOPPED',
  'INVALID_TARGET', 'INVALID_COMMAND', 'INVALID_MANIFEST',
  'MACRO_NOT_FOUND', 'MACRO_CIRCUIT_OPEN',
  'MACRO_PRECONDITION_FAILED', 'MACRO_POSTCONDITION_FAILED',
  'CANVAS_PAN_FAILED', 'CONNECTION_LOST', 'CHROME_CRASHED',
  'RECOVERY_FAILED', 'TAB_NOT_FOUND',
] as const
// Phase 15-01 추가: 'REPEAT_INDEX_OUT_OF_RANGE' (Phase 14-03 직후 새 블록)

export interface PageSnapshotGroup {
  groupId: string
  groupName?: string
  groupDesc?: string
  targetIds: string[]
  viewportTransform?: ViewportTransform
  meta?: unknown
  // Phase 15-01 추가: repeats?: Array<{...}>
}

export interface PageTarget {
  targetId: string
  groupId: string
  // ... 기존 필드 전부 유지 ...
  sourceFile: string
  sourceLine: number
  sourceColumn: number
  // Phase 15-01 추가: repeatInstance?: { repeatId: string; index: number; key: string }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ManifestRepeat.containerSelector 추가 + zod + 타입 음성 테스트</name>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/manifest/src/schema.ts (전체 — ManifestRepeat 위치 확인)
    - /Users/chenjing/dev/agrune/agrune/packages/manifest/tests/builders.spec.ts (defineRepeat 기존 테스트 패턴 학습)
    - /Users/chenjing/dev/agrune/agrune/packages/manifest/tests/validator.spec.ts (RepeatSchema 기존 테스트 패턴)
    - /Users/chenjing/dev/agrune/agrune/.planning/phases/15-repeat/15-RESEARCH.md (Pattern 1, Design Decision 1 — containerSelector 권장)
  </read_first>
  <files>packages/manifest/src/schema.ts, packages/manifest/tests/schema.spec.ts (신규 OR 기존 validator.spec.ts에 describe 추가)</files>
  <behavior>
    TDD RED 먼저:
    - Test 1: `ManifestRepeat` 타입에 `containerSelector?: SelectorLadder` 필드가 존재한다 — 객체 리터럴로 할당 시 typecheck 통과.
    - Test 2: `containerSelector` 없이 정의해도 타입 통과 (optional).
    - Test 3: `containerSelector: { css: '.post' }` 지정 가능.
    - Test 4: `containerSelector: {}` 는 `@ts-expect-error` — SelectorLadder의 AtLeastOne 강제.
    - Test 5: RepeatSchema zod — `{ ...validRepeat, containerSelector: { css: '.post' } }` parse 성공.
    - Test 6: RepeatSchema zod — `{ ...validRepeat, containerSelector: {} }` parse 실패 ("at least one of" 에러 메시지 포함).
    - Test 7: RepeatSchema zod — `containerSelector` 미지정 시 parse 성공 (optional).
  </behavior>
  <action>
    1. `packages/manifest/src/schema.ts`:
       - `ManifestRepeat` interface에 필드 추가:
         ```typescript
         /** Optional container element selector for row enumeration. If absent, RepeatExpander uses `document` as scope. */
         containerSelector?: SelectorLadder
         ```
         (기존 `targets: ManifestTarget[]` 필드 위, `strategy` 필드 아래에 배치).
       - `RepeatSchema` zod에 대응 필드 추가:
         ```typescript
         containerSelector: SelectorLadderSchema.optional(),
         ```
       - 기존 `SelectorLadderSchema.refine()` (AtLeastOne 강제)가 `containerSelector` 빈 객체도 거부한다는 것 확인.
    2. `packages/manifest/tests/schema.spec.ts` 신규 생성 (또는 기존 validator.spec.ts / builders.spec.ts에 `describe('ManifestRepeat.containerSelector', ...)` 블록 추가). **권장: 신규 파일 `schema.spec.ts`.**
       - Test 1-7 위 behavior 구현.
       - 타입 음성 테스트는 `// @ts-expect-error` 주석 사용 (11-01 SUMMARY `builders.spec.ts` 패턴 재사용).
    3. `defineRepeat` 빌더(`packages/manifest/src/builders.ts`) 는 `ManifestRepeat` 타입을 그대로 받으므로 수정 불필요하나, 이를 **검증**하기 위해 `defineRepeat({ ..., containerSelector: { css: '.post' } })` 테스트 케이스를 `builders.spec.ts`에 1개 추가.
    4. `Decision A` 구현: RepeatExpander 런타임 동작은 Plan 15-02에서 구현되지만, 타입 계약은 이 task에서 확정 — `containerSelector` 없으면 `document` 사용 (이 계약은 schema.ts JSDoc에 명시).
    5. JSDoc에 Phase 15 기원 명시: `/** Phase 15-01 (REPEAT-01): ... */`
  </action>
  <verify>
    <automated>pnpm --filter @agrune/manifest run typecheck && pnpm --filter @agrune/manifest run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "containerSelector" packages/manifest/src/schema.ts` → 2+ matches (interface + zod)
    - `grep -n "containerSelector" packages/manifest/tests/` → 5+ matches (테스트 7개 중 최소 5건)
    - `pnpm --filter @agrune/manifest run typecheck` → exit 0
    - `pnpm --filter @agrune/manifest run test` → 기존 43 + 신규 ≥7 = 50+ pass
    - `pnpm --filter @agrune/manifest run build` → exit 0 (dist 재생성)
  </acceptance_criteria>
  <done>
    ManifestRepeat에 `containerSelector?: SelectorLadder` 필드 추가됨. RepeatSchema zod가 optional SelectorLadderSchema로 검증. 타입 음성 테스트 + zod runtime 테스트 각각 통과. `defineRepeat` 빌더가 containerSelector를 그대로 받아 ManifestRepeat 리턴.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PageTarget.repeatInstance + PageSnapshotGroup.repeats + REPEAT_INDEX_OUT_OF_RANGE</name>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/core/src/index.ts (COMMAND_ERROR_CODES, PageTarget, PageSnapshotGroup, PageSnapshot 위치)
    - /Users/chenjing/dev/agrune/agrune/packages/core/tests/native-messages.spec.ts (PageSnapshot fixture 패턴 — Phase 12-01에서 schemaVersion: 3 추가한 선례)
    - /Users/chenjing/dev/agrune/agrune/.planning/phases/15-repeat/15-RESEARCH.md ("PageTarget additive 확장" + "PageSnapshotGroup additive 확장" 섹션)
  </read_first>
  <files>packages/core/src/index.ts, packages/core/tests/native-messages.spec.ts, packages/core/tests/page-target-repeat.spec.ts (신규)</files>
  <behavior>
    TDD RED 먼저:
    - Test 1: `PageTarget` 타입에 `repeatInstance?: { repeatId: string; index: number; key: string }` 필드가 존재 — 지정 시 typecheck 통과.
    - Test 2: `repeatInstance` 없이 할당도 통과 (optional).
    - Test 3: `PageSnapshotGroup.repeats?: Array<{ repeatId, strategy, instanceCount, logicalSize }>` 지정 가능.
    - Test 4: `logicalSize: null` 허용 (aria-rowcount 없음 시).
    - Test 5: `strategy: 'dom' | 'virtualized'` 외 값은 `@ts-expect-error`.
    - Test 6: `COMMAND_ERROR_CODES.includes('REPEAT_INDEX_OUT_OF_RANGE')` 런타임 테스트.
    - Test 7: `isCommandErrorCode('REPEAT_INDEX_OUT_OF_RANGE')` → true.
    - Test 8: 기존 `PageSnapshot` fixture가 repeatInstance / repeats 필드 없이도 여전히 typecheck 통과 (additive 확인).
  </behavior>
  <action>
    1. `packages/core/src/index.ts`:
       - `COMMAND_ERROR_CODES` 배열에 `'REPEAT_INDEX_OUT_OF_RANGE',  // Phase 15-01 (REPEAT-02)` 추가. 위치: Phase 14-03 블록(`MACRO_*`) 바로 아래, `CANVAS_PAN_FAILED` 바로 위.
       - `PageTarget` interface에 필드 추가:
         ```typescript
         /** Phase 15-01 (REPEAT-03): Repeat instance context — defined only for targets from ManifestRepeat expansion. */
         repeatInstance?: {
           repeatId: string
           index: number
           key: string
         }
         ```
         위치: `sourceColumn: number` 필드 바로 아래.
       - `PageSnapshotGroup` interface에 필드 추가:
         ```typescript
         /** Phase 15-01 (REPEAT-03): Repeat summary — instanceCount는 snapshot에 포함된 가시 인스턴스 수, logicalSize는 aria-rowcount 기반 (null=unknown). */
         repeats?: Array<{
           repeatId: string
           strategy: 'dom' | 'virtualized'
           instanceCount: number
           logicalSize: number | null
         }>
         ```
         위치: `meta?: unknown` 필드 바로 아래.
    2. `packages/core/tests/page-target-repeat.spec.ts` 신규 생성:
       - Test 1-8 구현. `@ts-expect-error` 음성 테스트는 Phase 12-01 `snapshot-v3.spec.ts` 패턴 따름.
       - `COMMAND_ERROR_CODES.includes(...)` + `isCommandErrorCode(...)` 런타임 검증.
    3. `packages/core/tests/native-messages.spec.ts`:
       - 기존 PageSnapshot / PageTarget fixture는 additive 필드 없이도 통과해야 함 — **변경 없이** 테스트 재실행으로 확인.
       - 만약 필수 필드 누락 에러가 난다면 fixture는 수정하지 말고 타입 정의가 optional인지 재확인.
    4. **주의 (D-06)**: 이 에러 코드는 **Decision B** 기반 targetId 파서가 Phase 15-03에서 `REPEAT_INDEX_OUT_OF_RANGE`로 발동 — 여기서는 코드만 추가, 발동 로직 없음.
  </action>
  <verify>
    <automated>pnpm --filter @agrune/core run typecheck && pnpm --filter @agrune/core run test && pnpm --filter @agrune/core run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "REPEAT_INDEX_OUT_OF_RANGE" packages/core/src/index.ts` → 1 match
    - `grep -n "repeatInstance" packages/core/src/index.ts` → 2+ matches (JSDoc + 필드)
    - `grep -n "repeats?:" packages/core/src/index.ts` → 1+ match (PageSnapshotGroup)
    - `grep -c "repeatInstance" packages/core/tests/page-target-repeat.spec.ts` → 5+ matches
    - `pnpm --filter @agrune/core run typecheck` → exit 0
    - `pnpm --filter @agrune/core run test` → 전부 pass (기존 + 신규 ≥8)
    - `pnpm --filter @agrune/core run build` → exit 0
    - `grep -n "repeatInstance" packages/core/tests/native-messages.spec.ts` → 0 matches (fixture 변경 없음)
  </acceptance_criteria>
  <done>
    `@agrune/core`가 `repeatInstance` / `repeats` / `REPEAT_INDEX_OUT_OF_RANGE`를 export. 기존 fixture 변경 없이 타입 확장 — Phase 12-01의 `schemaVersion: 3` 이후 **v3 shape**은 유지되고 additive 필드만 추가. Plan 15-02의 `captureTarget` 확장 + Plan 15-03의 에러 발동이 이 타입을 consume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest author → SelectorLadder schema | `containerSelector` 값은 manifest author가 작성 — MANIFEST-04 trusted boundary 내부 (T-11-21 accept 선례) |
| type-level contract → runtime consumer | PageTarget/PageSnapshotGroup은 타입 export만 — 신뢰 경계는 consumer(runtime/mcp)의 input validation 책임 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | ManifestRepeat.containerSelector (author 조작) | accept | manifest는 author가 자신의 파일을 작성 — Phase 11 T-11-21 accept 선례 재사용. `agrune manifest validate` CLI(15-03)가 schema 레벨 hash class / nth-child 거부 |
| T-15-02 | Info Disclosure | PageTarget.repeatInstance.key 값 로그 노출 | accept | `repeatInstance.key`는 trusted manifest author가 정의한 keyFrom 결과. MCP layer의 `toPublicTarget()`이 selector와 동일하게 필요시 제외 판단 — Plan 15-03 scope |
| T-15-03 | Denial of Service | `containerSelector: { css: ':root' }` 전체 DOM 스캔 | accept | Phase 15-02 RepeatExpander에 `maxInstances` cap 적용 — 이 plan에서는 타입만. cap 구현은 15-02의 책임 (hand-off) |
| T-15-04 | Input Validation | SelectorLadderSchema.refine이 containerSelector 빈 객체 차단 | mitigate | 기존 `SelectorLadderSchema.refine((v) => Boolean(v.role\|\|v.text\|\|...))` 재사용 — additive 없음. Task 1 Test 6으로 검증 |

**ASVS L1 체크**: V5.1 (Input Validation) — zod가 SelectorLadder minimum 1-field 강제. V14 (Build/Config) — Phase 14-03 에러 코드 추가 패턴을 동일하게 따름.
</threat_model>

<verification>
## Plan-Level Verification

```bash
pnpm --filter @agrune/manifest run typecheck
pnpm --filter @agrune/manifest run test
pnpm --filter @agrune/manifest run build
pnpm --filter @agrune/core run typecheck
pnpm --filter @agrune/core run test
pnpm --filter @agrune/core run build

# 다른 패키지 회귀 — 타입 breaking 없는지 확인
pnpm --filter @agrune/runtime run typecheck
pnpm --filter @agrune/devtools run typecheck
pnpm --filter @agrune/mcp run typecheck
```

모든 명령 exit 0 필수.
</verification>

<success_criteria>
- `ManifestRepeat.containerSelector?: SelectorLadder` 타입 노출
- `RepeatSchema` zod가 containerSelector optional SelectorLadderSchema 검증
- `PageTarget.repeatInstance?: { repeatId, index, key }` 타입 노출
- `PageSnapshotGroup.repeats?: Array<{ repeatId, strategy, instanceCount, logicalSize }>` 타입 노출
- `REPEAT_INDEX_OUT_OF_RANGE` in `COMMAND_ERROR_CODES`
- 기존 runtime/devtools/mcp 패키지 typecheck 통과 (breaking 없음)
- 모든 필드는 optional — v3 schemaVersion 유지, 추가 breaking bump 없음
</success_criteria>

<output>
After completion, create `.planning/phases/15-repeat/15-01-SUMMARY.md` using `get-shit-done/templates/summary.md`.
</output>
