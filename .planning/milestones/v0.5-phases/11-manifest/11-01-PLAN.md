---
phase: 11-manifest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/manifest/package.json
  - packages/manifest/tsconfig.json
  - packages/manifest/tsup.config.ts
  - packages/manifest/vitest.config.ts
  - packages/manifest/src/index.ts
  - packages/manifest/src/schema.ts
  - packages/manifest/src/builders.ts
  - packages/manifest/src/validator.ts
  - packages/manifest/tests/builders.spec.ts
  - packages/manifest/tests/validator.spec.ts
  - packages/manifest/tests/schema-types.test-d.ts
  - pnpm-workspace.yaml
autonomous: true
requirements:
  - MANIFEST-01
  - MANIFEST-02
  - MANIFEST-03
  - MANIFEST-04
tags: [manifest, schema, sdk, typescript, zod]

must_haves:
  truths:
    - "Author가 `defineManifest({ version: 3, groups: [defineGroup({ targets: [defineTarget({...})] })], macros: [defineMacro({...})] })` 를 작성하면 컴파일 타임에 `targetId` union, `actionKinds` union, `SelectorLadder` 최소 1필드, `sensitive: false` 금지가 타입 에러로 잡힌다."
    - "`defineRepeat({ repeatId, template, keyFrom, strategy: 'dom' | 'virtualized', targets })` 빌더가 template·keyFrom·nameFrom·strategy 필드를 받고 TS 타입이 strategy union을 강제한다."
    - "`defineMacro({ macroId, params, steps, precondition?, postcondition?, circuitBreaker? })` 빌더가 id·params·steps·pre/postcondition·circuitBreaker 필드를 받는다."
    - "`validateManifest(manifest)` 가 zod로 `version: 3` literal, `sensitive: z.literal(true).optional()`, 빈 SelectorLadder, hash class(`/\\.[a-zA-Z0-9]{8,}(?![a-zA-Z0-9-])/`), `:nth-child(`, `sensitive: false` 를 런타임에 거부한다."
    - "`pnpm --filter @agrune/manifest run test` 가 통과하고 `@agrune/manifest` 가 `defineManifest / defineGroup / defineTarget / defineRepeat / defineMacro / validateManifest / ManifestSchema / SelectorLadder / AgruneManifest / ManifestTarget / ManifestRepeat / ManifestMacro / ManifestGroup / ActionKind / SelectorForbiddenError` 를 export한다."
  artifacts:
    - path: "packages/manifest/package.json"
      provides: "@agrune/manifest 패키지 메타데이터 — name, exports, dependencies(zod), devDependencies(tsup, typescript, vitest)"
      contains: "@agrune/manifest"
    - path: "packages/manifest/src/schema.ts"
      provides: "v3 manifest 타입 (AgruneManifest, ManifestGroup, ManifestTarget, ManifestRepeat, ManifestMacro, MacroStep, SelectorLadder, ActionKind) + zod 스키마 (ManifestSchema, SelectorLadderSchema)"
      exports: ["AgruneManifest", "ManifestGroup", "ManifestTarget", "ManifestRepeat", "ManifestMacro", "MacroStep", "SelectorLadder", "ActionKind", "ManifestSchema", "SelectorLadderSchema", "TargetSchema", "GroupSchema", "MacroSchema", "RepeatSchema"]
    - path: "packages/manifest/src/builders.ts"
      provides: "타입 안전 빌더 함수"
      exports: ["defineManifest", "defineGroup", "defineTarget", "defineRepeat", "defineMacro"]
    - path: "packages/manifest/src/validator.ts"
      provides: "런타임 manifest 검증 함수 + 금지 selector 에러"
      exports: ["validateManifest", "SelectorForbiddenError", "HASH_CLASS_PATTERN", "NTH_CHILD_PATTERN", "assertNoHashClass", "assertNoNthChild"]
    - path: "packages/manifest/src/index.ts"
      provides: "public API barrel"
      exports: ["defineManifest", "defineGroup", "defineTarget", "defineRepeat", "defineMacro", "validateManifest", "ManifestSchema", "SelectorForbiddenError"]
    - path: "packages/manifest/tests/builders.spec.ts"
      provides: "defineTarget/defineRepeat/defineMacro 단위 테스트 + @ts-expect-error로 sensitive:false 타입 차단 테스트"
      min_lines: 80
    - path: "packages/manifest/tests/validator.spec.ts"
      provides: "zod schema 검증 테스트 (sensitive:false 거부, hash class 거부, nth-child 거부, 빈 SelectorLadder 거부)"
      min_lines: 80
  key_links:
    - from: "packages/manifest/src/builders.ts"
      to: "packages/manifest/src/schema.ts"
      via: "type import (ManifestTarget, ManifestGroup, SelectorLadder, ActionKind)"
      pattern: "from ['\"]\\./schema"
    - from: "packages/manifest/src/validator.ts"
      to: "packages/manifest/src/schema.ts"
      via: "ManifestSchema import + zod parse"
      pattern: "ManifestSchema\\.(parse|safeParse)"
    - from: "packages/manifest/src/index.ts"
      to: "packages/manifest/src/builders.ts"
      via: "re-export builders"
      pattern: "export \\{.*define"
---

<objective>
`@agrune/manifest` 패키지를 신설하고 v3 manifest schema (AgruneManifest, ManifestGroup, ManifestTarget, ManifestRepeat, ManifestMacro, SelectorLadder, ActionKind) + 타입 안전 빌더 (`defineManifest`, `defineGroup`, `defineTarget`, `defineRepeat`, `defineMacro`) + zod 기반 런타임 validator 를 제공한다. 이 플랜은 Phase 11의 authoring SDK 뿌리이며 Plans 03/04/05의 타입 소스가 된다.

Purpose: manifest가 모든 것의 뿌리가 된다 — inline `data-agrune-*` 어노테이션 폐기 후 유일한 authoring 경로. 이 플랜이 끝나면 Author가 `.ts` 파일에 `defineManifest` 를 쓰고 TS 컴파일러가 `targetId` union, `actionKinds` 유효성, `sensitive: false` 금지, `SelectorLadder` 최소 1필드를 컴파일 타임에 검증한다. zod validator는 JSON manifest 우회 경로(sensitive:false silent pass — Pitfall 5)를 닫는다.

Output:
- 신규 패키지 `packages/manifest/` (pnpm workspace 자동 탐지)
- 공개 API 4개 builder + 1개 validator + 타입/zod schema export
- 단위 테스트 (vitest) — 타입 음성 테스트(@ts-expect-error) + zod 거부 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-manifest/11-CONTEXT.md
@.planning/phases/11-manifest/11-RESEARCH.md

@packages/core/src/manifest.ts
@packages/core/package.json
@packages/core/tsup.config.ts
@packages/mcp/package.json

<interfaces>
<!-- v2 (현재 @agrune/core) 타입 — 이 플랜에서 교체할 레퍼런스 -->

From packages/core/src/manifest.ts:
```typescript
export type AgruneSupportedAction =
  | 'click' | 'fill' | 'dblclick' | 'contextmenu' | 'hover' | 'longpress'

export interface AgruneTargetEntry {
  targetId: string
  name: string | null
  desc: string | null
  selector: string
  sourceFile: string
  sourceLine: number
  sourceColumn: number
}

export interface AgruneManifest {
  version: 2
  generatedAt: string
  exposureMode: 'grouped' | 'per-element'
  groups: AgruneGroupEntry[]
}
```

<!-- v3 schema 이 플랜에서 신설 -->

Target shape (packages/manifest/src/schema.ts):
```typescript
export type ActionKind = 'click' | 'fill' | 'dblclick' | 'contextmenu' | 'hover' | 'longpress'

export type SelectorLadder = AtLeastOne<{
  role: { name: string; level?: string }
  text: string
  testId: string
  attr: string
  css: string
}>

export interface ManifestTarget {
  targetId: string
  name?: string
  desc?: string
  actionKinds: ActionKind[]
  selector: SelectorLadder
  sensitive?: true  // false 불가 — OR-only lock
}

export interface AgruneManifest {
  version: 3
  groups: ManifestGroup[]
  macros?: ManifestMacro[]
}
```

From packages/mcp/package.json:
```json
"dependencies": { "zod": "^4.3.6" }
"devDependencies": { "tsup": "^8.0.0", "tsx": "^4.0.0", "typescript": "^5.7.0", "vitest": "^4.0.0" }
```

Reference packages/core/tsup.config.ts (템플릿):
```typescript
export default defineConfig({
  entry: { index: 'src/index.ts' },
  format: ['esm'],
  clean: true, dts: true, sourcemap: true,
  target: 'es2022',
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `packages/manifest/` 패키지 초기화 + v3 schema 파일 작성</name>
  <files>
    packages/manifest/package.json,
    packages/manifest/tsconfig.json,
    packages/manifest/tsup.config.ts,
    packages/manifest/vitest.config.ts,
    packages/manifest/src/schema.ts
  </files>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/core/package.json (템플릿)
    - /Users/chenjing/dev/agrune/agrune/packages/core/tsup.config.ts (템플릿)
    - /Users/chenjing/dev/agrune/agrune/packages/core/tsconfig.json (템플릿)
    - /Users/chenjing/dev/agrune/agrune/packages/core/src/manifest.ts (v2 타입, 참고용)
    - /Users/chenjing/dev/agrune/agrune/pnpm-workspace.yaml (workspace 선언 확인)
    - /Users/chenjing/dev/agrune/agrune/.planning/phases/11-manifest/11-RESEARCH.md (섹션 3, 6, 8)
  </read_first>
  <behavior>
    - Test: `import { ManifestSchema } from '@agrune/manifest'` 가 동작.
    - Test: `ManifestSchema.safeParse({ version: 3, groups: [] })` 가 `success: true`.
    - Test: `ManifestSchema.safeParse({ version: 2, groups: [] })` 가 `success: false` (v3 literal).
    - Test: `TargetSchema.safeParse({ targetId: 't', actionKinds: ['click'], selector: { css: 'button' }, sensitive: false })` 가 `success: false` (sensitive:true literal optional).
    - Test: `TargetSchema.safeParse({ targetId: 't', actionKinds: ['click'], selector: {} })` 가 `success: false` (빈 SelectorLadder).
    - Types: `SelectorLadder`, `ManifestTarget`, `ManifestGroup`, `ManifestRepeat`, `ManifestMacro`, `MacroStep`, `AgruneManifest`, `ActionKind` 타입이 export된다.
  </behavior>
  <action>
    **1. `packages/manifest/package.json` 생성** (정확히 이 JSON):
    ```json
    {
      "name": "@agrune/manifest",
      "version": "0.4.1",
      "description": "Type-safe authoring SDK for agrune v3 manifests.",
      "type": "module",
      "main": "./dist/index.js",
      "module": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "types": "./dist/index.d.ts",
          "import": "./dist/index.js"
        }
      },
      "files": ["dist"],
      "scripts": {
        "build": "tsup",
        "typecheck": "tsc --noEmit -p tsconfig.json",
        "test": "vitest run"
      },
      "publishConfig": { "access": "public" },
      "license": "MIT",
      "dependencies": {
        "zod": "^4.3.6"
      },
      "devDependencies": {
        "@types/node": "^24.10.1",
        "tsup": "^8.5.1",
        "typescript": "^5.9.3",
        "vitest": "^4.0.0"
      }
    }
    ```

    **2. `packages/manifest/tsconfig.json`** (정확히):
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "ESNext",
        "moduleResolution": "bundler",
        "declaration": true,
        "declarationMap": true,
        "sourceMap": true,
        "outDir": "dist",
        "rootDir": ".",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "lib": ["ES2022"]
      },
      "include": ["src", "tests", "tsup.config.ts", "vitest.config.ts"],
      "exclude": ["dist"]
    }
    ```

    **3. `packages/manifest/tsup.config.ts`**:
    ```typescript
    import { defineConfig } from 'tsup'

    export default defineConfig({
      entry: { index: 'src/index.ts' },
      format: ['esm'],
      clean: true,
      dts: true,
      sourcemap: true,
      target: 'es2022',
    })
    ```

    **4. `packages/manifest/vitest.config.ts`**:
    ```typescript
    import { defineConfig } from 'vitest/config'

    export default defineConfig({
      test: {
        include: ['tests/**/*.spec.ts'],
        environment: 'node',
      },
    })
    ```

    **5. `packages/manifest/src/schema.ts`** — v3 스키마 구현. 필수 조항:
    - `export type ActionKind = 'click' | 'fill' | 'dblclick' | 'contextmenu' | 'hover' | 'longpress'`
    - `type AtLeastOne<T>` helper: `{ [K in keyof T]: Pick<T, K> & Partial<Omit<T, K>> }[keyof T]`
    - `export type SelectorLadder = AtLeastOne<{ role: { name: string; level?: string }; text: string; testId: string; attr: string; css: string }>`
    - `export interface MacroStep { targetId: string; action: ActionKind; value?: string; sensitive?: true }`
    - `export interface ManifestTarget { targetId: string; name?: string; desc?: string; actionKinds: ActionKind[]; selector: SelectorLadder; sensitive?: true }`
    - `export interface ManifestRepeat { repeatId: string; template: string; keyFrom: string; nameFrom?: string; strategy: 'dom' | 'virtualized'; targets: ManifestTarget[] }`
    - `export interface ManifestMacro { macroId: string; name?: string; desc?: string; params: Record<string, { type: 'string' | 'number' | 'boolean'; required?: boolean }>; steps: MacroStep[]; precondition?: string; postcondition?: string; circuitBreaker?: { maxRetries: number; resetAfterMs?: number } }`
    - `export interface ManifestGroup { groupId: string; name?: string; desc?: string; route?: string; targets: ManifestTarget[]; repeats?: ManifestRepeat[] }` (RegExp은 JSON 직렬화 불가이므로 string만)
    - `export interface AgruneManifest { version: 3; groups: ManifestGroup[]; macros?: ManifestMacro[] }`

    zod 스키마 (같은 파일에 병기):
    ```typescript
    import { z } from 'zod'

    export const ActionKindSchema = z.enum(['click','fill','dblclick','contextmenu','hover','longpress'])

    export const SelectorLadderSchema = z.object({
      role: z.object({ name: z.string(), level: z.string().optional() }).optional(),
      text: z.string().optional(),
      testId: z.string().optional(),
      attr: z.string().optional(),
      css: z.string().optional(),
    }).refine(
      (v) => Boolean(v.role || v.text || v.testId || v.attr || v.css),
      { message: 'SelectorLadder must define at least one of: role, text, testId, attr, css' },
    )

    export const TargetSchema = z.object({
      targetId: z.string().min(1),
      name: z.string().optional(),
      desc: z.string().optional(),
      actionKinds: z.array(ActionKindSchema).min(1),
      selector: SelectorLadderSchema,
      sensitive: z.literal(true).optional(),  // false 차단 — MANIFEST-04 OR-only lock
    })

    export const RepeatSchema = z.object({
      repeatId: z.string().min(1),
      template: z.string(),
      keyFrom: z.string(),
      nameFrom: z.string().optional(),
      strategy: z.enum(['dom','virtualized']),
      targets: z.array(TargetSchema),
    })

    export const MacroStepSchema = z.object({
      targetId: z.string().min(1),
      action: ActionKindSchema,
      value: z.string().optional(),
      sensitive: z.literal(true).optional(),
    })

    export const MacroSchema = z.object({
      macroId: z.string().min(1),
      name: z.string().optional(),
      desc: z.string().optional(),
      params: z.record(z.string(), z.object({
        type: z.enum(['string','number','boolean']),
        required: z.boolean().optional(),
      })),
      steps: z.array(MacroStepSchema).min(1),
      precondition: z.string().optional(),
      postcondition: z.string().optional(),
      circuitBreaker: z.object({
        maxRetries: z.number().int().nonnegative(),
        resetAfterMs: z.number().int().nonnegative().optional(),
      }).optional(),
    })

    export const GroupSchema = z.object({
      groupId: z.string().min(1),
      name: z.string().optional(),
      desc: z.string().optional(),
      route: z.string().optional(),
      targets: z.array(TargetSchema),
      repeats: z.array(RepeatSchema).optional(),
    })

    export const ManifestSchema = z.object({
      version: z.literal(3),
      groups: z.array(GroupSchema),
      macros: z.array(MacroSchema).optional(),
    })
    ```

    **6. `pnpm-workspace.yaml` 확인**: 현재 `packages/*` 패턴이면 수정 불필요. 만약 명시 목록이면 `packages/manifest` 추가. (cat 후 분기)

    **7. 의존성 설치**: `pnpm install` 을 실행해 `@agrune/manifest` 가 workspace에 등록되도록 한다.

    **중요**: `sensitive?: true` 는 `sensitive?: boolean` 와 다르다. `z.literal(true).optional()` 은 `{ sensitive: false }` 를 거부하지만 `z.boolean().optional()` 은 통과시킨다 — Pitfall 5 방어.
  </action>
  <verify>
    <automated>
      cd /Users/chenjing/dev/agrune/agrune &&
      pnpm install &&
      pnpm --filter @agrune/manifest run typecheck &&
      test -f packages/manifest/src/schema.ts &&
      grep -q "version: z.literal(3)" packages/manifest/src/schema.ts &&
      grep -q "sensitive: z.literal(true).optional()" packages/manifest/src/schema.ts &&
      grep -q "SelectorLadder must define at least one" packages/manifest/src/schema.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/manifest/package.json` 성공
    - `test -f packages/manifest/src/schema.ts` 성공
    - `grep -q '"name": "@agrune/manifest"' packages/manifest/package.json` 성공
    - `grep -q '"zod": "\\^4.3.6"' packages/manifest/package.json` 성공
    - `grep -q "version: z.literal(3)" packages/manifest/src/schema.ts` 성공
    - `grep -q "sensitive: z.literal(true).optional()" packages/manifest/src/schema.ts` 성공
    - `grep -q "SelectorLadderSchema" packages/manifest/src/schema.ts` 성공
    - `grep -q "type AtLeastOne" packages/manifest/src/schema.ts` 성공
    - `grep -Eq "export type ActionKind = 'click' \\| 'fill' \\| 'dblclick' \\| 'contextmenu' \\| 'hover' \\| 'longpress'" packages/manifest/src/schema.ts` 성공
    - `grep -q "strategy: z.enum(\\['dom','virtualized'\\])" packages/manifest/src/schema.ts` 성공
    - `grep -q "circuitBreaker" packages/manifest/src/schema.ts` 성공
    - `pnpm --filter @agrune/manifest run typecheck` exit 0
    - `pnpm -w list @agrune/manifest` 가 workspace에 등록된 `@agrune/manifest@0.4.1` 을 보여줌
  </acceptance_criteria>
  <done>
    `packages/manifest/` 신규 패키지가 pnpm workspace에 등록됨. v3 schema (TS 타입 + zod) 가 정의되었고 `sensitive: false` + 빈 SelectorLadder + `version != 3` 이 zod에서 거부됨. `pnpm --filter @agrune/manifest run typecheck` 통과.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 타입 안전 빌더 (`defineManifest`/`defineGroup`/`defineTarget`/`defineRepeat`/`defineMacro`) + `validateManifest` + public barrel</name>
  <files>
    packages/manifest/src/builders.ts,
    packages/manifest/src/validator.ts,
    packages/manifest/src/index.ts,
    packages/manifest/tests/builders.spec.ts,
    packages/manifest/tests/validator.spec.ts
  </files>
  <read_first>
    - /Users/chenjing/dev/agrune/agrune/packages/manifest/src/schema.ts (Task 1 산출)
    - /Users/chenjing/dev/agrune/agrune/.planning/phases/11-manifest/11-RESEARCH.md (섹션 4 regex, 섹션 6 builder)
  </read_first>
  <behavior>
    - Test `defineTarget`: actionKinds/selector 필드 전달 시 그대로 반환, `targetId` 가 제네릭으로 보존.
    - Test `defineTarget` 타입 에러: `@ts-expect-error` 로 `sensitive: false` 시도 시 TS 에러.
    - Test `defineTarget` 타입 에러: `@ts-expect-error` 로 `actionKinds: ['invalid']` 시도 시 TS 에러.
    - Test `defineTarget` 타입 에러: `@ts-expect-error` 로 `selector: {}` (빈) 시도 시 TS 에러.
    - Test `defineGroup`: 반환 객체에 `groupId`, `targets` 가 그대로 담김.
    - Test `defineRepeat`: strategy `'dom' | 'virtualized'` 허용, 그 외 타입 에러.
    - Test `defineMacro`: steps + circuitBreaker 필드 포함.
    - Test `defineManifest`: `{ version: 3, groups: [...] }` 반환, `version` 자동 주입.
    - Test `validateManifest(valid)` → `{ ok: true, manifest }`.
    - Test `validateManifest({ version: 3, groups: [{ groupId: 'g', targets: [{ targetId: 't', actionKinds: ['click'], selector: { css: 'button' }, sensitive: false }] }] })` → `{ ok: false }` + 에러에 "sensitive" 포함.
    - Test `assertNoHashClass('.abc12345xyz')` → `SelectorForbiddenError` throw.
    - Test `assertNoHashClass('.flex.items-center.bg-blue-500')` → pass (Tailwind utility, Pitfall 2).
    - Test `assertNoNthChild('div:nth-child(2)')` → throw.
    - Test `validateManifest` 가 hash class 포함 css selector 를 거부하고 에러 메시지에 target ID 를 포함한다.
  </behavior>
  <action>
    **1. `packages/manifest/src/builders.ts`**:
    ```typescript
    import type {
      ActionKind,
      AgruneManifest,
      ManifestGroup,
      ManifestMacro,
      ManifestRepeat,
      ManifestTarget,
      SelectorLadder,
    } from './schema.js'

    export function defineTarget<TId extends string>(input: {
      targetId: TId
      name?: string
      desc?: string
      actionKinds: ActionKind[]
      selector: SelectorLadder
      sensitive?: true
    }): ManifestTarget & { readonly targetId: TId } {
      return input as ManifestTarget & { readonly targetId: TId }
    }

    export function defineGroup<
      TTargets extends ReadonlyArray<ManifestTarget & { readonly targetId: string }>,
    >(input: {
      groupId: string
      name?: string
      desc?: string
      route?: string
      targets: TTargets
      repeats?: ManifestRepeat[]
    }): ManifestGroup & {
      readonly __targetIds: TTargets[number]['targetId']
    } {
      return input as ManifestGroup & { readonly __targetIds: TTargets[number]['targetId'] }
    }

    export function defineRepeat(input: {
      repeatId: string
      template: string
      keyFrom: string
      nameFrom?: string
      strategy: 'dom' | 'virtualized'
      targets: ManifestTarget[]
    }): ManifestRepeat {
      return input
    }

    export function defineMacro(input: ManifestMacro): ManifestMacro {
      return input
    }

    export function defineManifest<
      TGroups extends ReadonlyArray<ManifestGroup & { __targetIds: string }>,
    >(input: {
      groups: TGroups
      macros?: ManifestMacro[]
    }): AgruneManifest & {
      readonly __targetIds: TGroups[number]['__targetIds']
    } {
      const manifest: AgruneManifest = {
        version: 3,
        groups: input.groups as unknown as ManifestGroup[],
        ...(input.macros ? { macros: input.macros } : {}),
      }
      return manifest as AgruneManifest & { readonly __targetIds: TGroups[number]['__targetIds'] }
    }
    ```

    **2. `packages/manifest/src/validator.ts`**:
    ```typescript
    import { ManifestSchema } from './schema.js'
    import type { AgruneManifest, SelectorLadder } from './schema.js'

    // 해시 class: 순수 alphanumeric 8자 이상, 하이픈 없음 (Pitfall 2 방어 — Tailwind utility 오탐 방지)
    export const HASH_CLASS_PATTERN = /\.[a-zA-Z0-9]{8,}(?![a-zA-Z0-9-])/
    export const NTH_CHILD_PATTERN = /:nth-child\(/

    export class SelectorForbiddenError extends Error {
      constructor(public readonly selector: string, message: string) {
        super(message)
        this.name = 'SelectorForbiddenError'
      }
    }

    export function assertNoHashClass(selector: string): void {
      if (HASH_CLASS_PATTERN.test(selector)) {
        throw new SelectorForbiddenError(
          selector,
          `Selector "${selector}" contains a likely hash-based class. Use role, text, testId, or stable attribute instead.`,
        )
      }
    }

    export function assertNoNthChild(selector: string): void {
      if (NTH_CHILD_PATTERN.test(selector)) {
        throw new SelectorForbiddenError(
          selector,
          `Selector "${selector}" uses :nth-child which is position-dependent. Use a stable identifier instead.`,
        )
      }
    }

    export interface ValidateOk {
      ok: true
      manifest: AgruneManifest
    }
    export interface ValidateFail {
      ok: false
      errors: Array<{ path: string; message: string }>
    }
    export type ValidateResult = ValidateOk | ValidateFail

    function checkLadder(ladder: SelectorLadder, path: string): string[] {
      const errors: string[] = []
      for (const field of ['attr', 'css'] as const) {
        const value = (ladder as Record<string, unknown>)[field]
        if (typeof value === 'string') {
          if (HASH_CLASS_PATTERN.test(value)) {
            errors.push(`${path}.selector.${field}: hash class forbidden ("${value}")`)
          }
          if (NTH_CHILD_PATTERN.test(value)) {
            errors.push(`${path}.selector.${field}: :nth-child forbidden ("${value}")`)
          }
        }
      }
      return errors
    }

    export function validateManifest(input: unknown): ValidateResult {
      const parsed = ManifestSchema.safeParse(input)
      if (!parsed.success) {
        // Walk ZodError issues — detect sensitive:false specifically.
        const errors = parsed.error.issues.map((issue) => ({
          path: issue.path.join('.'),
          message: pathIndicatesSensitiveFalse(issue.path, input)
            ? 'sensitive:false is not allowed. The sensitive flag is OR-only: once detected as sensitive by manifest flag or runtime heuristic, it cannot be overridden to false. Remove the field or set to true.'
            : issue.message,
        }))
        return { ok: false, errors }
      }
      // Additional ladder checks on successfully-parsed manifest.
      const ladderErrors: Array<{ path: string; message: string }> = []
      parsed.data.groups.forEach((group, gi) => {
        group.targets.forEach((target, ti) => {
          const errs = checkLadder(target.selector as SelectorLadder, `groups[${gi}].targets[${ti}] (targetId=${target.targetId})`)
          for (const e of errs) ladderErrors.push({ path: '', message: e })
        })
        group.repeats?.forEach((repeat, ri) => {
          repeat.targets.forEach((target, ti) => {
            const errs = checkLadder(target.selector as SelectorLadder, `groups[${gi}].repeats[${ri}].targets[${ti}] (targetId=${target.targetId})`)
            for (const e of errs) ladderErrors.push({ path: '', message: e })
          })
        })
      })
      if (ladderErrors.length > 0) return { ok: false, errors: ladderErrors }
      return { ok: true, manifest: parsed.data as AgruneManifest }
    }

    function pathIndicatesSensitiveFalse(
      path: readonly (string | number)[],
      root: unknown,
    ): boolean {
      if (path[path.length - 1] !== 'sensitive') return false
      let cur: unknown = root
      for (const key of path) {
        if (cur && typeof cur === 'object') {
          cur = (cur as Record<string | number, unknown>)[key]
        }
      }
      return cur === false
    }
    ```

    **3. `packages/manifest/src/index.ts`**:
    ```typescript
    export {
      defineManifest,
      defineGroup,
      defineTarget,
      defineRepeat,
      defineMacro,
    } from './builders.js'

    export {
      validateManifest,
      SelectorForbiddenError,
      assertNoHashClass,
      assertNoNthChild,
      HASH_CLASS_PATTERN,
      NTH_CHILD_PATTERN,
    } from './validator.js'

    export type {
      ValidateResult,
      ValidateOk,
      ValidateFail,
    } from './validator.js'

    export {
      ManifestSchema,
      SelectorLadderSchema,
      TargetSchema,
      GroupSchema,
      RepeatSchema,
      MacroSchema,
      MacroStepSchema,
      ActionKindSchema,
    } from './schema.js'

    export type {
      ActionKind,
      SelectorLadder,
      ManifestTarget,
      ManifestGroup,
      ManifestRepeat,
      ManifestMacro,
      MacroStep,
      AgruneManifest,
    } from './schema.js'
    ```

    **4. `packages/manifest/tests/builders.spec.ts`** — 최소 구성:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { defineManifest, defineGroup, defineTarget, defineRepeat, defineMacro } from '../src/index.js'

    describe('defineTarget', () => {
      it('preserves targetId literal type and fields', () => {
        const t = defineTarget({
          targetId: 'login_submit',
          actionKinds: ['click'],
          selector: { role: { name: 'button', level: 'Sign in' } },
        })
        expect(t.targetId).toBe('login_submit')
        expect(t.actionKinds).toEqual(['click'])
      })

      it('rejects sensitive:false at type level', () => {
        // @ts-expect-error sensitive: false is not allowed
        defineTarget({
          targetId: 'pw',
          actionKinds: ['fill'],
          selector: { css: 'input' },
          sensitive: false,
        })
        expect(true).toBe(true)
      })

      it('rejects invalid action kind at type level', () => {
        // @ts-expect-error 'submit' is not an ActionKind
        defineTarget({
          targetId: 'x',
          actionKinds: ['submit'],
          selector: { css: 'button' },
        })
        expect(true).toBe(true)
      })

      it('rejects empty SelectorLadder at type level', () => {
        // @ts-expect-error SelectorLadder requires at least one field
        defineTarget({
          targetId: 'x',
          actionKinds: ['click'],
          selector: {},
        })
        expect(true).toBe(true)
      })
    })

    describe('defineRepeat', () => {
      it('accepts dom strategy', () => {
        const r = defineRepeat({
          repeatId: 'feed',
          template: 'post_${key}',
          keyFrom: 'el.dataset.postId',
          strategy: 'dom',
          targets: [],
        })
        expect(r.strategy).toBe('dom')
      })

      it('accepts virtualized strategy', () => {
        const r = defineRepeat({
          repeatId: 'rows',
          template: 'row_${key}',
          keyFrom: 'el.dataset.rowId',
          strategy: 'virtualized',
          targets: [],
        })
        expect(r.strategy).toBe('virtualized')
      })

      it('rejects invalid strategy at type level', () => {
        // @ts-expect-error 'fiber' is not a valid strategy
        defineRepeat({
          repeatId: 'x', template: 't', keyFrom: 'k', strategy: 'fiber', targets: [],
        })
        expect(true).toBe(true)
      })
    })

    describe('defineMacro', () => {
      it('accepts full macro shape', () => {
        const m = defineMacro({
          macroId: 'login',
          params: { email: { type: 'string', required: true }, password: { type: 'string', required: true } },
          steps: [{ targetId: 'login_email', action: 'fill', value: '${params.email}' }],
          precondition: 'document.querySelector(".login-form") !== null',
          postcondition: 'location.pathname !== "/login"',
          circuitBreaker: { maxRetries: 3, resetAfterMs: 60000 },
        })
        expect(m.macroId).toBe('login')
        expect(m.steps).toHaveLength(1)
        expect(m.circuitBreaker?.maxRetries).toBe(3)
      })
    })

    describe('defineManifest', () => {
      it('injects version: 3 and preserves groups', () => {
        const target = defineTarget({
          targetId: 'submit',
          actionKinds: ['click'],
          selector: { role: { name: 'button' } },
        })
        const group = defineGroup({ groupId: 'form', targets: [target] })
        const manifest = defineManifest({ groups: [group] })
        expect(manifest.version).toBe(3)
        expect(manifest.groups).toHaveLength(1)
      })
    })
    ```

    **5. `packages/manifest/tests/validator.spec.ts`**:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import {
      validateManifest,
      assertNoHashClass,
      assertNoNthChild,
      SelectorForbiddenError,
      HASH_CLASS_PATTERN,
    } from '../src/index.js'

    describe('validateManifest', () => {
      it('accepts a minimal valid manifest', () => {
        const result = validateManifest({
          version: 3,
          groups: [{ groupId: 'g', targets: [{ targetId: 't', actionKinds: ['click'], selector: { css: 'button.submit' } }] }],
        })
        expect(result.ok).toBe(true)
      })

      it('rejects version != 3', () => {
        const result = validateManifest({ version: 2, groups: [] })
        expect(result.ok).toBe(false)
      })

      it('rejects sensitive:false with OR-only error message', () => {
        const result = validateManifest({
          version: 3,
          groups: [{ groupId: 'g', targets: [{ targetId: 'pw', actionKinds: ['fill'], selector: { css: 'input' }, sensitive: false }] }],
        })
        expect(result.ok).toBe(false)
        if (!result.ok) {
          const msg = result.errors.map(e => e.message).join('\n')
          expect(msg).toContain('OR-only')
          expect(msg).toContain('sensitive:false is not allowed')
        }
      })

      it('rejects empty SelectorLadder', () => {
        const result = validateManifest({
          version: 3,
          groups: [{ groupId: 'g', targets: [{ targetId: 't', actionKinds: ['click'], selector: {} }] }],
        })
        expect(result.ok).toBe(false)
      })

      it('rejects hash class in css selector', () => {
        const result = validateManifest({
          version: 3,
          groups: [{ groupId: 'g', targets: [{ targetId: 't', actionKinds: ['click'], selector: { css: '.abc12345xyz' } }] }],
        })
        expect(result.ok).toBe(false)
        if (!result.ok) {
          expect(result.errors.map(e => e.message).join('\n')).toContain('hash class forbidden')
        }
      })

      it('accepts Tailwind utility classes (no hash pattern)', () => {
        const result = validateManifest({
          version: 3,
          groups: [{ groupId: 'g', targets: [{ targetId: 't', actionKinds: ['click'], selector: { css: '.flex.items-center.bg-blue-500' } }] }],
        })
        expect(result.ok).toBe(true)
      })

      it('rejects :nth-child in css selector', () => {
        const result = validateManifest({
          version: 3,
          groups: [{ groupId: 'g', targets: [{ targetId: 't', actionKinds: ['click'], selector: { css: 'div:nth-child(2)' } }] }],
        })
        expect(result.ok).toBe(false)
        if (!result.ok) {
          expect(result.errors.map(e => e.message).join('\n')).toContain(':nth-child forbidden')
        }
      })
    })

    describe('assertNoHashClass / assertNoNthChild', () => {
      it('throws SelectorForbiddenError on hash class', () => {
        expect(() => assertNoHashClass('.abc12345def')).toThrow(SelectorForbiddenError)
      })
      it('does not throw on Tailwind utility', () => {
        expect(() => assertNoHashClass('.flex.items-center')).not.toThrow()
      })
      it('throws on :nth-child', () => {
        expect(() => assertNoNthChild('div:nth-child(2)')).toThrow(SelectorForbiddenError)
      })
    })

    describe('HASH_CLASS_PATTERN', () => {
      it('matches 8+ alphanumeric class with no hyphen suffix', () => {
        expect(HASH_CLASS_PATTERN.test('.abc12345xy')).toBe(true)
      })
      it('does not match Tailwind like bg-blue-500', () => {
        expect(HASH_CLASS_PATTERN.test('.bg-blue-500')).toBe(false)
      })
    })
    ```

    **6. 실행 검증**: `pnpm --filter @agrune/manifest run test` 가 통과해야 한다.
  </action>
  <verify>
    <automated>
      cd /Users/chenjing/dev/agrune/agrune &&
      pnpm --filter @agrune/manifest run typecheck &&
      pnpm --filter @agrune/manifest run test
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/manifest/src/builders.ts` 성공
    - `test -f packages/manifest/src/validator.ts` 성공
    - `test -f packages/manifest/src/index.ts` 성공
    - `grep -q "export function defineManifest" packages/manifest/src/builders.ts` 성공
    - `grep -q "export function defineTarget" packages/manifest/src/builders.ts` 성공
    - `grep -q "export function defineGroup" packages/manifest/src/builders.ts` 성공
    - `grep -q "export function defineRepeat" packages/manifest/src/builders.ts` 성공
    - `grep -q "export function defineMacro" packages/manifest/src/builders.ts` 성공
    - `grep -q "export function validateManifest" packages/manifest/src/validator.ts` 성공
    - `grep -q "class SelectorForbiddenError" packages/manifest/src/validator.ts` 성공
    - `grep -q "HASH_CLASS_PATTERN = /\\\\.\\[a-zA-Z0-9\\]{8,}(?!\\[a-zA-Z0-9-\\])/" packages/manifest/src/validator.ts` 성공
    - `grep -q "NTH_CHILD_PATTERN" packages/manifest/src/validator.ts` 성공
    - `grep -q "OR-only" packages/manifest/src/validator.ts` 성공
    - `grep -q "@ts-expect-error" packages/manifest/tests/builders.spec.ts` 성공 (적어도 3건)
    - `grep -c "@ts-expect-error" packages/manifest/tests/builders.spec.ts` 결과 ≥ 3
    - `pnpm --filter @agrune/manifest run typecheck` exit 0
    - `pnpm --filter @agrune/manifest run test` exit 0 (vitest pass)
    - `pnpm --filter @agrune/manifest run build` exit 0 (dist 생성)
    - `test -f packages/manifest/dist/index.js` 성공
    - `test -f packages/manifest/dist/index.d.ts` 성공
  </acceptance_criteria>
  <done>
    `@agrune/manifest` 가 정의된 모든 빌더와 validator 를 export하고 builders.spec.ts + validator.spec.ts 가 녹색이며 dist 가 빌드된다. TS @ts-expect-error 음성 테스트가 컴파일 시점에 sensitive:false/invalid action/빈 ladder 를 차단함을 증명한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author's editor → zod validator | Author가 작성한 manifest .ts 파일이 타입 체크를 우회해 런타임에 주입될 때 zod가 최종 방어선 |
| JSON manifest → runtime loader (Phase 12에서 사용) | 이 플랜에서는 JSON 구조 검증만. Phase 12가 `__agrune_manifest__` 읽을 때 validateManifest 호출 필수 |
| 악성 manifest → sensitive 우회 | 악성 authoring이 `sensitive: false` 를 강제로 주입해 민감 필드를 로그에 노출하려는 시도 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | manifest JSON `sensitive` 필드 | mitigate | `z.literal(true).optional()` + validator 에러 메시지에 `OR-only` 명시. Phase 14 런타임 heuristic 가 2차 방어(이 플랜 scope 밖). |
| T-11-02 | Information Disclosure | hash class / `:nth-child` 셀렉터 | mitigate | `assertNoHashClass` / `assertNoNthChild` + zod refinement에서 reject. `/\\.[a-zA-Z0-9]{8,}(?![a-zA-Z0-9-])/` 로 Tailwind 오탐 회피(Pitfall 2). |
| T-11-03 | Denial of Service | 빈 SelectorLadder | mitigate | `SelectorLadderSchema.refine(...)` 에서 최소 1필드 요구. |
| T-11-04 | Tampering | `version: 3` 이 아닌 manifest 주입 | mitigate | `z.literal(3)` — v2/v4 무효. |
| T-11-05 | Elevation of Privilege | 악성 manifest의 `sensitive:false` 우회로 password valuePreview 노출 | mitigate | 스키마 레벨 거부(이 플랜) + Phase 14 런타임 OR heuristic(다른 플랜). 스키마 레벨에서 `sensitive: false` 가 literal type으로 차단됨 — JSON에서 `sensitive:false` 면 zod가 거부하므로 런타임에 도달 못함. |
| T-11-06 | Information Disclosure | validator 에러 메시지가 민감 정보를 포함 | accept | error.message는 targetId/path만 출력 — 원본 값 노출 없음. |
</threat_model>

<verification>
Phase-level 검증은 Plan 05 validate CLI E2E에서 통합. 이 플랜 독립 검증:
- `pnpm --filter @agrune/manifest run test` — 모든 vitest 통과
- `pnpm --filter @agrune/manifest run typecheck` — tsc strict 통과
- `pnpm --filter @agrune/manifest run build` — dist 생성
- `pnpm -w list @agrune/manifest` — workspace에 등록됨
</verification>

<success_criteria>
- MANIFEST-01: `defineManifest/defineTarget` 컴파일 타임 검증 (targetId union + actionKinds + SelectorLadder 최소 1필드)
- MANIFEST-02: `defineRepeat` 의 template/keyFrom/nameFrom/strategy 필드 타입 안전
- MANIFEST-03: `defineMacro` 의 id/params/steps/precondition/postcondition/circuitBreaker 필드
- MANIFEST-04 (schema layer): `sensitive?: true` 타입 + `z.literal(true).optional()` — `false` 차단. 런타임 OR heuristic은 Plan 02에서 담당.
</success_criteria>

<output>
After completion, create `.planning/phases/11-manifest/11-01-SUMMARY.md`
</output>
