---
phase: 12-inject
plan: "03"
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - packages/core/src/index.ts
  - packages/mcp/src/mcp-tools.ts
  - packages/mcp/src/index.ts
  - packages/mcp/src/tools.ts
  - packages/mcp/tests/manifest-load-tool.spec.ts
  - packages/mcp/tests/tools.spec.ts
  - packages/e2e/tests/user-flow/manifest-inject.spec.ts
  - packages/e2e/fixtures/manifest-inject-target.html
autonomous: true
requirements:
  - INJECT-02
  - RESOLVE-01
  - RESOLVE-03
must_haves:
  truths:
    - "COMMAND_ERROR_CODES 에 'INVALID_MANIFEST' 가 추가되어 타입 안전 에러 코드로 사용 가능"
    - "agrune_manifest_load MCP tool이 세 파일(mcp-tools.ts / index.ts switch / tools.ts)에 전부 등록되어 devtools 웹앱 목록과 SDK 등록이 동기화"
    - "agrune_manifest_load 가 zod schema + validateManifest() 를 통과한 manifest만 driver.injectManifest 에 전달"
    - "활성 세션이 없으면 SESSION_NOT_ACTIVE, unknown tabId 는 TAB_NOT_FOUND, schema 실패는 INVALID_MANIFEST 반환"
    - "E2E: manifest_load → fixture navigate → agrune_snapshot → target 1개 이상 resolve → agrune_act 성공 응답 (PLAYWRIGHT_SKIP_E2E=1 시 skip)"
  artifacts:
    - path: "packages/mcp/src/mcp-tools.ts"
      provides: "mcp.tool('agrune_manifest_load', ...) 등록 + zod schema"
      contains: "agrune_manifest_load"
    - path: "packages/mcp/src/index.ts"
      provides: "innerHandleToolCall switch case 'agrune_manifest_load'"
      contains: "agrune_manifest_load"
    - path: "packages/mcp/src/tools.ts"
      provides: "getToolDefinitions() 배열에 agrune_manifest_load JSON Schema 추가"
      contains: "agrune_manifest_load"
    - path: "packages/mcp/tests/manifest-load-tool.spec.ts"
      provides: "schema 실패 / 세션 없음 / 성공 / injectManifest 호출 검증 회귀"
      min_lines: 150
    - path: "packages/e2e/tests/user-flow/manifest-inject.spec.ts"
      provides: "local fixture 대상 manifest_load → snapshot → act E2E smoke"
      min_lines: 60
    - path: "packages/e2e/fixtures/manifest-inject-target.html"
      provides: "role=button + testId 가 있는 minimal 결정적 fixture"
      min_lines: 15
    - path: "packages/core/src/index.ts"
      provides: "COMMAND_ERROR_CODES에 'INVALID_MANIFEST' 추가"
      contains: "INVALID_MANIFEST"
  key_links:
    - from: "agrune_manifest_load handler (index.ts switch)"
      to: "driver.injectManifest(tabId, manifest)"
      via: "타입 가드 후 메서드 호출"
      pattern: "driver\\.injectManifest"
    - from: "mcp-tools.ts zod schema"
      to: "validateManifest() from @agrune/manifest"
      via: "SDK auto-validate + schema 2차 검증"
      pattern: "validateManifest"
    - from: "tools.ts getToolDefinitions()"
      to: "devtools 웹앱 tool 목록"
      via: "JSON Schema 배열"
      pattern: "name: 'agrune_manifest_load'"
    - from: "E2E spec"
      to: "agrune_manifest_load → agrune_snapshot → agrune_act"
      via: "createRealHarness + call()"
      pattern: "manifest_load"
---

<objective>
Plan 01(v3 snapshot) + Plan 02(CDP injector + driver.injectManifest)가 닫힌 뒤, MCP layer에 `agrune_manifest_load` tool을 노출해 AI 에이전트가 CLI에서 로드한 manifest를 활성 세션에 주입할 수 있게 한다. Success Criteria #5의 E2E 시나리오 — manifest_load → snapshot → target resolve → act 를 전 스택을 통과해 결정적으로 돌리는 smoke test를 남긴다.

Purpose: Phase 12의 마지막 퍼즐. RESEARCH Section 1이 지적한 MCP tool 등록의 이중 구조(mcp-tools.ts + index.ts switch + tools.ts)를 Pitfall 6에 걸리지 않게 동기화한다. E2E는 YouTube처럼 외부 URL에 의존하지 않고 packages/e2e/fixtures 에 최소 결정적 fixture를 추가해 CI skip 조건만 기존 PLAYWRIGHT_SKIP_E2E 를 그대로 따르게 한다.
Output: agrune_manifest_load MCP tool의 세 등록 경로 landing, INVALID_MANIFEST 에러 코드 추가, manifest-load-tool.spec.ts + manifest-inject.spec.ts + manifest-inject-target.html 신규.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-inject/12-CONTEXT.md
@.planning/phases/12-inject/12-RESEARCH.md
@.planning/phases/12-inject/12-01-SUMMARY.md
@.planning/phases/12-inject/12-02-SUMMARY.md
@.planning/phases/11-manifest/11-05-SUMMARY.md

<interfaces>
<!-- 실제 존재하는 contract — executor는 탐색 최소화 -->

packages/manifest/src/validator.ts — validateManifest 실제 반환 shape:
```typescript
export interface ValidateOk {
  ok: true
  manifest: AgruneManifest
}
export interface ValidateFail {
  ok: false
  errors: Array<{ path: string; message: string }>
}
export type ValidateResult = ValidateOk | ValidateFail
export function validateManifest(input: unknown): ValidateResult
```
→ 코드에서는 `validation.ok` / `validation.manifest` / `validation.errors` 로 접근.

packages/mcp/src/mcp-tools.ts 기존 등록 패턴 (line 33-44):
```typescript
mcp.tool(
  'agrune_snapshot',
  '...description...',
  {
    groupId: z.string().optional().describe('...'),
    ...optionalTabId,
  },
  async (args) => toMcpToolResult(await handleToolCall('agrune_snapshot', args)),
)
```
`optionalTabId` 는 파일 상단에서 `{ tabId: z.number().optional().describe('...') }` 로 이미 선언됨.

packages/mcp/src/index.ts innerHandleToolCall switch (line 94-126) — agrune_focus case:
```typescript
case 'agrune_focus': {
  const focusArg = resolveFocusTabId(args)
  if (focusArg == null) {
    return errorText('TAB_NOT_FOUND', 'agrune_focus requires tabId or numeric sessionId.')
  }
  // ...
}
```

errorText helper (line 252):
```typescript
function errorText(code: string, message: string, details?: Record<string, unknown>): ToolHandlerResult {
  return {
    text: JSON.stringify({ ok: false, error: { code, message, ...(details ? { details } : {}) } }, null, 2),
    isError: true,
  }
}
```

현재 COMMAND_ERROR_CODES (packages/core/src/index.ts line 4-20):
```typescript
export const COMMAND_ERROR_CODES = [
  'STALE_SNAPSHOT', 'TARGET_NOT_FOUND', 'NOT_VISIBLE', 'DISABLED',
  'FLOW_BLOCKED', 'TIMEOUT', 'SESSION_NOT_ACTIVE', 'AGENT_STOPPED',
  'INVALID_TARGET', 'INVALID_COMMAND', 'CANVAS_PAN_FAILED',
  'CONNECTION_LOST', 'CHROME_CRASHED', 'RECOVERY_FAILED', 'TAB_NOT_FOUND',
] as const
```
→ `'INVALID_MANIFEST'` 추가.

E2E harness (packages/e2e/tests/user-flow/helpers.ts):
```typescript
export function realE2eSkipReason(): string | null   // PLAYWRIGHT_SKIP_E2E=1, AGRUNE_E2E_REAL=0, Chrome 없음 체크
export async function createRealHarness(options: HarnessOptions): Promise<RealHarness>
// RealHarness = { driver: CdpDriver, call: (name, args) => Promise<McpToolResponse>, teardown }
export async function getFullTargets(call, tabId?): Promise<SnapshotTargetLite[]>
export async function waitForTargetByName(call, predicate, opts): Promise<SnapshotTargetLite | null>
```

packages/e2e/playwright.config.ts webServer 은 port 5555로 `packages/e2e/fixtures/` 를 HTTP로 서빙 (Phase 11-05 도입). 따라서 `manifest-inject-target.html` 을 그 디렉토리에 두면 `http://127.0.0.1:5555/manifest-inject-target.html` 로 접근 가능.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: INVALID_MANIFEST error code + agrune_manifest_load 세 파일 동기화 등록 + unit tests</name>
  <files>packages/core/src/index.ts, packages/mcp/src/mcp-tools.ts, packages/mcp/src/index.ts, packages/mcp/src/tools.ts, packages/mcp/tests/manifest-load-tool.spec.ts, packages/mcp/tests/tools.spec.ts</files>
  <read_first>
    - packages/core/src/index.ts:1-25 (COMMAND_ERROR_CODES 배열)
    - packages/mcp/src/mcp-tools.ts 전체 (기존 등록 패턴 + 상단 imports)
    - packages/mcp/src/index.ts:1-145 (innerHandleToolCall switch + errorText + imports)
    - packages/mcp/src/tools.ts 전체 (getToolDefinitions 배열 구조 + 마지막 항목 형식)
    - packages/mcp/tests/tools.spec.ts (기존 테스트가 두 목록을 비교하는 assertion 이미 있는지 확인)
    - packages/manifest/src/validator.ts:40-55 (validateManifest 반환 shape 확인)
    - packages/manifest/src/index.ts (validateManifest export 확인)
    - .planning/phases/12-inject/12-RESEARCH.md:70-137 (Section 1: MCP 이중 등록 구조)
    - .planning/phases/12-inject/12-RESEARCH.md:505-576 (Section 8, 9: response shape + error 패턴)
  </read_first>
  <behavior>
    - Test 1: `COMMAND_ERROR_CODES.includes('INVALID_MANIFEST')` true + 타입 레벨 export (`CommandErrorCode` 에 `'INVALID_MANIFEST'` 포함)
    - Test 2: 유효 manifest + 활성 세션 → handleToolCall 이 `{ ok: true, ...session, manifestSource: 'window' }` 반환 + `driver.injectManifest` 정확히 1회 호출 (args: tabId, manifest)
    - Test 3: `resolveTabId` 가 null 반환 → `{ ok: false, error: { code: 'SESSION_NOT_ACTIVE' } }`, isError: true
    - Test 4: `driver.injectManifest` 가 undefined (mock driver) → `{ ok: false, error: { code: 'INVALID_COMMAND' } }` 또는 내부 defensive 메시지
    - Test 5: schema 위반 manifest (예: `{ version: 2, groups: [] }` 또는 sensitive: false 포함) → `{ ok: false, error: { code: 'INVALID_MANIFEST', details: { errors: [...] } } }`, isError: true
    - Test 6: driver.injectManifest 가 code: 'TAB_NOT_FOUND' 에러를 throw → MCP 응답에 `code: 'TAB_NOT_FOUND'` 유지
    - Test 7: tools.ts `getToolDefinitions()` 배열이 `name: 'agrune_manifest_load'` 엔트리를 포함, 필수 필드 `['manifest']`
    - Test 8: mcp-tools.ts 등록 이름 집합 === tools.ts 목록 이름 집합 (tools.spec.ts 회귀 assertion — Pitfall 6)
  </behavior>
  <action>
    1. packages/core/src/index.ts 수정:
       - `COMMAND_ERROR_CODES` 배열에 `'INVALID_MANIFEST'` 를 `'INVALID_COMMAND'` 바로 아래 줄에 추가. 나머지 순서 보존. as const 유지.
    2. packages/mcp/src/mcp-tools.ts 수정:
       - 파일 상단 imports에 `import { validateManifest } from '@agrune/manifest'` 를 추가 (이미 없으면). 사용은 index.ts 에서 하지만 일관성을 위해 여기에도 type 참조 필요 시 추가.
       - `registerAgruneTools` 함수 안, `agrune_focus` 등록 아래 (또는 `agrune_config` 직전) 위치에 새 tool 등록:
         ```typescript
         mcp.tool(
           'agrune_manifest_load',
           'Load an AgruneManifest v3 into the active browser session. After loading, agrune_snapshot and agrune_act resolve targets defined in the manifest. Call this before using other tools on external sites (e.g. youtube.com) where the page does not ship its own window.__agrune_manifest__.',
           {
             manifest: z.object({
               version: z.literal(3),
               groups: z.array(z.any()),
               macros: z.array(z.any()).optional(),
             }).describe('AgruneManifest v3 object. Must pass validateManifest schema (see @agrune/manifest).'),
             ...optionalTabId,
           },
           async (args) => toMcpToolResult(await handleToolCall('agrune_manifest_load', args)),
         )
         ```
    3. packages/mcp/src/index.ts 수정:
       - 상단 imports에 추가:
         ```typescript
         import { validateManifest } from '@agrune/manifest'
         import type { AgruneManifest } from '@agrune/manifest'
         ```
       - `innerHandleToolCall` switch 안에 case 추가 (위치: `agrune_focus` 다음, `agrune_config` 전):
         ```typescript
         case 'agrune_manifest_load': {
           const validation = validateManifest(args.manifest)
           if (!validation.ok) {
             return errorText(
               'INVALID_MANIFEST',
               'Manifest failed schema validation.',
               { errors: validation.errors },
             )
           }
           if (tabId == null) {
             return errorText('SESSION_NOT_ACTIVE', 'No active session for manifest load.')
           }
           if (typeof driver.injectManifest !== 'function') {
             return errorText('INVALID_COMMAND', 'Driver does not support injectManifest.')
           }
           try {
             await driver.injectManifest(tabId, validation.manifest as AgruneManifest)
           } catch (error) {
             const shape = error as Partial<CommandErrorShape>
             if (shape && typeof shape.code === 'string' && shape.code === 'TAB_NOT_FOUND') {
               return errorText('TAB_NOT_FOUND', shape.message ?? 'Tab not found.', shape.details)
             }
             return errorText('INVALID_COMMAND', error instanceof Error ? error.message : String(error))
           }
           const session = driver.listSessions().find(s => s.tabId === tabId)
           const payload = {
             ok: true,
             session: session
               ? toPublicSessionMeta(session as Session, {
                   wasActive: session.active === true,
                   becameActive: false,
                 })
               : null,
             manifestSource: 'window' as const,
           }
           return { text: JSON.stringify(payload, null, 2) }
         }
         ```
    4. packages/mcp/src/tools.ts 수정:
       - `getToolDefinitions()` 배열에 새 항목 추가 (기존 agrune_config 또는 배열 끝 뒤):
         ```typescript
         {
           name: 'agrune_manifest_load',
           description: 'Load an AgruneManifest v3 into the active browser session. After loading, agrune_snapshot and agrune_act resolve targets defined in the manifest.',
           inputSchema: {
             type: 'object',
             properties: {
               manifest: { type: 'object', description: 'AgruneManifest v3 object' },
               tabId: { type: 'number', description: 'Tab ID (omit for active tab)' },
             },
             required: ['manifest'],
           },
         },
         ```
    5. packages/mcp/tests/manifest-load-tool.spec.ts 신규 생성 (Vitest, ≥150 lines):
       - Mock driver 생성 함수 `makeMockDriver(overrides)` — BrowserDriver 의 모든 메서드를 vi.fn() 으로, `isConnected: () => true`, `ensureReady: async () => null`, `resolveTabId: (id) => id ?? 1`, `listSessions: () => [{ tabId: 1, url: 'http://x', title: 'x', hasSnapshot: true, active: true, snapshotVersion: 1 }]`, `injectManifest: vi.fn(async () => {})` 기본값
       - `createMcpServer(driver)` 를 가져와 `handleToolCall('agrune_manifest_load', args)` 를 직접 호출
       - 위 behavior Test 1-6 을 cover + 최소 12개 assertion + manifest fixture 를 describe 블록 밖 상수로 선언 (유효/유효하지 않은 케이스 2종)
    6. packages/mcp/tests/tools.spec.ts 에 assertion 추가 (Pitfall 6 방지):
       - 만약 이미 mcp-tools.ts 등록 이름 집합과 tools.ts 목록 이름 집합을 비교하는 테스트가 있다면 agrune_manifest_load 가 그 목록에 포함되어 그대로 PASS 되어야 함 (자동으로 커버)
       - 없으면 새 `describe('tool registration parity')` 블록 추가:
         ```typescript
         const registeredNames: string[] = []
         const mockMcp = {
           tool: (name: string) => { registeredNames.push(name); return mockMcp },
         } as unknown as McpServer
         registerAgruneTools(mockMcp, async () => ({ text: '' }))
         const definitionNames = getToolDefinitions().map(d => d.name).sort()
         expect([...registeredNames].sort()).toEqual(definitionNames)
         ```
  </action>
  <verify>
    <automated>pnpm --filter @agrune/core run typecheck &amp;&amp; pnpm --filter @agrune/mcp run typecheck &amp;&amp; pnpm --filter @agrune/mcp run test -- manifest-load-tool &amp;&amp; pnpm --filter @agrune/mcp run test -- tools &amp;&amp; pnpm --filter @agrune/mcp run build</automated>
  </verify>
  <acceptance_criteria>
    - grep `"'INVALID_MANIFEST'"` packages/core/src/index.ts → 1 match (COMMAND_ERROR_CODES 배열 내부)
    - grep `"agrune_manifest_load"` packages/mcp/src/mcp-tools.ts → 1+ match (mcp.tool 호출)
    - grep `"agrune_manifest_load"` packages/mcp/src/index.ts → 1+ match (case 문)
    - grep `"agrune_manifest_load"` packages/mcp/src/tools.ts → 1+ match (name 필드)
    - grep `"validateManifest"` packages/mcp/src/index.ts → 2+ matches (import + 호출)
    - grep `"driver.injectManifest"` packages/mcp/src/index.ts → 1+ match
    - grep `"INVALID_MANIFEST"` packages/mcp/src/index.ts → 1+ match (errorText 호출)
    - packages/mcp/tests/manifest-load-tool.spec.ts 존재 + `wc -l` ≥ 150
    - `pnpm --filter @agrune/core run typecheck` exit 0
    - `pnpm --filter @agrune/mcp run typecheck` exit 0
    - `pnpm --filter @agrune/mcp run test -- manifest-load-tool` PASS
    - `pnpm --filter @agrune/mcp run test -- tools` PASS (parity assertion)
    - `pnpm --filter @agrune/mcp run build` exit 0
  </acceptance_criteria>
  <done>
    agrune_manifest_load MCP tool이 mcp-tools.ts / index.ts / tools.ts 세 곳 모두에 landing. validateManifest 실패는 INVALID_MANIFEST, 세션 없음은 SESSION_NOT_ACTIVE, unknown tabId는 TAB_NOT_FOUND, driver 미지원은 INVALID_COMMAND. tools.spec.ts parity assertion으로 Pitfall 6 차단. MCP 패키지 전체 typecheck + 단위 테스트 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E manifest-inject smoke (local fixture) — manifest_load → snapshot → act 전 스택 검증</name>
  <files>packages/e2e/fixtures/manifest-inject-target.html, packages/e2e/tests/user-flow/manifest-inject.spec.ts</files>
  <read_first>
    - packages/e2e/tests/user-flow/helpers.ts (전체 — createRealHarness / realE2eSkipReason / getFullTargets / waitForTargetByName)
    - packages/e2e/tests/user-flow/fill-real.spec.ts (기존 real-Chrome E2E 패턴 — startUrl / harness.call 호출 예시)
    - packages/e2e/playwright.config.ts (webServer 포트 + fixtures 디렉토리 확인)
    - packages/e2e/fixtures/validate-test.html (minimal fixture 구조 참고 — role=button/testId 패턴)
    - packages/e2e/fixtures/idle-boot.html (data-agrune-* 없는 fixture 참고 — bootstrap 로직 인라인 복제가 있는지)
    - .planning/phases/12-inject/12-RESEARCH.md:397-465 (Section 7: E2E 전략)
    - .planning/phases/11-manifest/11-04-SUMMARY.md (copy-runtime script 존재 확인)
  </read_first>
  <behavior>
    - Test 1: PLAYWRIGHT_SKIP_E2E=1 이면 `test.skip` 으로 skip (기존 realE2eSkipReason() 재사용)
    - Test 2: harness 시작 → fixture 로 navigate → `agrune_manifest_load({ manifest })` 호출 → 응답 `ok: true` + `manifestSource === 'window'`
    - Test 3: 이어서 `agrune_snapshot({ mode: 'full' })` 호출 → 1개 이상의 target이 resolve됨 (getFullTargets 헬퍼 활용)
    - Test 4: resolve된 target 중 하나에 대해 `agrune_act({ targetId })` 호출 → 응답 `ok: true`
    - Test 5: teardown 이 정상 종료 (driver.disconnect 실패 없음)
    - Test 6: 잘못된 manifest (version: 2) 를 주입하면 `ok: false`, `code: 'INVALID_MANIFEST'` 반환 (negative case)
  </behavior>
  <action>
    1. packages/e2e/fixtures/manifest-inject-target.html 신규 생성:
       ```html
       <!DOCTYPE html>
       <html>
         <head>
           <meta charset="utf-8" />
           <title>manifest-inject E2E fixture</title>
         </head>
         <body>
           <h1>manifest-inject fixture — no agrune annotations</h1>
           <button type="button" aria-label="Sign in" data-testid="signin-btn">Sign in</button>
           <div data-testid="user-card">Alice</div>
           <a href="#docs">Docs</a>
         </body>
       </html>
       ```
       주석: data-agrune-* 속성 0개. manifest는 테스트 코드가 주입. `role=button[name="Sign in"]` 또는 `testId=signin-btn` 으로 SelectorLadder resolve 대상.
    2. packages/e2e/tests/user-flow/manifest-inject.spec.ts 신규 생성 (≥60 lines):
       ```typescript
       import { test, expect } from '@playwright/test'
       import { createRealHarness, realE2eSkipReason, getFullTargets, waitForTargetByName } from './helpers'

       const skipReason = realE2eSkipReason()
       const FIXTURE_URL = 'http://127.0.0.1:5555/manifest-inject-target.html'

       const validManifest = {
         version: 3 as const,
         groups: [
           {
             groupId: 'auth',
             name: 'Auth',
             targets: [
               {
                 targetId: 'signin-button',
                 name: 'Sign in',
                 actionKinds: ['click'] as const,
                 selector: { role: { name: 'Sign in' } },
               },
             ],
           },
         ],
       }

       test.describe('INJECT — agrune_manifest_load → fixture site', () => {
         test.skip(!!skipReason, skipReason ?? '')

         test('happy path: manifest_load → snapshot resolves target → act succeeds', async () => {
           const harness = await createRealHarness({ startUrl: FIXTURE_URL })
           try {
             const loadResult = await harness.call('agrune_manifest_load', { manifest: validManifest })
             expect(loadResult.isError).not.toBe(true)
             const loadParsed = loadResult.parsed as { ok?: boolean; manifestSource?: string }
             expect(loadParsed.ok).toBe(true)
             expect(loadParsed.manifestSource).toBe('window')

             const target = await waitForTargetByName(
               harness.call,
               t => t.targetId === 'signin-button',
               { timeoutMs: 5_000 },
             )
             expect(target).not.toBeNull()

             const actResult = await harness.call('agrune_act', { targetId: 'signin-button' })
             const actParsed = actResult.parsed as { ok?: boolean }
             expect(actParsed.ok).toBe(true)
           } finally {
             await harness.teardown()
           }
         })

         test('invalid manifest (version 2) → INVALID_MANIFEST', async () => {
           const harness = await createRealHarness({ startUrl: FIXTURE_URL })
           try {
             const bad = { version: 2, groups: [] } as unknown
             const result = await harness.call('agrune_manifest_load', { manifest: bad })
             expect(result.isError).toBe(true)
             const parsed = result.parsed as { ok?: boolean; error?: { code?: string } }
             expect(parsed.ok).toBe(false)
             expect(parsed.error?.code).toBe('INVALID_MANIFEST')
           } finally {
             await harness.teardown()
           }
         })
       })
       ```
    3. 구동 확인: `packages/e2e/package.json` 의 test:e2e 스크립트가 `copy-runtime && playwright test` 형태인지 확인. fixture만 추가됐고 copy-runtime 은 이미 11-04 에서 세팅됨 — 추가 변경 불필요.
    4. (선택) `packages/e2e/playwright.config.ts` 가 fixture 디렉토리를 port 5555로 서빙하는지 확인. 이미 Phase 11-05에서 검증됨 (validate-test.html 이 같은 서버에서 served). 추가 변경 불필요.
  </action>
  <verify>
    <automated>PLAYWRIGHT_SKIP_E2E=1 pnpm --filter @agrune/e2e run test:e2e -- manifest-inject</automated>
  </verify>
  <acceptance_criteria>
    - packages/e2e/fixtures/manifest-inject-target.html 존재 + `wc -l` ≥ 10
    - `grep -q "data-agrune-"` packages/e2e/fixtures/manifest-inject-target.html → no match (annotation 없는 fixture)
    - packages/e2e/tests/user-flow/manifest-inject.spec.ts 존재 + `wc -l` ≥ 60
    - grep `"agrune_manifest_load"` packages/e2e/tests/user-flow/manifest-inject.spec.ts → 2+ matches (valid + invalid 케이스)
    - grep `"INVALID_MANIFEST"` packages/e2e/tests/user-flow/manifest-inject.spec.ts → 1+ match (negative case assertion)
    - grep `"waitForTargetByName"` packages/e2e/tests/user-flow/manifest-inject.spec.ts → 1+ match
    - grep `"manifestSource"` packages/e2e/tests/user-flow/manifest-inject.spec.ts → 1+ match
    - grep `"realE2eSkipReason"` packages/e2e/tests/user-flow/manifest-inject.spec.ts → 1+ match
    - `PLAYWRIGHT_SKIP_E2E=1 pnpm --filter @agrune/e2e run test:e2e -- manifest-inject` → 0 fail, 2 skipped (skip 로직 올바름)
    - (로컬 전용) `PLAYWRIGHT_SKIP_E2E=0 AGRUNE_E2E_REAL=1 pnpm --filter @agrune/e2e run test:e2e -- manifest-inject` 는 2/2 PASS 가 목표이나, CI 에서는 skip 된 상태가 정상
  </acceptance_criteria>
  <done>
    local fixture 기반 E2E smoke가 manifest_load → snapshot → act 전 스택을 한 번 통과하고, 잘못된 manifest 는 INVALID_MANIFEST 로 거부된다. CI 에서는 PLAYWRIGHT_SKIP_E2E=1 로 skip 되지만 skip 패턴이 기존 자원 규약과 일관. Phase 12 success criteria #5 종료.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP 에이전트 → driver | agrune_manifest_load tool의 args.manifest 는 untrusted — zod + validateManifest 이중 검증 필요 |
| MCP layer → BrowserDriver | driver.injectManifest 가 optional이라 mock driver에서 누락 시 defensive 처리 필요 |
| E2E harness → local fixture server | 127.0.0.1:5555 — 공격 표면 없음 (trusted local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | agrune_manifest_load args.manifest 입력 검증 — schema 우회 시도 | mitigate | zod schema(mcp-tools.ts) + validateManifest(index.ts) 이중 검증. validateManifest 가 HASH_CLASS / NTH_CHILD / sensitive:false 같은 심층 검사까지 수행 — T-11-24..T-11-27 패턴 재사용 |
| T-12-10 | Elevation of Privilege | driver.injectManifest 미구현 (optional interface) → MCP가 임의 동작 노출 | mitigate | `typeof driver.injectManifest !== 'function'` 타입 가드 → INVALID_COMMAND 반환. Mock driver (테스트)에서도 동일 경로 |
| T-12-11 | Information Disclosure | error details 에 manifest 원본 노출 | mitigate | validateManifest 가 반환하는 errors 는 `{ path, message }` 만 포함 — 원본 value 없음. errorText가 그대로 전달해도 안전 (T-11-26 패턴) |
| T-12-12 | Repudiation | manifest_load 호출이 CommandBroker 에 기록되는지 | accept | 기존 handleToolCall wrapper 가 자동으로 emit (commandBroker.emit) — 별도 작업 없음. devtools 웹앱 command log 에 자동 표시 |
| T-12-13 | DoS | manifest_load 연속 호출 → reloadRuntime 다중 trigger | mitigate | Plan 02에서 reloadRuntime debounce 50ms 추가 — 연속 호출도 1회로 수렴 |
</threat_model>

<verification>
전체 관문:
- `pnpm --filter @agrune/core run typecheck` PASS
- `pnpm --filter @agrune/mcp run typecheck` PASS
- `pnpm --filter @agrune/mcp run test` 전체 PASS (기존 + 신규 manifest-load-tool + tools parity)
- `pnpm --filter @agrune/mcp run build` PASS
- `PLAYWRIGHT_SKIP_E2E=1 pnpm --filter @agrune/e2e run test:e2e -- manifest-inject` → 0 fail (skip 정상)
- `grep -rn "agrune_manifest_load" packages/mcp/src` → matches in mcp-tools.ts + index.ts + tools.ts (세 파일 동기화 증거)
- 회귀: Plan 01 + Plan 02 의 테스트 스위트도 여전히 PASS (`pnpm --filter @agrune/runtime run test`, `pnpm --filter @agrune/browser run test`)
- 회귀: `pnpm build` 전 패키지 PASS
- MCP response parity: `agrune_manifest_load` 를 호출한 뒤 `agrune_snapshot` 이 그 manifest의 targetId를 반환 (E2E로 검증)
</verification>

<success_criteria>
INJECT-02 + RESOLVE-01 + RESOLVE-03 (전 스택 통합) 달성:
1. `agrune_manifest_load` MCP tool이 등록 3파일 모두에 존재하고 devtools 웹앱 목록과 동기화
2. zod + validateManifest 이중 검증으로 INVALID_MANIFEST 에러 코드 사용 가능
3. driver.injectManifest 를 거쳐 활성 세션에 `window.__agrune_manifest__` 로 주입 + reloadRuntime 호출
4. E2E: fixture navigate → manifest_load → snapshot → target resolve → act 전 경로 통과
5. 잘못된 manifest 는 INVALID_MANIFEST 로 차단 (negative case)
6. PLAYWRIGHT_SKIP_E2E=1 환경에서는 skip 되고 CI 에서 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/12-inject/12-03-SUMMARY.md` summarizing:
- What Was Built (Task 1 MCP tool 세 파일 등록 + Task 2 E2E)
- Verification Results (unit + E2E 결과 테이블)
- Threat Model Coverage (T-12-09..13 disposition)
- Known Stubs (없음 — Phase 12 closes)
- Commits 테이블
- Phase 12 close 시 success criteria 5개 전부 충족 확인
</output>
