---
phase: 12-inject
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - packages/browser/src/cdp-runtime-injector.ts
  - packages/browser/src/cdp-driver.ts
  - packages/core/src/driver.ts
  - packages/browser/tests/cdp-runtime-injector-preload.spec.ts
  - packages/browser/tests/cdp-driver.spec.ts
autonomous: true
requirements:
  - INJECT-01
  - RESOLVE-01
must_haves:
  truths:
    - "CdpRuntimeInjector.prepareSession이 { preloadManifest } 옵션을 수용한다"
    - "preloadManifest가 주어지면 window.__agrune_preload_manifest__가 source 앞단에 JSON.parse wrapper로 embed되어 현재 페이지 + 이후 새 문서 양쪽에 즉시 설정된다"
    - "BrowserDriver.injectManifest(tabId, manifest) 메서드가 interface에 optional로 추가되고 CdpDriver가 구현 — window.__agrune_manifest__ 설정 + reloadRuntime 호출"
    - "reloadRuntime이 debounce(50ms) + dispose 중복 호출을 방지한다 (Research Pitfall 2)"
    - "preloadManifest가 있는 세션과 없는 세션의 cachedInjectedSource가 섞이지 않는다 (Pitfall 1)"
    - "ManifestLoader 우선순위: window > preload > inline > idle 이 E2E 스타일 단위 테스트로 검증"
  artifacts:
    - path: "packages/browser/src/cdp-runtime-injector.ts"
      provides: "PrepareSessionOptions, buildPreloadManifestSource, safeJsonEmbed, debounced reloadRuntime"
      contains: "preloadManifest"
    - path: "packages/core/src/driver.ts"
      provides: "BrowserDriver.injectManifest optional 메서드 계약"
      contains: "injectManifest"
    - path: "packages/browser/src/cdp-driver.ts"
      provides: "CdpDriver.injectManifest 구현 — Runtime.evaluate 경로"
      contains: "injectManifest"
    - path: "packages/browser/tests/cdp-runtime-injector-preload.spec.ts"
      provides: "prepareSession preload + 우선순위 + race + safeJsonEmbed 회귀 테스트"
      min_lines: 120
  key_links:
    - from: "CdpDriver.injectManifest"
      to: "Runtime.evaluate: window.__agrune_manifest__ = manifest; reloadRuntime()"
      via: "this.connection.send('Runtime.evaluate', ...)"
      pattern: "__agrune_manifest__\\s*="
    - from: "prepareSession({ preloadManifest })"
      to: "Page.addScriptToEvaluateOnNewDocument with preload prepended"
      via: "buildPreloadManifestSource + getInjectedSource 합성"
      pattern: "__agrune_preload_manifest__"
    - from: "reloadRuntime 훅 (bootstrap source 내부)"
      to: "debounce 50ms + previous runtime dispose"
      via: "setTimeout guard"
      pattern: "debounce|setTimeout.*reload"
---

<objective>
Phase 11-04가 남긴 `reloadRuntime` 스텁을 완성해 debounce + dispose 계약을 닫고, `CdpRuntimeInjector.prepareSession`이 preloadManifest 옵션을 받아 외부 사이트 zero-RTT preload를 embed하게 만든다. 동시에 `BrowserDriver` 인터페이스에 `injectManifest` optional 메서드를 추가해 MCP layer(Plan 03)가 안전하게 호출할 수 있는 계약을 선언한다.

Purpose: RESOLVE-01(ManifestLoader 우선순위 ladder)는 Phase 11-04의 `resolveManifest()`에 이미 구현되어 있으나 현재 `window.__agrune_preload_manifest__`를 실제로 설정하는 경로가 없다. 이 plan이 그 양쪽 경로(preload embed + window 직접 주입)를 열어야 Plan 03의 MCP tool이 의미를 가진다.
Output: prepareSession({ preloadManifest }) 시그니처, safeJsonEmbed 헬퍼, BrowserDriver.injectManifest 인터페이스, CdpDriver.injectManifest 구현, reloadRuntime debounce + dispose, cdp-runtime-injector-preload.spec.ts 신규 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-inject/12-CONTEXT.md
@.planning/phases/12-inject/12-RESEARCH.md
@.planning/phases/11-manifest/11-04-SUMMARY.md
@.planning/phases/12-inject/12-01-SUMMARY.md

<interfaces>
<!-- 실제 존재하는 시그니처 — executor가 탐색 없이 수정 가능 -->

packages/browser/src/cdp-runtime-injector.ts (현재 시그니처):
```typescript
export const QUICK_MODE_RUNTIME_KEY = '__agrune_quick_mode__'
let cachedInjectedSource: string | null = null

function buildBootstrapSource(): string { /* ~220줄 */ }

function getInjectedSource(): string {
  if (cachedInjectedSource !== null) return cachedInjectedSource
  const runtimeSource = readFileSync(resolvePageRuntimePath(), 'utf8')
  cachedInjectedSource = `${runtimeSource}\n${buildBootstrapSource()}`
  return cachedInjectedSource
}

export class CdpRuntimeInjector {
  constructor(private readonly connection: CdpConnection) {}
  async prepareSession(sessionId: string): Promise<void> {
    const source = getInjectedSource()
    await this.connection.send('Page.enable', {}, sessionId).catch(() => {})
    await this.connection.send('Runtime.enable', {}, sessionId)
    await this.connection.send('Runtime.addBinding', { name: 'agrune_send' }, sessionId)
    await this.connection.send('Page.addScriptToEvaluateOnNewDocument', { source }, sessionId)
    await this.connection.send('Runtime.evaluate', { expression: source }, sessionId)
  }
}
```

packages/browser/src/cdp-driver.ts:388 현재 호출부:
```typescript
private async prepareTarget(target: TargetInfo): Promise<void> {
  if (!target.sessionId || this.preparedSessions.has(target.sessionId)) return
  this.preparedSessions.add(target.sessionId)
  const injector = new CdpRuntimeInjector(this.connection)
  try {
    await injector.prepareSession(target.sessionId)
    // ... applyConfig + setAgentActivity + refreshSnapshot
```

packages/core/src/driver.ts BrowserDriver 인터페이스 (현재):
```typescript
import type { PageSnapshot, CommandResult, AgruneRuntimeConfig } from './index.js'

export interface BrowserDriver {
  connect(): Promise<void>
  disconnect(): Promise<void>
  isConnected(): boolean
  listSessions(): Session[]
  getSnapshot(tabId: number): PageSnapshot | null
  onSessionOpen(cb: (session: Session) => void): void
  onSessionClose(cb: (tabId: number) => void): void
  onSnapshotUpdate(cb: (tabId: number, snapshot: PageSnapshot) => void): void
  execute(tabId: number, command: Record<string, unknown> & { kind: string }): Promise<CommandResult>
  updateConfig(config: Partial<AgruneRuntimeConfig>): void
  ensureReady(): Promise<string | null>
  resolveTabId(tabId?: number): number | null
  focusSession(tabId: number): Promise<FocusResult>
}
```

packages/browser/src/cdp-runtime-injector.ts buildBootstrapSource 내부 reloadRuntime (Phase 11-04 stub, line 238-244):
```javascript
reloadRuntime: () => {
  installRuntime();  // ← 현재 즉시 재실행. Phase 12에서 debounce + dispose 필요
},
```

AgruneManifest 타입 (import from `@agrune/manifest`):
```typescript
export interface AgruneManifest {
  version: 3
  groups: ManifestGroup[]
  macros?: ManifestMacro[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CdpRuntimeInjector preload embed + safeJsonEmbed + reloadRuntime debounce</name>
  <files>packages/browser/src/cdp-runtime-injector.ts, packages/browser/tests/cdp-runtime-injector-preload.spec.ts</files>
  <read_first>
    - packages/browser/src/cdp-runtime-injector.ts (전체 — 캐싱 구조, bootstrap source 내부 reloadRuntime 현 상태 파악)
    - packages/manifest/src/schema.ts (AgruneManifest 타입 정확한 import 경로)
    - .planning/phases/12-inject/12-RESEARCH.md:139-238 (Section 2, 3: prepareSession 변경 + JSON embed 보안)
    - .planning/phases/12-inject/12-RESEARCH.md:579-638 (Section 10: race condition 4가지)
    - packages/browser/tests/recovery-supervisor.spec.ts (기존 browser 테스트 패턴 참조 — CdpConnection 목 방법)
    - .planning/phases/11-manifest/11-04-SUMMARY.md (reloadRuntime stub 명시)
  </read_first>
  <behavior>
    - Test 1: `new CdpRuntimeInjector(conn).prepareSession(sid)` 호출 시 `Page.addScriptToEvaluateOnNewDocument` 와 `Runtime.evaluate` 각각 1회 send 됨 (기존 동작 보존)
    - Test 2: `prepareSession(sid, { preloadManifest })` 호출 시 embed된 source에 `window.__agrune_preload_manifest__ = JSON.parse(` 문자열이 포함됨
    - Test 3: preloadManifest 객체에 `</script>` 및 `\u2028` 이 포함돼도 safeJsonEmbed 경유 시 임의 JS 실행이 불가 (출력 문자열 검증 — 단순 substring 존재 + JSON.parse wrapper 확인)
    - Test 4: preloadManifest 있는 세션과 없는 세션이 같은 getInjectedSource cache를 공유하지 않음 (첫 세션에 preload 없이 호출 → 두번째 preload 있는 세션의 embed 결과에도 preload 포함) — Pitfall 1
    - Test 5: bootstrap source의 reloadRuntime 호출이 50ms debounce됨 — 연속 2회 호출 시 installRuntime은 1회만 실행 (bootstrap source grep으로 debounce/setTimeout 패턴 존재 검증 + 가능하다면 puppeteer 없이 Function constructor로 JS 실행 후 관찰)
    - Test 6: `prepareSession(sid, { preloadManifest })` 가 같은 manifest를 `Runtime.evaluate` 로도 주입해 **현재 페이지**에도 즉시 적용됨 (Research Pitfall 3)
  </behavior>
  <action>
    1. packages/browser/src/cdp-runtime-injector.ts 상단에 타입 import 추가:
       ```typescript
       import type { AgruneManifest } from '@agrune/manifest'
       ```
       (`@agrune/manifest`가 이미 `@agrune/core`의 dependency이므로 browser package.json에 추가 필요하면 workspace:* 로 추가. 우선 typecheck 실패 여부 확인 후 필요 시 추가.)
    2. 파일 내부에 helper 함수 추가 (파일 최상단, resolvePageRuntimePath 근처):
       ```typescript
       export interface PrepareSessionOptions {
         preloadManifest?: AgruneManifest
       }

       function safeJsonEmbed(json: string): string {
         // U+2028/U+2029는 JSON에서는 valid하지만 일부 JS 파서에서 LineTerminator로 해석
         return json.replace(/\u2028/g, '\\u2028').replace(/\u2029/g, '\\u2029')
       }

       function buildPreloadManifestSource(manifest: AgruneManifest): string {
         // JSON.parse(JSON.stringify(...)) 중첩: 외부 stringify는 JSON string을 JS string literal로 이스케이프.
         // 임의 JS statement boundary 이탈 불가. (Research Section 3 권장 패턴)
         const jsonLiteral = JSON.stringify(JSON.stringify(manifest))
         return `;(function(){try{window.__agrune_preload_manifest__ = JSON.parse(${safeJsonEmbed(jsonLiteral)});}catch(e){}})();`
       }

       function getInjectedSourceWithPreload(manifest: AgruneManifest): string {
         // preloadManifest가 있으면 캐시 우회 — 세션별로 다른 manifest 섞이면 안 됨 (Pitfall 1)
         const runtimeSource = readFileSync(resolvePageRuntimePath(), 'utf8')
         const preloadSource = buildPreloadManifestSource(manifest)
         return `${runtimeSource}\n${preloadSource}\n${buildBootstrapSource()}`
       }
       ```
    3. `CdpRuntimeInjector.prepareSession` 시그니처 변경:
       ```typescript
       async prepareSession(sessionId: string, options?: PrepareSessionOptions): Promise<void> {
         const source = options?.preloadManifest
           ? getInjectedSourceWithPreload(options.preloadManifest)
           : getInjectedSource()
         // 기존 나머지 동작 유지 (Page.enable, Runtime.enable, Runtime.addBinding,
         // Page.addScriptToEvaluateOnNewDocument, Runtime.evaluate)
       }
       ```
    4. `buildBootstrapSource()` 내부 `reloadRuntime` 훅 업데이트 (line ~238):
       - 기존: `reloadRuntime: () => { installRuntime(); }`
       - 변경: debounce (50ms) + 이전 install 타이머 취소. 이전 runtime 인스턴스가 있으면 dispose (`window.agruneDom?.dispose?.()` 같은 defensive 호출은 agruneDom interface 확장이 없으므로 생략 — 대신 pendingInstall 기반으로 중복 호출 자체를 막음). 구체 구현:
       ```javascript
       let reloadTimer = null;
       // ...
       reloadRuntime: () => {
         if (reloadTimer !== null) clearTimeout(reloadTimer);
         reloadTimer = setTimeout(() => {
           reloadTimer = null;
           // 이전 runtime이 busy면 scheduleInstall 로직을 재사용할 수도 있으나,
           // reloadRuntime은 명시적 외부 트리거이므로 즉시 재설치가 맞다.
           installRuntime();
         }, 50);
       },
       ```
       위 reloadTimer 변수는 `installTimer` 선언 근처(line ~69)에 `let reloadTimer = null;` 추가.
    5. packages/browser/tests/cdp-runtime-injector-preload.spec.ts 신규 생성 — Vitest 기반:
       - Mock CdpConnection: `send` 메서드를 spy로 만들고 호출 인자 기록
       - `describe('prepareSession — no preload')` — 기존 경로가 여전히 5번 send 호출 (Page.enable, Runtime.enable, Runtime.addBinding, Page.addScriptToEvaluateOnNewDocument, Runtime.evaluate) 확인. Runtime.evaluate의 expression에 `__agrune_preload_manifest__` 문자열이 **없는** 것 확인.
       - `describe('prepareSession — with preloadManifest')` — embed된 source에 `__agrune_preload_manifest__` 와 `JSON.parse(` 둘 다 포함. 또 `Runtime.evaluate` 의 expression에도 동일 포함(현재 페이지 적용).
       - `describe('safeJsonEmbed')` — U+2028, U+2029 이스케이프 확인. `</script>` 문자열이 JSON.stringify 이중 인코딩 경유 시 백슬래시 이스케이프로 코드 실행 불가 형태로 저장됨 확인.
       - `describe('cache isolation')` — preload 없이 호출 → 그 뒤 preload 있는 호출 → 두번째 호출의 source가 preload 포함. `getInjectedSourceWithPreload`는 캐시 우회이므로 side-effect 없음.
       - `describe('reloadRuntime debounce')` — `buildBootstrapSource()` 문자열을 받아 grep 검증: `clearTimeout` + `setTimeout.*reload` 패턴 존재 (런타임 실행 대신 source 정적 검사).
       - 최소 15개 assertion. ≥ 120 lines.
  </action>
  <verify>
    <automated>pnpm --filter @agrune/browser run typecheck &amp;&amp; pnpm --filter @agrune/browser run test -- cdp-runtime-injector-preload &amp;&amp; pnpm --filter @agrune/browser run build</automated>
  </verify>
  <acceptance_criteria>
    - grep `"PrepareSessionOptions"` packages/browser/src/cdp-runtime-injector.ts → 1+ match (interface 선언)
    - grep `"preloadManifest"` packages/browser/src/cdp-runtime-injector.ts → 3+ matches
    - grep `"safeJsonEmbed"` packages/browser/src/cdp-runtime-injector.ts → 2+ matches (선언 + 사용)
    - grep `"__agrune_preload_manifest__"` packages/browser/src/cdp-runtime-injector.ts → 1+ match (preload source 빌더)
    - grep `"JSON.parse("` packages/browser/src/cdp-runtime-injector.ts → 1+ match (wrapper 패턴)
    - grep `"reloadTimer"` packages/browser/src/cdp-runtime-injector.ts → 2+ matches (선언 + 사용)
    - packages/browser/tests/cdp-runtime-injector-preload.spec.ts 존재 + `wc -l` ≥ 120
    - `pnpm --filter @agrune/browser run typecheck` exit 0
    - `pnpm --filter @agrune/browser run test -- cdp-runtime-injector-preload` 모든 테스트 PASS
    - `pnpm --filter @agrune/browser run build` exit 0
  </acceptance_criteria>
  <done>
    prepareSession이 preloadManifest 옵션을 수용하고, safeJsonEmbed + JSON.parse wrapper로 XSS/statement boundary 이탈을 차단한다. reloadRuntime이 50ms debounce로 중복 호출을 억제한다. preload cache isolation을 회귀 테스트로 가둔다. browser 패키지 전체 typecheck + 테스트 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BrowserDriver.injectManifest 인터페이스 + CdpDriver 구현 + prepareTarget preloadManifest 전파</name>
  <files>packages/core/src/driver.ts, packages/browser/src/cdp-driver.ts, packages/browser/tests/cdp-driver.spec.ts</files>
  <read_first>
    - packages/core/src/driver.ts (전체 — BrowserDriver 인터페이스 구조)
    - packages/browser/src/cdp-driver.ts:380-420 (prepareTarget 현 구현 + evaluateInSession 헬퍼 참조)
    - packages/browser/src/cdp-driver.ts 전체에서 `QUICK_MODE_RUNTIME_KEY` 사용 위치 파악
    - packages/browser/tests/cdp-driver.spec.ts (기존 테스트 패턴 — prepareTarget 관련 목 구조)
    - .planning/phases/12-inject/12-RESEARCH.md:330-345 (Section 5: agrune_manifest_load 실행 흐름)
  </read_first>
  <behavior>
    - Test 1: `BrowserDriver` 인터페이스에 `injectManifest?(tabId: number, manifest: AgruneManifest): Promise<void>` 가 optional로 선언됨 (컴파일 타임 — tests.ts에서 타입 확인)
    - Test 2: `CdpDriver.injectManifest(tabId, manifest)` 호출 시 `Runtime.evaluate` 로 `window.__agrune_manifest__ = <JSON>` 표현식이 send됨
    - Test 3: `CdpDriver.injectManifest` 가 이어서 `window.__agrune_quick_mode__.reloadRuntime()` 을 호출하는 Runtime.evaluate도 send (또는 같은 expression에 이어붙이기)
    - Test 4: tabId 가 존재하지 않는 경우 `TAB_NOT_FOUND` 에러를 throw (기존 패턴 따름 — lookup 실패 시 undefined sessionId 처리)
    - Test 5: injectManifest가 safeJsonEmbed 경유로 manifest를 직렬화 — U+2028 포함된 manifest도 정상 embed
    - Test 6: `prepareTarget(target)` 호출부가 per-session preloadManifest가 있을 때 injector.prepareSession(sid, { preloadManifest }) 로 전달할 수 있는 구조 (이번 plan에서는 activePreloadManifest state 없이 항상 undefined 전달이어도 OK — Plan 03에서 추가 계약 가능성만 남김)
  </behavior>
  <action>
    1. packages/core/src/driver.ts 수정:
       - 상단 import에 `AgruneManifest` 추가: `import type { AgruneManifest } from './manifest.js'` (core는 이미 `@agrune/manifest`를 re-export — Phase 11-03 완료)
       - `BrowserDriver` 인터페이스에 optional 메서드 추가:
         ```typescript
         injectManifest?(tabId: number, manifest: AgruneManifest): Promise<void>
         ```
         기존 메서드 뒤 (focusSession 다음)에 삽입.
    2. packages/browser/src/cdp-driver.ts 수정:
       - `CdpDriver` 클래스에 public 메서드 추가:
         ```typescript
         async injectManifest(tabId: number, manifest: AgruneManifest): Promise<void> {
           const target = this.targetManager.getTarget(tabId)
           if (!target?.sessionId) {
             throw createCommandError('TAB_NOT_FOUND', `No session for tabId ${tabId}.`, { tabId })
           }
           const jsonLiteral = JSON.stringify(JSON.stringify(manifest))
           // safeJsonEmbed 호출: 현재 구현은 cdp-runtime-injector 내부 private.
           // 여기서는 작은 중복 inline. 문자열 두 줄이므로 OK.
           const escaped = jsonLiteral.replace(/\u2028/g, '\\u2028').replace(/\u2029/g, '\\u2029')
           const expression =
             `window.__agrune_manifest__ = JSON.parse(${escaped});` +
             `if (window[${JSON.stringify(QUICK_MODE_RUNTIME_KEY)}] && typeof window[${JSON.stringify(QUICK_MODE_RUNTIME_KEY)}].reloadRuntime === 'function') {` +
             `  window[${JSON.stringify(QUICK_MODE_RUNTIME_KEY)}].reloadRuntime();` +
             `}`
           await this.evaluateInSession(target.sessionId, expression)
           await this.refreshSnapshot(tabId)
         }
         ```
         (선택: safeJsonEmbed 를 cdp-runtime-injector.ts 에서 `export`로 바꿔 재사용해도 됨. 간단하므로 inline 복제도 허용.)
       - Import 보강: `AgruneManifest` 타입 import, `createCommandError` import (이미 있을 가능성 높음 — 확인 후 추가), `QUICK_MODE_RUNTIME_KEY` import (이미 있음).
    3. packages/browser/tests/cdp-driver.spec.ts 수정 또는 확장:
       - 기존 파일 끝에 `describe('injectManifest')` 블록 추가:
         - Case A: valid tabId + manifest → `Runtime.evaluate` 호출이 1회 이상, expression에 `__agrune_manifest__` + `reloadRuntime` 둘 다 포함
         - Case B: unknown tabId → promise reject + error.code === 'TAB_NOT_FOUND'
         - Case C: manifest 에 U+2028 포함 → expression에 `\\u2028` 로 이스케이프됨
         - Case D: 호출 후 `refreshSnapshot` 트리거 확인 (snapshot update callback fire 또는 sessions.updateSnapshot 호출 관찰 — 기존 테스트 패턴 따름)
         - 최소 8개 assertion.
       - 만약 기존 cdp-driver.spec.ts 구조상 targetManager mock 이 복잡하면 `injectManifest` 단위 테스트는 helper 함수 추출 후 직접 호출하는 방식도 허용 (단, acceptance criteria의 Integration assertion 은 유지).
  </action>
  <verify>
    <automated>pnpm --filter @agrune/core run typecheck &amp;&amp; pnpm --filter @agrune/browser run typecheck &amp;&amp; pnpm --filter @agrune/browser run test -- cdp-driver</automated>
  </verify>
  <acceptance_criteria>
    - grep `"injectManifest?"` packages/core/src/driver.ts → 1 match (optional 메서드)
    - grep `"injectManifest"` packages/browser/src/cdp-driver.ts → 2+ matches (선언 + 어쩌면 참조)
    - grep `"window.__agrune_manifest__"` packages/browser/src/cdp-driver.ts → 1+ match (Runtime.evaluate expression 내부)
    - grep `"reloadRuntime"` packages/browser/src/cdp-driver.ts → 1+ match
    - grep `"TAB_NOT_FOUND"` packages/browser/src/cdp-driver.ts injectManifest 함수 근처 → 1+ match
    - `pnpm --filter @agrune/core run typecheck` exit 0
    - `pnpm --filter @agrune/browser run typecheck` exit 0
    - `pnpm --filter @agrune/browser run test -- cdp-driver` 전체 PASS (기존 + 신규 injectManifest 테스트 포함)
    - `pnpm --filter @agrune/browser run build` exit 0
  </acceptance_criteria>
  <done>
    BrowserDriver.injectManifest가 인터페이스에 optional로 선언되고 CdpDriver가 Runtime.evaluate 경로로 구현한다. TAB_NOT_FOUND 처리 + safeJsonEmbed + reloadRuntime 호출이 단위 테스트로 가둬진다. MCP layer(Plan 03)가 driver.injectManifest(tabId, manifest)로 의존할 수 있는 안정된 계약 확정.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDP → page JavaScript context | `Page.addScriptToEvaluateOnNewDocument` + `Runtime.evaluate` 로 embed되는 manifest JSON은 author trust boundary 내부에서 왔으나 페이지 JS context 에서 실행됨 |
| CLI → manifest 저장 장소 | manifest는 validateManifest() 를 거친 author 입력. Phase 11-05에서 검증 완료 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | `window.__agrune_preload_manifest__` embed — malicious manifest value가 JS statement 이탈 시도 | mitigate | `JSON.stringify(JSON.stringify(manifest))` 중첩 + safeJsonEmbed 로 U+2028/U+2029 이스케이프. 최종 페이지 내 코드는 `JSON.parse("...")` wrapper → 임의 JS 실행 경로 차단. Research Section 3 권장 |
| T-12-05 | Tampering | `CdpDriver.injectManifest` Runtime.evaluate expression 조립 시 manifest 값이 statement boundary 이탈 | mitigate | 동일 JSON.stringify 중첩 + U+2028/U+2029 escape. Expression 은 `JSON.parse(...)` wrapper + `reloadRuntime()` 호출 시퀀스뿐 — 임의 코드 주입 불가 |
| T-12-06 | Denial of Service | reloadRuntime rapid-fire → installPageAgentRuntime 중복 실행 → listener 중복/메모리 누수 | mitigate | 50ms debounce + clearTimeout. Phase 11-04 known stub 해결 |
| T-12-07 | Elevation of Privilege | `BrowserDriver.injectManifest` 가 optional 이므로 mock driver에서 누락되면 MCP tool이 어떻게 동작? | accept | Plan 03에서 MCP layer가 `typeof driver.injectManifest === 'function'` 타입 가드로 defensively 처리. 현재 CdpDriver가 유일한 구현체 (RESEARCH Section 9 Open Q 2) |
| T-12-08 | Tampering | cachedInjectedSource 교차 오염 (preload 있는 세션 결과가 이후 preload 없는 세션에 재사용) | mitigate | preloadManifest 있을 때 `getInjectedSourceWithPreload`로 cache 우회. getInjectedSource() 자체는 preload 무관 — 즉 두 경로 분리 |
</threat_model>

<verification>
전체 관문:
- `pnpm --filter @agrune/core run typecheck` PASS
- `pnpm --filter @agrune/browser run typecheck` PASS
- `pnpm --filter @agrune/browser run test` 전체 PASS (기존 60+ 테스트 + 신규 preload 테스트 + 신규 injectManifest 테스트)
- `pnpm --filter @agrune/browser run build` PASS
- `grep -rn "injectManifest" packages/browser/src packages/core/src` → matches in driver.ts + cdp-driver.ts
- 회귀: `pnpm --filter @agrune/runtime run test` 여전히 PASS (Plan 01 결과 보존)
- Pitfall 1 (cache isolation): preload 테스트의 cache isolation case PASS
- Pitfall 2 (reloadRuntime idempotency): bootstrap source grep에서 debounce 패턴 확인
- Pitfall 3 (prepareSession 호출 순서): preload가 Runtime.evaluate + addScriptToEvaluateOnNewDocument 둘 다 커버하는지 source 검증
</verification>

<success_criteria>
INJECT-01 + RESOLVE-01 달성:
1. `prepareSession({ preloadManifest })` 로 외부 사이트 zero-RTT preload가 가능
2. `window.__agrune_preload_manifest__` embed 가 safeJsonEmbed + JSON.parse wrapper 경유로 안전
3. `BrowserDriver.injectManifest` 가 interface에 선언되고 CdpDriver가 구현
4. ManifestLoader 우선순위 ladder (window > preload > inline > idle) 가 테스트로 회귀 방지
5. reloadRuntime debounce — Phase 11-04 known stub 완전 해결
6. `@agrune/browser` 전체 테스트 PASS + typecheck PASS
</success_criteria>

<output>
After completion, create `.planning/phases/12-inject/12-02-SUMMARY.md` summarizing:
- What Was Built (Task 1 injector preload + Task 2 driver injectManifest)
- Verification Results (typecheck + test 결과 테이블)
- Threat Model Coverage (T-12-04..08 disposition 확인)
- Known Stubs (있으면: 예 — Plan 03에서 activePreloadManifest state 관리를 추가할 수 있음)
- Commits 테이블
</output>
