---
phase: "14"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/runtime/dom-utils.ts
  - packages/runtime/tests/sensitive-or-only.spec.ts
autonomous: true
requirements: [MACRO-03]
tags: [sensitive, dom-heuristic, i18n, word-boundary, regex, security]
must_haves:
  truths:
    - "`type=password` / autocomplete 기존 경로는 회귀 없이 유지된다"
    - "English word-boundary regex가 name/id/placeholder/aria-label에서 `password|passwd|pwd|cvv|ssn|secret|pin|otp|passcode`을 감지한다"
    - "`passwordless`·`passwords123`은 false (word boundary 오탐 방지)"
    - "한국어(`비밀번호`), 일본어(`パスワード`), 중국어(`密码`), 프랑스어(`mot de passe`), 독일어(`passwort`), 스페인어(`contraseña`) aria-label을 감지한다"
    - "aria-label이 `비밀번호 입력`처럼 공백 토큰을 포함해도 토큰 매칭으로 감지한다"
    - "`manifestFlag: false` argument는 TypeScript 컴파일 에러로 차단된다 (OR-only lock 유지)"
    - "`snapshot.ts` line 366의 valuePreview는 `isSensitive` 확장만으로 자동 마스킹된다 (추가 수정 없음)"
  artifacts:
    - path: "packages/runtime/src/runtime/dom-utils.ts"
      provides: "SENSITIVE_WORD_BOUNDARY / SENSITIVE_NAME_ATTR / SENSITIVE_ARIA_LABELS_MULTILANG exports + 확장된 isSensitive"
      contains: "SENSITIVE_WORD_BOUNDARY"
    - path: "packages/runtime/tests/sensitive-or-only.spec.ts"
      provides: "MACRO-03 회귀 테스트"
      contains: "multilang"
  key_links:
    - from: "packages/runtime/src/runtime/snapshot.ts"
      to: "packages/runtime/src/runtime/dom-utils.ts::isSensitive"
      via: "captureTargetState → isSensitive(element) → state.sensitive → valuePreview null"
      pattern: "isSensitive\\(element\\)"
---

<objective>
Runtime DOM heuristic을 확장해 manifest `sensitive:false` 악성 우회를 차단한다. `isSensitive`에 단어 경계 regex(영어)와 다국어 ARIA label set을 추가하고, 기존 OR-only 계약(`manifestFlag?: true | undefined`) + 기존 4단계 체크(`password` type / autocomplete whitelist / `data-agrune-sensitive` 레거시)를 유지한다. `snapshot.ts`의 `valuePreview` 마스킹은 이미 `isSensitive` 결과에 의존하므로 추가 수정 없이 자동 전파된다.

Purpose: Cross-cutting Pitfall 4 (sensitive 우회) 의 primary mitigation — MacroRunner/agrune_macro_run 이전에 먼저 배치해 이후 plan들이 확장된 `isSensitive`를 전제로 작성되게 한다.
Output: 확장된 `dom-utils.ts` + 회귀 테스트. valuePreview 자동 마스킹은 기존 snapshot.ts 코드로 자동 전파 (D-추가 없음).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-macro/14-CONTEXT.md
@.planning/phases/14-macro/14-RESEARCH.md
@.planning/phases/11-manifest/11-02-SUMMARY.md
@packages/runtime/src/runtime/dom-utils.ts
@packages/runtime/tests/sensitive-or-only.spec.ts
@packages/runtime/src/runtime/snapshot.ts

<interfaces>
<!-- 기존 isSensitive 계약 (Phase 11-02 완료) - 이 plan은 로직을 append할 뿐 시그니처는 보존 -->

From packages/runtime/src/runtime/dom-utils.ts (line 320-354, existing):
```typescript
const AUTOCOMPLETE_SENSITIVE = new Set([
  'current-password','new-password','one-time-code',
  'cc-number','cc-csc','cc-exp','cc-exp-month','cc-exp-year',
])

// OR-only 계약: manifestFlag: false 는 타입 레벨에서 금지
export function isSensitive(
  element: HTMLElement,
  manifestFlag?: true | undefined,
): boolean {
  if (manifestFlag === true) return true
  if (element instanceof HTMLInputElement && element.type === 'password') return true
  const autocomplete = element.getAttribute('autocomplete')
  if (autocomplete) {
    const normalized = autocomplete.toLowerCase().trim()
    if (AUTOCOMPLETE_SENSITIVE.has(normalized)) return true
  }
  if (element.getAttribute('data-agrune-sensitive') === 'true') return true
  return false
}
```

From packages/runtime/src/runtime/snapshot.ts (line 366, existing — downstream auto-consumer):
```typescript
// captureTarget 내부
const valuePreview =
  isFillableElement(element) && !state.sensitive ? element.value : null
// state.sensitive 는 captureTargetState 내에서 isSensitive(element) 로 계산됨
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend isSensitive with word-boundary regex + multilingual ARIA label (RED→GREEN)</name>
  <files>packages/runtime/src/runtime/dom-utils.ts, packages/runtime/tests/sensitive-or-only.spec.ts</files>
  <read_first>
    - packages/runtime/src/runtime/dom-utils.ts (lines 310-355) — 기존 isSensitive 구현 4단계 경로 확인
    - packages/runtime/tests/sensitive-or-only.spec.ts (전체) — 기존 13 tests 회귀 보장 대상 확인
    - .planning/phases/14-macro/14-RESEARCH.md "Q5 / Q6" + "Code Examples" 섹션
    - .planning/phases/11-manifest/11-02-SUMMARY.md "OR 결합 체인" 섹션
  </read_first>
  <behavior>
    Test coverage (tests/sensitive-or-only.spec.ts 에 추가 describe 블록):

    describe('Phase 14 — word-boundary regex'):
      - Test: input[placeholder="Password"] → true (word boundary match, 대소문자 무관)
      - Test: input[placeholder="Enter your password"] → true
      - Test: input[placeholder="passwordless login"] → false (word boundary 오탐 방지)
      - Test: input[placeholder="My favorite passwords123"] → false (연속 문자 boundary 통과 안 됨)
      - Test: input[placeholder="Enter PIN"] → true
      - Test: input[placeholder="What is your SSN?"] → true
      - Test: input[placeholder="CVV"] → true
      - Test: input[placeholder="OTP code"] → true (OR passcode)
      - Test: input[name="user_password"] → true (underscore 분리 boundary)
      - Test: input[name="user-pwd-field"] → true (dash 분리 boundary)
      - Test: input[id="my.password.input"] → true (dot 분리 boundary)
      - Test: input[name="crosswords"] → false (SENSITIVE_NAME_ATTR 비매칭 — "pwd"가 단어 내부)
      - Test: input[name="passwordless"] → false
      - Test: input[aria-label="Credit card CVV"] → true (aria-label word-boundary 체크)

    describe('Phase 14 — multilingual ARIA label'):
      - Test: input[aria-label="비밀번호"] → true (Korean)
      - Test: input[aria-label="비밀번호 입력"] → true (Korean, space-separated token match)
      - Test: input[aria-label="パスワード"] → true (Japanese)
      - Test: input[aria-label="暗証番号"] → true (Japanese)
      - Test: input[aria-label="密码"] → true (Chinese Simplified)
      - Test: input[aria-label="密碼"] → true (Chinese Traditional)
      - Test: input[aria-label="mot de passe"] → true (French phrase — lowercase Set entry + trim)
      - Test: input[aria-label="Mot de Passe"] → true (case-insensitive)
      - Test: input[aria-label="passwort"] → true (German)
      - Test: input[aria-label="contraseña"] → true (Spanish)
      - Test: input[aria-label="username"] → false (non-sensitive label)
      - Test: input[aria-label="   "] → false (empty after trim)
      - Test: input[aria-label=""] → false (missing/empty)

    describe('Phase 14 — regression (기존 계약 유지)'):
      - 기존 13 tests 그대로 pass 확인 (manifestFlag true / password type / autocomplete whitelist / legacy data-attr / false arg 타입 오류)
      - Test: manifestFlag === true → true (확장 후에도 최우선 경로)
      - Test: input[type="password"] → true (2번째 경로 유지)
      - Test: input[autocomplete="cc-number"] → true (3번째 경로 유지)
      - Test: element[data-agrune-sensitive="true"] → true (4번째 경로 유지)
      - @ts-expect-error test: isSensitive(el, false) — 컴파일 에러 유지
  </behavior>
  <action>
    **RED step** — 먼저 failing tests 작성:
    1. `packages/runtime/tests/sensitive-or-only.spec.ts` 에 위 3개 describe 블록 추가 (기존 테스트는 보존).
       - `new Set(...MULTILANG)` 과 `SENSITIVE_WORD_BOUNDARY` 을 dom-utils에서 import해 테스트에서 직접 참조(선택적 — 없으면 동작만 검증).
       - jsdom `document.createElement('input')` + `setAttribute` 패턴으로 element 구성 (기존 스펙과 동일).
       - 공백 토큰 테스트는 `setAttribute('aria-label', '비밀번호 입력')` 기대값 true로 검증.
       - `@ts-expect-error // @ts-expect-error false is not allowed` 재확인.
    2. `pnpm --filter @agrune/runtime run test -- sensitive-or-only` 실행 → RED (신규 테스트 fail) 확인.
    3. git commit: `test(14-01): add failing tests for word-boundary regex + multilang ARIA (TDD RED)`.

    **GREEN step** — dom-utils.ts 확장:
    1. `packages/runtime/src/runtime/dom-utils.ts` 상단 (기존 `AUTOCOMPLETE_SENSITIVE` 아래) 에 상수 추가 — `export` 필수:
       ```typescript
       // Phase 14 MACRO-03: sensitive DOM heuristic 확장
       // placeholder / aria-label 용 (공백으로 단어 분리됨)
       export const SENSITIVE_WORD_BOUNDARY =
         /\b(password|passwd|pwd|cvv|ssn|secret|pin|otp|passcode)\b/i

       // name / id 용 (underscore·dash·dot 을 단어 경계로 인식)
       export const SENSITIVE_NAME_ATTR =
         /(?:^|[_\-\s.])(?:password|passwd|pwd|cvv|ssn|secret|pin|otp|passcode)(?:[_\-\s.]|$)/i

       // 다국어 aria-label exact/token match. 모든 엔트리는 lowercase·trim·NFC 상태로 저장.
       export const SENSITIVE_ARIA_LABELS_MULTILANG: ReadonlySet<string> = new Set<string>([
         // 한국어
         '비밀번호', '패스워드', '핀번호', '보안코드',
         // 일본어
         'パスワード', 'ぱすわーど', '暗証番号',
         // 중국어 간체 / 번체
         '密码', '口令', '密碼',
         // 프랑스어 (lowercase + trim 형태)
         'mot de passe',
         // 독일어
         'passwort', 'kennwort',
         // 스페인어
         'contraseña',
       ])
       ```
    2. `isSensitive` 함수 return false 직전에 Phase 14 블록을 append (기존 4단계 경로 뒤):
       ```typescript
       // Phase 14 추가 — word-boundary regex
       const placeholder = element.getAttribute('placeholder') ?? ''
       if (placeholder && SENSITIVE_WORD_BOUNDARY.test(placeholder)) return true

       const nameAttr = element.getAttribute('name') ?? ''
       if (nameAttr && SENSITIVE_NAME_ATTR.test(nameAttr)) return true

       const idAttr = element.id ?? ''
       if (idAttr && SENSITIVE_NAME_ATTR.test(idAttr)) return true

       // Phase 14 추가 — aria-label (exact phrase + token + word-boundary)
       const ariaLabelRaw = element.getAttribute('aria-label') ?? ''
       const ariaLabel = ariaLabelRaw.trim().toLowerCase()
       if (ariaLabel) {
         if (SENSITIVE_ARIA_LABELS_MULTILANG.has(ariaLabel)) return true
         // 공백 분리 토큰 (e.g. "비밀번호 입력")
         for (const token of ariaLabel.split(/\s+/)) {
           if (token && SENSITIVE_ARIA_LABELS_MULTILANG.has(token)) return true
         }
         if (SENSITIVE_WORD_BOUNDARY.test(ariaLabelRaw)) return true
       }
       ```
    3. CJK 문자는 regex `\b`가 동작 안 한다는 것을 주석에 명시 — Set exact-match 가 의도적 설계임을 코드로 고정 (per research Q6).
    4. 기존 snapshot.ts·captureTargetState 는 **수정하지 않는다**. `isSensitive` 한 함수 확장만으로 `state.sensitive → valuePreview null` 이 자동 전파된다 (per research Q7, 14-CONTEXT.md의 "Anti-pattern 3").
    5. `pnpm --filter @agrune/runtime run test` 실행 → 신규 + 기존 전 테스트 GREEN 확인.
    6. `pnpm --filter @agrune/runtime run typecheck` → exit 0 확인.
    7. git commit: `feat(14-01): extend isSensitive with word-boundary + multilang ARIA (TDD GREEN)`.
  </action>
  <acceptance_criteria>
    grep-verifiable:
    - `grep -n "SENSITIVE_WORD_BOUNDARY" packages/runtime/src/runtime/dom-utils.ts` → 최소 3 matches (export 1 + isSensitive 내부 2)
    - `grep -n "SENSITIVE_NAME_ATTR" packages/runtime/src/runtime/dom-utils.ts` → 최소 3 matches (export 1 + name/id 각 1)
    - `grep -n "SENSITIVE_ARIA_LABELS_MULTILANG" packages/runtime/src/runtime/dom-utils.ts` → 최소 2 matches (export + has check)
    - `grep -c "비밀번호" packages/runtime/src/runtime/dom-utils.ts` ≥ 1
    - `grep -c "パスワード" packages/runtime/src/runtime/dom-utils.ts` ≥ 1
    - `grep -c "密码" packages/runtime/src/runtime/dom-utils.ts` ≥ 1
    - `grep -c "mot de passe" packages/runtime/src/runtime/dom-utils.ts` ≥ 1
    - `grep -c "Phase 14" packages/runtime/tests/sensitive-or-only.spec.ts` ≥ 3 (3 describe 블록)
    - `wc -l packages/runtime/tests/sensitive-or-only.spec.ts` ≥ 180 (기존 90 + 확장 ≥ 90)
    - `grep -n "isSensitive\\b" packages/runtime/src/runtime/snapshot.ts` 결과 수 변경 없음 (snapshot.ts 미수정 증명)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @agrune/runtime run test -- sensitive-or-only &amp;&amp; pnpm --filter @agrune/runtime run test &amp;&amp; pnpm --filter @agrune/runtime run typecheck</automated>
  </verify>
  <done>
    - sensitive-or-only.spec.ts: Phase 14 확장 describe 3개 전부 green, 기존 13 tests 회귀 없음
    - @agrune/runtime 테스트 전체 (기존 110+ tests) 전부 green
    - typecheck exit 0
    - RED / GREEN 커밋 각 1회 존재 (`git log --oneline -n 5` 로 확인)
    - snapshot.ts 미수정 (자동 전파 검증 — 회귀 테스트에서 valuePreview null 이 연쇄 확인되어야 함)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page DOM → runtime heuristic | 외부 사이트 / 악성 manifest 가 제공한 DOM 속성 (`aria-label`, `placeholder`, `name`, `id`) 이 runtime 입력. 문자열 길이·인코딩·homoglyph 공격 면에서 untrusted. |
| manifest `sensitive:false` 주장 → runtime | Phase 11-02 의 타입 레벨 OR-only 계약이 컴파일 시점 방어. 악성 문자열 manifest 가 schema 우회를 시도해도 runtime heuristic 이 2차 방어. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | `isSensitive` — 악성 manifest 가 `sensitive:false` 로 우회 시도 | mitigate | `manifestFlag?: true \| undefined` 타입 + runtime heuristic 5–7 OR 결합. Phase 14 확장으로 word-boundary + 다국어 aria-label 까지 커버 (14-CONTEXT.md §domain §2) |
| T-14-02 | Tampering | DOM 속성 regex 오탐 / 누락 | mitigate | Word-boundary negative test (`passwordless`, `passwords123`, `crosswords`) + token match test (`비밀번호 입력`) 의 의무 회귀. `\b` 는 CJK 비작동이므로 CJK 는 Set exact-match 로 분리 처리 (research Q6) |
| T-14-03 | Info Disclosure | `valuePreview` 로 민감 값 유출 | mitigate | snapshot.ts line 366 (`!state.sensitive ? element.value : null`) 이 `isSensitive` 결과에 의존 → 확장이 자동 전파. 이 plan 에서 별도 코드 추가 불필요. 회귀 테스트로 전파 경로 재확인 |
| T-14-04 | DoS | regex ReDoS (catastrophic backtracking) | mitigate | 두 regex 모두 선형 — alternation 만 있고 중첩 quantifier 없음. `(?:^\|[_\-\s.])` prefix 도 atomic 한 character class. 추가 테스트 대신 정적 분석으로 확정 (이 mitigation 은 코드 리뷰 단계에서 재확인) |
| T-14-05 | Tampering | Unicode homoglyph (e.g., Cyrillic "а" vs Latin "a") 로 우회 | accept | v0.5 범위 외. 방어 하면 오탐도 급증 (NFC/NFKC 정규화 + 스크립트 혼용 감지는 별도 프로젝트). Phase 18 REGISTRY PR bot 이 manifest 검토 단계에서 완화하는 secondary 경로가 있음 |
| T-14-06 | EoP | 다국어 Set 확장 — 서드파티 코드가 런타임에 override | accept | `ReadonlySet` 타입 표기 + Set 은 module-level const — 런타임 override 는 Object.assign 없이 불가. Phase 14 범위에서 API 추가 안 함 (확장 필요 시 Phase 18+) |
</threat_model>

<verification>
- `pnpm --filter @agrune/runtime run test` → 전 테스트 green
- `pnpm --filter @agrune/runtime run typecheck` → exit 0
- grep 기준 acceptance_criteria 모두 통과
- snapshot.ts·captureTargetState·command-handlers.ts 미수정 (git diff 로 확인)
</verification>

<success_criteria>
- MACRO-03 runtime heuristic 층 확장 완료 (manifest `sensitive:false` 우회 불가 계약 강화)
- word-boundary 오탐 방지 회귀 (`passwordless` / `passwords123` / `crosswords`) 확정 테스트로 락
- 다국어 ARIA label (한/일/중/프/독/스) 감지 락
- valuePreview 자동 마스킹 전파 검증 (snapshot.ts 미수정 증거)
- Phase 14 후속 plan (MacroRunner, agrune_macro_run) 가 확장된 `isSensitive` 를 전제로 진행 가능
</success_criteria>

<output>
After completion, create `.planning/phases/14-macro/14-01-SUMMARY.md`
</output>
