---
phase: 18-registry
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - registry-seed/README.md [NEW]
  - registry-seed/REGISTRY_GOVERNANCE.md [NEW]
  - registry-seed/maintainers.json [NEW]
  - registry-seed/incidents.json [NEW]
  - registry-seed/index.json [NEW]
  - registry-seed/manifests/news.ycombinator.com@1.0.0.json [NEW]
  - registry-seed/manifests/en.wikipedia.org@1.0.0.json [NEW]
  - registry-seed/manifests/developer.mozilla.org@1.0.0.json [NEW]
  - registry-seed/manifests/docs.python.org@1.0.0.json [NEW]
  - registry-seed/manifests/www.gutenberg.org@1.0.0.json [NEW]
  - registry-seed/manifests/arxiv.org@1.0.0.json [NEW]
  - registry-seed/manifests/pypi.org@1.0.0.json [NEW]
  - registry-seed/manifests/hn.algolia.com@1.0.0.json [NEW]
  - registry-seed/manifests/www.wikidata.org@1.0.0.json [NEW]
  - registry-seed/manifests/observablehq.com@1.0.0.json [NEW]
  - scripts/registry-seed/validate-seed.mjs [NEW]
  - package.json
autonomous: true
requirements:
  - REGISTRY-01
  - REGISTRY-04
tags:
  - registry
  - seed
  - governance
  - manifests

must_haves:
  truths:
    - "`registry-seed/` 디렉토리가 agrune repo root 에 존재하고 10 개 `manifests/<host>@1.0.0.json` 파일이 verified tier metadata 로 존재한다."
    - "모든 seed manifest 가 `RegistryEntrySchema.parse` 를 통과한다 (scripts/registry-seed/validate-seed.mjs 가 10/10 pass)."
    - "`REGISTRY_GOVERNANCE.md` 가 tier(verified/community/unlisted) / velocity limit (첫 3 PR 30일 holddown) / revocation (incidents.json) / maintainer absence default (30일→community 강등, 60일→disable-all 경고) 를 명시한다."
    - "`incidents.json` 은 `[]` (empty array) 로 초기화되어 있고 CODEOWNERS 로 maintainer 만 변경 가능함이 README 에 명시된다."
    - "`index.json` 은 10 개 host entry 의 machine-readable 카탈로그 (host, version, tier, contentHash, path) 이다."
    - "`maintainers.json` allow-list 가 velocity limit 면제 대상을 정의한다 (v0.5 초기는 solo maintainer)."
    - "루트 `package.json` 에 `validate:seed` 스크립트가 추가되어 `pnpm validate:seed` 로 10 manifest 검증이 로컬/CI 에서 실행 가능하다."
  artifacts:
    - path: "registry-seed/README.md"
      provides: "agrune/maps 공개 repo 의 루트 README — 사용 방법, tier 설명, 기여 가이드"
      min_lines: 50
    - path: "registry-seed/REGISTRY_GOVERNANCE.md"
      provides: "Tier/velocity/revocation/maintainer absence 전 섹션 governance doc"
      min_lines: 80
      contains: "verified"
    - path: "registry-seed/incidents.json"
      provides: "초기 empty array — revocation 경로 placeholder"
      contains: "[]"
    - path: "registry-seed/maintainers.json"
      provides: "velocity limit 면제 GitHub handles allow-list"
      contains: "maintainers"
    - path: "registry-seed/index.json"
      provides: "10 host machine-readable catalog (PR bot 이 나중에 regenerate)"
      contains: "news.ycombinator.com"
    - path: "registry-seed/manifests/news.ycombinator.com@1.0.0.json"
      provides: "Seed manifest #1 — HN story link / up-vote / login button targets"
      contains: "news.ycombinator.com"
    - path: "scripts/registry-seed/validate-seed.mjs"
      provides: "registry-seed 전체 스키마 검증 스크립트 (pnpm validate:seed)"
      contains: "RegistryEntrySchema"
  key_links:
    - from: "scripts/registry-seed/validate-seed.mjs"
      to: "@agrune/registry (RegistryEntrySchema)"
      via: "node import from workspace"
      pattern: "@agrune/registry"
    - from: "registry-seed/manifests/*.json"
      to: "@agrune/manifest schema v3 (via @agrune/registry RegistryEntrySchema)"
      via: "schema validation at build/CI time"
      pattern: "registry.*host.*manifest"
    - from: "package.json"
      to: "scripts/registry-seed/validate-seed.mjs"
      via: "npm script validate:seed"
      pattern: "validate:seed"
---

<objective>
외부 `github.com/agrune/maps` 공개 repo 의 "seed" 컨텐츠 (10 개 low-risk manifest + governance doc + catalog + incidents + maintainers) 를 agrune repo 내 `registry-seed/` 디렉토리에 프로토타입한다. 실제 repo 생성 및 초기 push 는 Phase 18-04 의 external-sync-instructions.md 로 사용자에게 위임 (17-04 external-sync 패턴 재사용, autonomous 제약).

Purpose: Registry public launch 의 "day-0 콘텐츠" 를 로컬에서 완전히 구성 + 스키마 검증해, 실제 repo 에 push 된 후 즉시 `agrune maps add <any-of-10-hosts>` 가 동작하는 상태를 보장. Governance doc 은 Phase 18 의 normative spec — PR bot (Plan 04) 가 코드로 집행할 velocity/tier/revocation 규칙의 single source of truth.

Output:
- 10 개 seed manifest (RESEARCH "Seed Manifest Selection" 권고안 채택: HN / Wikipedia / MDN / Python docs / Gutenberg / arXiv / PyPI / HN Algolia / Wikidata / Observable)
- `REGISTRY_GOVERNANCE.md` — 4 tier-transition / velocity 30일 holddown / incident 경로 / maintainer absence default
- `incidents.json = []`, `maintainers.json`, `index.json` (catalog)
- `validate-seed.mjs` 스크립트 + `pnpm validate:seed` 엔트리
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-registry/18-CONTEXT.md
@.planning/phases/18-registry/18-RESEARCH.md

@packages/manifest/src/schema.ts
@packages/manifest/src/builders.ts
@.planning/phases/17-remove/external-sync-instructions.md

<interfaces>
<!-- Plan 03 이 생성하는 JSON shape 은 Plan 01 의 RegistryEntrySchema 와 일치해야 함. -->
<!-- Plan 01 과 Plan 03 은 parallel — Plan 03 은 package 를 직접 의존하지 않음 (순수 JSON/Markdown). -->
<!-- 하지만 validate-seed.mjs 는 @agrune/registry 를 import 하므로 실행 순서는 Plan 01 build 후. -->

RegistryEntry JSON shape (이 plan 이 생성):
```json
{
  "registry": {
    "host": "news.ycombinator.com",
    "version": "1.0.0",
    "tier": "verified",
    "author": "agrune-maintainers",
    "submittedAt": "2026-04-20T00:00:00.000Z",
    "reviewedBy": ["agrune-maintainers"],
    "allowedEnvironments": ["dev", "prod"],
    "seedUrl": "https://news.ycombinator.com"
  },
  "manifest": {
    "schemaVersion": 3,
    "id": "news-ycombinator-v1",
    "groups": [
      {
        "groupId": "front-page",
        "targets": [
          {
            "targetId": "storyLink",
            "actionKinds": ["click"],
            "selector": [
              { "type": "role", "role": "link", "nameRegex": "." }
            ]
          }
        ]
      }
    ]
  }
}
```

Governance doc shape (REGISTRY_GOVERNANCE.md 섹션 헤더 — PR bot 이 parse 할 수 있게 안정된 heading 형식):
```markdown
# agrune/maps Registry Governance
## Tiers
### verified
### community
### unlisted
## Velocity Limit
## Revocation Path
## Maintainer Absence Default
## Tier Transition Triggers
## Staleness Detection
```

incidents.json:
```json
[]
```
(Incident 추가 형식: `{ host, affectedVersions: [...], reason, reportedAt, confirmedBy, action: 'unlisted' }`)

maintainers.json:
```json
{
  "version": 1,
  "maintainers": ["<github-handle-solo-maintainer>"],
  "notes": "Velocity limit 면제. 추가 시 REGISTRY_GOVERNANCE.md Tier Transition 섹션 2 명 승인 필요"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Governance docs + meta files (README, GOVERNANCE, incidents, maintainers, index)</name>
  <files>
    registry-seed/README.md [NEW]
    registry-seed/REGISTRY_GOVERNANCE.md [NEW]
    registry-seed/incidents.json [NEW]
    registry-seed/maintainers.json [NEW]
    registry-seed/index.json [NEW]
  </files>
  <action>
    1. `registry-seed/README.md`:
       - 첫 줄: `# agrune/maps — Public manifest registry for agrune v0.5+`
       - What / Why: manifest 기반 외부 사이트 자동화 커뮤니티 registry
       - Directory structure: `manifests/<host>@<version>.json` / `incidents.json` / `maintainers.json` / `index.json` / `REGISTRY_GOVERNANCE.md`
       - How to use: `agrune maps add <host>` (Plan 02 CLI 를 참조 — Phase 18 종료 후 link 활성)
       - How to contribute: `agrune maps submit <file.ts>` (Plan 02 submit CLI 참조). PR reviewer = maintainer (tier=community 초기값), tier promotion 은 `REGISTRY_GOVERNANCE.md` 참조
       - License: MIT (JSON manifest 내용), 컨텐츠는 각 사이트의 ToS 준수 기여자 책임
       - v0.5 seed 10 개 host 리스트 (RESEARCH 권고안)
       - `CODEOWNERS` 경로 안내: `incidents.json`, `REGISTRY_GOVERNANCE.md`, `maintainers.json` 은 maintainer-only 변경. `manifests/*` 은 community 기여 가능
       - Minimum 50 lines

    2. `registry-seed/REGISTRY_GOVERNANCE.md` — RESEARCH "Governance Doc Structure" 권고안 기반, 섹션 순서:
       ```
       # agrune/maps Registry Governance
       _Last updated: 2026-04-20 (Phase 18 v0.5 kickoff)_

       ## Tiers
         ### verified
           - 2명 이상 maintainer review + 주간 health check 2주 연속 green
           - allowedEnvironments: ['dev','prod'] 허용
           - Tier promotion: community 에서 3개월 stable + incident 0건 (Tier Transition 참조)
         ### community
           - PR bot schema pass + maintainer 1명 review
           - allowedEnvironments: ['dev'] 강제 (schema 레벨에서 거부)
           - 모든 신규 기여의 기본 tier
         ### unlisted
           - 과거 verified/community 가 revocation 받은 경우
           - CLI 는 경고 + 사용자 접근 차단 (auto-disable)
           - incidents.json 에 기록됨

       ## Velocity Limit
         - 신규 저자 (first GitHub interaction with agrune/maps)의 첫 3 PR:
           - PR 간 최소 간격 30일 (spray attack 방지)
           - PR bot 이 `velocity:holddown` 라벨 부착 → 병합 지연
           - 4번째 PR 부터 자동 해제
         - maintainers.json allow-list 는 면제

       ## Revocation Path
         1. Incident 보고: `agrune/maps` issues 에 `incident` 라벨로 submit
         2. Maintainer triage — 확인 시 `incidents.json` 업데이트 + 해당 manifest 를 `unlisted` tier 로 이동 (PR)
         3. 사용자가 다음 `agrune maps doctor --refresh` 실행 시:
            - CLI 가 incidents.json fetch
            - 해당 host 의 로컬 lockfile 에 `disabled: { reason: 'revoked', at: ... }` 기록
            - 이후 `add` 시도 시 경고

       ## Maintainer Absence Default
         - 모든 maintainer 30일 무응답 → 모든 `verified` tier manifest 가 자동 `community` 로 강등 (PR bot scheduled workflow)
         - 60일 무응답 → CLI doctor 가 `disable-all` 경고, 신규 `agrune maps add` 는 confirm 필요

       ## Tier Transition Triggers
         | From | To | Trigger | Who approves |
         |------|----|---------|--------------|
         | 신규 PR | community | PR bot schema pass + 1 maintainer review | Maintainer (단일) |
         | community | verified | 3개월 stable + 0 incidents + 2 maintainers + health check green 12/12주 | Maintainer ×2 |
         | any | unlisted | Incident 확정 or revocation vote | Maintainer ×1 (emergency) |
         | verified | community | Maintainer absence 30일 (자동) | Bot |

       ## Staleness Detection
         - Weekly selector health check (GitHub Actions cron, Plan 04 에서 구현)
         - 2주 연속 fail → `stale` 라벨 + incidents 보고 고려
         - 4주 fail → manifest 에 `registry.staleSince` 필드 자동 추가 (PR bot)
         - 8주 무응답 → 자동 `unlisted` 전환 (maintainer approval 필요)
         - Client-side threshold (CLI doctor):
           - fetchedAt 이내 7일: fresh
           - 7-28일: week_old (info)
           - 28-56일: stale (warning + suggest --auto-disable)
           - 56일+: auto_disabled (confirmed)

       ## Security Guardrails
         - PR bot 이 `sensitive:true` 필드 삭제 / `sensitive:false` 전환을 `requires-human-review:sensitive` 라벨로 강제
         - allowedEnvironments 는 schema 에서 community ≤ ['dev'] 강제
         - seedUrl privacy: public-resolvable host 만 허용 (localhost / private IP / *.internal 거부) — PR bot 이 DNS lookup 수행
         - submit 은 `public_repo` scope 만 요구 (device flow OAuth)

       ## Reporting a Security Issue
         - security-sensitive vulnerabilities: `SECURITY.md` (추후 추가) — Phase 18 v0.5 는 issues 로 접수
       ```
       Minimum 80 lines. 모든 RESEARCH § Governance 섹션 다 포함.

    3. `registry-seed/incidents.json` — `[]\n` (empty array 한 줄 + trailing newline)

    4. `registry-seed/maintainers.json`:
       ```json
       {
         "version": 1,
         "maintainers": ["agrune-solo"],
         "updatedAt": "2026-04-20T00:00:00.000Z",
         "notes": "Velocity limit holddown 면제. v0.5 초기 solo maintainer. 추가는 REGISTRY_GOVERNANCE.md 'Tier Transition' 에 따름."
       }
       ```
       (actual GitHub handle 은 사용자가 repo 생성 시 교체할 수 있도록 placeholder 명시)

    5. `registry-seed/index.json` — 10 host 카탈로그 (manifest 파일 생성 후 Task 2 에서 업데이트되지만 헤더/shape 을 먼저 정립):
       ```json
       {
         "version": 1,
         "generatedAt": "2026-04-20T00:00:00.000Z",
         "generatedBy": "phase-18-03-seed",
         "entries": [
           { "host": "news.ycombinator.com", "versions": ["1.0.0"], "tier": "verified", "path": "manifests/news.ycombinator.com@1.0.0.json" },
           ...(10 개 전부)
         ]
       }
       ```
       contentHash 는 Task 2 (manifest 파일 생성) 완료 후 Task 3 에서 채움.

    [보안: governance doc 이 Pitfall 3 (velocity false positive), Pitfall 5 (staleness threshold), Pitfall 7 (prod root-import), Pitfall 8 (sensitive false bypass) 전부 policy 로 기록. Schema enforcement 는 Plan 01 에서, label enforcement 는 Plan 04 에서, 여기는 normative doc.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && test -f registry-seed/README.md && test -f registry-seed/REGISTRY_GOVERNANCE.md && test -f registry-seed/incidents.json && test -f registry-seed/maintainers.json && test -f registry-seed/index.json && node -e "JSON.parse(require('fs').readFileSync('registry-seed/incidents.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('registry-seed/maintainers.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('registry-seed/index.json','utf8'))" && wc -l registry-seed/REGISTRY_GOVERNANCE.md | awk '{ if ($1 >= 80) exit 0; else exit 1 }' && wc -l registry-seed/README.md | awk '{ if ($1 >= 50) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    - 5 개 meta 파일 생성, JSON 3 개 모두 parse 성공
    - REGISTRY_GOVERNANCE.md ≥ 80 lines, 모든 sectio (Tiers/Velocity/Revocation/Maintainer Absence/Tier Transition/Staleness/Security) 포함
    - README ≥ 50 lines, 10 seed 목록 + contribution 경로 + CODEOWNERS 안내
  </done>
</task>

<task type="auto">
  <name>Task 2: 10 seed manifest JSON 파일 작성</name>
  <files>
    registry-seed/manifests/news.ycombinator.com@1.0.0.json [NEW]
    registry-seed/manifests/en.wikipedia.org@1.0.0.json [NEW]
    registry-seed/manifests/developer.mozilla.org@1.0.0.json [NEW]
    registry-seed/manifests/docs.python.org@1.0.0.json [NEW]
    registry-seed/manifests/www.gutenberg.org@1.0.0.json [NEW]
    registry-seed/manifests/arxiv.org@1.0.0.json [NEW]
    registry-seed/manifests/pypi.org@1.0.0.json [NEW]
    registry-seed/manifests/hn.algolia.com@1.0.0.json [NEW]
    registry-seed/manifests/www.wikidata.org@1.0.0.json [NEW]
    registry-seed/manifests/observablehq.com@1.0.0.json [NEW]
  </files>
  <action>
    각 manifest 는 RESEARCH "Proposed 10 Seeds" 기준 사이트별 "최소 한 개 이상의 resolvable target" 을 포함 (RESEARCH Assumptions Log A1: "각 사이트 1 target resolve 검증을 gate 로"). 모든 파일은 **common skeleton** 를 따름:

    ```json
    {
      "registry": {
        "host": "<domain>",
        "version": "1.0.0",
        "tier": "verified",
        "author": "agrune-maintainers",
        "submittedAt": "2026-04-20T00:00:00.000Z",
        "reviewedBy": ["agrune-maintainers"],
        "allowedEnvironments": ["dev", "prod"],
        "seedUrl": "<root URL>"
      },
      "manifest": {
        "schemaVersion": 3,
        "id": "<slug>-v1",
        "groups": [
          { "groupId": "<primary-group>", "targets": [ ... ] }
        ]
      }
    }
    ```

    **주의:** RESEARCH Assumption A1 는 LOW confidence. 실제 DOM 이 변했을 수 있으므로 **각 파일의 selector 는 "stable 선택 ladder" 로 보수적 작성** — role+name 우선, CSS 는 가능한 최소. 실제 매칭은 Plan 04 health check 가 검증. 각 host 당 **최소 2 개 target** (click-able + navigable) + 1 group.

    구체 targets (사이트 DOM 분석 기반 보수적 선택):

    1. `news.ycombinator.com`:
       - group "front-page": target `storyLink` (role:link, nameRegex:".+") / `upVoteButton` (role:link, nameRegex:"^upvote$"|css 'a.clicky')
       - group "nav": target `newLink` (role:link, nameRegex:"^new$")
       - seedUrl: `https://news.ycombinator.com`

    2. `en.wikipedia.org`:
       - group "article": `searchBox` (role:searchbox) / `firstSearchResult` (role:link)
       - group "nav": `mainPageLink` (role:link, nameRegex:"^Main page$")
       - seedUrl: `https://en.wikipedia.org`

    3. `developer.mozilla.org`:
       - group "search": `searchInput` (role:searchbox, nameRegex:"search") / `searchButton` (role:button, nameRegex:"^Search$")
       - group "nav": `docsLink` (role:link, nameRegex:"^Docs$")
       - seedUrl: `https://developer.mozilla.org`

    4. `docs.python.org`:
       - group "search": `searchInput` (role:searchbox) / `versionSwitcher` (role:combobox, nameRegex:"version"|css 'select#version')
       - seedUrl: `https://docs.python.org`

    5. `www.gutenberg.org`:
       - group "search": `searchQuery` (role:textbox, nameRegex:"^Search$"|css 'input[name="query"]') / `searchSubmit` (role:button)
       - group "browse": `browseByLanguage` (role:link, nameRegex:"^Browse by Language$")
       - seedUrl: `https://www.gutenberg.org`

    6. `arxiv.org`:
       - group "search": `searchInput` (role:searchbox, nameRegex:"search term") / `searchTypeSelect` (role:combobox)
       - group "browse": `newListingsLink` (role:link)
       - seedUrl: `https://arxiv.org`

    7. `pypi.org`:
       - group "search": `searchQuery` (role:searchbox, nameRegex:"search projects") / `searchSubmit` (role:button, nameRegex:"^Search$")
       - seedUrl: `https://pypi.org`

    8. `hn.algolia.com`:
       - group "search": `searchInput` (role:searchbox|css 'input[type="search"]') / `filterByType` (role:combobox)
       - seedUrl: `https://hn.algolia.com`

    9. `www.wikidata.org`:
       - group "search": `searchInput` (role:searchbox) / `goButton` (role:button, nameRegex:"^Go$")
       - seedUrl: `https://www.wikidata.org`

    10. `observablehq.com`:
        - group "explore": `searchInput` (role:searchbox|role:textbox, nameRegex:"^Search") / `exploreLink` (role:link, nameRegex:"^Explore$")
        - seedUrl: `https://observablehq.com`

    **모든 target 의 actionKinds 최소화** (`['click']` for link/button, `['fill']` for searchbox/textbox). 복합 macro / repeat 는 초기 seed 에서 **생략** (macro 는 로그인 플로우 등 sensitive 영역 — low-risk seed 는 read-only 만).

    selector ladder 원칙 (packages/manifest/src/schema.ts 의 SelectorLadderSchema 준수):
    - 1순위: role (+ nameRegex)
    - 2순위: text (필요 시)
    - 3순위: testId (이 사이트들은 data-testid 부재라 생략)
    - 4순위: attr (예: input[name="query"])
    - 5순위: css (최후 — 해시 class / :nth-child 금지. Phase 11 MANIFEST-04 계약)

    각 JSON 은 **2-space indent + trailing newline**. tabs 금지.

    [보안: T-18-19 (seed URL privacy leak) = 모든 seedUrl 이 public apex/WWW 도메인만, private/internal 0. T-18-20 (selector fragile) = 해시 class/:nth-child 0 — Phase 11 MANIFEST-04 ladder 정책 준수.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && for f in registry-seed/manifests/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" || exit 1; done && ls registry-seed/manifests/ | wc -l | awk '{ if ($1 == 10) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    - 10 개 manifest JSON 파일 존재
    - 모두 JSON.parse 성공 (syntax valid)
    - 각 파일에 `registry.host` / `manifest.schemaVersion: 3` / `manifest.groups[0].targets.length >= 1` 존재 (Task 3 validate-seed.mjs 가 전체 스키마 검증)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: validate-seed.mjs 스크립트 + `pnpm validate:seed` 배선 + index.json 재생성</name>
  <files>
    scripts/registry-seed/validate-seed.mjs [NEW]
    package.json
    registry-seed/index.json
  </files>
  <behavior>
    validate-seed.mjs 의 contract:
    - glob `registry-seed/manifests/*.json`
    - 각 파일에 대해 `RegistryEntrySchema.parse(JSON.parse(content))` 실행
    - 실패 시 stderr 에 path + zod issues 출력 후 exit 1
    - 모두 통과 시 stdout 에 `✓ 10/10 seed manifests valid`
    - 옵션: `--fix-index` 플래그 — `index.json` 을 `{version, generatedAt, entries: [{host, versions, tier, path, contentHash}]}` 형식으로 재생성. contentHash 는 `contentHash(entry.manifest)` 계산.

    테스트 대체 (스크립트 자체를 run 으로 검증):
    - `node scripts/registry-seed/validate-seed.mjs` exit 0 (10/10 pass)
    - `node scripts/registry-seed/validate-seed.mjs --fix-index` 재실행 후 index.json 이 10 entries + contentHash 필드 포함
    - 일부러 invalid JSON 파일 추가 후 실행 시 exit 1 + 해당 파일명 stderr 에 등장 (negative test — temp 파일 add/remove)
  </behavior>
  <action>
    1. `scripts/registry-seed/validate-seed.mjs`:
       ```javascript
       #!/usr/bin/env node
       import { readdirSync, readFileSync, writeFileSync } from 'node:fs'
       import { join } from 'node:path'
       import { RegistryEntrySchema, contentHash } from '@agrune/registry'

       const SEED_DIR = 'registry-seed/manifests'
       const INDEX_PATH = 'registry-seed/index.json'
       const fixIndex = process.argv.includes('--fix-index')

       const files = readdirSync(SEED_DIR).filter(f => f.endsWith('.json')).sort()
       const results = []
       let failed = 0

       for (const name of files) {
         const full = join(SEED_DIR, name)
         try {
           const raw = JSON.parse(readFileSync(full, 'utf8'))
           const entry = RegistryEntrySchema.parse(raw)
           const hash = contentHash(entry.manifest)
           results.push({
             host: entry.registry.host,
             versions: [entry.registry.version],
             tier: entry.registry.tier,
             path: `manifests/${name}`,
             contentHash: hash,
           })
           process.stdout.write(`  ✓ ${name}\n`)
         } catch (err) {
           failed++
           process.stderr.write(`  ✗ ${name}: ${err.message}\n`)
           if (err.issues) {
             for (const issue of err.issues) {
               process.stderr.write(`      ${issue.path?.join('.') ?? ''}: ${issue.message}\n`)
             }
           }
         }
       }

       if (failed > 0) {
         process.stderr.write(`\n${failed}/${files.length} failed\n`)
         process.exit(1)
       }

       process.stdout.write(`\n✓ ${results.length}/${files.length} seed manifests valid\n`)

       if (fixIndex) {
         const index = {
           version: 1,
           generatedAt: new Date().toISOString(),
           generatedBy: 'scripts/registry-seed/validate-seed.mjs',
           entries: results.sort((a, b) => a.host.localeCompare(b.host)),
         }
         writeFileSync(INDEX_PATH, JSON.stringify(index, null, 2) + '\n')
         process.stdout.write(`✓ wrote ${INDEX_PATH}\n`)
       }
       ```

    2. 루트 `package.json` 에 scripts 추가:
       ```
       "validate:seed": "node ./scripts/registry-seed/validate-seed.mjs",
       "validate:seed:fix": "node ./scripts/registry-seed/validate-seed.mjs --fix-index"
       ```

    3. `pnpm build` 후 (registry dist 가 있어야 함) `pnpm validate:seed` 실행 → 10/10 pass 확인. 실패한 파일이 있으면 Task 2 로 돌아가 seed manifest 수정.

    4. `pnpm validate:seed:fix` 실행 → `registry-seed/index.json` 이 10 entries + contentHash 로 업데이트됨. `git diff` 로 확인.

    5. `pnpm lint:no-legacy` 실행 → exit 0 (seed manifest 내용에 `data-agrune-` 리터럴 없음 — role/name/css 만 사용하므로 안전하지만 최종 확인).

    6. regression guard allow-list 확인: `registry-seed/` 디렉토리는 `data-agrune-` literal 을 도입하지 않음 → allow-list 변경 불필요.

    [Dependency note: 이 task 는 Plan 01 의 `@agrune/registry` dist 가 있어야 실행 가능. Plan 01 / Plan 03 모두 Wave 1 이지만 validate-seed.mjs 실행 자체는 Plan 01 build 완료 후. CI 에서는 `pnpm build && pnpm validate:seed` 순서로 가드.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && pnpm build && pnpm validate:seed && pnpm validate:seed:fix && node -e "const idx = JSON.parse(require('fs').readFileSync('registry-seed/index.json','utf8')); if (idx.entries.length !== 10) { process.exit(1) } for (const e of idx.entries) { if (!e.contentHash || !e.contentHash.startsWith('sha256:')) process.exit(2) }" && pnpm lint:no-legacy</automated>
  </verify>
  <done>
    - `pnpm validate:seed` exit 0 + stdout "10/10 seed manifests valid"
    - `pnpm validate:seed:fix` 가 index.json 10 entries + contentHash 로 업데이트
    - 각 index entry 에 `sha256:...` hash 필드 존재
    - `pnpm lint:no-legacy` exit 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 18 planner → seed manifest JSON content | 단방향 — 플래너가 정하는 "day-0 콘텐츠" 이므로 입력 검증 불필요하지만 schema conformance 는 CI 로 강제 |
| seed manifest → registry repo push (Plan 04 external sync) | Plan 04 가 push 지시서 생성 — 사용자 수동 수행 |
| seed manifest → agrune maps add (Plan 02 CLI) | Plan 02 가 registry-client 로 읽어 들임 (fetchRegistryEntry). 같은 schema 강제 |
| incidents.json → CLI doctor --refresh | Plan 02 가 fetch 하는 revocation 시그널. CODEOWNERS 로 maintainer-only 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-19 | Information Disclosure | seedUrl 필드가 private/internal URL 유출 | mitigate | 모든 seed 의 seedUrl 이 public apex 도메인 (`https://<host>`), pre-push review 로 확인. PR bot (Plan 04) 이 이후 기여에 대해 DNS 검증 강제 |
| T-18-20 | Tampering | Seed manifest selector 가 해시 class / :nth-child 사용 | mitigate | 모든 selector 가 role+name+attr 우선, 해시 class 0. validate-seed.mjs + Phase 11 validator 이중 체크 (SelectorForbiddenError) |
| T-18-21 | Elevation of Privilege | verified tier seed 가 잘못된 allowedEnvironments 를 가짐 | mitigate | schema superRefine 로 verified+prod 만 허용, community+prod 거부. 10 seed 전부 verified + ['dev','prod'] 로 의도적 |
| T-18-22 | Tampering | incidents.json 이 community 기여로 조작됨 | mitigate | CODEOWNERS (Plan 04 에서 생성) 로 maintainer-only gate. README + GOVERNANCE doc 에 명시 |
| T-18-23 | Tampering | maintainers.json 이 community 기여로 조작됨 | mitigate | 동일 — CODEOWNERS gate. velocity limit 면제 allow-list 라 escalation 방지 critical |
| T-18-24 | Repudiation | 10 seed manifest 의 author 가 "agrune-maintainers" 로만 되어 있어 개별 기여자 추적 불가 | accept | v0.5 seed 는 프로젝트 자체 기여로 취급, 개별 author 는 community 기여부터 기록 |
| T-18-25 | Denial of Service | Seed 10 개 전부 health check fail 시 (bot detection 등) | transfer | RESEARCH A1 LOW confidence. Plan 04 health check 이 2 주 연속 fail 시 `stale` 라벨 → 수동 대체. 신규 phase 로 대응 |
</threat_model>

<verification>
1. `pnpm validate:seed` exit 0 + 10/10 pass
2. `registry-seed/REGISTRY_GOVERNANCE.md` 의 섹션 8 개 (Tiers / Velocity / Revocation / Maintainer Absence / Tier Transition / Staleness / Security / Reporting) 모두 존재 (grep ^## 으로 확인)
3. `registry-seed/incidents.json` = `[]\n`, `registry-seed/maintainers.json` JSON valid, `registry-seed/index.json` 10 entries + contentHash
4. 모든 seed manifest 에 seedUrl 존재 + https:// 로 시작
5. `pnpm lint:no-legacy` exit 0 (seed 내용에 `data-agrune-` 리터럴 없음)
6. Phase 11 validator 가 모든 seed manifest 의 selector ladder 를 pass — SelectorForbiddenError 0 (validate-seed.mjs 내부에서 RegistryEntrySchema.parse → ManifestSchema 재사용으로 자동 증명)
</verification>

<success_criteria>
- ROADMAP Success Criteria #1 대부분 달성: 10 seed manifest (verified tier) 로컬 완성. **공개 repo 로의 실제 merge 는 Plan 04 external-sync-instructions 이후 사용자 수동 push.**
- ROADMAP Success Criteria #3 대부분 달성: `REGISTRY_GOVERNANCE.md` 가 tier/velocity/revocation/maintainer absence 전부 명시.
- REGISTRY-01 대부분 달성: 저장소 구조 초기화 (registry-seed/ = 공개 repo 의 미러) + 10 seed 완성.
- REGISTRY-04 완전 달성: Governance doc 완성 — 모든 요구 섹션 (tier / velocity 30일 holddown / revocation via incidents.json / maintainer absence default).
- Phase 17 regression guard 무영향.
- Pitfall 7 (prod root-import abuse): community tier schema-level 차단 정책이 governance doc 에 명시.
- Pitfall 4 (seed URL privacy leak) 구조적 차단: 10 seed 전부 public apex.
</success_criteria>

<output>
완료 후 `.planning/phases/18-registry/18-03-SUMMARY.md` 작성. decisions 에 (a) 10 seed 후보 RESEARCH 권고안 채택 (ASSUMED A1 — Plan 04 health check 이 실제 resolve 검증), (b) verified tier + allowedEnvironments=['dev','prod'] 초기화 (프로젝트 자체 기여 의미), (c) `validate-seed.mjs` 가 registry dist 에 의존 → CI 에서 `pnpm build && pnpm validate:seed` 순서 강제, (d) seed manifest 는 macro/repeat 생략 (read-only 저위험 우선) 기록.
</output>
