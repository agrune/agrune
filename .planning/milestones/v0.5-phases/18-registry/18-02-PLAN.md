---
phase: 18-registry
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - packages/registry/src/cli/add.ts [NEW]
  - packages/registry/src/cli/types.ts [NEW]
  - packages/registry/src/cli/doctor.ts [NEW]
  - packages/registry/src/cli/submit.ts [NEW]
  - packages/registry/src/cli/shared.ts [NEW]
  - packages/registry/src/index.ts
  - packages/registry/tests/cli-add.test.ts [NEW]
  - packages/registry/tests/cli-types.test.ts [NEW]
  - packages/registry/tests/cli-doctor.test.ts [NEW]
  - packages/registry/tests/cli-submit.test.ts [NEW]
  - packages/mcp/bin/agrune-mcp.ts
  - packages/mcp/package.json
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - REGISTRY-02
  - REGISTRY-03
  - REGISTRY-06
tags:
  - registry
  - cli
  - subcommand
  - doctor
  - submit

must_haves:
  truths:
    - "`agrune maps add <host> [version]` 이 registry 에서 fetch → 캐시 → lockfile 기록까지 완주하고 exit 0 을 반환한다."
    - "`agrune maps types` 가 lockfile 의 모든 entry 에 대해 `node_modules/.agrune/maps.d.ts` (또는 project 지정 경로) 에 union 타입 선언을 emit 한다."
    - "`agrune maps doctor` 가 로컬 캐시 + lockfile 의 모든 manifest 를 staleness.ts 로 분류하고 stale 이면 warning + auto-disable 경로 제안, --auto-disable 플래그 시 lockfile 에 disabled 필드 기록한다."
    - "`agrune maps submit <file>` 이 device flow 인증 (client_id placeholder) + octokit fork+PR 플로우 스켈레톤을 실행해 사용자에게 verification URL/user_code 를 출력한다."
    - "기존 `agrune manifest …` 서브커맨드는 영향 없이 동작한다 (dispatch 분기 비침범성 증명)."
    - "`agrune maps --help` 가 4 개 서브커맨드 요약 출력."
  artifacts:
    - path: "packages/registry/src/cli/add.ts"
      provides: "runAddCli(argv) — host 파싱 + fetchRegistryEntry + contentHash 재계산 비교 + writeCacheEntry + lockfile 갱신"
      exports: ["runAddCli"]
    - path: "packages/registry/src/cli/types.ts"
      provides: "runTypesCli(argv) — lockfile 읽어서 host/targetId union .d.ts emit"
      exports: ["runTypesCli"]
    - path: "packages/registry/src/cli/doctor.ts"
      provides: "runDoctorCli(argv) — classifyStaleness + incidents.json fetch (--refresh 시) + auto-disable"
      exports: ["runDoctorCli"]
    - path: "packages/registry/src/cli/submit.ts"
      provides: "runSubmitCli(argv) — device flow auth + octokit fork/commit/PR"
      exports: ["runSubmitCli"]
    - path: "packages/registry/src/cli/shared.ts"
      provides: "printUsage / withPicocolors / errorExit / readLocalManifestFile — 4 CLI 공용 헬퍼"
      exports: ["printMapsUsage", "color", "errorExit", "parseArgs"]
    - path: "packages/mcp/bin/agrune-mcp.ts"
      provides: "'maps' dispatch 블록 — 기존 'manifest' 블록 바로 아래에 추가"
      contains: "if (args[0] === 'maps')"
  key_links:
    - from: "packages/mcp/bin/agrune-mcp.ts"
      to: "@agrune/registry/cli/*"
      via: "dynamic import('@agrune/registry/cli/add').then(m => m.runAddCli(sub))"
      pattern: "import\\('@agrune/registry"
    - from: "packages/registry/src/cli/add.ts"
      to: "@agrune/registry/registry-client.ts + cache.ts + lockfile.ts + content-hash.ts"
      via: "internal imports"
      pattern: "from '..\\/(registry-client|cache|lockfile|content-hash)'"
    - from: "packages/mcp/package.json"
      to: "@agrune/registry"
      via: "dependencies 추가 (workspace:*)"
      pattern: "\"@agrune\\/registry\""
    - from: "packages/registry/src/cli/submit.ts"
      to: "@octokit/rest + @octokit/auth-oauth-device"
      via: "Octokit + createOAuthDeviceAuth"
      pattern: "@octokit/"
---

<objective>
Phase 18 의 사용자 표면 — `agrune maps {add, types, doctor, submit}` CLI 4 서브커맨드 — 를 `@agrune/registry/cli/*` 에 구현하고 `packages/mcp/bin/agrune-mcp.ts` 의 dispatch 블록을 확장해 노출한다. 기존 `agrune manifest …` 서브커맨드 패턴 (L66-82) 을 그대로 미러링해 codebase consistency 유지.

Purpose: Plan 01 이 완성한 library (schema/hash/cache/lockfile/client/staleness) 를 얇은 CLI wrapper 로 감싸 사용자가 `agrune maps add news.ycombinator.com` 한 줄로 registry 사용을 시작하게 한다. `submit` 은 device flow 를 통한 PR 자동화로 "submit" 의미를 실제로 채운다 (RESEARCH Alternatives Considered — "fork-based unauthenticated" 대비 device flow 의 DX 우위).

Output:
- `packages/registry/src/cli/` — 4 command 각각 독립 파일 (RESEARCH Open Q 1 권고: mcp bin 는 manifest 와 동일 패턴 유지)
- `packages/mcp/bin/agrune-mcp.ts` 에 `maps` 블록 + dispatch
- `packages/mcp/package.json` 에 `@agrune/registry: workspace:*` 의존성 추가
- README / AGENTS 업데이트 (4 개 새 subcommand 문서화)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-registry/18-CONTEXT.md
@.planning/phases/18-registry/18-RESEARCH.md
@.planning/phases/18-registry/18-01-PLAN.md

@packages/mcp/bin/agrune-mcp.ts
@packages/mcp/package.json
@packages/manifest/src/index.ts
@README.md
@AGENTS.md

<interfaces>
<!-- Plan 01 이 이미 export 한 @agrune/registry public API (consume 대상) -->

From @agrune/registry (Plan 01):
```typescript
export const RegistryEntrySchema: z.ZodSchema<RegistryEntry>
export type RegistryEntry = { registry: {...}; manifest: AgruneManifest }
export function contentHash(manifest: AgruneManifest): string
export function getCacheDir(): string
export async function readCacheEntry(host: string, version: string): Promise<RegistryEntry | null>
export async function writeCacheEntry(entry: RegistryEntry): Promise<void>
export async function readLockfile(cwd: string): Promise<Lockfile>
export async function writeLockfile(cwd: string, lock: Lockfile): Promise<void>
export const LOCKFILE_NAME: 'agrune.maps.lock.json'
export async function fetchRegistryEntry(host, version, options?): Promise<RegistryEntry>
export function classifyStaleness(entry, now?): StalenessState
export const STALENESS_THRESHOLDS
export class RegistryError extends Error
```

From @agrune/manifest (existing):
```typescript
export { validateManifest, ManifestSchema } from './validator'
export type AgruneManifest
```

From existing packages/mcp/bin/agrune-mcp.ts (dispatch pattern to mirror):
```typescript
// L63-83 — 'manifest' subcommand 블록 구조
if (args[0] === 'manifest') {
  const subArgs = args.slice(1)
  if (subArgs[0] === 'validate') {
    const { runValidateCli } = await import('../src/manifest-validate-cli.js')
    const code = await runValidateCli(subArgs.slice(1))
    process.exit(code)
  }
  if (subArgs[0] === 'dev') {
    const { runManifestDevCli } = await import('../src/manifest-dev-watcher.js')
    const code = await runManifestDevCli(subArgs.slice(1))
    process.exit(code)
  }
  process.stderr.write(`Unknown manifest subcommand: ${subArgs[0] ?? '(none)'}\n`)
  process.stderr.write(`Usage: agrune manifest validate <file> [--url <url>]\n`)
  process.stderr.write(`       agrune manifest dev <file>\n`)
  process.exit(1)
}
```

New exports from @agrune/registry/cli/* (Plan 02 가 추가):
```typescript
// 각 CLI 는 Promise<number> (exit code) 반환 — manifest-validate-cli 패턴과 동일
export async function runAddCli(args: string[]): Promise<number>     // packages/registry/src/cli/add.ts
export async function runTypesCli(args: string[]): Promise<number>   // packages/registry/src/cli/types.ts
export async function runDoctorCli(args: string[]): Promise<number>  // packages/registry/src/cli/doctor.ts
export async function runSubmitCli(args: string[]): Promise<number>  // packages/registry/src/cli/submit.ts
```

Since packages/mcp/bin/agrune-mcp.ts uses `dist/src/` paths, dynamic imports must target
compiled output. Use sub-path re-exports in package.json for clean imports:
```json
{
  "exports": {
    ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" },
    "./cli/add": { "import": "./dist/cli/add.js", "types": "./dist/cli/add.d.ts" },
    "./cli/types": { "import": "./dist/cli/types.js", "types": "./dist/cli/types.d.ts" },
    "./cli/doctor": { "import": "./dist/cli/doctor.js", "types": "./dist/cli/doctor.d.ts" },
    "./cli/submit": { "import": "./dist/cli/submit.js", "types": "./dist/cli/submit.d.ts" }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: add / types / doctor CLI 구현 + 단위 테스트</name>
  <files>
    packages/registry/src/cli/shared.ts [NEW]
    packages/registry/src/cli/add.ts [NEW]
    packages/registry/src/cli/types.ts [NEW]
    packages/registry/src/cli/doctor.ts [NEW]
    packages/registry/src/index.ts
    packages/registry/package.json
    packages/registry/tsup.config.ts
    packages/registry/tests/cli-add.test.ts [NEW]
    packages/registry/tests/cli-types.test.ts [NEW]
    packages/registry/tests/cli-doctor.test.ts [NEW]
  </files>
  <behavior>
    cli-add.test.ts (mock fetchRegistryEntry / cache / lockfile):
    - runAddCli([]) returns 1 with usage written to stderr
    - runAddCli(['news.ycombinator.com']) fetches 'latest', writes cache, updates lockfile, returns 0
    - runAddCli(['news.ycombinator.com', '1.0.0']) fetches exact version
    - runAddCli detects contentHash mismatch (fetched content vs computed) → returns 1 + REGISTRY_ERROR code print
    - runAddCli idempotent: running twice updates same entry (host unique in lockfile entries)
    - runAddCli with --offline uses cache only, fails if cache miss

    cli-types.test.ts:
    - runTypesCli([]) reads agrune.maps.lock.json from process.cwd() + emits `.d.ts` to `./node_modules/.agrune/maps.d.ts` (default path)
    - emitted d.ts contains `export type AgruneMapsHost = 'news.ycombinator.com' | 'en.wikipedia.org' | ...` (lockfile entries union)
    - emitted d.ts contains `export type AgruneMapsTargetId<H> = ...` with targetId per host (manifest 의 group.targets.targetId 수집)
    - runTypesCli --out <path> writes to 지정 경로
    - runTypesCli returns 0 + exit when lockfile 비어 있음 (빈 union 타입 never)

    cli-doctor.test.ts:
    - runDoctorCli([]) reads lockfile + classifies each entry, prints 색상 출력 (picocolors)
    - runDoctorCli exit 0 when all entries 'fresh' / 'week_old'
    - runDoctorCli exit 0 but prints warning when entries 'stale'
    - runDoctorCli --auto-disable on 'stale' entry writes disabled field to lockfile
    - runDoctorCli --refresh fetches incidents.json (mocked) + auto-disables hosts listed as revoked
    - runDoctorCli does NOT hit network without --refresh (RESEARCH Pitfall: rate limit abuse) — fetch spy 가 0 회 호출됨 assertion
  </behavior>
  <action>
    1. `packages/registry/package.json` `exports` 블록 확장 (interfaces 블록 참조) — 4 개 `./cli/*` sub-path. `tsup.config.ts` 의 `entry` 에 `src/cli/add.ts` / `types.ts` / `doctor.ts` / `submit.ts` 추가해 각각 개별 dist 파일 생성.

    2. `packages/registry/src/cli/shared.ts`:
       - `parseArgs(args, flags)` — 간단 argv parser (manifest-validate-cli.ts 와 동일 패턴 미러링, no commander dependency)
       - `errorExit(code, message, details?)` — stderr 에 RegistryError 포맷 출력
       - `color` 객체 — picocolors lazy import (terminal isTTY check 후 색상 적용)
       - `printMapsUsage()` — 4 서브커맨드 help text

    3. `packages/registry/src/cli/add.ts`:
       ```
       입력: [host, version?] + flags: --offline, --refresh, --registry-base-url <url>
       flow:
         1. argv 파싱 (host 필수 — 없으면 usage + exit 1)
         2. host 화이트리스트 검증 (Pitfall: invalid host → CACHE path traversal 방지, schema 재사용)
         3. --offline 이면 readCacheEntry 우선, 없으면 REGISTRY_ENTRY_NOT_FOUND
         4. --offline 아니면 fetchRegistryEntry (정확 매칭 + latest 만; v0.5)
         5. zod re-validation (fetchRegistryEntry 내부에서 이미 하지만 defense-in-depth)
         6. contentHash(entry.manifest) 계산
         7. writeCacheEntry(entry) — 0700/0600
         8. readLockfile(cwd) → entries 에서 같은 host filter out → 새 entry push → writeLockfile (sort+atomic)
         9. stdout 에 green "✓ Added news.ycombinator.com@1.0.0 (tier=verified, hash=sha256:...)" + source URL
         10. return 0
       에러:
         - REGISTRY_ENTRY_NOT_FOUND → code 1, stderr red
         - schema 실패 → code 1, zod 에러 포매팅
         - LOCKFILE_WRITE_FAILED → code 2 (재시도 권고 메시지)
       ```

    4. `packages/registry/src/cli/types.ts`:
       ```
       입력: flags: --out <path> (default: `node_modules/.agrune/maps.d.ts`), --cwd <path>
       flow:
         1. readLockfile(cwd)
         2. 각 entry 에 대해 cache 에서 전체 manifest 읽기 (readCacheEntry)
         3. emit .d.ts:
            - export type AgruneMapsHost = union of hosts (or never if empty)
            - per-host target id union: export type AgruneMapsTargetIds[H]
            - optional macro id union, repeat id union
         4. mkdirp + writeFile
       Emit example (10 seed 이면):
         declare namespace AgruneMaps {
           export type Host = 'news.ycombinator.com' | 'en.wikipedia.org' | ...
           export type TargetId_news_ycombinator_com = 'storyLink' | 'loginButton' | ...
           // etc
         }
       간단: v0.5 MVP 는 flat `export type AgruneMapsHost = union`. Per-host targetId 는 nested record.
       ```

    5. `packages/registry/src/cli/doctor.ts`:
       ```
       입력: flags: --refresh, --auto-disable, --cwd <path>
       flow:
         1. readLockfile(cwd)
         2. per entry: classifyStaleness(entry, now) → { fresh, week_old, stale, auto_disabled }
         3. --refresh 일 때만: fetchRegistryEntry 로 incidents.json (Plan 03 에서 생성) → revoked host 마킹
         4. 출력: 각 entry 에 대해
            - fresh: silent
            - week_old: stdout info
            - stale: stdout yellow warning + "run with --auto-disable to disable"
            - auto_disabled / revoked: stdout red
         5. --auto-disable 플래그 존재 + stale entry 발견:
            - entry.disabled = { reason: 'stale', at: now.toISOString() }
            - writeLockfile 재호출
         6. return 0 (exit code 는 informational — CI 에서 `--strict` 플래그 있을 때만 stale → exit 1; v0.5 는 default lenient)
       네트워크 접근 기본 OFF — RESEARCH Open Q 3 권고: add 는 network default, doctor 는 cache-first.
       ```

    6. `src/index.ts` 에는 cli/* 를 **re-export 하지 않음** — bin 은 항상 `@agrune/registry/cli/*` sub-path 로 import (tree shaking + mcp bundle 크기 제어).

    7. 단위 테스트: vitest `vi.mock('../../src/registry-client', () => ({ fetchRegistryEntry: vi.fn(...) }))` 로 네트워크 차단. `AGRUNE_CACHE_DIR` override 로 tmp dir. 각 CLI 당 5-6 케이스.

    [보안: Threat T-18-11 (add 시 MITM) = fetchRegistryEntry 의 HTTPS 강제 + defense-in-depth 재검증. T-18-12 (doctor 네트워크 남용) = --refresh 명시 opt-in.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && pnpm --filter @agrune/registry test && pnpm --filter @agrune/registry build && pnpm --filter @agrune/registry typecheck</automated>
  </verify>
  <done>
    - 3 CLI 파일 생성 (add/types/doctor + shared)
    - 단위 테스트 15+ 케이스 pass
    - dist 에 `cli/add.js`, `cli/types.js`, `cli/doctor.js` 각각 존재 (tsup multi-entry build 검증)
    - Sub-path export 경유 import 가능: `await import('@agrune/registry/cli/add')` resolve (typecheck 로 간접 증명)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: submit CLI (device flow + octokit) + 테스트</name>
  <files>
    packages/registry/src/cli/submit.ts [NEW]
    packages/registry/tests/cli-submit.test.ts [NEW]
    packages/registry/src/index.ts
  </files>
  <behavior>
    cli-submit.test.ts (mock Octokit + createOAuthDeviceAuth + fs):
    - runSubmitCli([]) returns 1 with usage
    - runSubmitCli(['path/to/manifest.ts']) — env AGRUNE_GITHUB_TOKEN 설정 시 PAT 경로: Octokit 바로 생성, device flow 건너뜀
    - runSubmitCli with no env token: createOAuthDeviceAuth 호출 → verification_uri + user_code 를 stdout 에 출력
    - After auth: octokit.repos.getContent → 404 이면 new, 200 이면 update
    - octokit.repos.createFork 호출 (idempotent — 이미 존재해도 OK)
    - octokit.repos.createOrUpdateFileContents 호출 (branch='submit/<host>-<version>', path='manifests/<host>@<version>.json')
    - octokit.pulls.create 호출 (base='agrune/maps:main', head='<user>:submit/<host>-<version>')
    - Token 이 disk 에 저장되지 않음 (test 가 AGRUNE_TOKEN_PATH 확인 — 이런 file 존재하지 않아야 함) — Pitfall 2 구조적 보장
    - client_id 가 placeholder 'AGRUNE_DEVICE_FLOW_CLIENT_ID' 이면 warning 출력 (env AGRUNE_OAUTH_CLIENT_ID 로 override 안내)
  </behavior>
  <action>
    1. `packages/registry/src/cli/submit.ts`:
       ```
       입력: [manifest-file-path] + flags: --dry-run, --base-branch <branch>, --registry-repo <owner/name>
       (기본 registry-repo = 'agrune/maps', base = 'main')

       flow:
         1. 로컬 manifest.ts 파일 읽기 → tsx/ts-node? — 아니오, v0.5 MVP 는 .json 또는 이미 serialize 된 RegistryEntry 파일만 허용. TS compile 은 사용자가 별도 수행
            (또는 @agrune/manifest validateManifest 를 통해 .ts evaluate — manifest-validate-cli 패턴 재사용. 후자 채택: 이미 검증된 패턴)
            실제로는: manifest 파일 경로 받아 → esbuild (manifest 이미 의존) 또는 tsx dynamic import → defineManifest 결과 추출. manifest-validate-cli.ts 구현 모방.
         2. RegistryEntry 래핑 재료 수집:
            - host: argv 또는 manifest 최상위 meta 필드 (없으면 prompt — v0.5 는 명령 인자 강제)
            - version: argv 또는 기본 '1.0.0'
            - author: GitHub handle (device flow 인증 후 octokit.users.getAuthenticated 에서 login 가져옴)
            - tier: default 'community' (verified 로 submit 불가 — maintainer 승인 사항)
            - allowedEnvironments: default ['dev']
         3. 인증:
            const envToken = process.env.AGRUNE_GITHUB_TOKEN
            if (envToken) new Octokit({ auth: envToken })
            else:
              const clientId = process.env.AGRUNE_OAUTH_CLIENT_ID ?? 'AGRUNE_DEVICE_FLOW_CLIENT_ID'
              if (clientId === 'AGRUNE_DEVICE_FLOW_CLIENT_ID') stderr.yellow('⚠ placeholder client_id — OAuth App 등록 후 AGRUNE_OAUTH_CLIENT_ID 설정 필요')
              createOAuthDeviceAuth({ clientType: 'oauth-app', clientId, scopes: ['public_repo'], onVerification: v => stdout.write(`Open ${v.verification_uri} and enter code: ${v.user_code}\n`) })
              const { token } = await auth({ type: 'oauth' })
              new Octokit({ auth: token })
            // Pitfall 2: token 을 disk 에 저장하지 않음. 변수 스코프에서만 사용.
         4. octokit.users.getAuthenticated → { login: author }
         5. octokit.repos.getContent({ owner:'agrune', repo:'maps', path: `manifests/${host}@${version}.json` })
            - 200 → update, 404 → create
         6. octokit.repos.createFork({ owner, repo }) — 이미 존재 시 error code 202 — swallow
         7. octokit.repos.createOrUpdateFileContents({ owner: user.login, repo: 'maps', path, message, content: base64(JSON.stringify(registryEntry, null, 2)), branch: `submit/${host}-${version}` })
         8. octokit.pulls.create({ owner:'agrune', repo:'maps', title: `Add ${host}@${version}`, head: `${user.login}:submit/${host}-${version}`, base: 'main', body: PR body template })
         9. stdout green ✓ `PR created: https://github.com/agrune/maps/pull/<num>`
         10. return 0
       --dry-run 모드: 1~3 단계 (auth + entry 준비) 까지만 + JSON 출력, 네트워크 write 호출 없음.
       ```

    2. submit 의 manifest 파일 로딩은 `packages/mcp/src/manifest-validate-cli.ts` 의 dynamic import 패턴 재사용 (`import(path)` on .js / .ts). TS 파일의 경우 `tsx` runtime loader 의존 (이미 `@agrune/mcp` devDep). submit 은 `@agrune/mcp` 에 의존하지 않으려면 manifest-validate-cli 의 로직을 `@agrune/registry` 로 복사. RESEARCH Architectural Responsibility Map 에 따라 registry-owned 로직은 registry 에 둠.

    3. 단위 테스트: Octokit 전체 mock. device flow mock (onVerification callback 호출 확인). filesystem 확인 (token persistence 가 0 건).

    [보안: T-18-13 (device flow phishing) = onVerification 메시지는 github.com/login/device 도메인 정규화만. T-18-14 (token leakage) = `~/.agrune/maps/.auth*` file 생성 금지 (test assertion). T-18-15 (submit 이 contents:write 없는 bot 경로와 충돌) = submit 은 user token 사용이므로 해당 없음.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && pnpm --filter @agrune/registry test && pnpm --filter @agrune/registry build</automated>
  </verify>
  <done>
    - `submit.ts` 생성 + mock-based 테스트 7+ pass
    - Token persistence 부재 증명 (test assertion)
    - dist 에 `cli/submit.js` 생성
  </done>
</task>

<task type="auto">
  <name>Task 3: `packages/mcp/bin/agrune-mcp.ts` 'maps' dispatch + 문서 갱신</name>
  <files>
    packages/mcp/bin/agrune-mcp.ts
    packages/mcp/package.json
    README.md
    AGENTS.md
  </files>
  <action>
    1. `packages/mcp/package.json` — dependencies 에 `"@agrune/registry": "workspace:*"` 추가. `pnpm install` 재실행 → `packages/mcp/node_modules/@agrune/registry` symlink 생성 확인.

    2. `packages/mcp/bin/agrune-mcp.ts` — line 83 (현재 `// ── 서브커맨드 분기 끝 ──`) 직전에 `maps` 블록 추가:
       ```typescript
       // ── maps 서브커맨드 분기 ─────────────────────────────────────────────────
       // maps 는 Phase 18 REGISTRY 에서 추가. 기존 manifest 블록과 동일하게
       // CdpDriver / createMcpServer 를 건드리지 않고 process.exit() 로 종료.
       if (args[0] === 'maps') {
         const subArgs = args.slice(1)
         const sub = subArgs[0]
         if (sub === '--help' || sub === '-h' || sub === undefined) {
           process.stdout.write(
             `Usage: agrune maps <command>\n\n` +
             `Commands:\n` +
             `  add <host> [version]  registry 에서 manifest 를 받아 ~/.agrune/maps/ 캐시 + lockfile 에 기록\n` +
             `  types [--out <path>]  lockfile 을 읽어 host / targetId union 타입 선언을 emit\n` +
             `  doctor [--refresh]    로컬 캐시 staleness 진단 (+ --refresh 시 incidents.json 조회)\n` +
             `  submit <file>          manifest 를 agrune/maps registry 에 PR 로 제출 (device flow 인증)\n`,
           )
           process.exit(sub === undefined ? 1 : 0)
         }
         if (sub === 'add') {
           const { runAddCli } = await import('@agrune/registry/cli/add')
           process.exit(await runAddCli(subArgs.slice(1)))
         }
         if (sub === 'types') {
           const { runTypesCli } = await import('@agrune/registry/cli/types')
           process.exit(await runTypesCli(subArgs.slice(1)))
         }
         if (sub === 'doctor') {
           const { runDoctorCli } = await import('@agrune/registry/cli/doctor')
           process.exit(await runDoctorCli(subArgs.slice(1)))
         }
         if (sub === 'submit') {
           const { runSubmitCli } = await import('@agrune/registry/cli/submit')
           process.exit(await runSubmitCli(subArgs.slice(1)))
         }
         process.stderr.write(`Unknown maps subcommand: ${sub}\n`)
         process.stderr.write(`Usage: agrune maps {add,types,doctor,submit}\n`)
         process.exit(1)
       }
       // ── maps 분기 끝 ─────────────────────────────────────────────────────────
       ```
       기존 `manifest` 블록 스타일 정확히 미러 (dynamic import / exit code pattern).

    3. HELP_TEXT (L12) 에 `agrune maps {add,types,doctor,submit}` 라인 + Subcommands 블록 안에 4 줄 짧은 설명 추가.

    4. `README.md` — `agrune maps` 섹션을 "Quick Start" 또는 "CLI commands" 섹션에 4 예제 포함 (add 한 줄, types, doctor, submit). Manifest pivot 문서 (Phase 17-03) 스타일에 맞춰 간결하게. `lint:no-legacy` 위반 방지 — `data-agrune-` literal 비사용.

    5. `AGENTS.md` — "실행 모드" 섹션 (L36-44) 마지막에 4 줄 `agrune maps …` 명령 추가:
       ```
       - `agrune maps add <host> [version]` — registry 에서 manifest 가져오기 + 캐시/lockfile 기록
       - `agrune maps types` — lockfile 기반 host/targetId union 타입 선언 emit
       - `agrune maps doctor [--refresh]` — 로컬 캐시 staleness 진단 + auto-disable 제안
       - `agrune maps submit <file>` — manifest 를 `agrune/maps` 공개 registry 에 PR 로 제출
       ```
       Test 섹션 (L46-53) 에 `pnpm build` 후 `agrune maps --help` 가 4 서브커맨드 표시되는지 수동 검증 스텝 추가 (automated 는 Task 4).

    6. `pnpm -r build` 재실행 → `packages/mcp/dist/bin/agrune-mcp.js` 가 maps 블록 포함하는지 grep 으로 확인.

    [regression guard: README / AGENTS 변경분은 `data-agrune-` literal 0 삽입. `pnpm lint:no-legacy` 가 exit 0 유지되어야 함.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && pnpm --filter @agrune/mcp build && node packages/mcp/dist/bin/agrune-mcp.js maps --help > /tmp/maps-help.txt 2>&1 && grep -q 'add <host>' /tmp/maps-help.txt && grep -q 'submit' /tmp/maps-help.txt && pnpm lint:no-legacy</automated>
  </verify>
  <done>
    - `agrune maps --help` 가 4 서브커맨드 나열
    - `agrune maps add` / `types` / `doctor` / `submit` 이 각각 dynamic import → CLI runner 호출
    - 기존 `agrune manifest validate`/`dev` 이 기존 exit code 보존 (regression 없음)
    - README / AGENTS 에 maps 섹션 추가됨
    - `pnpm lint:no-legacy` exit 0
    - `pnpm -r build` 전체 성공
  </done>
</task>

<task type="auto">
  <name>Task 4: 엔드투엔드 스모크 테스트 (로컬 registry fixture)</name>
  <files>
    packages/registry/tests/e2e-smoke.test.ts [NEW]
  </files>
  <behavior>
    e2e-smoke.test.ts (통합 테스트 — network 차단 + tmp fixture 서버):
    - Node http.createServer 로 localhost:0 에 mock registry 서빙 (manifest JSON 2 개)
    - AGRUNE_REGISTRY_BASE_URL 로 override
    - runAddCli(['test-host.local']) → 캐시 작성 + lockfile 갱신 모두 성공
    - runTypesCli([]) → 생성된 d.ts 에 'test-host.local' union 멤버 등장
    - runDoctorCli([]) → 'fresh' 분류 (방금 추가)
    - runDoctorCli(['--refresh']) → incidents.json (empty) fetch → 추가 disable 없음
    - 두 번째 host 추가 후 types 재실행 → union 에 2 개 host 포함
    - Cache 파일 권한 mode 0o600 (stat.mode & 0o777 === 0o600) — POSIX only
  </behavior>
  <action>
    1. `packages/registry/tests/e2e-smoke.test.ts` — mock registry 서버 설정:
       ```
       beforeAll: spawn http.createServer((req, res) => { ... map URLs to fixture files ... })
       env: AGRUNE_REGISTRY_BASE_URL=http://127.0.0.1:<port>, AGRUNE_CACHE_DIR=tmp, LOCKFILE 은 tmp cwd
       ```
       Fixture 로 2 개 minimal RegistryEntry JSON (host='test-host.local', 'test-host2.local') 을 제공.

    2. 각 CLI 를 순차 호출하며 exit code + 부작용 (파일 생성, stat mode, JSON 내용) assertion.

    3. 테스트는 `vitest run` 안에서 완결 (network mock 이므로 CI 안전). NO real network.

    [본 테스트가 Plan 02 의 integration proof. Plan 04 의 PR bot 이 같은 fixture shape 을 기대하므로, fixture 스펙이 두 plan 간 contract 역할.]
  </action>
  <verify>
    <automated>cd /Users/chenjing/dev/agrune/agrune && pnpm --filter @agrune/registry test</automated>
  </verify>
  <done>
    - e2e-smoke.test.ts 가 vitest 에서 pass (통합: HTTP mock → CLI add → types → doctor round trip)
    - Test 실행 중 실제 네트워크 outbound 0 (fetch mock 또는 localhost HTTP only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → runAddCli/runTypesCli/runDoctorCli/runSubmitCli | 사용자 shell 입력. host, path, URL 전부 validate 필요 |
| `agrune maps add` fetch → registry HTTPS | 외부 registry 경계 (Plan 01 threat model 과 동일) |
| `agrune maps submit` device flow → GitHub OAuth | OAuth 인증 경계. token 은 메모리에만, disk 절대 금지 |
| submit → octokit fork/commit/PR | user account 경계 — scopes='public_repo' 만 |
| doctor --refresh → incidents.json fetch | 같은 HTTPS 경계. incidents.json 은 registry repo 내 파일 (CODEOWNERS 로 maintainer-only 변경) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Spoofing | `agrune maps submit` device flow | mitigate | createOAuthDeviceAuth 가 github.com/login/device 표준 flow. verification_uri 는 Octokit lib 에서 생성 — 조작 불가. onVerification 메시지만 stdout 출력 (Pitfall 2) |
| T-18-12 | Information Disclosure | Submit 후 token persistence | mitigate | Token 을 변수 스코프에만 저장. `~/.agrune/maps/.auth*` 파일 생성 금지 — test assertion (Pitfall 2) |
| T-18-13 | Tampering | CLI argv host parameter (path traversal) | mitigate | host 화이트리스트 regex `/^[a-z0-9][a-z0-9\-\.]{0,253}$/i` — cache.ts 에서 이미 강제 (T-18-10 재사용) |
| T-18-14 | Elevation of Privilege | `AGRUNE_OAUTH_CLIENT_ID` 환경변수 | accept | OAuth client_id 는 공개 가능 (client_secret 없음, device flow 특성). 사용자가 placeholder 대신 본인 clientId 설정 가능 |
| T-18-15 | DoS (external) | `doctor --refresh` 네트워크 호출 빈도 | mitigate | `--refresh` opt-in, 기본 cache-only. cron 이나 자동 스케줄 금지 (사용자 명시 실행만) |
| T-18-16 | Information Disclosure | `types` emit 된 d.ts 가 registry 내부 정보 노출 | accept | d.ts 는 host/targetId union 만 — public registry 내용 기반이므로 민감 정보 아님 |
| T-18-17 | Tampering | 'maps' dispatch 가 기존 'manifest' dispatch 를 침해 | mitigate | 'maps' 블록은 manifest 블록 다음에 배치, `if (args[0] === 'maps')` 가드로 격리 — 기존 테스트 (manifest validate/dev) 가 계속 pass 해야 함 |
| T-18-18 | Spoofing | 사용자가 submit 으로 typosquatting host 제출 | transfer | PR bot (Plan 04) 이 DNS lookup 으로 거부. CLI 는 signal 만 |
</threat_model>

<verification>
1. `pnpm --filter @agrune/registry test` — 30+ tests (Plan 01 의 20+ + Plan 02 의 15+ cli + 8 e2e) 모두 pass
2. `pnpm --filter @agrune/registry build` — dist 에 cli/{add,types,doctor,submit}.js 각각 생성
3. `pnpm --filter @agrune/mcp build` — dist/bin/agrune-mcp.js 가 'maps' dispatch 포함
4. `node packages/mcp/dist/bin/agrune-mcp.js maps --help` → 4 서브커맨드 표시, exit 0
5. `node packages/mcp/dist/bin/agrune-mcp.js manifest --help` → 기존 manifest 서브커맨드 영향 없음 (regression 확인)
6. `pnpm lint:no-legacy` → exit 0 (Phase 17 invariant)
7. `pnpm typecheck` → exit 0 (mcp bin 이 @agrune/registry sub-path exports 를 올바르게 consume)
8. `pnpm test:e2e` → pre-existing failures 수가 변하지 않음 (baseline 5 개, maps CLI 도입으로 악화 없음)
</verification>

<success_criteria>
- ROADMAP Success Criteria #2 완전 달성: `agrune maps {add, types, doctor, submit}` 4 동작 전부 구현 + help 노출
- REGISTRY-02 완전 달성: CLI 4 서브커맨드
- REGISTRY-03 완전 달성: add 가 `~/.agrune/maps/` 캐시 + `agrune.maps.lock.json` 생성
- REGISTRY-06 완전 달성: doctor staleness 진단 + auto-disable 경로
- 기존 manifest validate/dev 서브커맨드 regression 없음
- Phase 17 regression guard 유지 (lint:no-legacy exit 0)
</success_criteria>

<output>
완료 후 `.planning/phases/18-registry/18-02-SUMMARY.md` 작성. decisions 에 (a) CLI 는 `@agrune/registry/cli/*` sub-path exports 로 tree-shake, (b) device flow client_id placeholder 는 env `AGRUNE_OAUTH_CLIENT_ID` override, (c) doctor 기본 offline (`--refresh` opt-in), (d) submit 은 token persistence 금지 (Pitfall 2 구조적) 기록.
</output>
